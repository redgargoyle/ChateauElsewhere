# Architecture migration report

## Current phase

**The final-overhaul continuation now accepts Phase 2 Slice `2.2-10`. The first eleven reciprocal passage pairs, through Grand Entrance Hall <-> Grand Entrance Hall Rear, have canonical data/scene identity while the compatibility seam remains for unmigrated triggers. Group10 adds deterministic destination-region ownership for its bottom-edge interactions and preserves the title-case `Grand Entrance Hall Rear View` compatibility spelling. Accepted totals are Gameplay `6,041` documents / twelve RoomViews / twenty-two stage-2 Passages / 45 triggers; `GameDatabase 41` (`19` rooms / `22` passages); GameRoot `43` scene behaviours / `51` references including eight services; dependencies/callers `22 bound / 23 null`; and inventory `22 complete / 18 queued / 2 blocked-one-way / 3 blocked-parallel`. Static controls and Unity `6000.4.10f1` compilation pass; focused EditMode passes `4/4`, affected architecture `67/67`, rendered Group10 `1/1` with exact locked SHA, and genuine Group10 plus cold-start PlayMode `2/2`. No legacy path, trigger, railing cutout, or room boundary was pruned. Human review remains. The next safe slice is tests-first Group `11` Grand Entrance Hall Rear <-> Billiard Room characterization, including the `doors.txt` endpoint conflict.**

This report records what is implemented in the repository at this commit. It must be updated after every Unity-validated migration phase.

### Typed-identity boundary decisions

- The six value types validate canonical lexical form and prevent accidental cross-domain assignment. Catalog membership remains the responsibility of typed definitions and `GameDatabase` indexes; parsing a token alone never proves that authored content exists.
- `ChapterId` preserves the approved `chapter_01_arrivals` compatibility format. Scheduler event strings are not chapter identities and must never be parsed at the chapter boundary; the Phase 1 data index and Phase 5 chapter definitions will reject unregistered chapter tokens.
- New actor identities use `actor.*`. Existing `guest_1` strings, object names, and speaker labels remain legacy aliases until Phase 4 provides an explicit authored mapping; they are deliberately rejected rather than silently slugified.
- New beats and objectives use `beat.*` and `objective.*`. The old `StoryBeatBase(string)` constructor remains compatibility-only and exposes `TryGetId == false` for a noncanonical legacy label; all new beats use the typed constructor.
- Room and passage IDs intentionally validate lexical identity rather than topology. Endpoint references, directed-edge shape, one-way/shared-return rules, and uniqueness are validated by `RoomDefinition`, `PassageDefinition`, and the later typed database indexes.

### Typed-`GameContext` boundary decisions

- The seven public role interfaces do not extend `IGameService`. Typed context consumers therefore cannot initialize, shut down, or validate services; lifecycle remains owned by `GameRoot`. Slice 1.4 promotes only `IClockService` and `ISchedulerService` from markers to their narrow behavioral contracts; Camera, Navigation, Lighting, Dialogue, and GameFlow remain lifecycle-free markers until their owning slices.
- `GameContext.Services` is a copied, read-only snapshot in deterministic context-binding order. Preserved `GameRoot.Services` remains the serialized Inspector registration order. They intentionally answer different questions and no new consumer should enumerate either list as a locator.
- Additional role-less transition/future services are allowed when the complete snapshot has unique, strictly increasing order values. Required canonical roles remain exactly one each, so missing, duplicate, multi-role, null, tied, or reversed composition fails explicitly without freezing the project at exactly eight services.
- `SubtitleService` remains the transitional service at order `600`; it is not a permanent Core role. Final `DialogueService` owns subtitle state and `SubtitleScreen` renders it, so later deletion does not require breaking the new context API.
- The explicit order governs context binding and `GameServiceBase.Initialize`, while much legacy side-effect setup still occurs in `Awake`/`OnEnable`/`Start`. Slice 1.3 now certifies deterministic initialization, reverse shutdown, partial-failure rollback, registration errors, and scene binding; it does not claim those independent legacy callbacks have already migrated under Core ownership.
- The legacy Camera/Navigation coupling remains documented debt. This slice orders Camera before Navigation because Navigation invokes Camera during room application; it does not add a reverse reference, lookup, or parallel system to disguise the later Camera-to-Navigation read.

### Strict-`GameRoot` boundary decisions

- Strictness is production-scene scoped. Gameplay serializes `failStartupOnValidationErrors: 1`, the installer always authors `true`, and editor validation rejects a production root with the flag disabled. The default remains compatibility-safe for temporary migration/test roots until their consumers are migrated.
- Runtime `GameRoot` validates only serialized registrations and never searches, creates, repairs, or globally registers dependencies. The Editor-only validator compares every inactive-or-active in-scene `GameServiceBase` and non-service `ChateauBehaviour` against the two serialized lists, rejects unsupported direct `IGameService` components, and leaves the scene untouched.
- Initialization is services `100` through `800`, then scene behaviours in serialized order. Teardown is scene behaviours in reverse serialized order while services remain usable, then services `800` through `100`. Partial failures execute the same reverse rollback and always clear context ownership.
- `GameRoot` and `GameServiceBase` explicitly reject unsafe reentry and cross-context lifecycle calls. Cleanup continues after individual callback failures, and a primary lifecycle exception is preserved alongside any cleanup exception.
- Fatal root validation is context-fatal, not yet globally game-fatal: unrelated legacy components can still run their own `Awake`/`OnEnable`/`Start` until later ownership slices migrate them. This slice makes no broader readiness claim.

### Clock/Scheduler boundary decisions

- `ChapterClock` and `ChapterEventScheduler` keep their existing classes, files, script GUIDs, serialized fields, concrete public APIs, and Gameplay file IDs. They are in-place compatibility services until their remaining concrete consumers and final paths migrate; no second clock or scheduler was introduced.
- Clock advancement is ignored outside `GameRoot` lifecycle. A valid advance updates time first and then raises `TimeAdvanced`; Scheduler subscribes during ordered initialization and evaluates every due callback from that exact post-advance state. The old independent Scheduler `Update` race is gone.
- `GameClockState` is immutable and round-trips start hour/minute, seconds per game minute, sub-minute elapsed seconds, and running state in one atomic restore. Shutdown stops the clock without resetting elapsed/configuration values.
- Scheduler queues are transient derived state because their callbacks are Story-owned delegates. Shutdown clears them; restore orchestration must explicitly clear Scheduler, restore clock/Story values, and then let the owning Story beat re-arm callbacks. Serializing callback-free event metadata is deliberately deferred rather than creating dead scheduled entries or putting Story knowledge in Game Scheduler.
- Same-day absolute clock-time behavior is preserved. Midnight rollover was ambiguous in the legacy contract and is not silently redefined in this slice.
- `RuntimeSettingsMenu`/`GameplayRuntimeState` writes to Unity `Time.timeScale`, Doorbell cadence, animation timers, audio timers, and presentation timers remain separate ownership debt for their UI/prop/presentation slices; they do not write canonical in-game clock state.

### Canonical-definition and typed-database boundary decisions

- `GameDatabase.definitions` and the existing `Definitions` API remain intact for serialized/public compatibility. The original 22 ordered room/passage references are byte-preserved; the 11 new room assets are appended once for an exact `33 = 19 rooms + 14 passages` catalog.
- `RoomDefinitions` and `PassageDefinitions` are read-only derived views. `TryGetRoomDefinition(RoomId, ...)` and `TryGetPassageDefinition(PassageId, ...)` are the only new lookup APIs; this slice deliberately adds no generic or legacy-string locator.
- Typed indexes fail closed when a valid typed ID is duplicated. Invalid/default/malformed IDs do not throw or become index entries. The cache detects serialized-list order/reference changes and referenced stable-ID edits before reuse, while `OnEnable`, `OnValidate`, and full configuration validation rebuild deterministically.
- Display and legacy room aliases remain compatibility data on `RoomDefinition`. Validation permits a room to repeat its own display spelling, but rejects a trimmed case-insensitive alias shared by different rooms. Grand Entrance Hall Rear `view/View`, Side Stair with/without `&`, and Butler pantry apostrophe spellings resolve uniquely.
- A registered PassageDefinition must reference the exact source room, destination room, and reverse passage assets registered in the same database; a different asset with the same stable ID cannot satisfy referential integrity.
- The eleven new rooms are data only. They do not gain RoomViews, Passages, scene bindings, activation ownership, or callers until their dedicated Phase 2 slices. `RoomVisualCatalog`, `doors.txt`, legacy triggers, and exceptional topology remain intact until consumer migration proves deletion safe.

## Source baseline

- Unity editor version: `6000.4.10f1`
- Runtime C# files: 113
- Runtime C# lines: 50,212
- Direct `MonoBehaviour` declarations: 48
- Architecture-smell counts are recorded in `Baseline/architecture_guard_baseline.json`.

## Implemented and Unity-validated

- Added the explicit `GameRoot`/`GameContext` composition spine.
- Added service, chapter, room, interaction, actor, motor, presenter, UI, definition, story-beat and state-machine bases.
- Rebased selected major managers/controllers while retaining their existing script filenames and `.meta` GUIDs.
- Added strict production configuration validation, deterministic service-before-binder initialization, binder-first reverse teardown, transition guards, and complete partial-failure rollback.
- Removed the scheduler's clock search/repair fallback and Chapter 1's competing clock-polling fallback coroutine; Clock now deterministically drives the sole Scheduler through an explicit initialized-service signal.
- Added immutable full-value clock capture/restore, lifecycle freeze, Scheduler cancellation/clear/rearm behavior, eight focused EditMode contracts, and one real Gameplay PlayMode pause/resume callback contract.
- Authored all 19 canonical room definitions, retained exact legacy aliases and scene-truth textures, and expanded GameDatabase with read-only typed room/passage indexes plus fail-closed uniqueness and exact-reference validation.
- Added an Editor-only GameRoot installer and active-scene validator.
- Added static architecture inventory, serialized-reference scan and debt-ceiling guard.
- Added CI guard workflow.
- Deleted three proven-unused scripts: `NewBehaviourScript`, `PickupObject`, and the behaviorally-zero `GameClockHandsDisplay` runtime hook.
- Serialized exactly one `Chateau_GameRoot`, eight unique services, 31 unique scene behaviours, and `GameDatabase.asset` in `Gameplay.unity`.
- Preserved all 5,937 unrelated pre-existing Gameplay YAML documents byte-for-byte during the root graft.
- Added a real MainMenu-to-Gameplay lifecycle test with an Entrance/Drawing Room round trip and exact-one service assertions.
- Made that rendered route gate explicit about its Game-view contract: it now uses Unity's public `PlayModeWindow` rendering-resolution API, restores the prior view after every test (including failures), neutralizes headless edge-pan input, and validates the authored `1366x768` reference view instead of accidentally treating the batch runner's `50x29` target as gameplay truth.
- Separated room-stage coordinate layout from Butler presentation scaling. The approved `0.7528645` presentation baseline is explicit on `Player.prefab`; raw room calibration and guest scale data remain unchanged.
- Removed `RoomNavigationBootstrap` after proving that the serialized root supplies exactly one navigation manager and prompt controller from MainMenu startup through a room round trip.
- Removed `ChapterManager.BootstrapChapterManagerForGameplay`; the serialized Chapter 1 stack now owns startup. Chapter 2 controller ownership was migrated in a separate gate.
- Serialized one inert `Chapter2Controller`, wired its existing dependencies, and bound it through GameRoot. Repeated Chapter 2 debug skips reuse the same controller and HUD while preserving the characterized feature behavior.
- Removed the `ChapterManager` factory for `Chapter2Controller`; every chapter transition and debug-skip entry path now resolves the single serialized controller.
- Removed the Chapter 2 HUD factory; Chapter 2 now reuses the single serialized HUD on every entry path.
- Serialized the three inert Chapter 2 feature owners (monster stinger, guest panic, and guest search), bound their stable scene references, registered them with GameRoot, and removed their independently gated creation fallbacks.
- Removed `Chapter2Controller.ResolveReferences` and its dormant scene-wide repair searches. Chapter 2 now validates and uses fourteen serialized manager, navigation, clock, player, UI, feature, subtitle, speech, and clock-strike audio dependencies directly.
- Bound Chapter 1 to the existing serialized guest-scale applier, preserving the single ownership chain from applier to calibration to approved Butler source; every identity is lifecycle-tested before factory retirement.
- Removed runtime creation of guest-scale applier/calibration owners; the Guest Size Master retains an Editor-only, Undo-aware authoring action, and runtime creation is limited to per-guest participants.
- Explicitly wired the serialized dialogue and subtitle services, subtitle line bank, navigation edge, and Chapter 1 consumers while preserving lazy voice/indicator/subtitle-view creation.
- Removed the core dialogue/subtitle `FindOrCreate` factories and every caller; GameRoot is now the only owner of these services, while voice/indicator child ownership remains separately gated.
- Serialized one voice-playback owner with a dedicated AudioSource and one speaking-indicator owner, explicitly wiring their catalog, navigation, sprite, and service consumers while preserving lazy subtitle/bubble child views.
- Removed the voice-playback and speaking-indicator root factories; GameRoot validation now rejects missing dialogue ownership, while the actual subtitle canvas and bubble renderer remain first-use presentation children.
- Bound SubtitleService to the same serialized speaking-indicator owner used by DialogueSpeechService, preparing direct cleanup without global lookup.
- Replaced redundant global dialogue cleanup with direct serialized-owner calls and removed `SpeakingCharacterIndicator.HideAnyCurrent` plus `DialogueSpeechService.StopAnyCurrentSpeech`.
- Bound ChapterManager directly to the serialized dialogue/subtitle services and added strict composition validation before removing its remaining skip-time global cleanup.
- Routed chapter skips and settings teleports through ChapterManager's direct dialogue cleanup command, then removed `GuestVoiceLinePlayback.StopAnyCurrentLine` and all associated global subtitle searches.
- Added a real cataloged-voice lifecycle using `SUB_CH01_BUTLER_WELCOME_001`: it verifies authored subtitle/clip GUID resolution, Dialogue-channel volume math, primary source/binding reuse, and restoration of both previously enabled and previously disabled player input.
- Serialized DialogueSpeechService's exact Butler movement owner and the primary voice source's `GameAudioSourceVolume`. Stable speech now configures that authored Dialogue binding directly and validates the playback graph; the intentional temporary overlap source/binding remains dynamic.
- Replaced coroutine-local blocking-input restoration with a token-safe lease owned by DialogueSpeechService. Cancellation releases synchronously before chapter transitions apply their state, older routines cannot release newer speech, and service disable/shutdown cannot strand the Butler input-disabled.
- Removed SubtitleService's line-bank resource/navigation repair and GuestVoiceLinePlayback's catalog/navigation repair. Both owners now use validated serialized dependencies directly; subtitle canvas/EventSystem composition and temporary overlap audio remain explicitly deferred presentation behavior.
- Deleted the legacy `GameClockHandsDisplay` hook and its `.meta` after exhaustive code/serialized/binary/resource/reflection/animation checks and a MainMenu-to-Gameplay lifecycle proved it attached zero components and created zero analog-clock canvases. Current ticking, disabled generic close-up, and Chapter 2 seven-o'clock presentation remain owned elsewhere.
- Serialized the inert Chapter 1 HUD owner on the Chapter 1 controller while preserving the characterized first-use canvas/text construction and sorting order.
- Removed Chapter 1 HUD global lookup/runtime attachment and the obsolete `createRuntimeHud` flag; HUD child presentation remains lazy and owner-scoped.
- Serialized the RuntimeSettingsMenu owner and correctly scaled overlay canvas under GameRoot, explicitly wiring navigation, chapter, clock, and exploration-music dependencies while keeping controls lazy.
- Removed RuntimeSettingsMenu's global/root/canvas factory and made navigation initialize the serialized owner directly; nested controls remain lazy inside that owner.
- Serialized the exploration-music `GameAudioSourceVolume` owner and bound it directly to RuntimeSettingsMenu. This removes its active component factory and fixes the migration regression that clamped the authored `0.125` base volume to zero whenever the already-serialized AudioSource bypassed legacy discovery.
- Removed RuntimeSettingsMenu's eight scene-wide dependency searches, EventSystem creation, RectTransform repair, manager/clock/music resolvers, and per-frame source reconfiguration. Its complete serialized graph is now delegated into architecture validation; only the twelve owner-scoped control constructors and their eight UI-component additions remain dynamic.
- Characterized fireplace and clock ambience before changing ownership: each has a distinct 2D looping source and assigned entrance clip, only fireplace has an enabled high-pass filter, and both owner/source identities survive room travel.
- Serialized dedicated fireplace and clock ambience owners under GameRoot, explicitly binding navigation, catalogs, separate AudioSources, and the fireplace high-pass filter while retaining the old factories for a separate removal gate.
- Removed both ambience root factories plus their global navigation lookup, Resources catalog repair, and AudioSource/filter component repair; room navigation now initializes only its serialized owners.
- Characterized the first Drawing Room set piece without changing it: the tea table's exact sprite/material/transform/profile and four-point collision footprint are frozen, and tests prove `RoomProjectedEntity` plus `ObjectMovementBlocker2D` both overwrite its renderer. The intended order remains `6627`; the collider-bounds writer varied between `1358` and `1452` across valid runs.
- Added the target `World / Rooms / Props / Set Pieces` foundation: a pure `RoomDepthResolver` plus a static `SetPieceView` that owns only sorting layer/order/pivot and has no frame loop, bounds lookup, search, factory, transform, tint, or collision mutation.
- Migrated `tea_service_table` in Gameplay and both Drawing Room prefabs beneath literal `Props / Set Pieces` owners. The original GameObject/Transform/SpriteRenderer/component IDs, art, material, authored transform, depth anchor/order, blocker identity, and polygon are preserved; the blocker now owns collision only and cannot rewrite presentation.
- Characterized `purple_armchair_back` as the second set-piece candidate across Gameplay and both Drawing Room prefabs: shared chair art/material/profile/anchor resolves to order `8289`, exact transforms and component IDs are frozen, and the Gameplay four-point lower-seat collision footprint is preserved before migration.
- Migrated `purple_armchair_back` in place beneath the existing `Props / Set Pieces` owners. Its original GameObject/Transform/SpriteRenderer/component IDs, chair art/material, authored transforms, depth anchor/order, blocker identity, and four-point lower-seat polygon are preserved; its blocker now owns collision only and cannot rewrite presentation.
- Characterized `purple_sofa` as the third set-piece candidate across Gameplay and both Drawing Room prefabs: its sofa art/material/transform/component identities are exact across all three assets, the room-local anchor resolves to order `5385`, and the Gameplay four-point seating footprint is frozen before migration.
- Migrated `purple_sofa` beneath the existing `Props / Set Pieces` owners with one explicit `SetPieceView` per asset. Its original GameObject/Transform/SpriteRenderer IDs, art/material, authored transform, blocker identity, and four-point seating polygon are preserved; the blocker now owns collision only and cannot rewrite presentation.
- Characterized ChapterManager's remaining player/Chapter 2 repair before removal: runtime resolves the exact Player `PointClickPlayerMovement` and already-serialized Chapter 2 controller, keeps point-click enabled, derives the public Player root correctly, and leaves both legacy Player controllers disabled.
- Serialized ChapterManager's exact Player input (`81962842`) and removed all player, Chapter 2, and obsolete debug-canvas repair searches. Its full eight-owner graph is architecture-validated, the public Player root derives from the serialized input, and only the actual Player prefab instance authors legacy movement disabled.
- Characterized Chapter2Controller's manager identity before tightening its entry boundary: the controller starts with ChapterManager `3301000004` and retains that exact serialized owner through first and repeated Chapter 2 debug entry.
- Made Chapter2Controller's four external entry commands enforce their serialized ChapterManager instead of rebinding it from a caller. Valid and null convenience calls keep existing behavior; a missing or different manager is rejected before phase, navigation, clock, guest, or UI mutation.
- Grafted a dedicated `Audio_Chapter2ClockStrike` child beneath Chapter 2 with its own serialized non-looping 2D `AudioSource`, Game-Sounds `GameAudioSourceVolume` at base volume `0.4`, and imported clip GUID `d7084eafa9124afcbcbf12529e08bc70`. Seven-PM playback reuses those exact identities, and the obsolete resource/component/runtime-tone fallback is removed.
- Characterized and retired the monster stinger's structural repair: Begin/Stop reuses the exact serialized monster, Drawing Room start/target anchors, navigation service, and Image, restores the authored sprite, and creates no placeholder; validation now rejects a missing/mismatched graph instead of searching or repairing it.
- Characterized and serialized Guest Search's navigation owner: the pre-migration field was null until seven-PM staging, while the migrated owner is the exact GameRoot service from boot through repeated Chapter 2 and seven-PM paths; all five lazy repair calls and the resolver are removed.
- Characterized the monster presentation fallback before authoring it: first visible use creates one host violin source and Game-Sounds binding, loads clip GUID `69f06d321e4549cdcad1133332661f6d`, adds one sorted Canvas to the monster, loads eight ordered run sprites, and reuses every identity on repetition.
- Grafted the violin source and Game-Sounds binding directly onto the existing inactive monster object, serialized the imported clip, and removed every host/resource/Editor/component fallback. Playback configures only that owned graph without a hierarchy expansion or ChapterManager-host collision.
- Grafted the characterized overlay Canvas directly onto the same monster object with screen-space overlay and People order `10000`, then removed its dormant Get/Add component fallback. The stinger validates and configures that exact Canvas without adding a hierarchy object or component.
- Serialized the eight approved monster run sprites in exact raw frame `01` through `08` order, then removed the resource-path loader and name-sort fallback. Configuration validation rejects an incomplete array, and lifecycle proof shows the same array exists at boot and survives repeated stingers.
- Characterized the Entrance coat-hanger ownership failure before changing it. Fresh boot attempts repair before the authored hanger is discoverable and leaves only the pantry closet; a later explicit repair creates one stable Entrance closet, action, and trigger collider, preserves stored coats across repeated resolution, and retains identities through a room round trip.
- Moved existing closet component `3303000001` onto authored Entrance hanger `1592234992`, added serialized action `1592234995` and trigger `1592234996`, and bound the controller approach directly to transform `1592234993`. The pantry placeholder remains for separate review but no longer owns a duplicate closet.
- Removed the Entrance hanger name search, closet/action/collider factories, computed-collider fallback, pantry-anchor fallback, and global closet scan. Chapter 1 now validates its two serialized hanger edges; repeated resolution retains them without mutation.
- Characterized Chapter 1's remaining core-reference repair: the resolved ChapterManager, clock, scheduler, camera, navigation, Player movement, and Butler root retain exact identities through repeated resolution, room travel, repeated Chapter 2 entry, and seven-PM staging.
- Serialized Chapter 1's exact clock `3301000001`, camera `2050006783`, navigation `1878886997`, and Player movement `81962842` edges in its existing component document, with validation for the complete core graph and a derived Butler root.
- Removed all Chapter 1 discovery for its manager, clock, scheduler, camera, navigation, and Player across reference resolution, projection, room checks, subscriptions, and guest-scale setup. The Butler root now derives only from serialized Player movement.
- Characterized the final Chapter 1 core-graph ownership hole: `BeginChapter1` still replaces its serialized manager from a non-null caller, while valid startup and repeated transition lifecycles retain manager `3301000004`.
- Hardened the Chapter 1 entry boundary: valid and null convenience calls retain serialized manager `3301000004`; a mismatched manager logs one explicit error and returns before reset, scheduling, or world/UI mutation.
- Characterized the front-door ownership failure: authored trigger `1180734296` with action `1180734300` and collider `1180734299` remains unchanged, but startup creates a second `Door_answer_trigger` and binds Chapter 1 to that duplicate. Repeated repair, room travel, and chapter skips retain the duplicate without further component growth.
- Serialized Chapter 1's direct edge to authored action `1180734300` and validated its existing trigger collider. The authored action is now preferred from boot, eliminating the runtime duplicate while the old search/factory path remains isolated for the next cleanup gate.
- Removed the front-door alias lookup, runtime trigger/action/renderer/collider/UI factories, collider-size derivation, and generic front-door click-target branch. Chapter 1 now only activates and configures its validated serialized action; the approach fallback addresses that same action directly.
- Pruned the unowned pantry `CoatCloset / Anchors / ApproachFront` placeholder subtree (documents `3503000002`–`3503000008`) and removed its dead `FindPropAnchor`/`IsUnderNamedTransform` lookup path. The Entrance hanger remains the sole serialized closet owner.
- Characterized Chapter 1's remaining immutable data lookups: Resources resolves footstep catalog GUID `0e780686c6653db1a1c74916a591d484`; dynamic searches resolve the exact Entrance placemark, Drawing Room door target, Entrance/Drawing room-content owners, and ordered eight authored guest points without generating fallback seats.
- Serialized the characterized catalog, Entrance placemark, Drawing Room door target, Entrance/Drawing `RoomContentGroup` owners, and ordered eight guest points directly on Chapter 1. Configuration validation now requires that exact complete graph while the lookup code remains staged for its cleanup gate.
- Removed Chapter 1's footstep Resources fallback, RoomAnchor/object-name discovery, duplicate door-trigger scan, room-content scan, legacy three-seat fields, and generated-seat fallback. Guest placement now reads only the validated ordered eight-point graph; configuration rejects point reordering by exact `RoomAnchor` identity, and startup diagnostics consume the same authoritative report while unrelated dynamic guest creation remains intact.
- Characterized Chapter 1's runtime doorbell graph before migration: first startup creates exactly one `DoorbellSystem`, `AudioSource`, and Game-Sounds binding on the Chapter 1 host; it binds the Chapter clock, loads approved clip GUID `67dc6970d473422a86e0c071ef23abd1` / fileID `8300000` on first ring, and reuses every identity thereafter.
- Serialized that exact doorbell graph on the existing Chapter 1 host as components `3302000003`–`3302000005`, with direct clock/source/binding/imported-clip edges and configuration validation. The AudioSource resource remains empty because `DoorbellSystem` owns the one-shot clip, matching characterized behavior; repair paths remain staged for the cleanup gate.
- Removed Chapter 1's doorbell discovery/component factory and `DoorbellSystem` clock/source/binding/resource/generated-tone fallbacks. Initialization rejects a different clock, configures only its direct source/binding, and rings only the validated imported clip.
- Characterized the runtime-only Drawing Room exit click target before pruning. ChapterManager preparation creates one transparent `160x160` trigger under inactive Drawing Room anchors; another initialization duplicates it because `GameObject.Find` cannot see the inactive owner, and those identities persist into Chapter 2. Its action performs no navigation or state mutation beyond calling the same completion gate already owned by empty-door answer, coat storage, guest seating, and Drawing Room entry.
- Pruned that runtime Drawing Room exit target and its factory/remover, wrapper, enum role, serialized toggle, and private coat-sprite cache without a replacement. The real authored passage owns navigation; empty-door answer, coat storage, guest seating, and the Drawing Room room-change event own completion. This also removes a transparent collider that persisted into Chapter 2 and could bypass answering the final empty door.
- Characterized the legacy `GrandfatherClockInteraction` before retirement. The rendered lifecycle selects the empty `Grand Entrance Hall / Props / GrandfatherClock` placeholder in this environment, injects exactly one interaction, `AudioSource`, Atmosphere binding at base `0.28`, and in-memory `RuntimeGrandfatherClockTicking` clip (mono, 44.1 kHz, 88,200 samples), and retains every identity through repeated initialization, the Entrance/Drawing Room round trip, and Chapter 2. Its close-up stays disabled and creates no canvas. The separate serialized room-aware ambience owner/source/catalog and Chapter 2 strike/HUD graph remain exact, proving the injected loop is duplicate compatibility behavior rather than a missing requirement.
- Retired `GrandfatherClockInteraction`, its script GUID, unordered global host selection, duplicate synthetic ticking/source/binding, unreachable modal, trailing `Chapter1SceneAction` role, and unused HUD/time pass-through. Both authored clock placeholders retain only their Transform/anchor data; the room-aware ambience and Chapter 2 clock presentation/strike graphs remain their sole intended owners.
- Characterized the runtime-created `ChapterTimeSettingsUI` graph before changing ownership. It is the sole persistent current-time readout, not redundant settings code: startup injects one owner on Chapter 1, binds clock `3301000001`, creates one root overlay Canvas at order `9000`, and creates the exact bottom-left Liberation Sans SDF TMP label plus shadow. Every owner/view/font/material/EventSystem identity survives repeated initialization, room travel, repeated Chapter 2 entry, and repeated seven-o'clock staging; the label reaches `7:00 PM`. The obsolete editable panel is absent, and `RuntimeSettingsMenu` remains the sole owner of debug time-speed controls.
- Moved that script and byte-identical `.meta` to `Assets/_Chateau/Runtime/UI/GameTimeHUD.cs`, renamed it `Chateau.UI.GameTimeHUD`, and made it a `UIScreenBase`. Gameplay now serializes exactly one global Canvas/HUD beneath GameRoot with direct clock, Canvas, TMP text, and Shadow edges; GameRoot registers and validates that owner exactly once. Chapter 1 temporarily retains a serialized compatibility edge and the old repair branches only for the separately gated cleanup slice.
- Removed Chapter 1's temporary HUD field, serialized edge, validation, lookup, attachment, and initialization coupling. `GameTimeHUD` now contains only its four serialized references, exact graph validation, initial refresh, and current-time rendering; it cannot search, create, attach, or repair any owner, view, Shadow, EventSystem, or legacy panel.
- Characterized `ChapterIntroUI` without production or scene changes. Cold boot keeps owner `3301000003` on ChapterManager, emits exactly two missing-view warnings, creates one UI-layer root `Canvas_ChapterIntroOverlay` at order `12000`, one stretched `ChapterIntroUI_Runtime` root, one black `10000x10000` raycast Image, and one centered `900x180` Liberation Sans SDF title at size `72`; repeated repair calls and the full Chapter 1/2 lifecycle reuse every identity and the authored EventSystem.
- Serialized that exact four-object Chapter Intro view graph beneath GameRoot and bound its Canvas, overlay RectTransform, fade Image, and title TMP directly to the existing ChapterManager-hosted owner. The compatibility repair path is intentionally staged for one commit, but cold boot and repeated `EnsureUI` now reuse the authored graph without warnings or hierarchy/component growth. The component host, ChapterManager/Chapter 2 callers, timings, `Act 1` scene override, UI layer, sorting, font/material, and script GUID remain unchanged.
- Removed every Chapter Intro discovery, factory, component-addition, reparenting, recursive layer/layout mutation, fallback warning/name, and `PostProcessSafeCanvasUtility` dependency. The final Canvas is an authored scene-root Screen-Space Overlay Canvas—the Unity-supported form and the original runtime hierarchy—with its stretched overlay as the serialized full-screen boundary. `ChapterIntroUI` now has only four direct view references, three transition values, presentation methods, and graph validation; it retains the same owner, callers, IDs, timings, title, fade/title visuals, sorting, EventSystem, and GUID.
- Characterized the first canonical passage slice without changing production code or serialized assets. Exact YAML guards freeze the Grand Entrance Hall `GEH_Drawing_Room` trigger (`109889178`) and reciprocal Drawing Room `DrawingRoom_GEH` trigger (`2300000104`), their owning rooms, parents, geometry, script GUID, Inspector routes, and null legacy repair edges. The rendered MainMenu lifecycle freezes room-event order (`Drawing Room` then `Grand Entrance Hall`), camera/room/background ownership, prompt and cursor release, stable object/component counts, Chapter 1 continuity, and the unchanged Butler presentation multiplier `0.752865`; its later explicit-viewport extension supersedes the accidental tiny batch-view arrival samples with the neutral reference values recorded below.
- Characterized two migration debts on that same route before removing them: first door use adds and then reuses one Game-Sounds volume binding on shared source `2201000013`, and re-enabling a `RoomContentGroup` currently force-enables a deliberately disabled descendant renderer. The latter is a recorded defect to remove when the migrated `RoomView` takes visibility ownership, not target behavior to preserve indefinitely.
- Added the pure `Chateau.World.Rooms` / `Passages` / `Navigation` contract layer: stable-ID `RoomDefinition`, directed reciprocal `PassageDefinition`, typed `PassageKind`, logical `PassageAnchorData`, passive validated `RoomView`/`Passage` bindings, and `INavigationService`. These files contain no search, resource load, factory, lifecycle mutation, singleton, or current-room state. `RoomView` deliberately has no visibility command, `Passage` has no interaction lifecycle, and `RoomNavigationManager` does not implement the interface yet, so the slice introduces no second owner or parallel runtime path.
- Added contract tests using only temporary definitions and objects. They prove trimmed stable identity remains separate from display/legacy names, validate reciprocal directed endpoints without recursive validation, preserve logical `(0,0)` as valid authored anchor data, enforce room-owned passive scene bindings, and verify Gameplay/GameDatabase remain untouched and unbound.
- Authored the first canonical data graph without touching Gameplay: `room.grand-entrance-hall` and `room.drawing-room` directly own the exact current background texture GUIDs `3e163816317a638f5adedc338ec34d98` and `28c74b6dea1ed8e2c9c7d612355f9734`; directed passages `passage.grand-entrance-hall.drawing-room` and `passage.drawing-room.grand-entrance-hall` hold swapped endpoints, reciprocal asset links, Door kind, `Open Door` prompt, and their temporary legacy door IDs.
- Registered those four definitions directly and deterministically in `GameDatabase.asset` (Entrance room, Drawing room, forward passage, reverse passage). Unity 6000.4.10f1 imported the hand-authored YAML without rewriting it; every asset uses fileID `11400000`, schema version `1`, unique literal GUIDs, and direct type-2 references. Both current room perspective profiles remain deliberately null to preserve behavior until actor/presentation calibration is separately characterized.
- Grafted passive `RoomView` components `4100000001` and `4100000002` onto the existing Grand Entrance Hall and Drawing Room roots. Each directly binds its canonical definition and same-root `RoomContentGroup`; both are registered once with GameRoot for context and validation. `RoomView` still exposes no activation command or lifecycle method, so `RoomNavigationManager` remains the sole visibility writer while the view simply reports the root's `activeSelf` state.
- Extended the rendered lifecycle to retain both exact RoomView/definition/content identities and clean validation through cold boot, Entrance ↔ Drawing travel, the characterized renderer-reactivation defect, Chapter 1 completion, Chapter 2 entry/re-entry, and debug travel. Exactly two RoomViews and zero canonical Passages exist throughout.

## Historical static result at the foundation checkpoint

| Metric | Baseline | Candidate | Delta |
|---|---:|---:|---:|
| Runtime C# files | 90 | 112 | +22 |
| Runtime C# lines | 49,902 | 48,789 | -1,113 |
| Direct `MonoBehaviour` declarations | 63 | 48 | -15 |
| `FindObject*`/`GameObject.Find` | 199 | 106 | -93 |
| `Resources.Load` | 27 | 17 | -10 |
| runtime `new GameObject` | 98 | 67 | -31 |
| runtime `AddComponent<T>` | 100 | 51 | -49 |
| runtime initialization hooks | 9 | 4 | -5 |

The temporary source increase is the migration spine and verification tooling. It is not evidence that the cleanup is finished.

## Validation completed

- architecture debt-ceiling guard passed;
- `git diff --check` passed;
- all C# scripts have `.meta` files;
- no duplicate script GUIDs were found;
- static source inventory was regenerated;
- serialized text-asset reference inventory was regenerated;
- new architecture files passed lightweight delimiter/preprocessor checks.
- Unity `6000.4.10f1` compiled the project and produced result XML for every automated run;
- monster overlay cleanup passed its static ownership guard and rendered lifecycle gate; the full suite remained exactly 240 total / 190 passing / 50 known failures with an unchanged failure-name set;
- the Canvas cleanup changed no scene, prefab, asset, `.meta`, component ID, or script GUID;
- monster sprite authoring passed its exact ordered-reference guard and rendered lifecycle gate; the full suite remained exactly 240 total / 190 passing / 50 known failures with an unchanged failure-name set;
- the sprite graft kept all 5,982 scene documents in identical order, changed only controller document `3301000007`, and preserved `SceneRoots`, every sprite/importer/`.meta`, and every component ID;
- monster sprite fallback cleanup passed source guards, the rendered repeated-use lifecycle, and the exact full-suite comparison; only the resource-path property was removed from controller document `3301000007`;
- Entrance coat-hanger characterization passed exact serialized art/transform ownership checks and rendered before/after-repair lifecycle checks; the full suite remained 240 total / 190 passing / 50 known failures with an unchanged failure-name set;
- Entrance coat-hanger graft passed exact serialized-owner checks and rendered boot/repeated-resolution/room-round-trip lifecycle checks; the scene stayed at the same roots, preserved every prior document in order, added only action/collider documents, changed only four intended owner/reference documents, and kept the authored Transform and SpriteRenderer byte-identical;
- Entrance coat-hanger cleanup passed ownership/source guards and the rendered lifecycle; the full suite improved to 191 passing / 49 known failures because its stale source-extraction regression was corrected, with no new failure names;
- Chapter 1 core-reference characterization passed its static null-edge/source-path guard and rendered multi-transition lifecycle; the full suite remained 191 passing / 49 known failures with an unchanged failure-name set;
- Chapter 1 core-reference graft passed exact serialized-edge/configuration guards and the rendered multi-transition lifecycle; all 5,984 scene documents/roots remained ordered and only component `3302000001` changed; the full suite remained 191/49;
- Chapter 1 core-reference cleanup passed zero-discovery source guards and the rendered multi-transition lifecycle; the full suite remained 191/49 with an unchanged failure-name set;
- Chapter 1 manager-boundary characterization passed its source guard and rendered valid-manager lifecycle; the full suite remained 191/49;
- Chapter 1 manager-boundary guards passed static, explicit mismatch, valid lifecycle, and full-suite tests; the suite remained 191/49 with an unchanged failure-name set;
- front-door characterization passed exact authored-document/source guards and a rendered two-owner lifecycle; the full suite remained 191/49 with an unchanged failure-name set;
- front-door serialization passed its exact one-document scene audit: all 5,984 document IDs/order, uniqueness, and 13 roots remain unchanged; the rendered lifecycle retains one authored trigger/action/collider through repeated initialization and a room round trip; the full suite remains 191/49 with an unchanged failure-name set;
- front-door fallback cleanup changes no serialized asset; source guards ban every alias/search/factory/repair method, the rendered lifecycle keeps the exact action/collider and one trigger through repeated configuration/travel/skips, and the full suite remains 191/49 with an unchanged failure-name set;
- pantry placeholder pruning removes exactly seven Gameplay documents, preserves all other document order and all 13 roots, leaves no reference to the removed IDs, and passes the Entrance ownership guard plus stored-coat/room-round-trip lifecycle; the full suite remains 191/49 with an unchanged failure-name set;
- Chapter 1 immutable-data characterization passes exact null-edge/source-path guards and a rendered lifecycle that freezes catalog GUID/fileID, room/anchor identities, all eight guest-point transforms, repeated resolution, room travel, skips, and zero runtime-seat growth; the full suite remains 191/49 with an unchanged failure-name set;
- Chapter 1 immutable-data serialization changes only controller document `3302000001`, preserves all 5,977 document IDs/order and 13 roots, leaves every referenced object/asset document byte-identical, and passes strict configuration plus the same rendered identity lifecycle; the full suite remains 191/49 with an unchanged failure-name set;
- Chapter 1 immutable-data cleanup removes only five obsolete properties from controller `3302000001`; document IDs/order/roots and every catalog/anchor/room document remain exact, source guards ban every retired resolver/factory path, and the rendered lifecycle retains all identities/transforms, rejects and recovers from an intentional point-order swap, and creates zero seats. The full suite is 194/46 with no new failures; corrected method/document extractors resolve the former false-red `WorldSpaceGuestsUseWorldSpaceDrawingRoomTargets`, `DrawingRoomGuestMovementUsesEditableScenePoints`, and `LiveDoorAnswerUsesStableEntranceWorldPositions` gates;
- Chapter 1 doorbell characterization passes its pre-graft source/scene guard and rendered first-ring/repeated-resolution lifecycle, pinning one same-host owner/source/binding plus the exact imported clip while the existing factories remain intentionally staged; the full suite remains 194/46 with an unchanged failure-name set;
- Chapter 1 doorbell serialization adds only documents `3302000003`–`3302000005`, changes only host `1696549391` and controller `3302000001`, preserves every prior document's order plus SceneRoots, and passes exact YAML, imported-clip, configuration, first-ring, repeated-resolution, and full-suite gates at 194/46 with an unchanged failure-name set;
- Chapter 1 doorbell cleanup removes only the obsolete resource-path property from document `3302000003`, preserves all 5,980 document IDs/order/roots, bans every retired discovery/factory/resource/tone symbol, and retains the exact owner/source/binding/clip through first ring and room travel; the full suite remains 194/46 with an unchanged failure-name set;
- Drawing Room exit-target characterization passes exact source/scene guards and a rendered lifecycle that freezes its inactive-parent component contract, proves repeat initialization grows the target count from one to two, and retains both identities through room travel and Chapter 2 while independent completion-gate callers remain pinned; the full suite remains 194/46 with an unchanged failure-name set;
- Drawing Room exit-target pruning removes only `createRuntimeClickTargets` from controller document `3302000001`, preserves all 5,980 document IDs/order/roots and both authored action GUIDs, bans the entire runtime-target path, and leaves exactly the front-door/closet actions through repeated initialization, room travel, and Chapter 2. A completion-ready canonical Entrance→Drawing Room door transition requests Chapter 2 once, unsubscribes Chapter 1, disables input, and starts the same serialized Chapter 2 controller; the full suite remains 194/46 with no failure-name change;
- legacy grandfather-clock characterization passes its static zero-serialized-instance/action guard and rendered full lifecycle, recording the selected Entrance placeholder plus the exact injected source/binding/generated-clip graph while proving no modal UI appears and the canonical ambience/Chapter 2 owners stay separate; the full suite remains 194/46 with an unchanged failure-name set;
- grandfather-clock retirement deletes the zero-reference source and `.meta`, removes exactly three null YAML properties without adding/removing/reordering any of Gameplay's 5,980 documents, preserves `SceneRoots`, both ten-document authored clock subtrees, both Drawing Room prefabs, every clock art/audio asset, ambience documents `2201000031`–`2201000034`, and the Chapter 2 clock graph byte-for-byte; static/architecture/rendered lifecycle gates prove no injected component/source/binding/clip/modal returns, and the full suite remains exactly 194/46 with no failure-name delta or missing-script warning;
- Chapter time-readout characterization passes its zero-serialized-owner/static factory guard and rendered full lifecycle, pinning the complete owner/clock/canvas/text/font/material/shadow/EventSystem graph through room and chapter transitions before any production or scene change; the full suite remains exactly 194/46 with no failure-name delta;
- GameTimeHUD serialization adds exactly documents `1878887120`–`1878887125` and `1878887130`–`1878887134`, changes only GameRoot `1878886998`, its Transform `1878886999`, and Chapter 1 `3302000001`, preserves every prior document's relative order and identical `SceneRoots`, and keeps script GUID `12bd9a3afa94b05b1e9ce52146c9c7f4`. Focused static and rendered lifecycle gates pass; the full suite remains exactly 194/46 with an identical failure-name set;
- GameTimeHUD cleanup removes only Chapter 1 property `gameTimeHUD` from document `3302000001`; all 5,991 document headers/order, `SceneRoots`, the eleven HUD documents, GameRoot ownership, and the byte-identical script `.meta` remain exact. Static guards ban every retired search/factory/API, the rendered lifecycle retains the exact view/time/EventSystem identities through Chapter 2 and seven PM, and the full suite remains 194/46 with an identical failure-name set;
- Chapter-intro characterization passes its null-edge/factory source guard and rendered full lifecycle, including exact runtime hierarchy/components/layers/layout/font/material, two expected creation warnings, repeated `EnsureUI`, and Chapter 2 identity reuse; the full suite remains exactly 194/46 with no failure-name delta;
- Chapter-intro serialization adds exactly documents `1878887140`–`1878887144`, `1878887150`–`1878887151`, `1878887160`–`1878887163`, and `1878887170`–`1878887173`; only GameRoot Transform `1878886999` and intro owner `3301000003` change among prior scene documents, all other prior documents/order and `SceneRoots` remain byte-identical, and the script `.meta` hash is unchanged. Focused static and rendered lifecycle gates pass without fallback warnings; the full suite remains exactly 194/46 with an identical failure-name set;
- Chapter-intro cleanup retains all 6,006 document IDs/order and all fifteen view documents, changes only GameRoot Transform `1878886999`, root Canvas RectTransform `1878887141`, intro owner `3301000003`, and `SceneRoots`, and preserves the script `.meta` hash. The Canvas returns to the original top-level hierarchy with canonical engine-driven root values while the stretched overlay retains exact screen coverage; source guards report zero Find/new-GameObject/AddComponent repair in the class. Focused static/rendered gates pass and the full suite remains exactly 194/46 with an identical failure-name set;
- the strict GameRoot graft audit passed 53/53 checks;
- the Chapter 2 feature graft audit passed 22/22 checks: three documents added, only three intended existing documents changed, and all other scene documents/order/roots preserved;
- the guest-scale ownership-chain audit passed 6/6 checks: no documents added/deleted, only the Chapter 1 component changed, and document order stayed exact;
- the dialogue-core binding audit passed 6/6 checks: no documents added/deleted, exactly three intended components changed, and SceneRoots/document order stayed exact;
- the dialogue-auxiliary graft audit passed 11/11 checks: one AudioSource and two owner documents added, only their three intended consumers changed, and all old scene documents/order/roots stayed exact;
- the SubtitleService indicator-binding audit passed 5/5 checks: no document churn, one intended service document changed, and document order stayed exact;
- the ChapterManager dialogue-binding audit passed 6/6 checks: no document churn, one intended manager document changed, and exact service-reference counts were preserved;
- the Chapter 1 HUD graft audit passed 5/5 checks: one owner document added, only its GameObject/controller changed, and all old document order stayed exact;
- the RuntimeSettings owner graft audit passed 7/7 checks: eight documents added under GameRoot, only navigation/root-transform changed, and SceneRoots/old document order stayed exact;
- the exploration-music ownership graft added exactly one component document, changed only its GameObject component list and RuntimeSettings binding, preserved the original AudioSource document hash (`3ed1ddba...2487`), old document order, root Transform, and SceneRoots; the desired `.125` lifecycle contract was red before the fix and green after it;
- the RuntimeSettings dependency-cleanup static, scene-reset, and full lifecycle gates pass; the only scene edit authors the already-enforced `playOnAwake = false` value on the existing music AudioSource, with document order, component identity, roots, clip, pitch, loop, and base volume unchanged;
- the dialogue stable-owner graft added one volume-owner document and changed only the GameRoot component list plus the speech/playback binding documents; old document order is exact, the voice AudioSource hash remains `eb0ec0c...6486`, the Player stripped component remains `d2182512...db56`, SubtitleService remains `9938c314...79a08`, and GameRoot lists/Transform/SceneRoots are unchanged;
- the real voice/input lifecycle initially exposed nondeterministic global player selection, then passed with the serialized `81962842` owner and pre-existing `1878887003` voice binding through repeated cancellation/reuse;
- the blocking-input lease lifecycle proves enabled and disabled restoration, synchronous cancel-before-transition ordering, cancelled-coroutine non-interference, and same-frame replacement ownership; focused/static and full-suite gates pass;
- the dialogue dependency cleanup changes only the serialized SubtitleService and GuestVoiceLinePlayback documents by removing their obsolete resource-path strings; document order, all references, GameRoot lists/Transform, and SceneRoots remain exact, while real voice/input lifecycle and full suite pass;
- the clock-hands absence, navigation close-up, Chapter 2 clock-strike, lifecycle, architecture guard, serialized scan, and full-suite gates pass after deletion; all 51 baseline failure names remain exact and no missing-script warning appears;
- the focused ambience characterization passed and the full-suite gate retained the exact baseline failure-name set;
- the ambience-owner graft audit passed 6/6 checks: nine documents added, only navigation/root-transform changed, every old document retained its exact order, and SceneRoots stayed byte-identical;
- the ambience-factory cleanup audit passed 5/5 checks: no document churn, only the two ambience owner documents changed, document order stayed exact, and SceneRoots stayed byte-identical;
- the tea-table static characterization and full MainMenu/room-loop lifecycle passed, including direct proof that both legacy owners can write the same renderer while the polygon remains unchanged;
- both SetPiece foundation unit/static tests passed, and the full-suite failure-name set remained unchanged;
- the tea-table asset audit passed 16/16 structural checks across Gameplay and both prefabs: six documents added total, none deleted, only the exact hierarchy/owner documents changed, all prior document order stayed exact, and SceneRoots stayed byte-identical;
- the migrated lifecycle keeps one bound set-piece identity through activation/travel, resolves order `6627`, preserves all four collider points, and proves blocker sorting is a no-op;
- the purple-armchair static/lifecycle characterization passes across all three assets; at runtime legacy projection writes `8289` and the blocker then overwrites it with `1498`, proving the same competing-writer defect without freezing the bounds-derived blocker value;
- the purple-armchair migration changes no YAML document IDs or order: the three existing projection documents become `SetPieceView` in place, only the intended hierarchy/renderer/blocker/GameRoot documents change, and all three `SceneRoots` sections remain byte-identical;
- the migrated purple-armchair lifecycle keeps the same bound view/blocker identities through activation and travel, resolves order `8289`, preserves its art, authored transform and all four collider points, and proves blocker sorting is a no-op;
- the purple-sofa static/lifecycle characterization passes across all three assets; at runtime its only legacy sort writer changes the renderer to bounds-derived order `1225` while the intended room-local order is `5385`, and the trigger polygon remains unchanged;
- the purple-sofa asset audit passes across all three assets: exactly one `SetPieceView` document was added per asset, every prior document keeps its relative order, only the intended seven Gameplay/five-per-prefab documents change, the collider hash is exact, and `SceneRoots` stays byte-identical;
- the migrated purple-sofa lifecycle keeps the same view/blocker identities through activation and travel, resolves order `5385`, preserves its art, authored transform and all four collider points, and proves blocker sorting is a no-op;
- the ChapterManager dependency-cleanup audit changes only scene documents `81962841` and `3301000004`: the exact Player instance gains two disabled legacy-controller overrides, the manager binds Player input `81962842`, every existing transform/scale/calibration value remains byte-identical, and document order/`SceneRoots` are unchanged;
- ChapterManager now contains zero global searches or resolver methods; focused architecture/debug-skip/handoff tests and the full MainMenu/room-loop/repeated-Chapter-2 lifecycle pass with the same Player and controller identities;
- the Chapter 2 manager-boundary test proves a mismatched manager cannot replace the serialized owner or reset phase state, while all four entry methods share the same guard; the valid repeated-entry lifecycle retains manager `3301000004`;
- the Chapter 2 dependency-cleanup static, regression, and lifecycle gates pass; all fourteen serialized references resolve exactly once, `ResolveReferences` and its seven scene-wide searches are absent, and repeated entry/debug paths retain the same owners;
- the Chapter 2 clock-strike graft audit passes: Gameplay documents increase exactly from 5,975 to 5,979; the only added documents are IDs `3301000010` through `3301000013`; the only changed existing documents are parent Transform `2099709258` and `Chapter2Controller` `3301000006`; no document is removed; `SceneRoots`, host GameObject `2099709257`, the imported WAV, and its `.meta` remain byte-identical;
- the focused clock-strike lifecycle and static architecture gates pass, proving exact serialized source/binding/clip reuse, no AudioSource or volume-binding collision on the Chapter 2 host, and no component growth across repeated seven-PM playback;
- the clock-strike cleanup changes only the existing Chapter 2 controller document, removes its obsolete resource-path property, and leaves all 5,979 document headers, `SceneRoots`, dedicated audio documents, imported WAV, and `.meta` exact;
- the cleanup-specific source guards ban `Resources.Load`, `AudioClip.Create`, source/binding factories, and the obsolete resolver/fields; two consecutive seven-PM lifecycles reuse the exact serialized graph without component growth;
- the monster structural characterization pins controller `3301000007`, monster `3700000000`, Image `3700000003`, anchors `98514617`/`382498960`, their RoomAnchor owners/room IDs, exact authored positions, and navigation `1878886997`; focused lifecycle/static gates and the full suite pass before any repair removal;
- the monster structural cleanup keeps all 5,979 document headers and `SceneRoots` exact; only controller document `3301000007` loses its two obsolete repair fields, while every monster/anchor/navigation document and every deferred audio, sprite, timing, shake, sorting, visibility, and overlay field stays byte-identical;
- source guards prove the stinger has zero structural object/anchor searches, primitive placeholder creation, or Image/SpriteRenderer child repair; configuration validation, the unchanged Begin/Stop lifecycle, and the full 240-test failure-name baseline all pass;
- the Guest Search navigation characterization pins the pre-migration null-to-service transition, all five resolver call sites, and exact owner identity through repeated seven-PM staging; focused/static and full-suite failure-name gates pass before serialization;
- the Guest Search migration keeps all 5,979 document headers and `SceneRoots` exact; only component `3301000009` gains `navigationManager: {fileID: 1878886997}`, and the navigation/GameRoot documents remain byte-identical;
- Guest Search validation and source guards require the serialized manager and ban its resolver/global lookup; boot, repeated Chapter 2, repeated seven-PM, architecture, and full-suite gates retain the exact owner and 50-failure baseline;
- the monster presentation characterization freezes source/clip/binding properties, Canvas layer/order, all eight sprite GUIDs in approved order, original-sprite restoration, visible Drawing Room behavior, and repeated-use no-growth; focused/static and full-suite gates pass before scene authoring;
- the monster violin graft adds exactly AudioSource `3700000004` and binding `3700000005`, changing only monster GO `3700000000` and controller `3301000007`; all prior 5,979 document order, `SceneRoots`, monster transform/Image, ChapterManager host, clock audio, MP3, and `.meta` remain exact;
- boot/visible/repeated-use lifecycle gates prove the monster owns one exact source/binding/clip, the shared controller host remains component-free, activation precedes playback, stop restores inactivity, and the full suite retains the same 50 failure names;
- the violin cleanup keeps all 5,981 document headers and `SceneRoots` exact; only controller `3301000007` loses its obsolete fallback-name property, while source/binding/monster/clip/MP3/meta remain byte-identical;
- source guards ban host source discovery/attachment, Resources and Editor clip searches, binding creation, and runtime base-volume discovery; direct serialized configuration plus focused/lifecycle/full gates retain exact behavior and failure names;
- the overlay graft adds exactly Canvas `3700000006`, changing only monster GO `3700000000` and controller `3301000007`; the predicted 5,982-document header hash, `SceneRoots`, transform/Image/audio/host documents, People layer ID, and Canvas schema all verify exactly;
- boot/inactive, visible, stop, and restart lifecycle checks prove the same serialized Canvas remains the sole overlay with unchanged render mode/layer/order; the full suite retains the exact 50 failure names;
- the rendered full EditMode suite discovers 240 tests: 190 pass and the exact same 50 pre-existing failure names remain, with no clock-strike graft or cleanup regression;
- the separate `-nographics` invalid-viewport issue was independently hardened in commit `4d8a6d9a` and is not attributed to the clock-strike graft;
- the MainMenu boot/navigation lifecycle passed three independent cold Unity processes;
- each cold lifecycle run produced the same entrance multiplier (`0.752865`) at startup, after settling, and after the room round trip;
- the ChapterManager dialogue-binding gate produced two consecutive clean full-suite reruns after one transient full-run GameView zoom assertion; no files changed between those three runs, and both reruns restored the exact `0.752865` entrance multiplier;
- Gameplay scene hashing confirmed that batch validation did not rewrite the reviewed scene;
- all 147 C# scripts have `.meta` files and unique GUIDs.
- the first-passage static characterization passes `1/1`, its rendered lifecycle passes `1/1`, and the full EditMode suite now discovers 241 tests: 195 pass and the exact same 46 pre-existing failure names remain, with no new failure;
- the first-passage gate confirms `doors.txt` is not authoritative for this route: Gameplay contains zero serialized `DoorButton` instances, the forward legacy entry is absent, and both live directions remain Inspector-owned pending canonical data authoring;
- the pure canonical-contract gate passes `4/4`; the architecture guard reports no new debt, all 154 C# scripts have matching unique GUIDs, and serialized-reference generation reports zero instances for every new runtime script;
- generated architecture inventory now records 112 runtime files / 48,500 lines while every smell total remains unchanged (`FindObject` 106, `Resources.Load` 17, runtime `new GameObject` 67, `AddComponent` 51, runtime initialization 4, direct `MonoBehaviour` 48);
- the rendered MainMenu/Entrance/Drawing Room lifecycle remains green with the exact characterized arrivals and `0.752865` Butler multiplier; the full EditMode suite now discovers 245 tests, 199 pass, and the same 46 pre-existing failure names remain with no new or removed failure;
- Gameplay, MainMenu, and `GameDatabase.asset` hashes are byte-identical to the prior passing commit; the contract slice adds only new runtime contracts/tests/metas plus architecture documentation and generated inventories;
- the canonical data gate passes `5/5`, including exact asset/database GUIDs, stable IDs, texture identities, reciprocal endpoints, order, uniqueness, schema, and complete `GameDatabase` validation; serialized-reference inventory now reports exactly two `RoomDefinition` and two `PassageDefinition` assets;
- the unchanged rendered round trip passes with exact arrivals and Butler multiplier; the full EditMode suite discovers 246 tests, 200 pass, and the same 46 pre-existing failure names remain with no new or removed failure;
- Gameplay, MainMenu, and `GameDatabase.asset.meta` remain byte-identical to `b7b6311c`; Unity left all four data YAML files unchanged after import, and the repository-wide meta scan reports no duplicate GUID;
- the passive RoomView static gate passes `5/5` plus the exact scene-count guard; the rendered lifecycle passes `1/1`; the full suite remains 246 total / 200 passing / the same 46 pre-existing failure names;
- Gameplay changes from 6,006 to 6,008 documents by adding only RoomViews `4100000001`/`4100000002`. Existing changed documents are limited to the two room GameObject component lists and GameRoot's `sceneBehaviours`; all other 6,003 prior documents, prior order, both RectTransforms, both `RoomContentGroup`s, both route triggers, GameRoot GameObject/Transform, and `SceneRoots` remain byte-identical;
- serialized-reference inventory reports exactly two RoomView instances and zero Passage instances; architecture smell totals remain unchanged and the Butler presentation multiplier remains `0.752865`;
- the real far-approach lifecycle now passes in both directions at a neutral `1366x768`: far activation starts movement without audio or room mutation, `ArrivedAtDestination` precedes `MovementStopped`, door audio starts only after movement stops, `OnCurrentRoomChanged` fires before the destination warp, and the pending trigger/player subscription clears exactly once;
- the representative far-route reference values are now frozen as forward approach `(-7.576081, -1.986423)` / Drawing arrival `(5.267176, -2.104616)` and reverse approach `(5.280546, -2.015396)` / Entrance arrival `(-7.703568, -2.000136)`; both subsequent near activations are synchronous and add zero movement events;
- the source-position defect is explicit: at the same neutral `1366x768`, the legacy forward far route arrives at `(5.267176, -2.104616)` while the reciprocal near route arrives at `(5.231221, -2.002137)`. A single authored canonical anchor will intentionally remove this legacy variance rather than reproducing it;
- the rendered aspect matrix passes for `1366x768`, `1440x1080`, `1920x1080`, and `2560x1080`. Null-click reference envelopes remain bounded; preferred left/center/right clicks can select distinct exact walkable points, but every returned point projects inside the `145`-pixel auto-activation boundary. Approach and arrival coordinates vary materially by viewport, and the batch runner's old `50x29` viewport cannot exercise a far approach;
- all three `GameplayLifecycleCharacterizationTests` pass together with resolution restoration and test-order independence. This characterization slice changes only tests and architecture documentation; runtime scripts, scenes, prefabs, data assets, `.meta` files, and GUIDs remain byte-identical;
- the focused canonical contract gate passes `5/5`, the exact static route guard passes `1/1`, and the full rendered EditMode suite discovers 248 tests: 202 pass and the exact same 46 pre-existing failure names remain, with no added or removed failure;
- passive Passages `4100000011` and `4100000012` are now co-located with the unchanged forward/reverse legacy trigger owners, directly bound to the existing definitions and source RoomViews, reciprocally linked, registered once with GameRoot, and serialized with the four neutral reference vectors. Neither exposes nor receives an interaction/traversal command;
- the exact scene audit passes: Gameplay grows only from 6,008 to 6,010 documents; the two Passage documents are the only additions; only trigger GameObjects `109889176`/`2300000100` and GameRoot `1878886998` change among prior documents; all other 6,005 prior documents, prior relative order, legacy trigger documents, RectTransforms, RoomViews, and `SceneRoots` remain byte-identical;
- the passive-binding gates pass: canonical contracts `5/5`, architecture foundation `17/17`, exact navigation static guard `1/1`, and rendered lifecycle `3/3`. The full rendered EditMode suite remains `248` discovered / `202` passed / `46` known failures, with the failure-name set exactly equal to the pre-slice baseline;
- only legacy trigger documents `109889178` and `2300000104` now directly reference navigation manager `1878886997`, shared door AudioSource `2201000013`, Player Transform proxy `81962843`, and catalog `{fileID: 11400000, guid: 9a77542e25184fbc945d6a79f77007e7, type: 2}`. Exactly 2 of 45 scene door triggers carry all four bindings; the other 43 retain all four null edges, and every resolver remains intact;
- the type-correct Player edge adds one stripped Transform proxy for source Transform `7967904164350347880` on Player prefab instance `81962841`. The scene audit passes at 6,011 documents: only that proxy is added, only the two target trigger documents change, no document is removed, all prior relative order and `SceneRoots` are byte-identical, and Unity preserves candidate SHA-256 `49257b27e097b4db4406a4342a6acf5da4074223d7660716c6107bb7d9e3461e` after import and testing;
- the direct-binding gates pass: canonical contracts `5/5`, architecture foundation `17/17`, exact navigation static guard `1/1`, and rendered lifecycle `3/3`. Far/near event order, audio timing and single binding reuse, all four viewport envelopes, inactive reverse-trigger identity, room activation, and component counts remain exact. The full rendered suite remains `248` discovered / `202` passed / the identical `46` known failure names;
- the architecture guard reports no new debt; inventory remains 112 runtime files / 48,500 lines with unchanged smell totals and 48 direct `MonoBehaviour` classes. Serialized script-reference inventory remains byte-identical, and the Y-axis occlusion audit reports zero hard errors plus the same 38 explicitly tracked design-required set-piece findings for later one-prop-at-a-time migration;
- at the implementation-only façade gate, `RoomNavigationManager` began implementing `INavigationService` without a second owner: `CurrentRoomDefinition` mapped the existing `currentRoom` through registered canonical room definitions, `CanTraverse` failed closed on an invalid/unregistered/mismatched Passage graph, and `TryTraverse` delegated exactly once through `MoveThroughInspectorDoor`. That gate retained exactly one `currentRoom` assignment and one room event before the later caller and arrival cutovers;
- the implementation-only audit proves Gameplay remains at SHA-256 `49257b27e097b4db4406a4342a6acf5da4074223d7660716c6107bb7d9e3461e`, `DoorTriggerNavigation.cs` remains at `86ff517a0242de06076657f1bdb88e873ce4d419eea5b0181b883764c858cece`, their `.meta` files are byte-identical, and no scene, prefab, asset, GUID, serialized edge, trigger caller, or resolver changes. The direct façade test proves rejected calls mutate nothing; each valid direction emits one event before the legacy warp, produces no movement callback or door audio, and creates nothing;
- façade gates pass: canonical contracts `5/5`, architecture foundation `17/17`, exact legacy route/static ownership `1/1`, and rendered lifecycle `4/4`. The full suite discovers `249` tests: `203` pass and the exact same `46` baseline failure names remain. The architecture guard reports no new smell occurrence or direct `MonoBehaviour`; runtime inventory is 112 files / 48,616 lines;
- the rendered route characterization now requests the already documented far samples directly instead of selecting the farthest of 15 near-tied candidates. It continues to enforce the `145`-pixel boundary, bounded 1366 branch envelope, exact event/audio order, reciprocal arrivals, and all four viewport envelopes while no longer treating viewport-dependent floor clamping as authored identity;
- `DoorTriggerNavigation` now owns one optional serialized `canonicalPassage` compatibility edge. Only trigger docs `109889178` and `2300000104` bind `4100000011` and `4100000012`; exactly 43 other scene trigger documents omit the field and retain null/fallback behavior. The canonical branch runs after the existing approach/proximity gate, starts and conditionally stops the same audio, calls the sole `INavigationService`, and returns on rejection so invalid canonical data cannot silently bypass through legacy routing;
- the exact caller-cutover scene audit passes at 6,011 documents: no document is added, removed, or reordered; only the two target trigger documents change; all other 6,009 documents and `SceneRoots` remain byte-identical. Unity preserves Gameplay SHA-256 `07650a847f1bae4f742fd42093bffa062d4c5c985b9fa40f50e054c59f8736f8`; the trigger script is `37a5cc3a84937d8946557dc6b2e12b3a730ae92c24e6ebea61f18ca45c3246c6`, and both script metas are unchanged;
- rendered fault injection proves the call paths without production counters: both far directions succeed while each legacy `sourceRoom` is an impossible sentinel, so they must use the canonical Passage; both subsequent near directions succeed after the Passage fields are temporarily null, so the legacy fallback remains live. Pending approach state, event-before-warp order, audio-after-movement timing, single volume-binding reuse, synchronous near behavior, exact arrivals, activation, and component counts remain unchanged;
- caller-cutover gates pass: canonical contracts `5/5`, architecture foundation `17/17`, exact static ownership `1/1`, and rendered lifecycle `4/4`. The full suite remains `249` discovered / `203` passed / the identical `46` known failures. The architecture guard adds no debt; inventory is 112 runtime files / 48,628 lines with all smell totals and 48 direct `MonoBehaviour` declarations unchanged;
- canonical traversal now rejects missing or non-finite arrival data before mutation, uses the sole existing `SetCurrentRoom` assignment/event, and then calls `PointClickPlayerMovement.TryWarpToExact`. Exact warp validates collision/walkability without clamping, projection, or a path from the previous room; it emits no movement callbacks and adds no state, event, component, service, discovery, or audio owner;
- the canonical Drawing Room arrival remains `(5.267176, -2.104616)`. A rendered shared-point search showed the old neutral legacy Entrance sample `(-7.703568, -2.000136)` lies on incompatible layout edges; the authored canonical Entrance arrival is therefore `(-7.75, -2.22)`, a collision-safe point inside the reciprocal `145`-pixel activation boundary at `1366x768`, `1440x1080`, `1920x1080`, and `2560x1080`;
- repeated-source and four-aspect gates prove both canonical directions stop exactly at their authored anchors. Deliberately nulling both trigger Passage fields still executes the byte-preserved source-sensitive Inspector sampler, including the old neutral Entrance sample, so canonical arrival ownership does not alter any unmigrated route;
- the arrival slice changes one Gameplay scalar only: Passage `4100000012`'s arrival coordinate. No YAML document is added, removed, or reordered; `DoorTriggerNavigation`, Passage/interface contracts, every `.meta` file, every GUID, and the legacy `MoveThroughInspectorDoor`, `PlacePlayerAtDestinationDoor`, `SetCurrentRoom`, and `FindArrivalDoorTrigger` bodies remain byte-identical;
- arrival gates pass: canonical contracts `5/5`, architecture foundation `17/17`, exact static ownership `1/1`, and rendered lifecycle `4/4`. The full suite remains `249` discovered / `203` passed / the exact same `46` baseline failures, with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The architecture guard and serialized-reference scan remain clean; inventory is 112 runtime files / 48,691 lines with unchanged smell totals and 48 direct `MonoBehaviour` declarations; Y-axis occlusion reports zero hard errors and the same 38 tracked design-required findings;
- the old sampler-derived approach vectors failed the exact authored-point gate, so the two Passage documents now use one collision-safe logical point per room side: forward approach equals reverse arrival at Entrance `(-7.75, -2.22)`, and reverse approach equals forward arrival in Drawing Room `(5.267176, -2.104616)`. Only those two serialized scalar values change; no document, component, reference, `.meta`, GUID, or data asset changes;
- `RoomNavigationManager.CanTraverse` rejects missing or non-finite approach data. `DoorTriggerNavigation` dispatches null Passage fields through the byte-preserved sampler and bound Passage fields through a fail-closed canonical selector that reads only `ApproachAnchor.LogicalPosition`, ignores preferred-click projection, and enforces the existing screen-distance limit before the unchanged `TrySetDestination` call;
- rendered production-path gates prove both shared anchors are finite, exact, path-reachable, source-independent, and inside the `145`-pixel envelope at `1366x768`, `1440x1080`, `1920x1080`, and `2560x1080`. A malformed bound anchor starts no movement/audio/state, and left/center/right preferred clicks converge to the same authored point in both directions;
- canonical far order remains `ArrivedAtDestination` -> `MovementStopped` -> audio -> room event -> exact reciprocal arrival. Null-Passage compatibility fields complete the same far event/audio sequence in both directions while retaining the legacy sampled destinations and arrivals, including forward `(5.231221, -2.002137)` and reverse `(-7.703568, -2.000136)`; their near round trip remains synchronous. Pending subscriptions clear, near canonical activations add no movement callbacks, and every path reuses the same single `GameAudioSourceVolume` binding;
- approach gates pass: the combined static/contract/foundation set is `23/23`, rendered lifecycle is `4/4`, and the full suite remains `249` discovered / `203` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The architecture guard adds no debt; inventory is 112 runtime files / 48,741 lines with unchanged smell totals and 48 direct `MonoBehaviour` declarations; serialized references are unchanged; and Y-axis occlusion remains at zero hard errors plus the same 38 tracked set-piece findings;
- the first route is now automatically certified as a canonical compatibility-seam template, not as final target navigation; human doorway/camera/visibility review remains pending. The checked [template](PASSAGE_COMPATIBILITY_TEMPLATE.md) freezes its definitions/views/passages, shared anchors, direct dependencies, exact caller/service ownership, complete canonical and null-Passage round trips, legacy body hashes, Unity serialization surface, and repeatable one-pair gate. Interaction/proximity/prompt/audio, room activation/camera compatibility, and the future `InteractionRouter`, `PassageInteraction`, `RoomViewService`, and `CameraService` transfers remain explicit debt;
- the machine-checked [Gameplay inventory](RemainingRouteInventory.csv) contains exactly 45 unique trigger records and door IDs in 23 connectivity groups: 12 complete template triggers in 6 reciprocal pairs, 2 tests-only characterized Group `06` triggers, 26 queued triggers in 13 reciprocal pairs, 2 blocked one-way triggers, and a blocked three-trigger GEH/Upper Gallery stair cluster. A separate read-only hierarchy audit confirms all 45 already descend from the correct source room root; Butlers Pantry is the seventh canonical room, leaving 12 missing RoomDefinitions and RoomViews to introduce exactly once before closing the Nursery/Blue Bedroom cycle;
- topology exceptions are frozen instead of forced: components `1615236111` (GEH Rear -> Library) and `2300000159` (Service Corridor -> Billiard Room) have no scene reverse; GEH/Upper Gallery has two outbound stair triggers `106972347`/`2300000069` and one return `2300000194`, which the strict one-to-one reverse contract cannot represent. Three bottom-edge routes and four runtime-inferred stairway owners retain their exact legacy interaction profiles;
- a separate read-only repository audit proves parallel navigation data is not authoritative: Gameplay's 45 triggers share only 30 IDs and 22 exact route triples with the 44-entry `doors.txt`; the text graph also has 15 scene-only IDs, 14 data-only IDs, and 8 shared-ID endpoint/alias mismatches. `DoorCameraSequence` owns no active serialized trigger because all 58 triggers across Gameplay, MainMenu, and the two unused Drawing Room prefab variants disable it. Chapter 2 guest-exit BFS still consumes legacy trigger strings, so those fields cannot be pruned during incremental passage migration;
- certification gates pass: the new manifest/template/topology fixture is `3/3`, combined manifest/static/contract/foundation safety is `26/26`, rendered lifecycle is `4/4`, and the full suite is `252` discovered / `206` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The architecture guard adds no debt; runtime inventory remains 112 files / 48,741 lines with unchanged smells and 48 direct `MonoBehaviour` declarations; Y-axis occlusion remains zero hard errors / 38 tracked findings. Production serialized references are unchanged; generated script-reference inventory adds only the new certification test at zero references and preserves all prior 154 rows;
- group `01` static characterization freezes Drawing trigger `2300000099` on RectTransform `2300000096` and Music trigger `2300000089` on RectTransform `2300000086`, their exact room-root/Doors ancestry, rectangles, standard Door profile, null canonical/direct edges, and four-component owners at that checkpoint. No Music RoomView or Passage existed at characterization. `Chapter2GuestPanicController.leftExitTarget` remains the Drawing-to-Music RectTransform `2300000096`, with the exact name `DoorTrigger_DrawingRoom_MusicRoom`;
- the rendered legacy round trip passes from far and near starts in both directions. At neutral `1366x768`, Drawing-side approach/reverse arrival is `(-7.106010, -1.508934)` and Music-side forward arrival/reverse approach is `(-7.737432, -3.180156)`. Far movement retains `ArrivedAtDestination` -> `MovementStopped` -> audio -> room event -> sampled destination warp; near traversal adds no movement callbacks. The one door-volume binding is reused, pending subscriptions clear, prompt/cursor state releases, the correct room content and trigger direction activate, and camera background plus active content/stage follow Drawing -> Music -> Drawing;
- the rendered aspect samples remain legacy evidence rather than canonical data: Drawing/Music are `(-6.152483, -1.306456)` / `(-6.699176, -2.753424)` at `1440x1080` and `(-7.104277, -1.508566)` / `(-7.735544, -3.179381)` at `1920x1080`. At `2560x1080`, Drawing forward approach, Music arrival/reverse approach, and Drawing reverse arrival are `(-8.188315, -1.742414)`, `(-8.932235, -3.671232)`, and `(-8.188308, -1.741942)`. Every preferred-click sample remains finite and inside the existing activation boundary. The rendered tests synchronize 2D physics after changing GameView layout; this removed a test-only stale PolygonCollider transform that had briefly appeared as a second sampler result without changing production code or the tight reviewed envelopes;
- the Drawing/Music characterization changes tests, its two manifest statuses, and architecture documentation only. Production scripts, Gameplay/MainMenu, prefabs, data assets, all existing `.meta` files/GUIDs, canonical bindings, and serialized production references remain unchanged;
- group `01` characterization gates pass: the manifest fixture is `4/4`, the combined manifest/static/contract/foundation set is `27/27`, and rendered lifecycle is `5/5`. The accepted complete rerun is `254` discovered / `208` passed / the exact same `46` baseline failures, with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. A full-order gate exposed stale 2D physics transforms after synthetic GameView resizing in both aspect probes; explicit test-side synchronization restored the original tight values and exact anchor-walkability checks without any production change. The architecture guard and generated serialized-reference report remain unchanged at 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations / 155 script rows, and Y-axis occlusion remains zero hard errors plus the same 38 tracked findings;
- `Room_MusicRoom.asset` is now the unique definition for stable ID `room.music-room`, with asset GUID `c0f34d74a30db58bb2b87b6ec316120b`, display/legacy name `Music Room`, scene-authoritative background texture GUID `028084782cdcf3d4ab3b596624c8b7c5`, schema `1`, and a null perspective profile;
- reciprocal assets `Passage_DrawingRoom_MusicRoom.asset` (GUID `3167361ca4c671298c0e84f43320619b`, stable ID `passage.drawing-room.music-room`, legacy ID `DrawingRoom_MusicRoom`) and `Passage_MusicRoom_DrawingRoom.asset` (GUID `01544de8f55723585d60e5c0915345fd`, stable ID `passage.music-room.drawing-room`, legacy ID `MusicRoom_DrawingRoom`) are Door / `Open Door` definitions with exact swapped Drawing/Music endpoints and direct reciprocal asset links;
- `GameDatabase.asset` appends Music Room and those two directed passages exactly once after the prior four entries, for seven ordered definitions total. Focused canonical-data plus staged-manifest tests pass `9/9`. No scene, prefab, runtime script, or existing `.meta`/GUID changes; the only new metas belong to the three new assets. No Music RoomView, Passage component, direct trigger edge, canonical caller, or approach/arrival anchor is authored in this data-only gate;
- group `01` data gates pass: canonical-data plus manifest `9/9`, combined manifest/static/contract/foundation safety `27/27`, rendered lifecycle `5/5`, and full suite `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The architecture audit remains 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations with unchanged smell totals; Y-axis occlusion remains zero hard errors / 38 tracked findings. The 155-row serialized-reference report changes only `RoomDefinition` from 2 to 3 data assets and `PassageDefinition` from 2 to 4 data assets; scene instances remain RoomView 2 / Passage 2 and Gameplay remains byte-identical;
- At the completed passive-view checkpoint, Music root GameObject `354156755` gained exactly RoomView document `4100000003`, which references definition GUID `c0f34d74a30db58bb2b87b6ec316120b` and existing co-located `RoomContentGroup` `2102000001`. GameRoot registers `4100000003` exactly once after RoomViews `4100000001` and `4100000002`; existing Drawing view `4100000002` remains the Drawing-to-Music source owner;
- the view-only scene diff is exactly 16 added YAML lines: one component-list reference in the existing Music GameObject document, one `sceneBehaviours` reference in the existing GameRoot document, and one new 14-line RoomView document. No document is removed, all pre-existing document order is preserved, and no prefab, data asset, runtime script, `.meta`/GUID, trigger document, or Passage changes. Gameplay advances from `6,011` to `6,012` YAML documents and contains exactly 3 RoomViews / 2 Passages;
- focused canonical plus staged-manifest gates pass `9/9`, combined safety passes `27/27`, and rendered lifecycle passes `5/5`. The lifecycle proof confirms Music RoomView has GameContext, its definition/content graph is valid, and `IsVisible` mirrors root `activeSelf` through far and near Drawing -> Music -> Drawing traversal. The same legacy manager still owns room activation and camera/background/active-stage changes; the RoomView is the sole added component and creates no state, event, lifecycle command, activation writer, or presentation writer;
- the full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations with unchanged smell totals; Y-axis validation remains zero hard errors / 38 tracked findings; the 155-row serialized-reference inventory changes only RoomView scene instances from 2 to 3;
- Passage `4100000013` is now co-located on Drawing trigger-owner GameObject `2300000095` (trigger component `2300000099`), bound to definition GUID `3167361ca4c671298c0e84f43320619b`, source RoomView `4100000002`, and reverse Passage `4100000014`. Its passive approach/arrival data is `(-7.106010, -1.508934)` / `(-7.737432, -3.180156)`;
- Passage `4100000014` is now co-located on Music trigger-owner GameObject `2300000085` (trigger component `2300000089`), bound to definition GUID `01544de8f55723585d60e5c0915345fd`, source RoomView `4100000003`, and reverse Passage `4100000013`. Its passive approach/arrival data is `(-7.737432, -3.180156)` / `(-7.106010, -1.508934)`. These shared reciprocal values satisfy staged finite-data certification only; no production caller consumes them before the later `arrival-owned` and `approach-owned` gates;
- GameRoot registers both additions once after the template pair, preserving the exact Passage order `4100000011`, `4100000012`, `4100000013`, `4100000014`. The passage-only scene delta is exactly 42 added YAML lines: two GameRoot list references, one component-list reference on each existing trigger-owner GameObject, and two new 19-line Passage documents. Gameplay advances from `6,012` to `6,014` YAML documents, retains 3 RoomViews, and advances from 2 to 4 Passages. No document is removed and every pre-existing document retains its relative order;
- both `DoorTriggerNavigation` documents remain byte-unmodified with `navigationManager`, `player`, `doorOpenAudioSource`, and `doorOpenSoundCatalog` null and `canonicalPassage` absent. No direct dependency, canonical caller, runtime script, prefab, data asset, existing `.meta`/GUID, or unrelated scene content changes. Focused canonical/manifest tests pass `9/9`, combined safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations, and Y-axis validation remains zero hard errors / 38 tracked findings. The 155-row serialized-reference report keeps RoomView at 3 and advances only Passage from 2 to 4;
- At the completed direct-dependency checkpoint, trigger documents `2300000099` (Drawing -> Music) and `2300000089` (Music -> Drawing) each gained exactly navigation manager `1878886997`, Player Transform `81962843`, shared door AudioSource `2201000013`, and door catalog `{fileID: 11400000, guid: 9a77542e25184fbc945d6a79f77007e7, type: 2}`. Their stairway catalogs remained null and `canonicalPassage` remained absent at that checkpoint, so traversal still used the unchanged legacy path;
- the dependency-only scene diff is exactly eight removed null-field lines plus eight replacement reference lines across those two trigger documents. No YAML document is added, removed, or reordered; Gameplay remains `6,014` documents / 3 RoomViews / 4 Passages, with every Passage definition/source/reverse/anchor, owner edge, GameRoot registration/order, and unrelated document unchanged. Nulling only those eight references exactly reproduces the prior scene. Focused canonical/manifest tests pass `9/9`, combined safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations, Y-axis remains zero hard errors / 38 tracked findings, and the 155-row serialized-reference report is unchanged;
- a pre-caller audit found that the global completed-route code would otherwise consume arrival and approach anchors together as soon as any later trigger gained a canonical caller. The temporary `PassageAnchorMigrationStage` therefore restricts every directed Passage to the monotonic sequence `LegacySampling = 0`, `AuthoredArrival = 1`, `AuthoredAnchors = 2`; invalid values or reciprocal-stage mismatches fail both configuration and traversal. Complete GEH/Drawing Passages explicitly serialize `2`, while Drawing/Music explicitly serializes `0`;
- mode 0 reuses the existing legacy approach sampler and existing `PlacePlayerAtDestinationDoor` after canonical identity/current-room validation; mode 1 changes only exact arrival; mode 2 also changes approach. The scene adds exactly four scalar fields to the four existing Passage documents and changes no document count/order, object, component, reference, anchor value, trigger caller/dependency, GameRoot entry, prefab, data asset, or `.meta`. A rendered poison test binds Drawing/Music callers only in memory, replaces all four passive coordinates with remote finite values, proves the frozen far/near legacy lifecycle in both directions, and restores all mutations in `finally`;
- rollout-preparation gates pass focused `9/9`, combined safety `27/27`, rendered lifecycle `5/5`, and full suite `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture is 112 runtime files / 48,787 lines / 48 direct `MonoBehaviour` declarations with unchanged smell totals; serialized references remain 155 rows; Y-axis remains zero hard errors / 38 tracked findings. The 46 temporary runtime lines have an explicit deletion gate after every Passage reaches stage 2;
- Group `01` caller binding adds only `canonicalPassage: {fileID: 4100000013}` to trigger `2300000099` and `canonicalPassage: {fileID: 4100000014}` to trigger `2300000089`. The two-line scene delta changes only those trigger documents; all 6,014 document IDs/order, four Passage documents/stages/anchors, dependencies, topology, GameRoot, SceneRoots, assets, prefabs, metas, and runtime code remain unchanged. Canonical callers advance from 2 to 4 while direct bindings remain 4 and the other 41 triggers retain both null edges;
- At the completed canonical-caller checkpoint, both Group `01` Passages remained stage 0, so their real serialized callers traversed through canonical identity while the byte-preserved legacy samplers still owned approach and arrival. The poison-anchor round trip passed far and near in both directions with exact event/audio/camera/visibility/prompt/story behavior, and restored the original non-null caller identities plus all coordinates and subscriptions in `finally`. Focused `9/9`, combined safety `27/27`, rendered lifecycle `5/5`, and full suite `254` discovered / `208` passed / the exact same `46` baseline failures all passed with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remained 112 runtime files / 48,787 lines / 48 direct `MonoBehaviour` declarations, serialized references remained 155 rows, and Y-axis remained zero hard errors / 38 tracked findings;
- the initial Group `01` arrival-ownership run failed safely before commit: exact warp rejected Drawing `(-7.106010, -1.508934)`. An explicit physics-sync retry and a settled-layout query both remained false; geometry then proved the rounded legacy sample lay exactly on the Drawing floor-polygon edge. The legacy placement path had accepted the visual outcome only through its clamped fallback, so production exact-warp validation was correct and was not weakened;
- reciprocal anchor calibration moves Drawing to `(-7.16, -1.78)` and Music to `(-7.94, -3.27)`. Both remain walkable, blocker-free, and within the reciprocal `145`-pixel trigger envelope across `1366x768`, `1440x1080`, `1920x1080`, and `2560x1080`; both also pass exact warp and reciprocal proximity at widest-aspect maximum zoom `1.22`. The runtime legacy approaches retain every prior characterized value because stage 1 does not consume approach data;
- Group `01` Passages `4100000013` and `4100000014` now serialize `AuthoredArrival = 1` together. Their scene delta from caller-bound is limited to four anchor-vector/`logicalPosition` line replacements (eight serialized coordinate-scalar occurrences) and two stage-scalar replacements in those two existing Passage documents. Gameplay remains exactly 6,014 documents / 3 RoomViews / 4 Passages with unchanged IDs/order and byte-identical `SceneRoots`; no caller, dependency, definition, topology, component, GameRoot entry, runtime source, prefab, asset, `.meta`, or GUID changes;
- arrival-owned gates pass focused `9/9`, combined safety `27/27`, rendered lifecycle `5/5`, and full suite `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard and generated audits pass at 112 runtime files / 48,787 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; both Y-axis audits remain zero hard errors / 38 tracked findings; all 155 C# scripts retain metas, all 7,316 asset meta GUIDs are unique, and no existing meta changed;
- the separate Group `01` stage-1 approach preflight makes no production or serialized change. Drawing `(-7.16, -1.78)` and Music `(-7.94, -3.27)` each pass the exact non-projected collision/path evaluator from two distinct resolved far starts at all four rendered aspects and at widest-aspect maximum zoom `1.22`, for 20 total probes. Every start is outside the trigger threshold, every future approach is inside it, direct canonical-candidate validation returns the exact future anchor, live traversal dispatch still returns a distinct legacy sample, and player position/idle state restore in `finally`;
- approach-preflight lifecycle remains `5/5`, and the full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Both Drawing/Music Passages remain `AuthoredArrival = 1`, inventory remains `arrival-owned`, and scene/runtime/prefab/asset/meta/GUID/serialized-reference state is byte-unchanged;
- the first maximum-zoom production traversal exposed a same-call collider-cache bug before the approach-owned slice was accepted. Unity's automatic 2D transform synchronization is disabled, so a destination room last displayed at zoom `1.22` could be reactivated and resized to default while its collider cache still held the old scale. Prerequisite commit `71b9848f` synchronizes 2D transforms immediately before destination-floor refresh and exact authored placement. The fix changes no serialization, scene, GUID, API, state owner, or normal route semantics; it only makes the placement query see the destination room's current transform;
- the current Group `01` approach-owned scene slice replaces only `anchorMigrationStage: 1` with `anchorMigrationStage: 2` in Passage documents `4100000013` and `4100000014`. Both calibrated points are therefore authoritative in both roles: Drawing `(-7.16, -1.78)` is Drawing-side approach/Music-to-Drawing arrival, and Music `(-7.94, -3.27)` is Music-side approach/Drawing-to-Music arrival. Relative to the prerequisite fix, canonical callers, direct dependencies, definitions, coordinates, owner/component/document topology, GameRoot, SceneRoots, runtime code, prefabs, assets, metas, GUIDs, and serialized-reference totals remain unchanged;
- the approach-owned acceptance contract evaluates exact production reachability and stage-2 dispatch from two distinct far starts per room at all four rendered aspects and again at `2560x1080` maximum zoom `1.22`. One real `ActivateDoor` movement per direction and aspect, both maximum-zoom directions, and a symmetric forward replay after Music was itself left at maximum zoom reach the exact authored approach and reciprocal arrival. Near synchronous canonical traversal, pair-local null-Passage far/near compatibility, callbacks, event/audio order, camera/background/visibility, prompt/cursor, Chapter 2 guest-panic target identity, cleanup, and the complete lifecycle baseline remain exact;
- approach-owned gates pass focused `9/9`, combined safety `27/27`, rendered lifecycle `5/5`, and full suite `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard and generated audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; both Y-axis audits remain zero hard errors / 38 tracked findings;
- the no-scene Group `01` complete certification advances only the two Drawing/Music inventory rows from `approach-owned` to `complete`, changes their notes to `template-certified`, and updates matching expectations/current-state documentation. Both Passage documents remain `AuthoredAnchors = 2`. This slice changes no scene, runtime source, prefab, data asset, `.meta`, GUID, serialized reference, caller, dependency, coordinate, GameRoot entry, YAML document identity/order, SceneRoots, or topology edge. Focused gates pass `9/9`, combined safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; both Y-axis audits remain zero hard errors / 38 tracked findings;
- Group `02` static characterization freezes Music -> Library trigger `552135204` on owner/RectTransform `552135202`/`552135203` and Library -> Music trigger `2300000079` on `2300000075`/`2300000076`, their exact root/Doors ancestry, sibling arrays, rectangles, standard Door profiles, null serialized dependencies/callers, and zero Passage components. Music retains RoomView `4100000003`; Library retains only legacy content. The Library chair/lamp blocker and both room PlayerBoundary polygons remain exact. Runtime lookup resolves the existing manager, Player, shared door source, and door catalog without serializing those fields; Chapter 2 hide anchors remain `Ch2_Hide_Guest01` at `(-255, -181)` in Library and `Ch2_Hide_Guest02` at `(306, -162)` in Music and follow active-room visibility;
- primary `1366x768` far Music -> Library traversal starts from completed Group `01` arrival `(-7.94, -3.27)`, approaches `(7.439471, -2.846709)`, and lands at source-sensitive far Library result `(-7.287828, -2.936489)`. From Library `(0, -2)`, reverse approach/arrival are `(-7.244175, -2.799095)` / `(7.439471, -2.846709)`. The immediate near forward instead lands at `(-7.244175, -2.799095)`, while near reverse returns to `(7.439471, -2.846709)`. Null/left/center/right candidates are identical within `0.0001` in both directions. Common-start forward/reverse approach pairs at `1440x1080`, `1920x1080`, and `2560x1080` are `(6.44117, -2.464709)` / `(-6.272121, -2.423502)`, `(7.43764, -2.846008)` / `(-7.242414, -2.798415)`, and `(8.625211, -3.301598)` / `(-8.3582, -3.229418)`. At `2560x1080` maximum zoom `1.22`, forward approach/arrival are `(8.625211, -3.301599)` / `(-8.3582, -3.229418)` and reverse approach/arrival are `(-8.356381, -3.228665)` / `(8.676323, -3.178201)`. Both locked runs produce the same seven-line observation-message SHA-256, with `\n` separators and no trailing newline: `f2a4b15ccee94282032102f6b6c93a2673444d5d8d099a8bd1f24be6190fc2ab`;
- Group `02` characterization changes only two tests, two inventory statuses/notes, and current architecture evidence. It changes no runtime source, scene, data asset, prefab, `.meta`, GUID, serialized reference, hierarchy, geometry, profile, topology, dependency, caller, or anchor. Focused gates pass `10/10`, combined safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256` discovered / `210` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; both Y-axis audits remain zero hard errors / 38 tracked findings;
- Group `02` data authoring adds `Room_Library.asset` at GUID `8da3a3e936712e7b9f534786110323e4`, stable ID `room.library`, primary/display `Library`, scene-authoritative background GUID `0a85e4fdd73e4714fabde63002a457e7`, schema `1`, and a null perspective profile. `Passage_MusicRoom_Library.asset` at GUID `aefe77f20874eb81b83fccb6ff5b8046` owns stable/legacy IDs `passage.music-room.library` / `MusicRoom_Library`; `Passage_Library_MusicRoom.asset` at GUID `3a641d5febbfd7aec481ada678ba9fe4` owns `passage.library.music-room` / `Library_MusicRoom`. Both are Door / `Open Door`, swap exact existing-Music/new-Library endpoints, and link directly to one another;
- `GameDatabase.asset` appends exactly Library, Music -> Library, and Library -> Music once after the prior seven definitions, advancing `7 -> 10` in logical room/forward/reverse order. This data-only slice adds only those three assets and their new `.meta` files and changes the database plus matching tests/manifest/docs. It changes no production/runtime source, scene, prefab, existing `.meta`/GUID, RoomView, Passage component, direct trigger dependency, canonical caller, or authored anchor. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; both Y-axis audits remain zero hard errors / 38 tracked findings;
- Group `02` view binding adds passive RoomView `4100000004` to existing Library root `1367921344`, binds definition `8da3a3e936712e7b9f534786110323e4` and existing content `2102000003`, and registers the view once after Music RoomView `4100000003` and before Passage `4100000011`. The scene delta is exactly 16 added YAML lines and no removals: one Library component reference, one GameRoot reference, and one 14-line RoomView document. Gameplay advances `6,014 -> 6,015` documents and `3 -> 4` RoomViews while retaining four Passages. No hierarchy, Transform, SceneRoots, trigger field, Passage, runtime source, prefab, data asset, `.meta`, collider, set-piece, occlusion, activation, or camera ownership changes. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; RoomView scene references advance from three to four, and both Y-axis audits remain zero hard errors / 38 tracked findings;
- Group `02` passive Passage binding adds Music -> Library Passage `4100000015` and Library -> Music Passage `4100000016` to the existing trigger owners and registers them after Passage `4100000014`. The components bind the accepted definitions/RoomViews, directly reverse-link, and serialize Music `(7.439471, -2.846709)` and Library `(-7.244175, -2.799095)` reciprocally at `LegacySampling = 0`. These finite fields are validation data only; the distinct source-sensitive far Library result `(-7.287828, -2.936489)` remains legacy-authoritative. The current schema adds exactly 44 YAML lines and removes none: two owner refs, two GameRoot refs, and two 20-line Passage documents. Gameplay advances `6,015 -> 6,017` documents and `4 -> 6` Passages while retaining four RoomViews. Both legacy trigger documents and their null dependencies/callers remain byte-identical; no runtime source, prefab, data asset, `.meta`, hierarchy, collider, set-piece, occlusion, activation, or camera ownership changes. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; Passage scene references advance from four to six, and both Y-axis audits remain zero hard errors / 38 tracked findings;
- Group `02` dependency binding changes only four serialized references in each trigger document: navigation manager `1878886997`, Player Transform `81962843`, door AudioSource `2201000013`, and door catalog GUID `9a77542e25184fbc945d6a79f77007e7`. Stair catalogs remain null and canonical callers remain absent. The scene delta is exactly eight replacement lines added and eight removed, with 6,017 documents / four RoomViews / six Passages unchanged. Passages `4100000015/16`, their stage-0 coordinates/topology, GameRoot, owners, hierarchy, colliders, set pieces, occlusion, activation/camera ownership, runtime source, prefabs, assets, and `.meta` files remain unchanged. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The seven-line observation hash changes only the dependency-profile tokens to `cb6e7c14702f7e9adcedfda1d5a0fba5f14462581c607d203c53152b5a3b40a7`; all coordinate evidence remains exact. Architecture remains 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows, and both Y-axis audits remain zero hard errors / 38 tracked findings;
- Group `02` caller binding adds only `canonicalPassage: 4100000015` to trigger `552135204` and `canonicalPassage: 4100000016` to trigger `2300000079`, for an exact two-line additive scene delta. Gameplay remains 6,017 documents / four RoomViews / six Passages; direct dependencies remain six and canonical callers advance from four to six. Both Passages remain stage 0 with exact coordinates/topology. A rendered proof poisons/restores all four passive coordinates by more than 100 units while callers remain bound, and a separate pair-local fallback proof nulls/restores both callers. The untouched primary retains first-entry far Library `(-7.287828, -2.936489)` versus near `(-7.244175, -2.799095)`; both post-primary proofs retain the correct history-sensitive `(-7.244175, -2.799095)` result and eight-event/audio lifecycle. No dependency, Passage document, GameRoot, topology, hierarchy, collider, set-piece, occlusion, activation/camera ownership, runtime source, prefab, asset, or `.meta` change. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The seven-line observation hash changes only `callers=null` to `callers=bound`, to `46d857f57e7e41d6a7facaa3a39d0f97bf6dd4996d7f7d10e3da809d8eced2b7`. Architecture remains 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows, and both Y-axis audits remain zero hard errors / 38 tracked findings;
- the bounded Group `02` ownership/certification batch represents arrival preflight, arrival ownership, approach preflight, approach ownership, and complete certification together. Passages `4100000015` and `4100000016` both advance from stage `0` to `AuthoredAnchors = 2`; Music `(7.714471, -3.121709)` and Library `(-7.744175, -3.059095)` become authoritative in both reciprocal roles. Music -> Library retains `145`-pixel proximity. Library -> Music changes pair-locally `145 -> 149` because exhaustive rendered search found the structurally safest invariant Library point requires `148.573` pixels at the worst profile; four pixels is the minimum whole-pixel compatibility calibration. Apart from the two reciprocal anchor vectors, two stage scalars, and one proximity scalar, the batch changes no GUID, `.meta`, topology, caller, dependency, GameRoot registration, serialized object identity, or runtime source. The resulting manifest contains 6 complete, 34 queued, 2 blocked-one-way, and 3 blocked-parallel rows, and all six scene Passage components are stage 2. Focused contracts pass `10/10`, combined safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings;
- Group `03` Library/Ballroom's first bounded batch combines characterization, Ballroom data authoring, Ballroom RoomView binding, passive reciprocal Passage binding, and direct-dependency binding. Characterization locks Library `L0 = (7.465074, -2.665671)`, Ballroom `B0 = (-8.107888, -2.079877)`, and the distinct first far Ballroom arrival `(-8.144631, -2.043134)` over four rendered aspects and widest-aspect maximum zoom. Joining its observation lines with `\n` and no trailing newline yields SHA-256 `39f311ffa5df816fe7a9b9d510bda7451109c35cb119b2540b26f5128a0e35db`;
- Ballroom and its reciprocal Passage definitions advance `GameDatabase` from 10 to 13 entries. The passive Ballroom RoomView plus two reciprocal stage-0 Passages advance Gameplay to 6,020 documents / five RoomViews / eight Passages, with stage distribution `2 / 0 / 6` for stages `0 / 1 / 2`. Both trigger thresholds remain `145`; both direct-dependency sets are bound, both canonical callers remain null, and production remains on the unchanged legacy sampling path. Inventory is 6 complete, 2 dependencies-bound, 32 queued, 2 blocked-one-way, and 3 blocked-parallel. The batch changes no runtime source, prefab, or existing `.meta` file. The dependency-bound observation hash is `ab7f0108d462d26d45571923d33ff88469a98fb3b8c5a192bc9142a54fae3017`; targeted/focused/safety/lifecycle gates pass `1/1`, `11/11`, `29/29`, and `7/7`; the full suite is `258/212/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; and architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings;
- Group `03` caller binding adds only `canonicalPassage: 4100000017` to Library -> Ballroom trigger `2300000084` and `canonicalPassage: 4100000018` to Ballroom -> Library trigger `2101000025`, for an exact `+2/-0` scene delta. Gameplay remains 6,020 documents / five RoomViews / eight Passages, with stage distribution `2 / 0 / 6`; direct dependencies remain eight bound / 37 fully null, and canonical callers advance to eight bound / 37 null. Both thresholds remain `145`, and both new Passages remain `LegacySampling = 0`, so canonical identity delegates approach and arrival to the byte-preserved legacy samplers. Rendered proofs poison/restore all four passive coordinates and independently null/restore only this pair's callers while retaining the frozen movement, room, audio, prompt, camera, visibility, and cleanup behavior. The seven-line observation hash changes only `callers=null` to `callers=bound`, to no-trailing-newline SHA-256 `22e969882f5c3481bd0957a46db3008ea556726bf395d30c1997f2f775e17118`. Inventory is 6 complete, 2 caller-bound, 32 queued, 2 blocked-one-way, and 3 blocked-parallel. No dependency, coordinate, Passage document, threshold, GameRoot entry, topology, hierarchy, collider, set piece, occlusion, runtime source, prefab, asset, `.meta`, or GUID changes. Targeted/focused/safety/lifecycle gates pass `1/1`, `11/11`, `29/29`, and `7/7`; the full suite remains `258/212/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; and architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings;
- the bounded Group `03` ownership/certification batch represents arrival preflight, arrival ownership, approach preflight, approach ownership, and complete certification together. Passages `4100000017` and `4100000018` advance from stage `0` to `AuthoredAnchors = 2`; Library `(7.95, -3)` and Ballroom `(-8.607888, -2.439877)` become authoritative in both reciprocal roles while both trigger thresholds remain `145`. The points pass exact non-projected path/walkability checks from two far starts across four rendered aspects and widest-aspect maximum zoom; their worst measured trigger distances are `123.877` and `106.234` pixels. Production traversal, near synchronous traversal, pair-local null-caller fallback, callbacks, audio/event order, camera/background/visibility, prompt/cursor cleanup, and room state remain exact. Apart from two reciprocal anchor vectors and two stage scalars, the batch changes no GUID, `.meta`, topology, caller, dependency, threshold, GameRoot registration, serialized object identity, runtime source, prefab, or data asset. The seven-line authored observation hash is no-trailing-newline SHA-256 `ba483637abce4dc40b5c2910ff90a8ac9c364f686e1a7f4b45fd0c766b82848f`; the inventory contains 8 complete, 32 queued, 2 blocked-one-way, and 3 blocked-parallel rows, and all eight scene Passages are stage 2. Targeted/focused/safety/lifecycle gates pass `1/1`, `11/11`, `29/29`, and `7/7`; the full suite remains `258/212/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; and architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings;

## Validation still requiring human/golden review

- visual confirmation of the Butler-to-painted-entrance-door proportion in the interactive Game view;
- complete manual Chapter 1 and Chapter 2 golden-path smoke tests;
- dialogue/subtitle/voice, modal input, ambience, camera-shake and lighting visual/audio checks;
- visual confirmation that the Drawing Room tea table occludes the Butler/guests correctly and retains the accepted no-walk footprint;
- visual confirmation that the Drawing Room purple armchair occludes the Butler/guests correctly and retains the accepted no-walk footprint;
- visual confirmation that the Drawing Room purple sofa occludes the Butler/guests correctly and retains the accepted no-walk footprint;
- human visual confirmation that walking to and landing on the shared doorway points `(5.267176, -2.104616)` and `(-7.75, -2.22)` place the Butler's feet on the intended side of each painted doorway;
- human visual confirmation that Drawing/Music arrivals `(-7.16, -1.78)` and `(-7.94, -3.27)` place the Butler's feet naturally at the painted doorway sides at default and maximum zoom;
- human visual confirmation that Music/Library arrivals `(7.714471, -3.121709)` and `(-7.744175, -3.059095)` place the Butler's feet naturally at the painted doorway sides at default and maximum zoom;
- a player build and save/load trace after `SaveService` exists.

## Compatibility adapters still present

The following remain intentionally because their replacements have not yet passed behavioral migration and deletion proof:

- runtime manager/bootstrap creation paths;
- current room/navigation implementation and parallel navigation data;
- legacy and point-click movement paths;
- duplicate actor presentation/scale/sort writers;
- chapter god controllers;
- feature-created UI;
- static/global audio and session state;
- runtime/editor lighting mutation paths.

## Completed Group 04 pre-edit gate

Before the foundation batch, Group `04` Grand Entrance Hall <-> Dining Room was characterized without a production change.

That gate froze both existing trigger owners, hierarchy, profiles, rectangles, dependencies/callers, far and near reciprocal traversal, audio/event order, camera/background/visibility, prompt/cursor cleanup, collision geometry, and source-sensitive arrival behavior across every rendered aspect and widest-aspect maximum zoom before Dining data or scene bindings were authored.

## Group 04 bounded foundation result

The characterization and foundation gates are now accepted through `dependencies-bound`. Dining definition `0eb3282aded74fc4889f4321df8c5258` retains background `004ab4cca930d0387892725fe69b4f72` and non-null profile `a63248cfbd6b4a72af45c62cff7e94d0`; reciprocal Passage definitions use GUIDs `30b5c4cfef2b45e2970b4cdac4b7a3ef` and `94e16c6eca714188bced397612d48fff`. RoomView `4100000006` and Passages `4100000019` / `4100000020` preserve Entrance `(8.205841, -1.986406)` and Dining `(-6.692237, -1.380209)` as passive stage-0 values. Both direct dependency sets are bound, both canonical callers remain null, and behavior remains legacy. Current totals: 6,023 scene documents, 16 database definitions, six RoomViews, ten Passages, stages `2 / 0 / 8`, dependencies `10 / 35`, callers `8 / 37`, inventory `8 complete / 2 dependencies-bound / 30 queued / 2 blocked-one-way / 3 blocked-parallel`. Pre-edit hash `5479746e65e27c0d22985f3e02f9a9c6d77562d2497f5c743168f162c99200b3`; dependency-bound hash `b30fa6610435667265b8b3c32965713f1af317ccddb9a5cce11618112dfcc578`. Targeted/focused/safety/lifecycle gates pass `1/1`, `12/12`, `30/30`, and `8/8`; the full suite remains `260/214/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits remain `112 / 48,789 / 48 / 155 / zero hard errors / 38 findings`. The exact next safe step is caller-only binding for Group 04 at stage `0`.

## Group 04 caller-only result

Group `04` is now accepted at `caller-bound`. Trigger `340611600` gains only `canonicalPassage: 4100000019`, and trigger `2300000109` gains only `canonicalPassage: 4100000020`, for an exact two-line additive production-scene delta. Gameplay remains 6,023 documents / six RoomViews / ten Passages, and the database remains 16 definitions. Direct dependencies stay `10 bound / 35 fully null`; canonical callers advance to `10 bound / 35 null`. Both new Passages remain at stage `0`, leaving the scene distribution at `2 stage 0 / 0 stage 1 / 8 stage 2`, so canonical identity still delegates approach and arrival to the legacy samplers. The seven-line observation profile changes only `callers=null` to `callers=bound`, producing no-trailing-newline SHA-256 `9519761fb43d7e8eb6f76edb036462d22595a607a8b5844bf9fa720874b76b66`. Inventory is 8 complete, 2 caller-bound, 30 queued, 2 blocked-one-way, and 3 blocked-parallel. No dependency, anchor coordinate, Passage document, threshold, topology, hierarchy, collider, set piece, prop, occlusion, GameRoot registration, runtime source, prefab, data asset, `.meta`, or GUID changed. The exact next safe step is tests-only reciprocal arrival calibration/preflight before either stage scalar changes.

## Group 04 bounded ownership/certification result

Group `04` is now accepted through `complete` in one bounded five-change batch: arrival preflight, arrival ownership, approach preflight, approach ownership, and complete certification. Passages `4100000019` and `4100000020` both advance from stage `0` to `AuthoredAnchors = 2`. Entrance `(8.705841, -2.346406)` and Dining `(-7.192237, -1.740209)` become authoritative in both reciprocal roles. Stage-1 rendered distances are Entrance `23.943, 67.613, 33.707, 5.751, 6.619` and Dining `23.943, 58.835, 33.694, 59.736, 68.753`; each probe is exact, path-reachable, walkable, non-projected, and inside its unchanged trigger envelope. The global threshold distribution remains 44 triggers at `145` and one at `149`.

The scene delta from caller-bound is limited to two reciprocal anchor-vector replacements and two stage-scalar replacements. There is no GUID, `.meta`, topology, caller, dependency, threshold, GameRoot registration, serialized object identity, runtime source, prefab, data asset, hierarchy, collider, set-piece, prop, or occlusion change. Gameplay remains 6,023 documents / six RoomViews / ten Passages, the database remains 16 definitions, direct dependencies and canonical callers remain `10 bound / 35 null`, and all ten Passages are now stage 2 (`0 / 0 / 10`). Inventory is 10 complete, 30 queued, 2 blocked-one-way, and 3 blocked-parallel, with no intermediate bound rows. The no-trailing-newline authored seven-line observation SHA-256 is `93a264ab16817e8864461da376c9631ae36ae3808f770bee5d5094f89a60e509`. Targeted/focused/safety/lifecycle gates remain `1/1`, `12/12`, `30/30`, and `8/8`; the full suite remains `260/214/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits remain `112 / 48,789 / 48 / 155 / zero hard errors / 38 findings`. Human review should confirm that both new shared doorway points place the Butler's feet naturally at the painted Entrance and Dining doorways at default and maximum zoom. The exact next safe step is tests-only Group `05` Dining Room <-> Butlers Pantry characterization.

## Group 05 bounded foundation result

Group `05` Dining Room <-> Butlers Pantry is accepted at `dependencies-bound` after one bounded five-change batch: pre-edit characterization, Butlers Pantry data, passive RoomView binding, passive reciprocal Passage binding, and direct-dependency binding. RoomDefinition GUID `f2e9016bf08c45ebba8600eabc9e0b4d` owns stable ID `room.butlers-pantry`, primary/display alias `Butlers Pantry`, secondary exact alias `Butler's Pantry`, background GUID `e73e44419d3782452bb6abd0e8edd452`, and a null perspective profile. PassageDefinition GUIDs `1dedaedb6c544e9e8ca4fd2a5be912cf` / `d42e018868914021a713f19df8fe60e8`, RoomView `4100000007`, and Passages `4100000021` / `4100000022` preserve reciprocal reference points Dining `(2.891918, 0)` and Butlers Pantry `(-4.663103, -3.103186)` at `LegacySampling = 0`.

Both Group `05` triggers serialize the existing direct navigation manager, Player, shared door AudioSource, and door catalog while their canonical callers remain null; both retain standard Door / `Open Door`, threshold `145`, and unchanged legacy traversal. `GameDatabase` advances `16 -> 19` definitions. Gameplay advances `6,023 -> 6,026` documents, `6 -> 7` RoomViews, and `10 -> 12` Passages; stage distribution is `2 / 0 / 10`, direct dependencies are `12 bound / 33 null`, and canonical callers remain `10 bound / 35 null`. Inventory is `10 complete / 2 dependencies-bound / 28 queued / 2 blocked-one-way / 3 blocked-parallel`. Pre-edit no-trailing-newline observation SHA-256 is `32c4e3227cf5135f264c1b6f720a771ff3a5333a4cec1c032954bed69f5f5dbb`; direct dependency binding changes only the serialized-dependency profile token and yields `319285f885882c452f985bcb6eafb3a69d79065efc1476a990ade17094610123`. Gates pass targeted `1/1`, focused `13/13`, safety `31/31`, lifecycle `9/9`, and full `262/216/46`, with the existing failure-name SHA-256 unchanged; architecture/serialization/Y-axis audits remain `112 / 48,789 / 48 / 155 / zero hard errors / 38 findings`. No runtime source, prefab, existing `.meta`, existing GUID, hierarchy, collider, set piece, prop, occlusion, activation, camera owner, caller, threshold, or stage ownership changes. The exact next safe step is a caller-only Group `05` checkpoint at stage `0`.

## Group 05 bounded caller/ownership result

Group `05` Dining Room <-> Butlers Pantry is authored through `complete` in one bounded caller-plus-five-ownership batch. Trigger `2300000119` binds Passage `4100000021`, trigger `2300000139` binds Passage `4100000022`, and the pair then passes through arrival preflight/ownership, approach preflight/ownership, and complete certification. Both Passages serialize `AuthoredAnchors = 2`; Dining `(3.391918, -0.36)` and Butlers Pantry `(-5.163103, -3.463186)` become authoritative in both reciprocal roles. Dining rendered distances are `23.943, 38.849, 33.662, 38.87, 44.737`; Butlers Pantry rendered distances are `23.943, 83.785, 33.733, 27.022, 31.101`. The worst distance is `83.785`, safely inside both unchanged `145`-pixel envelopes.

Relative to the accepted foundation, Gameplay changes by `8 insertions / 6 deletions`: two caller lines are added, four anchor-coordinate lines are replaced, and two stage lines are replaced. No GUID, `.meta`, topology, direct dependency, threshold, GameRoot registration, serialized identity, runtime source, prefab, data asset, hierarchy, collider, set piece, prop, occlusion, activation, or camera ownership changes. Gameplay remains 6,026 documents / seven RoomViews / twelve Passages, `GameDatabase` remains 19 definitions, stage distribution becomes `0 / 0 / 12`, and both dependencies and callers become `12 bound / 33 null`. Inventory becomes `12 complete / 28 queued / 2 blocked-one-way / 3 blocked-parallel`, with Group `05` marked `template-certified`. The authored seven-line no-trailing-newline observation SHA-256 is `78267865829752279aaa796771f8d51c92b0fac44dc2fe93a4dd12885ccf2d7e`.

The final gates pass targeted `1/1`, focused `13/13`, safety `31/31`, rendered lifecycle `9/9`, and full `262/216/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits pass at `112 runtime files / 48,789 lines / 48 direct MonoBehaviour declarations / 155 serialized rows / zero hard errors / 38 findings`. Human review still needs to confirm that both authoritative points place the Butler's feet naturally at the painted Dining and Butlers Pantry doorway sides at default and maximum zoom. The exact next safe step is tests-only Group `06` Butlers Pantry <-> Billiard Room characterization; no Billiard Room data or ownership should be authored before it.

## Group 06 tests-only characterization result

Group `06` Butlers Pantry <-> Billiard Room is accepted at `characterized` without changing production code or serialized content. Both manifest rows now say `legacy-behavior-locked-data-next`; every canonical field remains empty. The current six-pair production summary is unchanged: `GameDatabase 19`, Gameplay `6,026` documents / seven RoomViews / twelve stage-2 Passages, and direct dependencies/callers each `12 bound / 33 null`. The manifest is `12 complete / 2 characterized / 26 queued / 2 blocked-one-way / 3 blocked-parallel`.

The exact structural contract is locked. Forward GameObject `1505671644` owns RectTransform `1505671645` and trigger component `1505671646` beneath Butlers Pantry root/content/Doors `2300000020` / `2300000022` / `2300000024`; its rectangle is `(304.7408, 0.153)` / `(187.9324, 422.4507)`. Reverse GameObject `2300000130` owns RectTransform `2300000131` and trigger `2300000134` beneath Billiard root/content/Doors `2300000010` / `2300000012` / `2300000014`; its rectangle is `(565, 52.91918)` / `(120, 333.8383)`. Both are four-component owners, standard Door, threshold `145`, exact Inspector routes, null serialized manager/player/audio/catalog dependencies, null callers, no co-located Passage, and successfully runtime-resolved legacy dependencies.

Butlers Pantry remains background `e73e44419d3782452bb6abd0e8edd452`, null perspective profile, and RoomView `4100000007`. Billiard remains background `5987c5a8b3a09fc1ca848ac0ece03658`, null perspective profile, and no canonical RoomView. Its exactly three blockers and corresponding foreground cutouts are preserved: `PlayerBlocker_billiard_table` -> `billiard_table`, `PlayerBlocker_billiard_left_armchair` -> `billiard_left_armchair`, and `PlayerBlocker_billiard_left_lamp_table` -> `billiard_left_lamp_table`. `doors.txt` contains neither exact Inspector ID (`Butlers_Pantry_BilliardRoom`, `BilliardRoom_ButlersPantry`); its different Dining/Billiard and pantry graph cannot author this pair.

The following seven rendered messages, joined with `\n` and no trailing newline, have SHA-256 `528af110846bebb59cc866eb6f94f14e3db1e4fdaad971434eec2e057e9b872e`:

```text
[ButlersBilliardLegacyPrimary] events=arrived:Butlers Pantry:audio-idle|movement-stopped:Butlers Pantry:audio-idle|room-changed:Billiard Room:audio-started|arrived:Billiard Room:audio-idle|movement-stopped:Billiard Room:audio-idle|room-changed:Butlers Pantry:audio-started|room-changed:Billiard Room:audio-started|room-changed:Butlers Pantry:audio-started forwardStart=0,-2 forwardScreenDistance=189.196 forwardNull=2.744461,-2.748338 forwardLeft=3.111516,-2.748338 forwardCenter=3.967978,-2.748338 forwardRight=4.824442,-2.748338 forwardDispatch=2.744461,-2.748338 forwardProductionApproach=2.744461,-2.748338 forwardArrival=6.575521,-1.484375 reverseStart=0,-2 reverseScreenDistance=438.675 reverseNull=6.575521,-1.484375 reverseLeft=6.809896,-1.484375 reverseCenter=6.575521,-1.484375 reverseRight=6.575521,-1.484375 reverseDispatch=6.575521,-1.484375 reverseProductionApproach=6.575521,-1.484375 reverseArrival=5.191498,-2.748338 nearForwardArrival=6.575521,-1.484375 nearReverseArrival=5.191498,-2.748338
[ButlersBilliardLegacyAspect] viewport=1366x768 forwardStart=0,-2 forwardScreenDistance=189.196 forwardNull=2.744461,-2.748338 forwardLeft=3.111516,-2.748338 forwardCenter=3.967978,-2.748338 forwardRight=4.824442,-2.748338 forwardDispatch=2.744461,-2.748338 forwardProductionApproach=2.744461,-2.748338 forwardArrival=6.575521,-1.484375 reverseStart=0,-2 reverseScreenDistance=438.675 reverseNull=6.575521,-1.484375 reverseLeft=6.809896,-1.484375 reverseCenter=6.575521,-1.484375 reverseRight=6.575521,-1.484375 reverseDispatch=6.575521,-1.484375 reverseProductionApproach=6.575521,-1.484375 reverseArrival=5.191498,-2.748338
[ButlersBilliardLegacyAspect] viewport=1440x1080 forwardStart=0,-2 forwardScreenDistance=259.674 forwardNull=2.376193,-2.37955 forwardLeft=2.693994,-2.37955 forwardCenter=3.435532,-2.37955 forwardRight=4.17707,-2.37955 forwardDispatch=2.376193,-2.37955 forwardProductionApproach=2.376193,-2.37955 forwardArrival=5.693179,-1.285192 reverseStart=0,-2 reverseScreenDistance=619.196 reverseNull=5.693179,-1.285192 reverseLeft=5.896104,-1.285192 reverseCenter=5.693179,-1.285192 reverseRight=5.693179,-1.285192 reverseDispatch=5.693179,-1.285192 reverseProductionApproach=5.693179,-1.285192 reverseArrival=4.494872,-2.37955
[ButlersBilliardLegacyAspect] viewport=1920x1080 forwardStart=0,-2 forwardScreenDistance=265.915 forwardNull=2.743791,-2.747668 forwardLeft=3.110757,-2.747668 forwardCenter=3.96701,-2.747668 forwardRight=4.823266,-2.747668 forwardDispatch=2.743791,-2.747668 forwardProductionApproach=2.743791,-2.747668 forwardArrival=6.573917,-1.484013 reverseStart=0,-2 reverseScreenDistance=616.589 reverseNull=6.573917,-1.484013 reverseLeft=6.808235,-1.484013 reverseCenter=6.573917,-1.484013 reverseRight=6.573917,-1.484013 reverseDispatch=6.573917,-1.484013 reverseProductionApproach=6.573917,-1.484013 reverseArrival=5.19023,-2.747668
[ButlersBilliardLegacyAspect] viewport=2560x1080 forwardStart=0,-2 forwardScreenDistance=364.763 forwardNull=3.168257,-3.172733 forwardLeft=3.591993,-3.172733 forwardCenter=4.580709,-3.172733 forwardRight=5.569427,-3.172733 forwardDispatch=3.168257,-3.172733 forwardProductionApproach=3.168257,-3.172733 forwardArrival=7.590901,-1.71359 reverseStart=0,-2 reverseScreenDistance=820.181 reverseNull=7.590901,-1.71359 reverseLeft=7.861468,-1.71359 reverseCenter=7.590901,-1.71359 reverseRight=7.590901,-1.71359 reverseDispatch=7.590901,-1.71359 reverseProductionApproach=7.590901,-1.71359 reverseArrival=5.993162,-3.172733
[ButlersBilliardLegacyMaximumZoom] viewport=2560x1080 zoom=1.22 forwardStart=0,-2 forwardScreenDistance=419.821 forwardNull=3.168258,-3.172733 forwardLeft=3.591992,-3.172733 forwardCenter=4.580709,-3.172733 forwardRight=5.569427,-3.172733 forwardDispatch=3.168258,-3.172733 forwardProductionApproach=3.168258,-3.172733 forwardArrival=7.590901,-1.71359 reverseStart=0,-2 reverseScreenDistance=943.982 reverseNull=7.590905,-1.71359 reverseLeft=7.861472,-1.71359 reverseCenter=7.590905,-1.71359 reverseRight=7.590905,-1.71359 reverseDispatch=7.590905,-1.71359 reverseProductionApproach=7.590905,-1.71359 reverseArrival=5.993162,-3.172733
[ButlersBilliardLegacyProfile] forwardGeometry=304.7408,0.153/187.9324,422.4507 reverseGeometry=565,52.91918/120,333.8383 profiles=none callers=null serializedDependencies=null runtimeDependencies=resolved blockers=0/3
```

Final gates pass targeted `1/1`, focused `14/14`, safety `32/32`, rendered lifecycle `10/10`, and full `264/218/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits pass at `112 runtime files / 48,789 lines / 48 direct MonoBehaviour declarations / 155 serialized rows / zero hard errors / 38 findings`. Human review must still confirm doorway foot placement, active-room visibility, camera behavior, and the three Billiard cutout/blocker occlusion relationships. The exact next safe step is the bounded Group `06` foundation: Billiard Room data, one passive Billiard RoomView, two passive reciprocal stage-0 Passages, and both direct dependency sets. Do not bind canonical callers, transfer arrival/approach ownership, alter either rectangle/threshold, or touch the three Billiard cutout/blocker pairs.

## Group 06 foundation result

Group `06` Butlers Pantry <-> Billiard Room is authored at `dependencies-bound` after one bounded five-change batch: the accepted characterization, Billiard Room data, passive RoomView `4100000008`, passive reciprocal Passages `4100000023/24`, and both direct-dependency bindings. RoomDefinition GUID `bed158a9affd015fcc961340d9be5dd8` owns stable ID `room.billiard-room`, primary/display name `Billiard Room`, background GUID `5987c5a8b3a09fc1ca848ac0ece03658`, and a null perspective profile. PassageDefinition GUID `71ea8ce4d4eb8fa7f107abe24d7c903e` owns `passage.butlers-pantry.billiard-room`; GUID `be2f1b94b724dcfa061876e33bce02ca` owns `passage.billiard-room.butlers-pantry`. They retain standard Door / `Open Door` profile and directly reverse-link.

Existing Billiard root/content `2300000010` / `2300000012` owns the new passive RoomView. Trigger `1505671646` co-locates Passage `4100000023`; trigger `2300000134` co-locates Passage `4100000024`. Their passive stage-0 anchors share Butlers Pantry `(2.744461, -2.748338)` and Billiard Room `(6.575521, -1.484375)` reciprocally. Direct navigation manager, Player Transform, shared door AudioSource, and door catalog dependencies are now serialized on both triggers; both canonical callers remain null, both thresholds stay `145`, and legacy approach/arrival sampling remains authoritative.

The database advances `19 -> 22`; Gameplay advances `6,026 -> 6,029` documents, `7 -> 8` RoomViews, and `12 -> 14` Passages. Stages become `2 / 0 / 12`, dependencies `14 bound / 31 null`, callers `12 bound / 33 null`, and inventory `12 complete / 2 dependencies-bound / 26 queued / 2 blocked-one-way / 3 blocked-parallel`. The exact scene delta relative to characterization is `68 insertions / 8 deletions`. No production source, prefab, existing `.meta`/GUID, caller, stage ownership, threshold, hierarchy, rectangle, collider, blocker, cutout, prop, occlusion, activation, camera owner, or exceptional topology changes.

The six coordinate/event lines remain byte-identical to characterization; only the profile token changes from `serializedDependencies=null` to `serializedDependencies=bound`. The accepted no-trailing-newline foundation observation SHA-256 is therefore `b6fbb280fed2f2310b32de4cefa85ce8dbb7ed5ebfebae72b55951ba0026093f`, replacing characterized hash `528af110846bebb59cc866eb6f94f14e3db1e4fdaad971434eec2e057e9b872e`. Final Group `06` foundation gates pass targeted `1/1`, focused `14/14`, safety `32/32`, rendered lifecycle `10/10`, and full `264/218/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits pass at `112 runtime files / 48,789 lines / 48 direct MonoBehaviour declarations / 155 serialized script records / zero hard errors / 38 findings`. Human review still must confirm doorway foot placement, active-room visibility, camera behavior, and each Billiard foreground cutout/blocker relationship. The exact next safe step is caller-only binding `1505671646 -> 4100000023` and `2300000134 -> 4100000024`; retain both Passages at stage `0` and do not start calibration or ownership transfer.

## Phase 1.5 complete definitions and typed database result

Slice `1.5` completes the passive canonical room catalog in one bounded data/index batch. Eleven new RoomDefinition assets are appended after the preserved first 22 GameDatabase references in dependency order: Service Corridor, Kitchen, Chapel, Grand Entrance Hall Rear View, Conservatory, Side Stair & Mudroom, Upper Sitting Hall, Upper Gallery, Master Bedroom Suite, Nursery, and Blue Bedroom. Together with the existing eight definitions, the database contains exactly `33` unique entries: `19` rooms and `14` passages. Every new asset uses schema `1`, the existing RoomDefinition script GUID, a unique new asset GUID, its exact Gameplay background texture, and a null perspective profile. Rear-view capitalization, Side-Stair ampersand, and Butler-pantry apostrophe aliases remain explicit compatibility data.

`GameDatabase` now owns derived typed indexes without replacing its serialized `definitions` field or public `Definitions` list. Typed collections are read-only, lookup accepts only `RoomId`/`PassageId`, and duplicate valid IDs are removed from the lookup rather than resolved first- or last-wins. Configuration validation rejects duplicate global IDs, cross-room case-insensitive alias collisions, and any Passage source, destination, or reverse object not registered by exact identity. A lightweight source snapshot catches list/reference/stable-ID edits before reusing an index; no service locator, generic string lookup, new singleton, or parallel data system is introduced.

The batch changes no scene, prefab, existing `.meta`, existing GUID, passage asset, route manifest, `doors.txt`, RoomView, Passage component, trigger, caller, direct dependency, anchor, stage, collider, prop, camera, activation, audio, or story behavior. Gameplay remains SHA-256 `bb851d116bb616d5d8e25dc863bdb71f48c9314bfcd6b63d6e23acc1b3aced0d`; `RemainingRouteInventory.csv` remains `1e821937d2561ac2239f5d5d5c1765c04f83dcb6d66e8b15f0b144b36b85b2cb`; legacy `doors.txt` remains `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. Unity import also preserves the authored GameDatabase and all 19 room assets byte-for-byte.

Static controls pass: architecture-tool tests `4/4`, no guard debt, `113/113` runtime ledger rows, `50,212` runtime lines, unchanged smell totals `106/17/67/51/4/6`, `48` direct MonoBehaviours, `164` current scripts, `1,937` serialized script references, and `856` external/package references. Unity `6000.4.10f1` compiles without errors. The final focused XML passes exactly `38/38/0/0`, including the new nine-case database contract plus stable identity, canonical data, passage certification, scene-root/startup, and rendered Group 06 coverage. Rendered cold start passes `1/1/0/0` with Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, sort `1075`, and exact digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.

Two runner/filter diagnostics are preserved. The first focused launch used `-nographics` and crashed in Linux `XcursorImageLoadCursor` after Gameplay loaded, producing no XML; graphics-enabled execution is required for the rendered Group 06 test. The next run discovered all 38 and passed 37; its sole failure was the pre-existing baseline `GameplayRoomsOwnBackgroundsAndDoorGroups` assertion for obsolete prefab name `Room_NewRoom`, confirmed in earlier full-suite XMLs. Replacing only that unrelated known-failing filter entry with the passing manual-room-root invariant yielded the accepted exact `38/38`; no source, test, asset, or serialized content changed between attempts.

No file is pruned in Slice `1.5`. `RoomVisualCatalog` now has complete canonical room-definition/background replacement data, but it and every legacy navigation representation remain quarantined until their consumers reach zero and Phase 2 passage/room-view behavior passes. The exact next safe step is Phase `2.1`: finish the already-authored Group `06` Butlers Pantry/Billiard Room caller and reciprocal anchor ownership/certification from the clean Slice 1.5 commit. Do not re-create either definition or alter the pair's characterized geometry, threshold, blockers, or cutouts.

## Phase 2.1 Group 06 complete result

Slice `2.1` completes the existing Group `06` Butlers Pantry <-> Billiard Room pair without re-authoring either RoomDefinition or PassageDefinition. Triggers `1505671646` / `2300000134` now bind co-located Passages `4100000023` / `4100000024`, and both Passages serialize `AuthoredAnchors = 2`. The authoritative reciprocal room-side points are Butlers Pantry `(3.244461, -3.108338)` and Billiard Room `(6.9, -1.6)`. The Pantry point's rendered stage-1 distances are `23.943, 78.646, 33.725, 6.953, 8.002`; the Billiard point's are `7.69, 33.972, 10.845, 75.599, 87.01`. Every probe is exact, walkable, path-reachable, non-projected, and inside the unchanged `145`-pixel trigger threshold across `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest-aspect maximum zoom.

The first uncommitted Billiard candidate `(7.075521, -1.844375)` was rejected by the rendered lifecycle gate: primary and `1366x768` passed, but the `1440x1080` reverse traversal remained in Billiard Room, so the gate stopped at `0/1` rather than accepting a viewport-dependent route. A tests-only Billiard sweep retained seven invariant candidates after the five rendered profiles (`185 / 136 / 108 / 14 / 7` survivors) and selected `(6.9, -1.6)` with worst distance `87.01`. The corrected-start Pantry sweep retained `504 / 482 / 482 / 447 / 423` candidates; the retained exact Pantry point then passed the production stage-1 evaluator at all five profiles. Temporary calibration tests were removed before certification.

Gameplay remains exactly `6,029` YAML documents / eight RoomViews / fourteen Passages / 45 legacy triggers, and `GameDatabase` remains exactly `33` definitions. Passage stages are now `0 stage 0 / 0 stage 1 / 14 stage 2`; direct dependencies remain `14 bound / 31 null`; canonical callers become `14 bound / 31 null`. The route inventory is `14 complete / 26 queued / 2 blocked-one-way / 3 blocked-parallel`. Relative to the dependencies-bound checkpoint, the production scene delta is only `8 insertions / 6 deletions`: two caller references, four reciprocal anchor replacements, and two stage replacements. No runtime source, prefab, definition asset, existing `.meta`, GUID, hierarchy, trigger rectangle, `145`-pixel threshold, blocker, cutout, prop, camera owner, or exceptional topology changes.

The accepted seven-line authored observation SHA-256 is `cd248f01301448b5cd807cc9331e58d99bd59a139bb772dab352869527b9a6eb`. Exact final Passage-document hashes are `61ae1a519da8a47275e2713b3182b7297c049b0eb9b2d590f5cb5037607abcc2` for `4100000023` and `b46ec8f0b48730348e80287e50393c153ce05621c35db00741dfd7af50e6b1ce` for `4100000024`. The caller-bound poisoned-anchor proof and pair-local null-caller proof both retain the stage-0 compatibility seam for later unmigrated pairs, while final Group `06` traversal uses only the authored reciprocal anchors.

Static architecture, GUID/meta, serialized-reference, and Unity compilation gates pass. Final Unity gates pass targeted Group `06` `1/1`, canonical/manifest `14/14`, navigation safety `32/32`, rendered lifecycle `10/10`, genuine Group `06` PlayMode `1/1`, and cold-start PlayMode `1/1`. Cold start retains Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, sort `1075`, and digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.

Human review remains required for Butler foot placement at both painted doorway sides, camera/active-room visibility, `Open Door` prompt and cursor clearing, and all three Billiard foreground cutout/blocker occlusion relationships. The exact next safe step is tests-only characterization of Group `07` Butlers Pantry <-> Service Corridor, preserving the Group `06` commit and applying no Group `07` data/caller/anchor change before its behavior is locked.

## Phase 2.2-07 Group 07 complete result

Slice `2.2-07` completes Butlers Pantry <-> Service Corridor only after its existing reciprocal behavior was locked. The legacy six-line observation profile hashes to `1a2d83cdf5e9cce175cf16333875f183629c6e67fe8d82241479bae79b4e8150`. Service Corridor retains its existing RoomDefinition/background/profile identity and now owns passive RoomView `4100000009`. New PassageDefinition GUIDs `1b2d5f64523942a08e10402e24e88738` / `b485e8a6f574414a84f77437e02147f1` own stable IDs `passage.butlers-pantry.service-corridor` / `passage.service-corridor.butlers-pantry`; existing triggers bind co-located Passages `4100000025` / `4100000026`, their direct dependencies, and their canonical callers.

Both Passages serialize `AuthoredAnchors = 2`. Butlers Pantry `(7, -2.8)` and Service Corridor `(4.2, -3.3)` are the authoritative reciprocal approach/arrival points. Across `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest-aspect maximum zoom, the Pantry point measures `18.338, 66.306, 25.839, 52.36, 60.263` pixels and the Service point measures `16.712, 71.262, 23.565, 29.137, 33.535` pixels. Worst distances are Pantry `66.306` and Service `71.262`; every probe remains exact, walkable, path-reachable, non-projected, and within the unchanged `145`-pixel envelope. Far and near traversal retain movement-stop, audio, room-event, visibility, camera, prompt, and cursor behavior. The final no-trailing-newline authored profile hashes to `7cc7c8706a02443b30c91c7a3cdcce40b7c04fec2f54e3f2eb7d5acad7058ea9`.

Gameplay now contains exactly `6,032` YAML documents / nine RoomViews / sixteen Passages / 45 triggers. Passage stages are `0 stage 0 / 0 stage 1 / 16 stage 2`; direct dependencies and canonical callers are each `16 bound / 29 null`. `GameDatabase` is exactly `35` definitions (`19` rooms / `16` passages), and the route inventory is `16 complete / 24 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact SHA-256 evidence is Gameplay `c48e6f4a68cd311e958677136b572e06c05266ca556153dafd8e2656ffb1d30c`, inventory `47440df151a302a10493d14f2258c470a4a07d8683b858caf73f1c82e07c8c15`, and GameDatabase `155e065c03ec20c6b7d5f8e0a8e1746d1da166d74dc8d8c4214db8f7446989f1`. The legacy catalog remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and still contains both Group07 route entries.

Static controls pass: architecture-tool tests `4/4`, architecture guard with no raised baseline, exact `113/113` runtime-ledger coverage, `50,212` runtime lines, `48` direct MonoBehaviours, unchanged smell totals `106/17/67/51/4/6`, `164` current scripts, `1,942` serialized script references, and `856` external/package references. Unity `6000.4.10f1` compiles without errors. Relevant EditMode execution accounts for 71 passing cases: 67 accepted first-pass cases plus four isolated correction reruns after test-only expectation repairs; no production content was changed to make those diagnostics pass. The real-menu Group07 PlayMode round trip passes `1/1`, and independent cold start passes `1/1` with Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, sort `1075`, and unchanged digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.

No deletion is claimed. Both Group07 `doors.txt` lines remain required compatibility data while 29 trigger callers are still null. Service Corridor's `service_corridor_left_table_0` and `service_corridor_right_desk_0` cutouts, their matching `PlayerBlocker_*` collision owners, and `PlayerBoundary` remain required world content and are explicitly protected from pruning. Human review must confirm Butler foot placement at both painted doorway sides, camera/active-room visibility, `Open Door` prompt and cursor clearing, and both prop occlusion/collision relationships. The exact next safe step is tests-first Group `08` Service Corridor <-> Kitchen characterization, with no Group08 production authoring until its legacy behavior is locked.

## Phase 2.2-08 Group 08 complete result

Slice `2.2-08` completes Service Corridor <-> Kitchen after preserving the exact legacy six-line fingerprint `9af5d16f40f2f186d02a777fb53ac80fd28fe898e0e8953db9455d047a5432de` and consuming the separately validated RoomView-local anchor capability from Slice `2.2-08a`. Kitchen retains RoomDefinition GUID `70531cbf9a67476f81f54b528029132e`, scene background/profile identity, hierarchy, props, and collision content, and now owns passive RoomView `4100000010`. New PassageDefinition GUIDs `2985cbdd527b4faaec13ff03091dbcd1` / `453ad73cf2df1107f56be7a00daa3145` own stable IDs `passage.service-corridor.kitchen` / `passage.kitchen.service-corridor`; existing triggers `2300000164` / `802263367` bind co-located Passages `4100000027` / `4100000028`, their direct dependencies, and canonical callers.

Both Passages serialize `AuthoredAnchors = 2` and use `RoomViewLocal = 1` for approach and arrival. The authoritative invariant native points are Service Corridor `(589.9897, -419.25894)` and Kitchen `(-478.36285, -156.76599)`. Across `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest-aspect maximum zoom, each resolves through the active room stage to the correct aspect-specific player-logical position while remaining finite, exactly walkable, path-reachable, non-projected, inside the unchanged `145`-pixel envelope, and round-trip equal to its authored RoomView-local point. Far and near traversal retain movement-stop, audio, room-event, visibility, camera, prompt, and cursor behavior. The five rendered records hash, with no trailing newline, to `6d5f83aa399b12d8945a660e0372f65206c2c04a52e2657422ea92966fe0393b`.

Gameplay now contains exactly `6,035` YAML documents / ten RoomViews / eighteen Passages / 45 triggers, with all eighteen Passages at stage 2. Direct dependencies and canonical callers are each `18 bound / 27 null`. `GameDatabase` is exactly `37` definitions (`19` rooms / `18` passages), and the route inventory is `18 complete / 22 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact SHA-256 evidence is Gameplay `5203d054870b01b2e6db55286bc4a2d6b25ee6795cb84765306728090b4e1d98`, inventory `b692f1333f0271903fd9ff60e20323ce628311f37592a481a23fad142b468ae3`, and GameDatabase `9b9b4ae428340a9fd6597e18873adbdce9b3a675786dbee568adb9a595d710b1`. The legacy catalog remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and still contains both Group08 route entries.

Static controls pass: architecture-tool tests `4/4`, architecture guard with no raised baseline, exact `113/113` runtime-ledger coverage, `164` current scripts, `1,947` serialized script references, `856` external/package references, and all `7,360` asset `.meta` GUID records paired and globally unique. Unity `6000.4.10f1` compiles without errors. All 76 relevant EditMode cases are accounted: 74 passed in the consolidated launch, then two focused test-only corrections passed without changing production behavior. The genuine real-menu Group08 round trip and independent cold start pass together at `2/2`; cold start retains exact digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.

No deletion is claimed. Both Group08 `doors.txt` lines remain required compatibility data while 27 trigger callers are still null. Service Corridor's left-table/right-desk cutouts, their movement blockers, and `PlayerBoundary` remain required world content; Kitchen's `kitchen_work_table`, `PlayerBlocker_kitchen_work_table`, and room boundary likewise remain required presentation/collision content. Human review must confirm Butler foot placement at both painted doorway sides, camera/active-room visibility, `Open Door` prompt and cursor clearing, and both rooms' occlusion/collision relationships. At that checkpoint, the exact next safe step was tests-first Group `09` Service Corridor <-> Chapel characterization, with no Group09 production authoring until its legacy behavior was locked.

## Phase 2.2-09 Group 09 complete result (historical)

Group `09` Service Corridor <-> Chapel now satisfies the RoomView-local compatibility-seam template from clean rollback commit `07bb61fff50f63dfdc36acf244b391b26fa48d61`. Legacy reciprocal behavior was locked first at no-trailing-newline six-line SHA-256 `9574b82e5099f96bdaef11dd20bdbab386f5b54baf9ad06278b40af7a58449b2`. Chapel RoomView `4100000029`, PassageDefinitions `fc2a0af2de3f4ade831c53f64fe0271b` / `47e06869bf2b47a2980b0d02a53ee1df`, and reciprocal Passages `4100000030` / `4100000031` establish canonical identity on the existing room and trigger owners. Stable IDs are `passage.service-corridor.chapel` / `passage.chapel.service-corridor`. Triggers `2300000169` / `2300000179` bind their direct dependency sets and canonical callers, both Passages serialize `AuthoredAnchors = 2`, and both standard-Door thresholds remain `145`.

The authoritative reciprocal RoomView-local points are Service Corridor `(-133.2642, -171.8258)` and Chapel `(461.4019, -190.7613)`. They remain finite, exact, walkable, path-reachable, non-projected, within threshold, and round-trip invariant across `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest-aspect maximum zoom. The accepted authored rendered fingerprint is `bff3067e14fd0710f8de6dd4f7e4434d9025a2b7593adf3cd54571020a622475`. Exact trigger rectangles, event/audio order, camera/background/active-room behavior, prompt/cursor cleanup, Chapel background/null profile, three bench cutout/blocker pairs, three additional foreground cutouts, 18-point `PlayerBoundary`, and `Ch2_Hide_Guest06` remain protected.

Gameplay now contains exactly `6,038` YAML documents / eleven RoomViews / twenty Passages / 45 triggers, with all twenty Passages at stage 2. Sixteen Passage documents use legacy-logical coordinates and four Group08/09 documents use certified RoomView-local anchors. Direct dependencies and canonical callers are each `20 bound / 25 null`. `GameDatabase` is exactly `39` definitions (`19` rooms / `20` passages); GameRoot registers `40` scene behaviours and has `48` total serialized references including eight services; and the route inventory is `20 complete / 20 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact SHA-256 evidence is Gameplay `220009ae5f8d2bbe3a3e62ccab6d9c88da56a4c66d9dde092da11f0cae8b8a93`, inventory `43a8134256b39291e7954141ef15d11c3a179b735f65f6437088779dd8abbc2c`, and GameDatabase `90734ca890e57a4b64062e10a7e2c2e04f281dc783beb96f012c2fa673674ebc`. The legacy catalog remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and still contains both Group09 route entries.

Static controls pass at `113` runtime files / `50,339` lines / `48` direct MonoBehaviours, exact `113/113` runtime-ledger coverage, `164` current scripts, and `1,952` serialized script references (`1,096` project / `856` external-package). All `7,362` asset `.meta` GUID records are paired and globally unique. Unity `6000.4.10f1` compiles without errors. Focused Group09 EditMode passes `4/4`, the final affected architecture suite passes `62/62`, rendered lifecycle passes `1/1`, and the genuine real-menu Group09 round trip plus independent cold start pass `2/2`. The historical full-suite baseline remains separately recorded and is not relabeled as a current full-suite run.

No deletion is claimed. Both Group09 `doors.txt` rows remain required compatibility data while 25 trigger callers are still null. Service Corridor's left-table/right-desk cutouts, matching blockers, and `PlayerBoundary` remain required world content. Chapel's bench cutouts/blockers, additional foreground cutouts, room boundary, and Chapter 2 hide anchor likewise remain required presentation, collision, and story-placement content. Human review must confirm Butler foot placement at both painted doorway sides, camera/active-room visibility, `Open Door` prompt and cursor clearing, and both rooms' occlusion/collision relationships. The exact next safe step is tests-first Group `10` Grand Entrance Hall <-> Grand Entrance Hall Rear characterization, explicitly preserving and resolving the existing `Grand Entrance Hall Rear View` / `Grand Entrance Hall Rear view` case alias before production authoring.

## Phase 2.2-10 Group 10 complete result

Slice `2.2-10` starts from clean rollback commit `f91ee2338261ee061a8502e06542fd49b5d81831` and preserves the reviewed pre-authoring six-line fingerprint at exact no-trailing-newline SHA-256 `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638`. That line set remains explicitly labeled legacy before-state evidence. Existing rear RoomDefinition GUID `64bc36c6e2d546d6bb878373c4e6d0b6` backs RoomView `4100000032`. PassageDefinition GUIDs `aa8a2282356d4ad0aa3c9499a6f6f064` / `d57bc53c2dfb4a10bd63739d37028899` back reciprocal scene Passages `4100000033` / `4100000034` on existing trigger owners `1858342503` / `70736571`. The scene/data reverse links, direct dependency sets, and canonical callers are bound; both Passages are stage `2`. The forward definition explicitly preserves destination spelling `Grand Entrance Hall Rear View`, while the scene's `Grand Entrance Hall Rear view` primary alias remains accepted.

Group10 is not a point-anchor pair. Its two authored approaches remain the exact bottom-edge trigger centers: Entrance `(0.00030518, -456.4991)` and Rear `(10.2463989, -437.093964)`. Arrival ownership is instead a four-corner destination region resolved deterministically in the destination RoomView. Entrance -> Rear owns `BL (-764.707458, -451.0935)`, `TL (-764.707458, -423.094452)`, `TR (785.200256, -423.094452)`, `BR (785.200256, -451.0935)`. Rear -> Entrance owns `BL (-835.9997, -470.4991)`, `TL (-835.9997, -442.4991)`, `TR (836.0003, -442.4991)`, `BR (836.0003, -470.4991)`. This represents the already characterized multi-lane behavior without inventing a lossy scalar arrival coordinate.

The accepted production state contains exactly `6,041` Gameplay YAML documents / twelve RoomViews / twenty-two Passages / 45 triggers, with all twenty-two Passages at stage 2. Direct dependencies and canonical callers are each `22 bound / 23 null`. `GameDatabase` has exactly `41` definitions (`19` rooms / `22` passages); GameRoot registers `43` scene behaviours and contains `51` serialized references including eight services; the inventory is `22 complete / 18 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact SHA-256 evidence is Gameplay `867e9395e38b4a2e675d5e0fc6abbf9449cb03f4e75ab2c3b4397172efde44ae`, GameDatabase `90417c118dfc1cda7bcb3cf17c1d92fda30af7433be7ae446162146925e2c39a`, inventory `3291b6e856a252d78411de186e76f818dda12bd9b63cae1972bfd7001187aa5d`, and byte-identical legacy catalog `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. `doors.txt` contains neither Group10 route ID.

The final rendered repair preserves legacy evaluation order, removes eager `Physics2D.SyncTransforms`, validates the serialized RoomView-local region, and projects bit-exact corners from the canonical reverse Passage RectTransform without `DoorTriggerNavigation`. Frozen-camera A/B calibration against the preserved legacy path accepts Rear `(785.5335, -391.4933)` and Entrance `(418.3665, -440.4239)` in the genuine PlayMode round trip.

Static evidence passes at `114` runtime files / `51,062` lines / `48` direct MonoBehaviours, exact `114/114` runtime-ledger coverage, `165` scripts, `1,957` serialized script references (`1,101` project / `856` external-package), and `7,365` paired, globally unique `.meta` GUID records. Unity `6000.4.10f1` compilation passes against the exact final bytes in `compile-exact-final.log`. Focused EditMode passes `4/4` in `editmode-focused-exact-final.xml`; affected architecture passes `67/67` in `editmode-architecture-exact-final.xml`; rendered Group10 passes `1/1` with exact SHA `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638` in `rendered-group10-exact-final.xml`; and real-menu Group10 plus independent cold-start PlayMode passes `2/2` in `playmode-group10-and-cold-exact-final.xml`. Human visual review remains required.

No deletion is claimed. The 49-point Entrance boundary, 5-point Rear boundary, both bottom-edge triggers, compatibility execution paths, and four railing cutouts (`Grand_entrance_railing_left_0`, `grand_entrance_railing_right_0`, `back_entrance_railing_left_0`, and `back_entrance_railing_right_0`) remain required world content. The exact next safe step is tests-first Group `11` Grand Entrance Hall Rear <-> Billiard Room. Its conflicting `doors.txt` endpoints must be characterized and locked before any Group11 definition, RoomView, Passage, caller, or ownership authoring.
