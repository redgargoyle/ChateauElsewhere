# Prune log

No file may be deleted merely because a text search looks empty. Each deletion needs code-reference, serialized-reference, UnityEvent/animation/reflection/resource, and behavioral-test evidence.

## Completed in the foundation patch

| File | Reason | Code references | Serialized references | Replacement | Proof / test |
|---|---|---:|---:|---|---|
| `Assets/Scripts/NewBehaviourScript.cs` | Empty Unity starter class; no behavior | 0 outside declaration | 0 | None | `ArchitectureFoundationTests.ProvenDeadStarterAndPickupScriptsStayPruned` and static GUID scan |
| `Assets/Scripts/PickupObject.cs` | Uninstantiated generic cursor-hover component; only consumer was a source-text regression assertion | 0 runtime | 0 | Existing interaction-specific hover behavior; future pickups must use `InteractionTargetBase` | Same test plus architecture guard |
| `Assets/Scripts/Story/GameClockHandsDisplay.cs` | Self-registering generic analog overlay attached to no authored renderer and created no runtime UI | 0 outside its own implementation | 0; GUID `484ce1d80a6a493782e1c3ae7c4da747` absent from text and raw binary asset scans | Serialized `ClockTickingAmbienceController` and Chapter 2 HUD's explicit 7:00 clock presentation | Zero-instance/canvas lifecycle across Entrance/Drawing/return; navigation and Chapter 2 clock tests; exhaustive code/resource/reflection/animation scan; full Unity gate. Its `.meta` was removed and the architecture guard requires the path to stay pruned. |
| `Assets/Scripts/Navigation/RoomNavigationBootstrap.cs` | Runtime repair duplicated the serialized composition root | 0 runtime outside the obsolete source-text test | 0; deleted GUID `ee39d7886e1b437f59e4e82b50f560c8` never appeared in serialized content | `Gameplay.unity` serializes one `GameRoot`, `RoomNavigationManager`, and `DoorPromptSequenceController` | `GameplayLifecycleCharacterizationTests`, `NavigationRegressionTests.GameplaySerializedRootOwnsNavigationStartup`, `ArchitectureFoundationTests.RuntimeNavigationBootstrapStaysPruned`, serialized-reference scan, and full Unity gate |

Their `.meta` files were also removed because no serialized asset referenced their script GUIDs.

## Pruned serialized placeholder hierarchies

| Asset / subtree | Reason | Code references | Serialized references | Replacement | Proof / test |
|---|---|---:|---:|---|---|
| `Gameplay.unity` pantry `CoatCloset / Anchors / ApproachFront` (documents `3503000002`–`3503000008`) | Empty placeholder stopped owning the closet after Entrance ownership was serialized; its anchor was reachable only through an uncalled private fallback | 0; `FindPropAnchor` and its sole helper were dead | 0 outside the subtree and its parent child link | Authored Entrance hanger `1592234992` owns closet `3303000001`, action `1592234995`, collider `1592234996`, and approach `1592234993` | Exhaustive ID/code/resource/reflection/animation scan; exact seven-document/order/root audit; static Entrance ownership guard; stored-coat/repeated-resolution/room-round-trip lifecycle; full suite remains 191/49. |

## Retired runtime repair paths

| Path | Former behavior | Replacement | Proof / test |
|---|---|---|---|
| `ChapterManager.BootstrapChapterManagerForGameplay` | A post-scene-load hook created an entire chapter manager stack with six `AddComponent` calls | `Gameplay.unity` serializes `ChapterManager`, `ChapterClock`, `ChapterEventScheduler`, `ChapterIntroUI`, and `Chapter1ArrivalController`; GameRoot validates and initializes the services | `GameplayLifecycleCharacterizationTests`, `ArchitectureFoundationTests.ChapterStackIsSerializedInsteadOfRepairedAtRuntime`, exact serialized GUID scan, and full Unity gate. Chapter 2 was migrated in the subsequent independently gated slice. |
| `ChapterManager.ResolveChapter2Controller(createIfMissing)` creation branch | Chapter transitions and debug skips could add `Chapter2Controller` at runtime | `Gameplay.unity` serializes one controller, binds `ChapterManager.chapter2Controller`, and registers it with GameRoot | The lifecycle test captures the inert serialized instance before Chapter 2 and proves both skips reuse it; the static guard bans `AddComponent<Chapter2Controller>`; serialized-reference scan and full Unity gate. |
| Remaining ChapterManager dependency/player/debug-canvas repair | Startup and every debug/transition path searched for the already-serialized Chapter 2 controller, globally selected a Player, repaired duplicate Player state, and searched for an obsolete debug canvas | Gameplay binds Player input `81962842` plus the complete chapter/dialogue graph; RuntimeSettingsMenu owns its debug controls; the actual Player instance authors both legacy controllers disabled | Desired identities characterized first; two-document scene audit; zero-search static gate; architecture/debug-skip/handoff/lifecycle tests; full suite removes one stale failure and adds none. |
| `Chapter2Controller.ResolveReferences` HUD creation branch | Chapter 2 startup could add `Chapter2InteractionHUD` at runtime | `Gameplay.unity` serializes and binds exactly one Chapter 2 HUD | The lifecycle test captures the HUD before Chapter 2 and proves repeated skips reuse it; the static guard bans `AddComponent<Chapter2InteractionHUD>`; serialized-reference scan and full Unity gate. |
| `Chapter2Controller.ResolveReferences` feature-owner creation branches | Chapter 2 startup added monster-stinger, guest-panic, and guest-search controllers at runtime | `Gameplay.unity` serializes and binds exactly one of each feature owner and GameRoot supplies their context | The lifecycle test captures all three inert instances before Chapter 2 and proves repeated skips reuse them; the static guard bans all three `AddComponent` calls; 22/22 scene-structure audit, serialized-reference scan, and full Unity gate. |
| Remaining `Chapter2Controller.ResolveReferences` dependency repairs | Chapter entry/debug paths repeatedly searched the scene or host object for eleven already serialized owners | `Gameplay.unity` binds the manager, navigation, intro UI, clock, Butler motor, HUD, three feature owners, subtitle service, and speech service; configuration validation rejects an incomplete graph | Static test verifies all serialized edges and bans the repair API/searches; focused regression/lifecycle gates pass; full suite discovers 237 tests with the exact baseline 51-failure name set. |
| Chapter2Controller caller-owned manager rebinding | Four entry commands silently replaced the serialized ChapterManager with any non-null caller | All commands retain and validate the serialized manager; mismatched commands fail before state mutation | Manager identity characterized through repeated entry; explicit mismatch behavior test plus four-method static guard; lifecycle and full Unity gate. |
| Chapter2Controller clock-strike resource/component/runtime-tone fallback | Seven-PM playback could attach an AudioSource to the shared ChapterManager host, load a Resources clip, synthesize a replacement tone, and attach a volume binding | Dedicated child `3301000010` owns serialized source `3301000012`, Game-Sounds binding `3301000013`, and imported clip GUID `d7084eafa9124afcbcbf12529e08bc70`; playback configures those exact owners directly | Before/after characterization; exact four-document graft and cleanup audits; source guards ban every fallback API/field; two repeated seven-PM lifecycles and the full 240-test suite retain the exact identities and failure-name baseline. |
| Chapter2MonsterStinger structural searches and primitive placeholder | Stinger start globally searched for its navigation, named anchors, named monster, and child visual, then could synthesize a capsule placeholder | Component `3301000007` validates and directly owns monster `3700000000`, Drawing Room anchors `98514617`/`382498960`, navigation `1878886997`, and Image `3700000003` | Exact structural characterization; two-line scene cleanup audit with all 5,979 headers/roots unchanged; source guards; Begin/Stop identity/sprite restoration/no-placeholder lifecycle; full 240-test failure-name baseline. |
| Chapter2GuestSearch navigation resolver | Search/debug paths lazily performed the same global navigation lookup from five call sites | Component `3301000009` owns and validates GameRoot navigation service `1878886997` from boot | Null-to-service characterization; exact one-line scene graft audit; resolver/source guards; boot/repeated-entry/repeated-seven-PM identity lifecycle; full 240-test failure-name baseline. |
| Chapter2MonsterStinger violin host/resource/component fallback | First visible use could attach audio to the shared ChapterManager host, search Resources/Editor assets by name, and attach a volume binding | Inactive monster `3700000000` owns source `3700000004`, binding `3700000005`, and direct clip GUID `69f06d321e4549cdcad1133332661f6d` | Runtime presentation characterization; exact two-document graft and one-line cleanup audits; source guards; boot/visible/repeated-use audio lifecycle; MP3/meta hashes; full 240-test failure-name baseline. |
| Chapter2MonsterStinger overlay Canvas Get/Add fallback | First visible use could discover or attach a Canvas component even though the presentation owner was known | Inactive monster `3700000000` owns serialized Canvas `3700000006`; the stinger validates and configures that exact field | Exact Canvas graft audit; source guard bans both component APIs and the repair method; repeated-use lifecycle proves one stable Canvas; full 240-test failure-name baseline. |
| Chapter2MonsterStinger run-sprite Resources fallback | First visible use loaded all frames by string path and name-sorted them into an initially empty array | Controller `3301000007` owns eight direct sprite references in approved raw frame `01`–`08` order and validates their completeness | Exact ordered YAML guard; boot/repeated-use identity and GUID lifecycle; one-document graft/cleanup audits; source guards; full 240-test failure-name baseline. |
| Chapter1ArrivalController Entrance coat-hanger repair | Startup searched by object name, could attach closet/action/collider components, derived a hitbox from presentation, then fell back to a pantry anchor and global closet scan | Authored hanger `1592234992` owns existing closet `3303000001`, action `1592234995`, trigger `1592234996`, and direct approach transform `1592234993`; Chapter 1 validates both serialized edges | Broken-boot/late-repair characterization; six-document graft audit with Transform/SpriteRenderer byte equality; source guards; stored-coat/repeated-resolution/room-round-trip lifecycle; full suite improved to 191/49 with no new failure. |
| Chapter1ArrivalController core-owner discovery | Reference resolution and several feature paths repeatedly searched for manager, clock, scheduler, camera, navigation, and Player owners | Component `3302000001` serializes manager `3301000004`, clock `3301000001`, scheduler `3301000002`, camera `2050006783`, navigation `1878886997`, and Player movement `81962842`; Butler root derives from Player movement | Exact identity characterization; one-document scene graft; source guards ban all six owner searches and Player selector; repeated resolution/room travel/chapter-skip lifecycle; full 240-test suite remains 191/49. |
| Chapter1ArrivalController caller-owned manager rebinding | `BeginChapter1` silently replaced the serialized manager with any non-null caller | The command accepts its serialized manager (or null convenience call) and rejects a different manager before mutation | Source characterization; explicit mismatch test proves manager/sequence state unchanged; valid MainMenu/Chapter lifecycle; full suite remains 191/49. |
| Chapter1ArrivalController front-door trigger repair | Startup searched four object-name aliases, could synthesize a trigger/renderer/action/collider or UI hit target, derived collision from presentation, and could reassign ownership from a generic click-target path | Component `3302000001` owns authored action `1180734300`; its GameObject already owns renderer `1180734298` and enabled trigger `1180734299`, all validated before use | Exact two-owner characterization; one-line serialized graft audit; source guards ban every alias/search/factory/repair path; one-owner repeated configuration/room-round-trip/skip lifecycle; full suite remains 191/49. |
| Chapter1ArrivalController immutable catalog/room/anchor discovery | Startup and guest paths loaded the footstep catalog by Resources path, scanned all RoomAnchors, object names, duplicate door triggers, and RoomContentGroups, then could synthesize Drawing Room seats | Component `3302000001` owns catalog GUID `0e780686c6653db1a1c74916a591d484`, Entrance placemark `3501000027`, door target `3501000021`, room owners `2102000002`/`2300000007`, and ordered points `3502000101`–`3502000122` | Exact dynamic-result characterization; one-document graft/cleanup audits; source guards; repeated-resolution/room-round-trip/skip lifecycle with exact transforms, order-swap rejection, and zero runtime seats; full suite improves to 194/46 with no new failure. |
| Chapter1/DoorbellSystem owner, audio, resource, and generated-tone repair | Chapter 1 globally searched/created the doorbell; the doorbell found its clock/source, added source/binding components, loaded by Resources path, then synthesized a replacement clip | Existing Chapter 1 host owns serialized doorbell `3302000003`, source `3302000004`, Game-Sounds binding `3302000005`, clock `3301000001`, and imported clip GUID `67dc6970d473422a86e0c071ef23abd1` | Exact runtime-result characterization; three-document graft and one-property cleanup audits; zero-repair source guards; first-ring/repeated-resolution/room-round-trip lifecycle; full suite remains 194/46. |
| Runtime `Chapter1_ClickTarget_DrawingRoomExit` and generic click-target factory | Preparation created an invisible `160x160` trigger under inactive Drawing Room anchors; repeated initialization duplicated it, and all copies persisted inert into Chapter 2 | No replacement: authored passage `DoorTrigger_GEH_DrawingRoom` owns navigation, and empty-door/coat/guest/room-change paths already own the completion gate | Exact duplicate/persistence characterization; one-property scene audit; zero-target/static guards; rendered completion-ready canonical door handoff plus repeated initialization/travel/skips; full suite remains 194/46. |
| `Assets/Scripts/Story/GrandfatherClockInteraction.cs` and dead Chapter 1 clock-action/HUD couplings | Zero serialized instances; startup globally selected an unordered empty clock placeholder, injected a second Atmosphere source/binding and synthetic two-second tick, while its generic close-up and action were unreachable | Serialized `ClockTickingAmbienceController` owns room-aware ticking; Chapter 2 HUD/controller own the reachable seven-o'clock presentation and strike | Exact injected-graph characterization; zero serialized GUID/action proof; three-null-property scene audit; rendered repeated-initialization/room-round-trip/Chapter-2 lifecycle proves both authored placeholders stay component-free and canonical clock owners remain exact; full suite remains 194/46 with an unchanged failure-name set. Its `.meta` was removed and the architecture guard requires the path to stay pruned. |
| Chapter 1 / `GameTimeHUD` owner and view repair | Chapter 1 searched or attached a time-HUD owner; that owner searched/created a Canvas, TMP text, Shadow, and EventSystem and hid a legacy settings panel | Global GameRoot child `1878887120` owns HUD `1878887125` plus direct clock/Canvas/text/Shadow edges; the authored EventSystem remains separate and `RuntimeSettingsMenu` owns debug controls | Exact pre-change lifecycle characterization; eleven-document graft audit; one-property cleanup audit; zero-search/factory source guards and debt ceiling; rendered room/Chapter-2/seven-PM identity lifecycle; full suite remains 194/46 with an unchanged failure-name set. |
| `ChapterIntroUI` Canvas/view repair | Startup and every transition globally searched/created a dedicated Canvas, overlay, fade Image, and TMP title, added scaler/raycaster components, and rewrote hierarchy/layer/layout | Owner `3301000003` directly references canonical scene-root Canvas `1878887142`, stretched overlay `1878887151`, fade `1878887163`, and title `1878887173`; validation reports an invalid graph without repairing it | Exact pre-change characterization and fifteen-document graft; root-Canvas layout red gate corrected to Unity's supported top-level hierarchy; source/debt guards; rendered Chapter 1/2/seven-PM identity lifecycle; full suite remains 194/46 with an unchanged failure-name set. |
| Chapter 1 guest-scale owner factories | Chapter 1 or `GuestRoomScaleApplier.EnsureInScene` could create a replacement applier/calibration at runtime | Chapter 1 binds the existing applier, which owns the authored calibration and approved Butler source; only per-guest `GuestScaleParticipant` creation remains dynamic | Lifecycle test proves exact owner identity and Butler-source equality through room travel and repeated Chapter 2 skips; static guard bans both owner factories while affirming participant creation; full Unity gate. |
| `DialogueSpeechService.FindOrCreate` / `SubtitleService.FindOrCreate` | Callers could create unregistered, context-free replacements for core GameRoot services | Gameplay serializes both services and explicitly binds their service/line-bank/navigation/chapter edges | Lifecycle test proves both exact services are initialized/context-bound while auxiliary views stay lazy; static guard bans both factories/call sites; 6/6 scene audit and full Unity gate. |
| `GuestVoiceLinePlayback.FindOrCreate` / `SpeakingCharacterIndicator.FindOrCreate` | First dialogue could create replacement root owners at runtime | Gameplay serializes one voice owner with a dedicated AudioSource and one indicator owner, then binds both to the dialogue services | Lifecycle test proves both owners are inert at boot and reused across repeated speech; static guard bans root factories while affirming the lazy bubble child; 11/11 scene audit and full Unity gate. |
| Dialogue player lookup and primary voice component repair | Blocking speech globally selected a Butler movement component; first real voice line attached its channel-volume owner at runtime | DialogueSpeechService serializes Player component `81962842`; the primary source serializes binding `1878887003` and GuestVoice configures it directly | Real cataloged line gate first exposed nondeterministic player selection, then proves exact player/source/binding identity and both input restoration states; byte-preserving scene audit and full-suite failure set pass. Temporary overlap audio remains intentionally dynamic. |
| Coroutine-local dialogue input restoration | A cancelled line restored input on its next coroutine frame, potentially overriding a chapter transition or a newer blocking line | DialogueSpeechService owns one tokenized lease and releases it synchronously on cancel, with token-checked natural release plus disable/shutdown cleanup | Lifecycle simulates cancel-then-transition and cancel-then-replacement ordering and proves stale routines cannot re-enable the Butler; full suite retains the exact baseline failure-name set. |
| Subtitle/voice asset and navigation repair | Every subtitle/voice resolution could reload already serialized assets or search globally for the already bound navigation service | SubtitleService owns its line bank/navigation; GuestVoiceLinePlayback owns its catalog/navigation/source/binding and is validated through DialogueSpeechService | Static guards ban the exact repair paths while preserving presentation/overlap factories; real cataloged voice, input lease, room-loop lifecycle, two-document scene audit, and full-suite failure set pass. |
| Redundant global dialogue cleanup lookups | Dialogue/Chapter 2/subtitle cleanup searched globally after already addressing their bound owners | DialogueSpeechService, SubtitleService, and Chapter2Controller call their serialized voice/indicator/service references directly | Repeated-speech lifecycle and room round trip pass; regression tests require direct owner cleanup; static guard bans `HideAnyCurrent` and `StopAnyCurrentSpeech`; full Unity gate. |
| `GuestVoiceLinePlayback.StopAnyCurrentLine` and debug-transition dialogue searches | Chapter skips/settings/teleports globally searched voice/subtitle owners | ChapterManager exposes one direct debug-transition command that cancels queued speech then clears SubtitleService; settings delegates before navigation | Lifecycle begins active speech and proves a repeated Chapter 2 skip stops it without replacing any owner; static guards ban the global API/searches; settings ordering regressions and full Unity gate. |
| Chapter 1 HUD owner repair | Chapter 1 searched globally and could add `Chapter1InteractionHUD` at runtime | Gameplay serializes one HUD on the Chapter 1 controller and binds it directly | Lifecycle proves exact HUD/canvas/status identity and sorting through room travel/repeated skips; static guard bans lookup/factory and the obsolete `createRuntimeHud` flag; full Unity gate. |
| `RuntimeSettingsMenu.FindOrCreate` / settings-canvas root factory | Navigation startup searched/created a settings owner and overlay canvas | Gameplay serializes one correctly scaled settings canvas/owner under GameRoot and binds all four external dependencies | Lifecycle opens/closes the menu and proves input/time-scale/identity across travel/skips; 7/7 scene audit; static guard bans root/canvas factories while affirming nested control construction; full Unity gate. |
| RuntimeSettings exploration-music volume-binding factory | Settings initialization/update added or reconfigured `GameAudioSourceVolume`; after source serialization its uncaptured base volume was clamped to zero | `Audio_ExplorationMusic` serializes one Music-channel binding with base volume `0.125`, and RuntimeSettings references/applies that exact owner | Desired-volume test first failed at `0.0`, then passed at `0.125`; lifecycle proves source/binding identity through reinitialization, travel, and skips; source-document hash and full-suite failure set remain exact. |
| RuntimeSettings external dependency/EventSystem/RectTransform repair | Settings startup and commands searched for serialized navigation, chapter, clock, music, EventSystem, and its own UI transform, with root/component creation fallbacks | RoomNavigationManager delegates validation of the menu's complete serialized graph; Gameplay owns one EventSystem and RectTransform; the menu directly uses those owners and constructs only its nested controls | Static guard proves zero RuntimeSettings scene searches and bans the repair APIs; scene-reset and MainMenu/round-trip lifecycle gates pass; full suite retains the exact baseline failure-name set. |
| Fireplace/clock ambience root and self-repair factories | Navigation and each ambience owner could globally search, load catalogs, create roots, and add missing audio/filter components | Gameplay serializes two dedicated owners with explicit navigation/catalog/source/filter references | Characterized audio contract and identity through travel; 9-document graft audit; 5/5 cleanup audit; static bans; lifecycle and full Unity gate. |
| Tea-table actor projection and blocker-owned sorting | `RoomProjectedEntity` and `ObjectMovementBlocker2D` both rewrote one static cutout every frame from incompatible depth spaces | One static `SetPieceView` owns room-local order `6627`; the same blocker/polygon owns navigation only | Before/after writer characterization; exact asset audit across scene/two prefabs; bound lifecycle identity; blocker no-op proof; full Unity gate. |
| Purple-armchair actor projection and blocker-owned sorting | `RoomProjectedEntity` and `ObjectMovementBlocker2D` both rewrote one static chair cutout from incompatible depth spaces | One static `SetPieceView` owns room-local order `8289`; the same blocker/polygon owns navigation only | Before/after writer characterization; in-place three-asset migration with unchanged document IDs/order; bound lifecycle identity; exact art/transform/polygon checks; blocker no-op proof; full Unity gate. |
| Purple-sofa blocker-owned sorting | `ObjectMovementBlocker2D` derived visual order from collider world bounds for a static sofa with no explicit presentation owner | One static `SetPieceView` owns room-local order `5385`; the same blocker/polygon owns navigation only | Before/after writer characterization; exact three-asset component graft and document-order audit; bound lifecycle identity; exact art/transform/polygon checks; blocker no-op proof; full Unity gate. |

## Slice 2.1 Group 06 no-prune evidence

Group 06 is a compatibility migration of the Butler's Pantry/Billiard Room reciprocal passage pair, not a deletion or final ownership slice. It deliberately deletes no source file, scene document, prefab, asset, `.meta` file, GUID, trigger, collider, blocker, cutout, or legacy navigation representation. The existing trigger and passage documents remain in their original order and retain their identities; the scene change is limited to binding the two existing callers, replacing the four authored anchor coordinates for this pair, and advancing the two existing rollout-stage values.

| Retained candidate / content | Current Slice 2.1 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Gameplay now contains 14 bound canonical callers covering seven reciprocal pairs, including Group 06; 31 of 45 trigger callers remain null and continue through the legacy-compatible route | Retain until all 45 triggers use canonical passages, the target `NavigationService`/`PassageInteraction` owns execution, serialized references are migrated, and every directed transition passes parity tests |
| Legacy door catalogs, parsers, prompt/camera sequences, and room visual representations | Group 06 certification protects exact passage-definition hashes, reciprocal topology, authored anchors, room visibility, event/audio behavior, and preserved legacy data | Retain until code, serialized, UnityEvent/animation/reflection/resource, and behavioral evidence proves every consumer has moved to the single canonical graph |
| Billiard Room's three blockers/cutouts and their collision/occlusion data | Group 06 certification records the exact blocker/cutout inventory while the lifecycle gate traverses the calibrated authored arrival | These are required world content, not dead-code candidates; any later `SetPieceView` ownership migration must preserve collision, occlusion, transforms, document IDs, and GUIDs before old writer logic can be removed |

No Group 06 deletion may be inferred from a passing text-reference scan alone. A future pruning slice must fill the deletion-proof template below and commit the removal separately from route migration.

## Slice 2.2-07 Group 07 no-prune evidence

Group07 completes the Butler's Pantry/Service Corridor reciprocal passage ownership seam; it is not a deletion slice. No source, scene document, prefab, existing `.meta`/GUID, legacy catalog entry, room prop, collider, blocker, floor boundary, presentation owner, or compatibility API was removed. The accepted state is Gameplay `6,032` documents / nine RoomViews / sixteen stage-2 Passages / 45 triggers, `GameDatabase 35` (`19` rooms / `16` passages), dependencies/callers `16 bound / 29 null`, and inventory `16 complete / 24 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained candidate / content | Current Slice 2.2-07 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Group07 certification advances canonical callers to `16 bound / 29 null`; authored profile `7cc7c8706a02443b30c91c7a3cdcce40b7c04fec2f54e3f2eb7d5acad7058ea9` preserves far/near traversal, event/audio order, prompt/cursor cleanup, camera, and visibility | Retain until all 45 triggers use canonical passages, target navigation/interaction owners replace compatibility execution, all serialized consumers are migrated, and every directed transition passes parity gates |
| `Assets/Resources/Navigation/doors.txt` Group07 entries | The file remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and intentionally retains `ButlersPantry_ServiceCorridor` and `ServiceCorridor_ButlersPantry` | The legacy parser/catalog remains active for unmigrated routes and compatibility behavior; remove entries only with consumer-zero proof in a separate prune commit |
| `service_corridor_left_table_0` and `service_corridor_right_desk_0` cutouts | Static and rendered characterization preserve both SpriteRenderers, their art/transform/sort behavior, and their Service Corridor ownership | These are required physical-world presentation, not dead code; any later presentation-owner migration must preserve occlusion, identity, transforms, asset GUIDs, and rendered parity |
| `PlayerBlocker_service_corridor_left_table_0`, `PlayerBlocker_service_corridor_right_desk_0`, and `PlayerBoundary` | Navigation characterization preserves both exact prop collision owners and the room's walkable boundary while both authored anchors remain exact/path-reachable/non-projected | These colliders define approved collision and traversal space; they may be replaced only by an explicitly serialized, behaviorally equivalent world-ownership migration |

Group07 deletion cannot be inferred from canonical route success or text-reference scans. Exact evidence hashes are scene `c48e6f4a68cd311e958677136b572e06c05266ca556153dafd8e2656ffb1d30c`, inventory `47440df151a302a10493d14f2258c470a4a07d8683b858caf73f1c82e07c8c15`, and GameDatabase `155e065c03ec20c6b7d5f8e0a8e1746d1da166d74dc8d8c4214db8f7446989f1`. Static/serialization/GUID/meta and compilation gates pass; relevant EditMode execution accounts for 71 passing cases, and real Group07/cold-start PlayMode each pass `1/1`. Human review remains for both doorway landings, camera/active-room visibility, prompt/cursor clearing, and both Service Corridor cutout/blocker relationships. The next safe migration is tests-first Group08 Service Corridor <-> Kitchen characterization, not pruning.

## Slice 2.2-08 Group 08 no-prune evidence

Group08 completes the Service Corridor/Kitchen reciprocal passage ownership seam; it is not a deletion slice. No runtime source, scene document, prefab, existing `.meta`/GUID, legacy catalog entry, room prop, collider, blocker, floor boundary, presentation owner, compatibility API, or fallback representation was removed. The accepted state is Gameplay `6,035` documents / ten RoomViews / eighteen stage-2 Passages / 45 triggers, `GameDatabase 37` (`19` rooms / `18` passages), dependencies/callers `18 bound / 27 null`, and inventory `18 complete / 22 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained candidate / content | Current Slice 2.2-08 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Group08 certification advances canonical callers to `18 bound / 27 null`; RoomView-local profile `6d5f83aa399b12d8945a660e0372f65206c2c04a52e2657422ea92966fe0393b` preserves reciprocal traversal, event/audio order, prompt/cursor cleanup, camera, visibility, and invariant placement | Retain until all 45 triggers use canonical passages, target navigation/interaction owners replace compatibility execution, every serialized consumer migrates, and all directed transitions pass parity gates |
| `Assets/Resources/Navigation/doors.txt` Group08 entries | The file remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and intentionally retains `ServiceCorridor_Kitchen` and `Kitchen_ServiceCorridor` | The legacy parser/catalog remains active for unmigrated routes and compatibility behavior; remove entries only with consumer-zero proof in a separate prune commit |
| `service_corridor_left_table_0` and `service_corridor_right_desk_0` plus their matching movement blockers and `PlayerBoundary` | Static and rendered characterization preserve both cutout/blocker pairs, their transforms/sorting/collision behavior, and the exact Service Corridor walkable boundary while the RoomView-local anchor stays exact | These are required physical-world presentation and collision content; any later owner migration must preserve occlusion, identity, transforms, asset GUIDs, boundary behavior, and rendered parity |
| `kitchen_work_table`, `PlayerBlocker_kitchen_work_table`, and Kitchen's room boundary | Group08 certification preserves the work-table SpriteRenderer, its authored collision owner, the Kitchen hierarchy, and exact/path-reachable arrival at every rendered profile | These are required Kitchen presentation and navigation geometry, not dead code; replacement requires an explicitly serialized, behaviorally equivalent world-ownership migration |

Group08 deletion cannot be inferred from canonical route success or text-reference scans. Exact evidence hashes are scene `5203d054870b01b2e6db55286bc4a2d6b25ee6795cb84765306728090b4e1d98`, inventory `b692f1333f0271903fd9ff60e20323ce628311f37592a481a23fad142b468ae3`, and GameDatabase `9b9b4ae428340a9fd6597e18873adbdce9b3a675786dbee568adb9a595d710b1`. Static/serialization/GUID/meta and compilation gates pass; all 76 relevant EditMode cases are accounted, and real Group08 plus cold-start PlayMode pass `2/2`. Human review remains for both doorway landings, camera/active-room visibility, prompt/cursor clearing, Service Corridor cutout/blocker/boundary behavior, and Kitchen work-table/boundary occlusion and collision. At that checkpoint, the next safe migration was tests-first Group09 Service Corridor <-> Chapel characterization, not pruning.

## Slice 2.2-09 Group 09 no-prune evidence (historical)

Group09 completes the Service Corridor/Chapel reciprocal passage ownership seam from clean rollback commit `07bb61fff50f63dfdc36acf244b391b26fa48d61`; it is not a deletion slice. No runtime source, scene document, prefab, existing `.meta`/GUID, legacy catalog entry, room prop, collider, blocker, floor boundary, presentation owner, story-placement anchor, compatibility API, or fallback representation was removed. The accepted state is Gameplay `6,038` documents / eleven RoomViews / twenty stage-2 Passages / 45 triggers, `GameDatabase 39` (`19` rooms / `20` passages), GameRoot `40` scene behaviours / `48` total serialized references, dependencies/callers `20 bound / 25 null`, and inventory `20 complete / 20 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained candidate / content | Historical Slice 2.2-09 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Group09 certification advances canonical callers to `20 bound / 25 null`; RoomView-local profile `bff3067e14fd0710f8de6dd4f7e4434d9025a2b7593adf3cd54571020a622475` preserves reciprocal traversal, event/audio order, prompt/cursor cleanup, camera, visibility, and invariant placement | Retain until all 45 triggers use canonical passages, target navigation/interaction owners replace compatibility execution, every serialized consumer migrates, and all directed transitions pass parity gates |
| `Assets/Resources/Navigation/doors.txt` Group09 entries | The file remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and intentionally retains `ServiceCorridor_Chapel` and `Chapel_ServiceCorridor` | The legacy parser/catalog remains active for unmigrated routes and compatibility behavior; remove entries only with consumer-zero proof in a separate prune commit |
| Service Corridor left-table/right-desk cutouts, matching movement blockers, and `PlayerBoundary` | Static and rendered characterization preserve both cutout/blocker pairs, their transforms/sorting/collision behavior, and the exact Service Corridor walkable boundary while the RoomView-local anchor stays exact | These are required physical-world presentation and collision content; any later owner migration must preserve occlusion, identity, transforms, asset GUIDs, boundary behavior, and rendered parity |
| Chapel bench cutouts/blockers, additional foreground cutouts, `PlayerBoundary`, and `Ch2_Hide_Guest06` | Certification preserves `chapel_bench_0`, `chapel_bench_right1_0`, `chapel_bench_right_2_0`, their three exact blockers, the additional `chapel_char_right2_0`, `chapel_right_3_0`, and `chapel_seat_left3_0` cutouts, the 18-point boundary, and the Chapter 2 hide anchor | These are required Chapel presentation, navigation, and story-placement content, not dead code; replacement requires explicit serialized ownership plus collision, occlusion, transform, GUID, and Chapter 2 behavioral parity |

Group09 deletion cannot be inferred from canonical route success or text-reference scans. Exact evidence hashes are scene `220009ae5f8d2bbe3a3e62ccab6d9c88da56a4c66d9dde092da11f0cae8b8a93`, inventory `43a8134256b39291e7954141ef15d11c3a179b735f65f6437088779dd8abbc2c`, and GameDatabase `90734ca890e57a4b64062e10a7e2c2e04f281dc783beb96f012c2fa673674ebc`. Static controls pass at `113` runtime files / `50,339` lines / `48` direct MonoBehaviours / `113/113` runtime-ledger rows, `164` scripts, `1,952` serialized references (`1,096` project / `856` external-package), and `7,362` paired, unique meta GUID records. Unity compilation, focused EditMode `4/4`, final affected architecture `62/62`, rendered lifecycle `1/1`, and real Group09 plus cold-start PlayMode `2/2` pass. Human review remains for both doorway landings, camera/active-room visibility, prompt/cursor clearing, and every named Service Corridor/Chapel occlusion, collision, boundary, and hide-anchor relationship. The next safe migration is tests-first Group10 Grand Entrance Hall <-> Grand Entrance Hall Rear characterization, including the room-name case alias, not pruning.

## Slice 2.2-10 Group 10 accepted no-prune evidence

Group10 authors the Grand Entrance Hall/Grand Entrance Hall Rear reciprocal ownership seam from clean rollback commit `f91ee2338261ee061a8502e06542fd49b5d81831`; it is not a deletion slice. No runtime source, scene document, prefab, existing `.meta`/GUID, trigger, compatibility path, room prop, railing cutout, collider, floor boundary, or fallback representation is removed. The accepted state is Gameplay `6,041` documents / twelve RoomViews / twenty-two stage-2 Passages / 45 triggers, `GameDatabase 41` (`19` rooms / `22` passages), GameRoot `43` scene behaviours / `51` total references including eight services, dependencies/callers `22 bound / 23 null`, and inventory `22 complete / 18 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained candidate / content | Current Slice 2.2-10 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Group10 binds canonical callers `1858342503 -> 4100000033` and `70736571 -> 4100000034`, but 23 of 45 callers remain null; the six-line legacy fingerprint stays locked at `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638` | Retain until all triggers use canonical passages, target navigation/interaction owners replace compatibility execution, every serialized consumer migrates, and all directed transitions pass parity gates |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | The file contains neither `GEH_GEH_Rear` nor `GEH_Rear_GEH_Front` and remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; exact Inspector routes and the title-case `Grand Entrance Hall Rear View` compatibility name remain protected | The incomplete text graph cannot regenerate this pair, while its parser/catalog still serves unmigrated routes; retain all compatibility execution until separate consumer-zero proof |
| Entrance/Rear bottom-edge triggers and destination regions | Trigger components `1858342503` / `70736571`, approaches `(0.00030518, -456.4991)` / `(10.2463989, -437.093964)`, and both exact four-corner regions preserve the characterized multi-lane interaction instead of collapsing it to a point | These owners still provide input, hover/prompt transfer, audio/event order, and compatibility traversal; replacement requires verified canonical EditMode, rendered, PlayMode, and manual parity |
| Four railing cutouts and both walkable boundaries | `Grand_entrance_railing_left_0`, `grand_entrance_railing_right_0`, `back_entrance_railing_left_0`, and `back_entrance_railing_right_0` remain in place; the Entrance boundary remains 49 points and Rear remains 5 points | These are required occlusion and collision/traversal content, not dead code; any later ownership migration must preserve art, transforms, sort behavior, GUIDs, and rendered/path parity |

Exact evidence hashes are scene `867e9395e38b4a2e675d5e0fc6abbf9449cb03f4e75ab2c3b4397172efde44ae`, inventory `3291b6e856a252d78411de186e76f818dda12bd9b63cae1972bfd7001187aa5d`, and GameDatabase `90417c118dfc1cda7bcb3cf17c1d92fda30af7433be7ae446162146925e2c39a`. Static controls pass at `114` runtime files / `51,062` lines / `48` direct MonoBehaviours / `114/114` ledger rows, `165` scripts, `1,957` serialized references (`1,101` project / `856` external-package), and `7,365` paired, unique meta GUIDs. Unity `6000.4.10f1` compiles; focused EditMode passes `4/4`, affected architecture `67/67`, rendered Group10 `1/1` with exact locked SHA, and genuine Group10 plus cold-start PlayMode `2/2`. These gates certify replacement behavior, not deletion. Human review remains. The next safe migration is tests-first Group11 Grand Entrance Hall Rear <-> Billiard Room characterization, including the `doors.txt` endpoint conflict; it is not pruning.

## Slice 2.2-11b source-region approach prerequisite accepted no-prune evidence

Slice `2.2-11b` starts from clean rollback commit `78f61103b6c4582f72ee6d12f4031dd5c763d93e` and introduces only the characterized source-region approach prerequisite for Group11. No source file, scene document, prefab, existing `.meta`/GUID, production definition, Passage, caller, catalog entry, room content, collider, boundary, or compatibility API is removed. The only retired code is the duplicate standard-door approach sampler inside `DoorTriggerNavigation`; both compatibility and canonical region paths now delegate to the Passage-owned resolver.

| Retained candidate / content | Current Slice 2.2-11b evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | The trigger retains its legacy private seam but no longer owns a second sampler; existing counts remain `22 bound / 23 null`, and focused navigation passes `3/3` | Retain until all 45 callers use canonical Passages, target navigation/interaction ownership replaces compatibility execution, serialized consumers migrate, and every route passes parity gates |
| `Passage` and `PassageArrivalResolver` | The default exact-point policy preserves all 22 existing Passages; the explicit source-region policy derives reciprocal region ownership and five rendered profiles are bit-exact to both Group11 legacy triggers | These files are the new sole region-selection owner, while Group11 production content is not yet authored; deletion or renaming is premature |
| `Assets/Resources/Navigation/doors.txt` | The file remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and still contains the characterized Rear/Billiard endpoint conflict | The parser/catalog still serves unmigrated routes; preserve it until Group11 and all remaining consumers migrate and separate consumer-zero proof passes |
| Group11 triggers, Billiard/Rear geometry, boundaries, cutouts, and blockers | This prerequisite changes no scene or content bytes; tests characterize region selection without moving or deleting physical-world owners | These remain required interaction, presentation, occlusion, and collision content; production authoring and manual parity must precede any prune decision |

Production hashes remain scene `867e9395e38b4a2e675d5e0fc6abbf9449cb03f4e75ab2c3b4397172efde44ae`, inventory `3291b6e856a252d78411de186e76f818dda12bd9b63cae1972bfd7001187aa5d`, GameDatabase `90417c118dfc1cda7bcb3cf17c1d92fda30af7433be7ae446162146925e2c39a`, and legacy catalog `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. Static controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours / `114/114` ledger rows, `165` scripts, `1,957` serialized references, and `7,365` paired unique meta GUIDs. Unity compilation and focused navigation/canonical `7/7` pass; all `68/68` affected cases are accounted, including the direct near-player mismatch rejection; rendered characterization passes `2/2` with locked Group11 SHA `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`; and representative Group09/Group10 plus cold start passes `3/3`. Cold start retains digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. The next safe action is production Group11 authoring, not pruning.

## Slice 2.2-11 Group 11 accepted no-prune evidence

Group11 authors the Grand Entrance Hall Rear View/Billiard Room reciprocal ownership seam from clean rollback commit `9ed538fa209c239f50675dbc0b9a04c0bbaa01ea`; it is not a deletion slice. No runtime source, scene document, prefab, existing `.meta`/GUID, trigger, catalog row, compatibility path, room prop, collider, boundary, cutout, blocker, art, audio, or fallback representation is removed. The accepted state is Gameplay `6,043` documents / twelve RoomViews / twenty-four stage-2 Passages / 45 triggers, `GameDatabase 43` (`19` rooms / `24` passages), GameRoot `45` scene behaviours / `53` total references including eight services, dependencies/callers `24 bound / 21 null`, and inventory `24 complete / 16 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained candidate / content | Current Slice 2.2-11 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Group11 binds callers `357269799 -> 4100000035` and `2300000124 -> 4100000036`, but 21 of 45 callers remain null; locked legacy/canonical rendered parity passes `2/2` | Retain until all triggers use canonical Passages, target navigation/interaction owners replace compatibility execution, every serialized consumer migrates, and all directed transitions pass parity gates |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | The file remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` and still describes front Entrance/Billiard while live Inspector ownership is Rear View/Billiard | This known conflict is protected evidence, not permission to rewrite the catalog; parser/catalog consumers must reach zero before a separate reviewed prune |
| Group11 triggers and exact reciprocal regions | Existing triggers `357269799` / `2300000124` retain their rectangles and own input/prompt/audio compatibility; canonical Passages store exact reciprocal destination regions and derive source approaches without duplicate points | Trigger deletion requires a replacement interaction owner plus hover, preferred-click, movement, audio, event, prompt/cursor, camera, and rendered parity for every consumer |
| Billiard cutouts/blockers, Rear rails, and both room boundaries | The real Group11 round trip and cold-start gate pass without modifying Billiard's 11-point boundary or its table/left-armchair/left-lamp-table cutout-blocker pairs, Rear's 5-point boundary, or the two certified rear railing cutouts | These are required physical presentation, collision, and occlusion content, not dead code; later SetPiece/geometry ownership must preserve serialized IDs, GUIDs, transforms, sorting, and behavior |

Exact evidence hashes are scene `ae7a5c400b4edc9a874331bb54a1684d759239f9ccbb03975a3975b3e6649585`, inventory `c5cec68785b0699aedcd8a5dae46ecbb14a7a94ae969d04e1ac75a70cc05d4fe`, GameDatabase `2c8f1dfbb90fc81f8032e3f8c3010dc3e394e0e2a397f4a46cc34d0b5a05c9a0`, and legacy catalog `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. Static controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours / `114/114` ledger rows, `165` scripts, `1,961` serialized references (`1,105` project / `856` external-package), and `7,367` paired unique meta GUIDs. Unity compiles; focused EditMode passes `70/70`, rendered parity `2/2`, and Group11/representatives/cold PlayMode `4/4`. These gates certify ownership behavior, not deletion. Human review remains. The next safe migration is tests-first Group12 Rear View <-> Conservatory characterization, not pruning.

## Slice 2.2-12 Group 12 accepted no-prune evidence

Group12 completes the Grand Entrance Hall Rear View/Conservatory ownership seam; it is not a deletion slice. Accepted totals are Gameplay `6,046` documents / thirteen RoomViews / twenty-six stage-2 Passages / 45 triggers, `GameDatabase 45` (`19` rooms / `26` passages), GameRoot `48` scene behaviours / `56` references, dependencies/callers `26 bound / 19 null`, and inventory `26 complete / 14 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained content | Accepted Slice 2.2-12 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Callers `1119941194 -> 4100000038` and `2300000074 -> 4100000039` leave 19 of 45 callers legacy/null | Retain until all callers use target services, all serialized consumers migrate, and every directed transition passes parity and human review |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | The catalog still places `GEH_Conservatory` under front Entrance and uses `Conservatory_GEH`, while the live route is Rear View with reverse ID `Conservatory_GEH_Rear_View` | Preserve this known conflict until parser/catalog consumers reach zero; route migration is not permission to rewrite or delete it |
| Group12 triggers and mixed interaction behavior | Trigger `1119941194` owns standard-door preferred-click/movement behavior; trigger `2300000074` owns immediate bottom-edge behavior; Passages `4100000038/39` own canonical identity, reciprocal topology, and source/destination-region placement | Trigger deletion requires a replacement interaction owner with exact movement/immediacy, hover, prompt, cursor, event, audio, camera, visibility, and rendered parity |
| Rear and Conservatory physical content | Rear's rails/five-point boundary and Conservatory's seven-point boundary plus four cutout/blocker pairs remain protected | These are required collision/occlusion/presentation content; later SetPiece/geometry migration must preserve identities, transforms, sorting, and behavior |

Accepted identities are Conservatory RoomView `4100000037`, definition GUIDs `2388aec2b64647e2a7b6c50c3ee3c8b6` / `d54f1f34f2fb45428117d7b831c0ef40`, and Passages `4100000038` / `4100000039` on existing triggers `1119941194` / `2300000074`. Exact hashes are Gameplay `3138c43665353b0a3a10336ce36ab3c8aa72386ddb5c46f7d46ed683c33bac00`, GameDatabase `7d7520fb992cf2c39ba1ef6b696d4a2a49148c9c208205129b46593a1e14957d`, inventory `8e626c25cbca2e26d7390149540daf6745187707048ce40fc3eaf30e4f31f795`, and unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. Static controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours / `114/114` ledger rows, smells `106/17/67/51/4/6`, `165` scripts, `1,966` serialized references (`1,110` project / `856` external), and `7,369` paired unique meta GUIDs. Unity compiles; the preserved focused run is `72/71/1` and the isolated test-literal correction passes `1/1`, accounting for all `72`; rendered Group12/Group11 passes `2/2`; Group12/representatives/cold PlayMode passes `4/4`. These gates certify ownership behavior, not deletion. Human review remains for both landings, immediate return, prompt/cursor/audio, camera/visibility, and protected collision/occlusion. No deletion is authorized. The next safe action is tests-first Group13 Service Corridor <-> Side Stair & Mudroom characterization from the forthcoming Group12 commit, not pruning.

## Slice 2.2-13 Group 13 accepted no-prune evidence

Group13 completes the Service Corridor / Side Stair & Mudroom reciprocal ownership seam; it is not a deletion slice. Accepted totals are Gameplay `6,049` documents / fourteen RoomViews / twenty-eight stage-2 Passages / 45 triggers, `GameDatabase 47` (`19` rooms / `28` passages), GameRoot `51` scene behaviours / `59` references including eight services, dependencies/callers `28 bound / 17 null`, and inventory `28 complete / 12 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained content | Accepted Slice 2.2-13 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Callers `2300000174 -> 4100000041` and `2300000184 -> 4100000042` are canonical, but 17 of 45 callers remain legacy/null | Retain until all callers use target services, every serialized consumer migrates, and all directed transitions pass parity and human review |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | The catalog remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; Group13 aliases and IDs remain active compatibility input | Preserve until parser/catalog consumers reach zero and a separate reviewed prune proves no resource, reflection, serialized, or runtime consumer remains |
| Group13 triggers and reciprocal regions | Existing triggers `2300000174` / `2300000184` retain standard-door input/prompt/audio behavior; Passages `4100000041` / `4100000042` own canonical identity, reciprocal topology, and exact source/destination-region placement | Trigger deletion requires a replacement interaction owner with exact hover, preferred-click, movement, event, audio, prompt/cursor, camera, visibility, and rendered parity |
| Service Corridor and Side Stair & Mudroom physical content | The Group13 and representative/cold PlayMode gate passes `5/5` without deleting or re-authoring room collision and occlusion objects | These remain required physical presentation and traversal content; later SetPiece/geometry ownership must preserve serialized IDs, GUIDs, transforms, sorting, and behavior |

Accepted identities are RoomView `4100000040`, definition GUIDs `0491e7071cda47e7b779cf87f71d026e` / `5c0c635bc6c04da19e0909a6f81d0caf`, and Passages `4100000041` / `4100000042` on triggers `2300000174` / `2300000184`. Exact hashes are Gameplay `82e6b51a51cc3a5cf34afe6cfb26dfe35a292cafbdd6a3dd58e6221cbac3d119`, GameDatabase `13f8161b0a5f107e984c3c856bdc9f75bb922f2c3082370de571f412a42ab5f3`, inventory `2aaba6eed5c2bd28026dcca5dedb863b2ecbf646574da168799c1249b9c2fc29`, and unchanged `doors.txt` above. Static controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours / `114/114` ledger rows, smells `106/17/67/51/4/6`, `165` scripts / `1,971` serialized references (`1,115` project / `856` external), and `7,371` paired unique meta GUIDs. Unity compiles. The focused launches remain honestly recorded as `73/60/13`, `17/14/3`, and `3/3`; rendered Group13 passes in the initial combined run and the two corrected prior fixtures pass `2/2`; PlayMode passes `5/5`. These gates certify ownership behavior, not deletion. Human review remains. No deletion is authorized. The next safe action is tests-first Group14 Side Stair & Mudroom <-> Upper Sitting Hall characterization, not pruning.

## Slice 2.2-14 Group 14 accepted no-prune evidence

Group14 completes the Side Stair & Mudroom / Upper Sitting Hall reciprocal ownership seam; it is not a deletion slice. Accepted totals are Gameplay `6,052` documents / fifteen RoomViews / thirty stage-2 Passages / 45 triggers, `GameDatabase 49` (`19` rooms / `30` passages), GameRoot `54` scene behaviours / `62` references including eight services, dependencies/callers `30 bound / 15 null`, and inventory `30 complete / 10 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained content | Accepted Slice 2.2-14 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Callers `1915714844 -> 4100000044` and `2300000189 -> 4100000045` are canonical, but 15 of 45 callers remain legacy/null | Retain until all callers use target services, every serialized consumer migrates, and all directed transitions pass parity and human review |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | The catalog remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; the live inferred stairway and `Side Stair Mudroom` alias remain compatibility input | Preserve until parser/catalog consumers reach zero and a separate reviewed prune proves no resource, reflection, serialized, or runtime consumer remains |
| Group14 triggers and reciprocal regions | Existing triggers retain standard-door versus inferred-upward-stairway input, prompts, cursors, and audio; Passages own canonical identity, topology, and exact source/destination-region placement | Trigger deletion requires a replacement interaction owner with exact hover, movement, event, door/stair audio, prompt/cursor, camera, visibility, and rendered parity |
| Side Stair & Mudroom and Upper Sitting Hall physical content | The complete lifecycle gate passes `20/20` and PlayMode passes `5/5` without re-authoring boundaries, cutouts, blockers, lighting, or room hierarchy | These are required physical presentation and traversal content; later SetPiece/geometry ownership must preserve IDs, GUIDs, transforms, sorting, and behavior |

Accepted identities are Upper Sitting RoomView `4100000043`, definition GUIDs `d5c370a383ecaf7257b8f796d0164e72` / `77a3efbd4a250afa71e5998bb7118532`, and Passages `4100000044` / `4100000045` on triggers `1915714844` / `2300000189`. Exact hashes are Gameplay `7b802932693726a6a1c94736c715b9ffc03b53c0d31cb247db0d9cacb1082407`, GameDatabase `7bec731d33b2bd1631adfa842533f91af072523d5e9b81b0ef7822a3f8facfef`, inventory `d1c630d178b6c7ce8db765b590273b93eff3ea6372fcec503071f7ec786dd64a`, and unchanged `doors.txt` above. Static controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours / `114/114` ledger rows, smells `106/17/67/51/4/6`, `165` scripts / `1,976` serialized references (`1,120` project / `856` external), and `7,373` paired unique meta GUIDs. Unity compiles; focused EditMode passes `74/74`; complete lifecycle passes `20/20`; Group14/representatives/cold PlayMode passes `5/5`; the expanded PlayMode fixture is honestly accounted as `13/12/1` plus isolated unchanged Chapter 2 golden `1/1`. These gates certify ownership behavior, not deletion. Human review remains. No deletion is authorized. The next safe action is tests-first Group15 Upper Sitting Hall <-> Upper Gallery characterization, not pruning.

## Slice 2.2-15 Group 15 accepted no-prune evidence

Group15 completes the Upper Sitting Hall / Upper Gallery reciprocal ownership seam; it is not a deletion slice. Accepted totals are Gameplay `6,055` documents / sixteen RoomViews / thirty-two stage-2 Passages / 45 triggers, `GameDatabase 51` (`19` rooms / `32` passages), GameRoot `57` scene behaviours / `65` references including eight services, dependencies/callers `32 bound / 13 null`, and inventory `32 complete / 8 queued / 2 blocked-one-way / 3 blocked-parallel`.

| Retained content | Accepted Slice 2.2-15 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Callers `2300000209 -> 4100000047` and `2300000219 -> 4100000048` are canonical, but 13 of 45 callers remain legacy/null | Retain until all callers use target services, every serialized consumer migrates, and all directed transitions pass parity and human review |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | The catalog remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; both Group15 legacy IDs remain active compatibility input | Preserve until parser/catalog consumers reach zero and a separate reviewed prune proves no resource, reflection, serialized, or runtime consumer remains |
| Group15 triggers and reciprocal regions | Existing triggers retain standard-door input, prompts, cursor, and audio; Passages own canonical identity, topology, and exact source/destination-region placement | Trigger deletion requires a replacement interaction owner with exact hover, preferred-click, movement, event, audio, prompt/cursor, camera, visibility, and rendered parity |
| Upper Sitting Hall and Upper Gallery physical content | Rendered Group15/14 passes `2/2` and PlayMode representatives/cold passes `5/5` without re-authoring geometry or presentation | Boundaries, stairwell blocker, railing, plants/cutouts/generated blockers, guest anchor, and collision/occlusion content remain required and must preserve IDs, GUIDs, transforms, sorting, and behavior |

Accepted identities are Upper Gallery RoomView `4100000046`, definition GUIDs `9968ef27c1a4121f14100399fe85ab32` / `54dc2ad728e40e59f280711e65b3474d`, and Passages `4100000047` / `4100000048` on existing triggers `2300000209` / `2300000219`. Exact hashes are Gameplay `4ba54fac49bb37c9c693c0fee0e7c32e90aae0e29c75a5989b67da8fba64f5f2`, GameDatabase `db24627071eff29beaa11c072363d4c2abb1ebeb0012504ab39c17fee394fb7f`, inventory `e7e066f477ce3599526d0a9e3fb8404636752508113fe55dc005ba5a8d4c9820`, and unchanged `doors.txt` above. Static controls pass at `114/114` runtime-ledger rows, `165` scripts / `1,981` serialized references, and `7,375` paired unique meta GUIDs. Unity compiles; all 75 focused cases are accounted; rendered parity and PlayMode gates pass. Human review remains. No deletion is authorized. The next safe action is tests-first Group16 Upper Gallery <-> Master Bedroom Suite characterization, not pruning.

## Slice 2.2-16 Group 16 accepted no-prune evidence

Group16 completes the Upper Gallery / Master Bedroom Suite reciprocal ownership seam from clean characterization/rollback commit `9fb4a3d9131368818e6c6fb1f8009b844b9b3a5e`; it is not a deletion slice. Accepted totals are Gameplay `6,058` documents / seventeen RoomViews / thirty-four stage-2 Passages / 45 triggers, `GameDatabase 53` (`19` rooms / `34` passages), GameRoot `60` scene behaviours / `68` references including eight services, dependencies/callers `34 bound / 11 null`, inventory `34 complete / 6 queued / 2 blocked-one-way / 3 blocked-parallel`, and placement serialization `12` source regions / `14` destination regions / `10` explicit coordinate-space values.

| Retained content | Accepted Slice 2.2-16 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | Callers `2300000204 -> 4100000050` and `2300000214 -> 4100000051` are canonical, but 11 of 45 callers remain legacy/null | Retain until all callers use target services, every serialized consumer migrates, and all directed transitions pass parity and human review |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | The catalog remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; both Group16 legacy IDs remain active compatibility input | Preserve until parser/catalog consumers reach zero and a separate reviewed prune proves no resource, reflection, serialized, or runtime consumer remains |
| Group16 triggers and reciprocal regions | Existing triggers retain standard-door input, prompts, cursor, and audio; Passages own canonical identity, topology, and exact source/destination-region placement | Trigger deletion requires a replacement interaction owner with exact hover, preferred-click, movement, event, audio, prompt/cursor, camera, visibility, and rendered parity |
| Upper Gallery and Master Bedroom Suite physical content | All `856` RectTransforms are byte-identical; rendered Group16/15 passes `2/2` and PlayMode representatives/cold passes `5/5` without re-authoring geometry or presentation | Boundaries, cutouts, blockers, furnishings, collision/occlusion content, and the Master trigger's non-unit scale remain required and must preserve IDs, GUIDs, transforms, sorting, and behavior |

Accepted identities are Master Bedroom Suite RoomView `4100000049`, definition GUIDs `1dff2bb730264e9c81ecb2e35985f5a6` / `f1fd515e16c44606af75bf593f67bc07`, and Passages `4100000050` / `4100000051` on existing triggers `2300000204` / `2300000214`. Exact hashes are Gameplay `5f5b975165b3ed02d529cea132d6943278ef709f11c8913efda769b405617b5b`, GameDatabase `6df8c80cb0f98c78ac0a5fb651a44bf3096020ecc46bb02acab8116ad78e338d`, inventory `e901dde6232c67612ca877fd2258cf10f90f5eb2921eac7fded2aaa2e75a9477`, and unchanged `doors.txt` above. Static controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours / `114/114` ledger rows, smells `106/17/67/51/4/6`, `165` scripts / `1,986` serialized references (`1,130` project / `856` external), and `7,377` paired unique meta GUIDs. Unity compiles after one test-only syntax repair; focused EditMode accounts for all `76` cases (`74/2`, then `2/2`); rendered Group16/15 passes `2/2` at exact SHAs `f97e30d0807e46ee1846bc1421ebcb0f72a694988c8fda915bb7e87b4b3acb0f` / `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2`; Group16/15/14/13/cold PlayMode passes `5/5`, retaining cold SHA `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. Data audit finds no defects, with maximum region float error `0.00003220`. Human review remains. No deletion is authorized. The next safe action is tests-first Group17 Upper Sitting Hall <-> Nursery on triggers `2300000224` / `2300000234`, not pruning.

## Slice 2.2-17 Group 17 accepted production no-prune evidence

Group17 authors Nursery RoomView `4100000052`, reciprocal Passages `4100000053` / `4100000054`, and PassageDefinition GUIDs `e40848fe5ab54ec99ff3b664859e7c1b` / `c05a3dd92c9f45cf8231f1679bd06506` on existing triggers `2300000224` / `2300000234`. Its committed before-state characterization remains exact at SHA-256 `57c5dccca03303a443cde3756c02047021d934c5230e51da8e087d48a9649bc4`. Production acceptance certifies behavior parity; it is not deletion proof.

| Retained content | Accepted Slice 2.2-17 evidence | Why pruning is still unsafe / future gate |
|---|---|---|
| `DoorTriggerNavigation`, `RoomNavigationManager`, and `INavigationService` compatibility facade | The two Group17 callers are canonical, producing `36 bound / 9 null` across 45 triggers | Retain until every caller migrates, serialized consumers reach zero, all route parity/manual gates pass, and a dedicated deletion slice proves no fallback consumer remains |
| `Assets/Resources/Navigation/doors.txt` and Inspector compatibility routes | Both Group17 legacy IDs remain active compatibility input; no catalog rewrite or deletion is authorized | Preserve until parser/catalog consumers reach zero and separate resource/reflection/serialized/runtime searches pass |
| Group17 triggers and reciprocal regions | Passages own canonical identity/topology and exact reciprocal regions while existing triggers still own interaction, prompt, cursor, movement, and audio | Trigger deletion requires a replacement interaction owner plus exact focused, rendered, PlayMode, serialized-reference, and manual parity |
| Nursery and Upper Sitting Hall physical content | The accepted slice leaves authored boundaries, Nursery chair/table blockers, chest, dog toy, lighting, cutouts, room hierarchy, transforms, and neighboring routes in place | These are required world presentation/collision/occlusion assets; later consumer-zero deletion proof is still missing |

Accepted totals are Gameplay `6,061` documents / eighteen RoomViews / thirty-six stage-2 Passages / 45 triggers, `GameDatabase 55` (`19` rooms / `36` passages), GameRoot `63` scene behaviours / `71` references, direct catalogs `35` door / `1` stair / `9` unbound, inventory `36 complete / 4 queued / 2 blocked-one-way / 3 blocked-parallel`, and placement serialization `14` source regions / `16` destination regions / `10` explicit coordinate-space values. Blue Bedroom is the sole room definition without a RoomView. Architecture tools `4/4`, guard, ledger `114/114`, `165` scripts / `1,991` references (`1,135` project / `856` external), `7,379` paired unique metas, and Unity compilation pass. Focused EditMode accounts for all `77` cases (`76/1`, then isolated repair `1/1`); rendered Group17/16 passes `2/2` at SHAs `57c5dccca03303a443cde3756c02047021d934c5230e51da8e087d48a9649bc4` / `f97e30d0807e46ee1846bc1421ebcb0f72a694988c8fda915bb7e87b4b3acb0f`; Group17/16/15/14/cold PlayMode passes `5/5`, retaining cold SHA `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. Human doorway, camera, audio, and Nursery occlusion review remains, so **nothing is prunable from Group17**. The next safe action is tests-first Group18 Upper Sitting Hall <-> Blue Bedroom, not pruning.

## Quarantined for review, not deleted

| Candidate | Why it looks redundant | Why deletion is not yet safe |
|---|---|---|
| `OdditySpriteAnimator` | No current serialized instance; current requirements do not mention an oddity UI animator | Future chapter intent is unresolved and a regression test explicitly tracks the file |
| `UrpPostProcessingBootstrap` | Searches cameras and adds URP data at runtime | Render-rig parity has not yet been tested after authoring components in-scene |
| `PlayerMovement` / `CharacterController2D` | Legacy movement path beside point-click movement | Player and guest prefab bindings must be migrated and tested first |
| `DoorTriggerNavigation`, `RoomNavigationManager`, `DoorButton`, `DoorPromptSequenceController`, `DoorDataParser`, `DoorCameraSequence`, `RoomVisualCatalog` | Parallel navigation representations; Slice 1.5 places all 19 approved room backgrounds in canonical `RoomDefinition` data, and accepted Group17 extends centralized point/source/destination-region policy to eighteen reciprocal pairs | Group17 leaves 36 of 45 callers bound and 9 legacy/null, but creates no deletion proof; human review and all later runtime-consumer, route-content, interaction/prompt/audio/camera, and target-service ownership migrations must pass before any legacy file is deleted |
| `StaticNoisePlayer`, `StaticSetImagePlayer`, `StaticSet`, `StaticFrameGroup` | Overlapping frame/static presentation | Serialized content and behavior comparison is still required |
| guest/player footstep and room ambience pairs | Similar implementations | Data and behavioral differences must be captured before merging |

## Required deletion proof template

```text
Candidate:
Requirement/invariant it used to satisfy:
Replacement owner:
Code-reference result:
Serialized GUID-reference result:
UnityEvent/animation/reflection/resource result:
Scene/prefab migration command or record:
Tests protecting replacement:
Unity smoke test result:
Commit removing file:
Rollback commit:
```
