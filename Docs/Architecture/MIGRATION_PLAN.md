# Requirements-first migration plan

## Safety principle

This is not a one-shot rewrite. Each phase must leave a compiling, testable project and a separate commit. Existing scripts retain their Unity GUIDs until scene/prefab references have migrated.

## Phase 0 — Baseline and recovery point — completed in this patch

- Captured Unity version, C# inventory, line counts, direct `MonoBehaviour` count, dependency-repair patterns, and serialized script references.
- Added a static architecture debt ceiling.
- Added source-control guard automation.
- Created a prune log.

Exit condition: the uploaded project can always be reconstructed from the original zip or baseline commit.

## Phase 1 — Architecture foundations — completed in this patch

- Added `GameRoot`, `GameContext`, `ChateauBehaviour`, `GameServiceBase`, validation types, `StateMachine<TState>`, `DefinitionAssetBase`, `GameDatabase`, chapter/room/actor/UI bases, and `StoryBeatBase`.
- Rebased major existing service and chapter classes without changing script GUIDs or public APIs.
- Removed the global fallback search from `ChapterEventScheduler`; it now requires its explicit `ChapterClock` binding.
- Added foundation tests.
- Pruned two statically proven unused scripts.

Exit condition: project compiles in Unity and existing EditMode tests pass.

## Phase 2 — Serialize the composition root — completed

Run the Editor installer in Unity:

```text
Tools > Chateau > Architecture > Install or Refresh Gameplay GameRoot
```

The installer must:

- add exactly one `Chateau_GameRoot`;
- add the currently runtime-created `RoomNavigationManager` and `DoorPromptSequenceController` if absent;
- add `SubtitleService` and `DialogueSpeechService` if absent;
- create and assign `GameDatabase.asset`;
- serialize all current `GameServiceBase` components;
- save `Gameplay.unity` only if validation passes.

Exit condition:

- Gameplay scene has exactly one root and each required service;
- no missing scripts;
- EditMode tests pass;
- Chapter 1 and Chapter 2 smoke tests pass;
- scene changes are committed separately.

## Phase 3 — Remove runtime repair bootstraps — in progress

Only after Phase 2 passes:

- `RoomNavigationBootstrap` replaced by serialized GameRoot validation and removed;
- `ChapterManager` top-level manager-stack self-creation fallback removed; the separately gated Chapter 2 controller factory is also removed;
- exactly one inert `Chapter2Controller`, HUD, monster-stinger owner, panic owner, and guest-search owner serialized and explicitly wired; all five top-level Chapter 2 creation fallbacks are removed;
- the monster stinger validates and directly uses its serialized monster, Drawing Room anchors, navigation, and Image; its structural searches, child-visual repair, and primitive placeholder are removed while audio/sprite/overlay presentation remains separately gated;
- the inactive monster object owns its serialized looping violin source, Game-Sounds binding, and imported clip; host/resource/Editor/component fallbacks are removed;
- the monster object owns its serialized screen-space overlay Canvas at People order `10000`; the Get/Add component fallback is removed;
- the stinger owns the eight approved run sprites as an ordered serialized array from scene load; its resource-path loader and name-sort fallback are removed;
- the Entrance coat-hanger timing failure and late runtime repair are characterized before ownership changes: authored art/transform, generated action/collider/closet, duplicate pantry closet, repeated repair, coat retention, and room round-trip identities are frozen;
- the authored Entrance coat hanger owns the existing serialized closet identity plus one serialized scene action and exact trigger collider from scene load; the pantry placeholder no longer owns a duplicate closet, and all name-search/component/collider/anchor repair paths are removed;
- the now-unowned pantry `CoatCloset / Anchors / ApproachFront` placeholder subtree is pruned from Gameplay, and its dead prop-anchor/hierarchy-name lookup helpers are removed;
- Chapter 1's core-reference repair is characterized before removal: its exact clock, scheduler, camera, navigation, Player movement, and derived Butler root survive repeated resolution, room round trips, repeated Chapter 2 entry, and seven-PM staging;
- Chapter 1 directly serializes its clock, camera, navigation, and Player movement alongside its already-authored manager and scheduler; configuration validation requires the complete graph, all repeated discovery branches are removed, and the Butler root derives only from the Player movement edge;
- Chapter 1's caller-owned manager rebinding is removed; valid/null entry commands retain serialized manager `3301000004`, while a different manager is rejected before sequence or world mutation;
- the front-door interaction timing failure is characterized before ownership changes: authored trigger/action/collider identities and geometry stay intact, but startup creates and binds a second runtime trigger; repeated repair, room travel, and skips retain that duplicate without further growth;
- Chapter 1 directly serializes authored front-door action `1180734300`; startup, repeated repair, room travel, and skips now retain that sole action/trigger/collider graph while the obsolete fallback remains staged for its independent cleanup gate;
- the front-door alias search, runtime trigger/action/renderer/collider/UI factories, computed hitbox, and generic click-target reassignment branch are removed; one narrow method configures only the validated serialized action;
- Chapter 1's remaining immutable data lookups are characterized before migration: the exact footstep catalog, entrance placemark, Drawing Room door target, two RoomContentGroups, and ordered eight guest points retain their identities/transforms through repeated resolution, travel, and skips without runtime seat creation;
- Chapter 1 directly serializes that catalog/placemark/door-target/room-content/eight-point graph and validates ownership, room membership, uniqueness, and ordering while its lookup paths remain staged for cleanup;
- Chapter 1's footstep resource load, RoomAnchor/object-name scans, duplicate door-trigger scan, room-content scan, legacy three-seat fields, and generated-seat path are removed; the validated direct graph is the sole owner, and configuration rejects any Inspector reordering of the eight indexed guest-point anchors;
- Chapter 1's runtime-created doorbell graph is characterized before migration: the controller host, clock, single source, Game-Sounds volume binding, approved imported clip GUID/fileID, first-use load, and repeated-resolution identities are frozen;
- Chapter 1 directly serializes the characterized doorbell/source/Game-Sounds-binding/imported-clip graph on its existing host and validates the exact clock/owner/audio contract while the old fallback paths remain staged for cleanup;
- Chapter 1's doorbell owner/clock/source/binding discovery and factories, resource-path load, and generated-tone fallback are removed; initialization and ringing use only the validated serialized graph;
- the runtime-only Drawing Room exit click target is characterized before pruning: ChapterManager preparation creates one inactive transparent trigger, repeated initialization duplicates it while the room is inactive, every identity persists inert into Chapter 2, and its action only re-invokes completion paths already owned by room/state changes;
- the redundant Drawing Room exit target, inactive-object factory/remover, wrapper, enum role, serialized toggle, and private sprite cache are pruned without replacement; the authored passage event remains the sole room transition and drives the normal completion-ready Chapter 2 handoff;
- the runtime-only `GrandfatherClockInteraction` graph is characterized before retirement: the actual Entrance placeholder host, injected component/source/Atmosphere binding, two-second generated clip, disabled modal, and stable identities are frozen through repeated initialization, room travel, and Chapter 2; the serialized room-aware ambience and Chapter 2 strike/presentation owners remain distinct;
- `GrandfatherClockInteraction`, its script GUID, unordered host discovery, generated tick/modal factories, trailing action role, and unused Chapter 1 HUD/time pass-through are retired; the authored Entrance/Drawing clock placeholders and anchors remain component-free world data, while canonical room ambience and Chapter 2 clock owners are unchanged;
- the runtime-created `ChapterTimeSettingsUI` graph is characterized before changing ownership: its Chapter 1 host, authoritative clock, root Canvas, TMP font/material, text/shadow layout, authored EventSystem non-mutation, repeated initialization, room travel, Chapter 2, and seven-o'clock refresh identities are frozen; the readout is required, while its old editable settings panel is absent and superseded by `RuntimeSettingsMenu`;
- the same script GUID now identifies one global `Chateau.UI.GameTimeHUD` under GameRoot: its exact Canvas/TMP/Shadow graph and authoritative clock are serialized and architecture-validated from scene load, while the old Chapter 1 and HUD repair branches remain staged only for the immediately following cleanup gate;
- Chapter 1's temporary HUD field/YAML edge and every HUD search, Canvas/text/Shadow/EventSystem factory, legacy-panel repair, and compatibility initializer are removed; the four-edge global owner is the only readout path and the debt guard permits zero repair code in its target file;
- `ChapterIntroUI`'s first-use repair is characterized before authoring: the ChapterManager host, dedicated root Canvas, stretched overlay, full-screen black Image, centered Liberation Sans TMP title, exact two fallback warnings, existing EventSystem, repeated `EnsureUI`, Chapter 2 reuse, and zero object/component growth are frozen;
- that exact Chapter Intro Canvas/overlay/Image/TMP graph is now serialized beneath GameRoot and bound directly to existing owner `3301000003`; the compatibility repair code remains staged for one commit and is proven to create, attach, reparent, or warn about nothing;
- the Chapter Intro Canvas is finalized as a canonical scene-root Screen-Space Overlay Canvas with a stretched authored overlay child; all Canvas/view search, creation, component-add, reparent, layout mutation, fallback warnings/names, and safe-canvas coupling are removed while the four direct references and transition behavior remain exact;
- Guest Search owns the serialized GameRoot navigation service from scene load; its five lazy repair calls and global resolver are removed while room-change subscription stays idempotent;
- `Chapter2Controller` validates and directly uses its fourteen serialized stable dependencies; its monolithic `ResolveReferences` repair search is removed;
- Chapter 2 clock-strike playback owns a dedicated serialized child `AudioSource`, Game-Sounds `GameAudioSourceVolume`, and imported clip; its resource load, runtime tone generator, source factory, and binding factory are removed;
- every Chapter2Controller entry command accepts only its serialized ChapterManager (or a null convenience argument that keeps that owner); mismatched callers are rejected before any phase or world state changes;
- Chapter 1 binds the existing serialized guest-scale applier, which owns the serialized calibration and Butler source; both latent runtime owner factories are removed while per-guest participant creation remains deliberate;
- serialized dialogue/subtitle services, line bank, navigation edge, and Chapter 1 consumers are wired; both core-service `FindOrCreate` factories are removed while auxiliary views remain lazy until their own gate;
- one voice-playback owner, dedicated dialogue AudioSource, and speaking-indicator owner are serialized with explicit assets/navigation; both root factories are removed while lazy subtitle/bubble child-view creation remains deliberate;
- DialogueSpeechService owns the exact serialized Butler input edge, and the primary voice AudioSource owns one serialized Dialogue-channel volume component; real cataloged playback/input behavior is lifecycle-gated while transient overlap playback remains dynamic;
- blocking dialogue uses one token-safe service-owned input lease; cancellation releases before transition state is applied, stale routines cannot release a replacement lease, and shutdown/disable force a safe release;
- SubtitleService and GuestVoiceLinePlayback use validated serialized line-bank/catalog/navigation/source dependencies without resource or scene-search repair; lazy subtitle presentation and transient overlap playback remain separately gated;
- SubtitleService is bound to the shared speaking-indicator owner; redundant static cleanup searches are removed from dialogue, subtitle, and Chapter 2 paths;
- ChapterManager owns serialized dialogue/subtitle services and one direct debug-transition cleanup command; settings and teleports delegate to it, and the final static voice-stop/global subtitle searches are removed;
- ChapterManager owns the exact serialized Player input and Chapter 2 controller; its player/controller/debug-canvas repair searches are removed, the public Player root is derived from that input, and legacy movement components are disabled only on the authored Player instance;
- the inert Chapter 1 HUD owner is serialized on the Chapter 1 controller; its lookup/factory/flag are removed while lazy canvas/text presentation remains deliberate;
- RuntimeSettingsMenu and its correctly scaled canvas are serialized under GameRoot with explicit navigation/chapter/clock/music; root/canvas factories are removed while nested controls remain lazy and owner-scoped;
- exploration music has one serialized channel-volume owner at its authored `0.125` base volume; RuntimeSettings no longer creates/reconfigures that component and the prior zero-volume migration regression is lifecycle-gated;
- RuntimeSettings validates and directly uses its serialized navigation/chapter/clock/music graph; external searches plus EventSystem/RectTransform repair are removed, while owner-scoped runtime controls remain intentionally dynamic;
- fireplace and clock ambience ownership is behaviorally frozen, then serialized under GameRoot with distinct sources, explicit catalogs/navigation, and a fireplace-only high-pass filter; their root, resource, and component-repair factories are removed;
- remove `DialogueSpeechService.FindOrCreate` and `SubtitleService.FindOrCreate` call sites after callers receive serialized/service references;
- replace `UrpPostProcessingBootstrap` with a serialized render rig;
- replace runtime clock-hand attachment with an authored `ClockView` reference.
- `GameClockHandsDisplay` is deleted after complete reference/resource proof and a zero-instance lifecycle; the guard and static test require it to remain pruned.

Exit condition: required managers are never created via `new GameObject` or `AddComponent` at runtime.

## Phase 4 — Canonical rooms, navigation, and set-piece props

Build one complete route through the target architecture before touching every door:

```text
InteractionRouter -> PassageInteraction -> NavigationService
  -> RoomViewService -> CameraService -> player arrival
```

Create stable `RoomDefinition`, `PassageDefinition`, `RoomView`, `Passage`, and authored approach/arrival-anchor data. Migrate one round trip, test it, then migrate the remaining routes.

The first route characterization is complete. Before canonical code or scene binding, the tests now freeze:

- exact Entrance/Drawing trigger IDs, parents, rectangles, route strings, script GUID, and current null repair edges;
- one room-change event per direction, in transition-before-arrival order;
- neutral `1366x768` legacy reference samples: representative far forward approach `(-7.576081, -1.986423)` / Drawing arrival `(5.267176, -2.104616)`, and representative far reverse approach `(5.280546, -2.015396)` / Entrance arrival `(-7.703568, -2.000136)`;
- actual far activation ordering (`ArrivedAtDestination` -> `MovementStopped` -> audio -> room event -> destination warp), synchronous near activation, pending-subscription cleanup, and the `145`-pixel proximity boundary;
- viewport envelopes at `1366x768`, `1440x1080`, `1920x1080`, and `2560x1080`, plus proof that every left/center/right preferred-click result stays inside the `145`-pixel activation boundary even though exact walkable preferred points, aspect ratio, and source position can change the legacy logical result;
- camera active-room/background ownership, prompt/cursor release, Chapter 1 continuity, object/component counts, and the `0.752865` Butler presentation multiplier;
- shared door-audio first-use binding/reuse; and
- the current `RoomContentGroup` force-enable defect, which must be intentionally removed when `RoomView` assumes visibility ownership.

The route then migrates through separately reversible commits: pure contracts; definition assets and GameDatabase registration; passive room/passage scene bindings; direct serialized dependencies; façade-backed canonical traversal; authored arrival anchors; authored approach anchors; and automated compatibility-template certification. `RoomNavigationManager` remains the sole current-room owner until a later explicit owner cutover.

Pure-contract status: complete. The canonical definition/view/passage/interface types are validation-only and have zero serialized instances. No service implementation, room activation command, scene binding, or legacy caller changed in that gate.

Canonical-data status: complete. The two room definitions and two reciprocal directed passage definitions are exact, directly registered in `GameDatabase`, Unity-imported, and behaviorally inert; that data gate passed before any scene binding.

Passive-room-view status: complete. The Entrance and Drawing roots each carry one definition-backed RoomView registered for validation, but no view can change visibility and no canonical Passage exists in the scene. The legacy manager remains the sole activation writer.

Approach/viewport characterization status: complete. Rendered tests own and restore a real Game-view size, neutralize synthetic batch edge-pan, exercise both far walks and both near activations, and quantify the current viewport/source-position dependence. The old `50x29` batch result is no longer treated as an authored anchor. Next, serialize exactly two reciprocal passive Passage bindings with the neutral `1366x768` values while leaving legacy interaction/traversal untouched.

Passive-passage status: complete. The two existing trigger owners now each carry one definition-backed, reciprocal, GameRoot-registered Passage with the neutral reference approach/arrival data. Passage has no lifecycle or command and is not called by any legacy or canonical service. Next, bind the two legacy trigger owners' stable navigation, Player, shared door-audio, and catalog dependencies directly in one separately gated compatibility slice; do not implement `INavigationService` or change traversal yet.

Direct-dependency status: complete. Only the GEH/Drawing reciprocal trigger pair now directly references the existing `RoomNavigationManager`, exact live Player Transform, shared `Audio_DoorOpen` source, and approved `DoorOpenSoundCatalog`; the other 43 scene trigger documents retain all four null compatibility edges. The Player prefab instance required one type-correct stripped Transform proxy, so the proven minimum scene delta is two changed trigger documents plus one added proxy document. Every legacy resolver remains for unmigrated routes. Next, make the existing `RoomNavigationManager` implement `INavigationService` as a no-new-state compatibility façade, prove it delegates to the same inspector-owned route path, and do not cut over either trigger caller in that implementation-only slice.

Navigation-façade implementation status: complete. The existing `RoomNavigationManager` is the sole `INavigationService`; it derives `CurrentRoomDefinition` on demand from the one existing `currentRoom` string and registered `GameDatabase` definitions, validates a scene Passage without mutation, and delegates `TryTraverse` exactly once to `MoveThroughInspectorDoor`. It adds no field, cache, event, object, serialized edge, discovery path, audio behavior, activation writer, or Player-placement algorithm. Both `DoorTriggerNavigation` callers remain byte-identical and still call the legacy method directly. Next, bind only the two first-route triggers to their co-located Passages and cut only those two callers through the façade, retaining the exact legacy fallback for every unmigrated trigger.

First-route caller-cutover status: complete. Only trigger documents `109889178` and `2300000104` bind their co-located canonical Passages; those two callers use `INavigationService.TryTraverse`, while the other 43 scene triggers deserialize a null edge and execute the byte-preserved camera-sequence/Inspector fallback. Canonical rejection fails closed and cannot fall through. Far traversal with deliberately invalid legacy source strings proves both canonical directions; a subsequent near round trip with both Passage fields nulled in test proves the fallback remains synchronous and behaviorally exact. Next, replace only the canonical route's legacy destination sampler with `Passage.ArrivalAnchor` while leaving approach selection, trigger interaction/audio, and every unmigrated route unchanged.

Authored-arrival status: complete. `CanTraverse` rejects missing or non-finite arrival data before mutation. A successful canonical traversal uses the sole existing room-state/event transition and then `PointClickPlayerMovement.TryWarpToExact` to validate and stop at `Passage.ArrivalAnchor.LogicalPosition` without clamping, projection, or an old-room path requirement. Drawing arrival remains `(5.267176, -2.104616)`; the shared collision-safe Entrance arrival is `(-7.75, -2.22)`. Both are exact across different source positions and `1366x768`, `1440x1080`, `1920x1080`, and `2560x1080`. Null-Passage and unmigrated routes retain the byte-identical legacy sampler.

Authored-approach status: complete. `CanTraverse` now rejects missing or non-finite approach data before movement. A bound first-route trigger fails closed, ignores preferred click projection, verifies the exact anchor is inside its existing `145`-pixel envelope, then passes it into the unchanged collision/path-aware `TrySetDestination` and pending-stop flow. Entrance `(-7.75, -2.22)` and Drawing Room `(5.267176, -2.104616)` are shared reciprocal approach/arrival points and pass actual production-path checks across two source positions and all four rendered aspects. Far callback/audio/event order and near synchronous traversal are unchanged. Null-Passage far activations complete both directions through the byte-preserved sampler and legacy arrival path, while the null-Passage near round trip remains synchronous. Next, certify this complete reciprocal route as the reusable migration template before authoring any second route.

Automated compatibility-template certification status: six reciprocal pairs are complete in the manifest and pass the reusable contract; all twelve scene Passages own exact authored approach and arrival anchors at stage 2, and human doorway/camera/visibility review remains pending. Direct dependencies and canonical callers are each `12 bound / 33 null`. [PASSAGE_COMPATIBILITY_TEMPLATE.md](PASSAGE_COMPATIBILITY_TEMPLATE.md) freezes the data/view/passage graph, shared anchors, direct edges, caller/service ownership, null-Passage proof, serialization checks, and pair-by-pair instruction card. [RemainingRouteInventory.csv](RemainingRouteInventory.csv) is a machine-checked manifest of all 45 Gameplay trigger records: 12 complete in 6 reciprocal pairs, 2 tests-only characterized Group `06` triggers, 26 queued triggers in 13 reciprocal pairs, 2 blocked one-way triggers, and 3 blocked parallel-stair triggers. Group `06` characterization gates pass targeted `1/1`, focused `14/14`, safety `32/32`, lifecycle `10/10`, and full `264/218/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; audits pass at `112 / 48,789 / 48 / 155 / zero hard errors / 38 findings`. This certification intentionally does not claim `InteractionRouter`, `PassageInteraction`, `RoomViewService`, or final camera ownership.

Group `01` Drawing Room <-> Music Room characterization status: complete. Existing components `2300000099` (`DrawingRoom_MusicRoom`) and `2300000089` (`MusicRoom_DrawingRoom`) remain standard Door triggers with null canonical/direct edges and retain their exact parents, rectangles, `145`-pixel proximity, prompt/cursor, shared-audio, and Inspector traversal behavior. At neutral `1366x768`, the legacy room-side reference points are Drawing `(-7.106010, -1.508934)` and Music `(-7.737432, -3.180156)`; both far directions preserve `ArrivedAtDestination` -> `MovementStopped` -> audio -> room event -> sampled arrival, both near directions remain synchronous, and camera background, active content/stage, trigger activation, and the Chapter 2 guest-panic left-exit identity remain exact. The four-aspect characterization deliberately records legacy viewport dependence instead of promoting it to authored identity: at `1440x1080` the Drawing/Music points are `(-6.152483, -1.306456)` / `(-6.699176, -2.753424)`, at `1920x1080` they are `(-7.104277, -1.508566)` / `(-7.735544, -3.179381)`, and at `2560x1080` forward approach / Music arrival / reverse arrival are `(-8.188315, -1.742414)` / `(-8.932235, -3.671232)` / `(-8.188308, -1.741942)`. Rendered tests explicitly synchronize 2D physics after each GameView layout change so a stale PolygonCollider transform cannot masquerade as sampler variability. This tests-only slice changes no production script, scene, prefab, data asset, or `.meta` file. Next, author only the Music RoomDefinition and two reciprocal PassageDefinitions and register those three assets in `GameDatabase`; do not add a RoomView, Passage component, direct trigger edge, canonical caller, or authored approach/arrival anchor in that data-only slice.

Group `01` canonical-data status: complete. `Room_MusicRoom.asset` owns stable ID `room.music-room`, display/legacy name `Music Room`, scene-authoritative background GUID `028084782cdcf3d4ab3b596624c8b7c5`, and a null perspective profile under new asset GUID `c0f34d74a30db58bb2b87b6ec316120b`. Reciprocal Door definitions `passage.drawing-room.music-room` / `DrawingRoom_MusicRoom` (GUID `3167361ca4c671298c0e84f43320619b`) and `passage.music-room.drawing-room` / `MusicRoom_DrawingRoom` (GUID `01544de8f55723585d60e5c0915345fd`) use `Open Door`, swap exact endpoints, and link directly to one another. These three definitions are appended exactly once after the prior four in `GameDatabase`, which now contains seven definitions. Focused canonical/manifest tests pass `9/9`, combined safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254/208/46` with the unchanged failure-name hash. This slice adds only the three data assets and their new `.meta` files and changes the database/test/manifest/docs surface: no scene, prefab, runtime script, existing `.meta`/GUID, RoomView, Passage component, direct dependency, caller, or anchor changes. Next, bind exactly one passive RoomView to the existing Music Room root and register that view once with GameRoot; keep Passage components, trigger dependencies, caller cutover, and authored anchors for later separate slices.

Group `01` passive-view status: complete. Existing Music root `354156755` now owns exactly RoomView `4100000003`, bound to Music definition GUID `c0f34d74a30db58bb2b87b6ec316120b` and co-located legacy content `2102000001`; GameRoot registers it once immediately after the first two RoomViews. Existing Drawing RoomView `4100000002` remains the forward source. The scene diff is exactly 16 added YAML lines: one component-list reference in the existing Music GameObject document, one `sceneBehaviours` reference in the existing GameRoot document, and one new 14-line RoomView document. No document is removed, all pre-existing document order is preserved, and no prefab, data, runtime script, `.meta`, trigger, or Passage changes. Gameplay now contains `6,012` documents, exactly three RoomViews, and the original two Passages. Focused canonical/manifest tests pass `9/9`, combined safety passes `27/27`, and rendered lifecycle passes `5/5`; the lifecycle proof confirms the new view has GameContext, validates against the registered graph, and reports visibility exactly from `activeSelf` through far and near travel in both directions while the legacy manager/camera remain the only activation/presentation writers. The full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations, Y-axis validation remains zero hard errors / 38 tracked findings, and the 155-row serialized-reference report changes only RoomView scene instances from two to three. Next, add only two passive Passage components `4100000013` and `4100000014` to the existing Drawing/Music trigger-owner GameObjects and register them once with GameRoot after existing Passage `4100000012`. Their finite neutral reciprocal values are validation data only at `passage-bound`; neither trigger may consume them until the later arrival-owned and approach-owned gates.

At the completed Group `01` passive-passage checkpoint, Drawing trigger owner GameObject `2300000095` gained Passage `4100000013`, bound to definition GUID `3167361ca4c671298c0e84f43320619b`, source RoomView `4100000002`, and reverse Passage `4100000014`; its then-passive approach/arrival values were `(-7.106010, -1.508934)` / `(-7.737432, -3.180156)`. Music trigger owner GameObject `2300000085` gained Passage `4100000014`, bound to definition GUID `01544de8f55723585d60e5c0915345fd`, source RoomView `4100000003`, and reverse Passage `4100000013`; its then-passive approach/arrival values were `(-7.737432, -3.180156)` / `(-7.106010, -1.508934)`. GameRoot registered the complete Passage order `4100000011`, `4100000012`, `4100000013`, `4100000014`. Those reciprocal anchors were passive certification data only at that checkpoint.

The passage-only scene diff is exactly 42 added YAML lines: two GameRoot `sceneBehaviours` references, one component-list reference on each existing trigger-owner GameObject, and two new 19-line Passage documents. Gameplay advances from `6,012` to `6,014` documents and from two to four Passage instances while retaining three RoomViews. No document is removed; every pre-existing document keeps its relative order; the two `DoorTriggerNavigation` component documents, their four null direct-dependency fields, and their absent `canonicalPassage` fields remain unchanged. Runtime scripts, prefabs, data assets, existing `.meta` files/GUIDs, and all unrelated scene content remain unchanged. Focused canonical/manifest tests pass `9/9`, combined safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations, and Y-axis validation remains zero hard errors / 38 tracked findings. The 155-row generated serialized-reference report retains RoomView at three scene instances and advances only Passage from two to four.

At the completed Group `01` direct-dependency checkpoint, trigger documents `2300000099` (Drawing -> Music) and `2300000089` (Music -> Drawing) each gained exactly navigation manager `1878886997`, Player Transform `81962843`, shared door AudioSource `2201000013`, and door catalog `{fileID: 11400000, guid: 9a77542e25184fbc945d6a79f77007e7, type: 2}`. Their stairway catalogs remained `{fileID: 0}` and `canonicalPassage` remained absent. The complete scene change was exactly eight removed null-field lines plus eight replacement reference lines across those two existing trigger documents; no YAML document was added, removed, or reordered. Gameplay remained at `6,014` documents / 3 RoomViews / 4 Passages, and every owner/component topology edge, Passage definition/source/reverse/anchor field, GameRoot registration, and pre-existing document order remained byte-identical. An independent reversible byte audit replaced only those four references per trigger with their former null values and reproduced the prior trigger-document SHA-256 values exactly (`d1ff8a7ca08b1b687975c34a6f3ec09511e756751452104d85c88d05f5e93934` and `bad9c5f7df3c5034e4d46bb9cfcf7f88a465f6248502c38ad6c452075b973280`). The inventory was `dependencies-bound`; focused canonical/manifest gates passed `9/9`, combined safety passed `27/27`, rendered lifecycle passed `5/5`, and the full suite remained `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remained 112 runtime files / 48,741 lines / 48 direct `MonoBehaviour` declarations; Y-axis validation remained zero hard errors / 38 tracked findings; serialized script references remained unchanged at 155 rows.

Pairwise anchor-rollout gate status: complete. The original sequence assumed a new canonical caller could remain separate from arrival and approach ownership, but the already-complete global façade consumed both authored anchors whenever `canonicalPassage` was non-null. `PassageAnchorMigrationStage` now makes the only valid temporary sequence explicit: `LegacySampling = 0`, `AuthoredArrival = 1`, `AuthoredAnchors = 2`. Existing complete Passages `4100000011`/`4100000012` explicitly serialize `2`; Drawing/Music Passages `4100000013`/`4100000014` explicitly serialize `0`. `CanTraverse` still requires both anchor objects and a valid stage but checks coordinate finiteness only when that coordinate is authoritative; runtime validation and traversal also require both reciprocal Passages to have the same valid stage. A stage-0 canonical caller uses the existing legacy approach sampler and existing destination-door placement method; stage 1 changes only arrival placement; stage 2 changes approach selection as well. No second sampler, placement method, service, cache, object, or state owner exists.

The rollout-preparation scene delta is exactly four added scalar lines in the four existing Passage documents. Gameplay remains `6,014` documents / 3 RoomViews / 4 Passages, with no document, object, component, reference, anchor value, GameRoot entry/order, trigger caller, direct dependency, prefab, asset, or `.meta` change. The rendered lifecycle first mismatches a reciprocal pair and proves traversal fails closed, then temporarily binds both Drawing/Music callers in memory, poisons all four passive coordinates, proves unchanged far/near bidirectional movement, arrival, event/audio, camera/visibility, prompt/cursor, and story-target behavior, and restores every reference, coordinate, listener, and navigation-audio/pending state in `finally`. Focused canonical/manifest gates pass `9/9`, combined safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture is 112 runtime files / 48,787 lines / 48 direct `MonoBehaviour` declarations with unchanged smell totals, serialized references remain 155 rows, and Y-axis validation remains zero hard errors / 38 tracked findings. This 46-line compatibility seam is temporary: after every Passage is `complete` and serialized at stage 2, freeze that invariant, make authored paths unconditional, rerun all gates, then remove the enum, field, branches, and YAML scalars in one cleanup slice.

At the completed Group `01` canonical-caller checkpoint, trigger `2300000099` gained its co-located Drawing -> Music Passage `4100000013`, and trigger `2300000089` gained Music -> Drawing Passage `4100000014`. The scene delta was exactly two added YAML lines in those two existing trigger documents; Gameplay remained `6,014` documents / 3 RoomViews / 4 Passages. No Passage field or stage, anchor value, direct dependency, owner/component edge, GameRoot entry/order, document ID/order, runtime script, prefab, asset, or `.meta` changed. Exactly four of 45 triggers then had canonical callers and 41 remained null; direct bindings remained 4/41. Both Drawing/Music Passages stayed `LegacySampling = 0`, so the real serialized callers used canonical identity while the existing legacy approach and arrival samplers remained authoritative. The rendered poison proof exercised those real caller edges and retained the exact far/near bidirectional movement, arrival, event/audio, camera/visibility, prompt/cursor, and story behavior. Focused canonical/manifest gates passed `9/9`, combined safety passed `27/27`, rendered lifecycle passed `5/5`, and the full suite remained `254` discovered / `208` passed / the exact same `46` baseline failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remained 112 runtime files / 48,787 lines / 48 direct `MonoBehaviour` declarations with unchanged smells, serialized references remained 155 rows, and Y-axis remained zero hard errors / 38 tracked findings.

Group `01` authored-arrival status: complete. Its first stage-1 gate rejected the passive Drawing value `(-7.106010, -1.508934)` even after explicit physics synchronization and settled layout because that rounded legacy projection was exactly on the floor-polygon edge; legacy placement had concealed the defect with a clamped fallback. The reciprocal authored room-side values are now deliberately interior and trigger-safe: Drawing `(-7.16, -1.78)` and Music `(-7.94, -3.27)`. Both Passage stages advance together from `0` to `1`, so only arrival uses those values; approach remains the unchanged legacy sampler. The complete scene delta is four anchor-vector/`logicalPosition` line replacements (eight serialized coordinate-scalar occurrences) plus two stage-scalar replacements inside Passage documents `4100000013` and `4100000014`. Gameplay remains `6,014` documents / 3 RoomViews / 4 Passages with exact document IDs/order and `SceneRoots`; callers, direct dependencies, definitions, topology, components, GameRoot, runtime code, prefabs, assets, metas, and GUIDs remain unchanged.

Far and near round trips land exactly at both authored arrivals, preserve event/audio/camera/visibility/prompt/story behavior, and retain the original aspect-dependent legacy approaches. Both calibrated points remain exact, walkable, and within the reciprocal `145`-pixel activation envelope across all four rendered aspects and at `2560x1080` maximum zoom `1.22`. Focused `9/9`, combined safety `27/27`, rendered lifecycle `5/5`, and full suite `254/208/46` pass with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture remains 112 runtime files / 48,787 lines / 48 direct `MonoBehaviour` declarations, serialized references remain 155 rows, and Y-axis validation remains zero hard errors / 38 tracked findings.

Group `01` authored-approach preflight status: complete while both Passages remain stage `1`. The future Drawing `(-7.16, -1.78)` and Music `(-7.94, -3.27)` approaches each pass exact, non-projected, collision/path-aware evaluation from two distinct far starts at all four rendered aspects and again at `2560x1080` maximum zoom `1.22`, for 20 total probes. Direct canonical-candidate validation returns each future anchor, but live stage-1 traversal dispatch continues to return a distinct legacy-sampled point, proving the test does not consume the new approach early. Every resolved start is outside the real trigger envelope, every future point is inside it, and player position/idle state restore in `finally`. This preflight changes tests and architecture evidence only: no scene, runtime, prefab, asset, meta, GUID, serialized reference, inventory status, or production behavior changes. Rendered lifecycle remains `5/5`; the full suite remains `254/208/46` with the unchanged failure-name hash.

Group `01` authored-approach status: complete at the deliberately intermediate inventory state `approach-owned`. Passage documents `4100000013` and `4100000014` now serialize `AuthoredAnchors = 2` together, making calibrated Drawing `(-7.16, -1.78)` and Music `(-7.94, -3.27)` authoritative as both reciprocal approach and arrival. The scene delta from the accepted arrival-owned checkpoint is only the two stage-scalar replacements; both coordinates, canonical callers, direct dependencies, definitions, component/document topology, GameRoot, runtime code, prefabs, assets, metas, and GUIDs remain unchanged. The exact production query and stage-2 dispatch reach each non-projected point from two distinct far starts at all four rendered aspects and again at `2560x1080` maximum zoom `1.22`; one real `ActivateDoor` movement runs per direction and aspect, both maximum-zoom directions run, and a symmetric forward replay proves Music is also safe when its destination collider was last left at maximum zoom. Near synchronous traversal, pair-local null-Passage far/near fallback, callback/audio/event order, camera/visibility, prompt/cursor, story target, cleanup, and the wider lifecycle baseline remain exact.

That maximum-zoom proof exposed one prerequisite bug before the ownership slice could pass: with automatic 2D transform synchronization disabled, the destination room collider cache could retain its previous stage scale during same-call exact arrival placement. Commit `71b9848f` adds one explicit `Physics2D.SyncTransforms()` before refreshing the destination floor. It changes no serialization, scene, GUID, API, state owner, or normal route semantics; it only makes exact placement query the current destination transform. Focused gates pass `9/9`, safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254` discovered / `208` passed / the same `46` known failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; Y-axis remains zero hard errors / 38 tracked findings.

Group `01` complete-certification status: accepted. Both Drawing/Music rows are `complete` with `template-certified` notes, and both Passage documents remain exactly `AuthoredAnchors = 2`. This status-only slice changes no scene, runtime source, prefab, data asset, `.meta`, GUID, serialized reference, caller, dependency, coordinate, GameRoot entry, document identity/order, or topology edge. Focused gates pass `9/9`, safety passes `27/27`, rendered lifecycle passes `5/5`, and the full suite remains `254` discovered / `208` passed / the same `46` known failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; Y-axis remains zero hard errors / 38 tracked findings.

At the Group `02` Music Room <-> Library characterization checkpoint, existing trigger components `552135204` (`MusicRoom_Library`) and `2300000079` (`Library_MusicRoom`) retained their exact four-component owners, room-root/Doors ancestry, rectangles, standard Door profile, `145`-pixel proximity, null serialized dependencies, null canonical callers, prompt/cursor/audio lifecycle, and Inspector traversal. Music retained existing RoomView `4100000003`; Library still had no RoomDefinition, RoomView, PassageDefinition, Passage component, direct dependency, canonical caller, or authored anchor. Runtime resolution found the one navigation manager, Player, shared door AudioSource, and door catalog without changing those null serialized fields. The Library chair/lamp blocker and both room boundaries remained exact, and Chapter 2 hide anchors `Ch2_Hide_Guest01` at `(-255, -181)` in Library and `Ch2_Hide_Guest02` at `(306, -162)` in Music followed active-room visibility.

At primary `1366x768`, the completed Group `01` Music arrival `(-7.94, -3.27)` starts the far Music -> Library path at approach `(7.439471, -2.846709)` and source-sensitive far Library arrival `(-7.287828, -2.936489)`; from Library `(0, -2)`, the reverse approach is `(-7.244175, -2.799095)` and Music arrival is `(7.439471, -2.846709)`. The immediate near forward arrival is instead `(-7.244175, -2.799095)`, proving the legacy far/near Library result remains source-sensitive, while the near reverse returns to `(7.439471, -2.846709)`. Null, left, center, and right preferred-click candidates are identical within `0.0001` in both directions. With common `(0, -2)` far starts, the four aspect approach pairs are `(7.439471, -2.846709)` / `(-7.244175, -2.799095)`, `(6.44117, -2.464709)` / `(-6.272121, -2.423502)`, `(7.43764, -2.846008)` / `(-7.242414, -2.798415)`, and `(8.625211, -3.301598)` / `(-8.3582, -3.229418)` at `1366x768`, `1440x1080`, `1920x1080`, and `2560x1080`. At widest-aspect maximum zoom `1.22`, forward approach/arrival are `(8.625211, -3.301599)` / `(-8.3582, -3.229418)` and reverse approach/arrival are `(-8.356381, -3.228665)` / `(8.676323, -3.178201)`. The seven-line observation-message SHA-256, joining Primary + four Aspect + MaximumZoom + Profile with `\n` and no trailing newline, is identically `f2a4b15ccee94282032102f6b6c93a2673444d5d8d099a8bd1f24be6190fc2ab` in both locked runs. Focused gates pass `10/10`, combined safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256` discovered / `210` passed / the same `46` known failures with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; Y-axis remains zero hard errors / 38 tracked findings.

Group `02` canonical-data status: accepted at inventory state `data-authored`. `Room_Library.asset` uses GUID `8da3a3e936712e7b9f534786110323e4`, stable ID `room.library`, primary/display `Library`, background GUID `0a85e4fdd73e4714fabde63002a457e7`, and a null perspective profile. Passage GUID `aefe77f20874eb81b83fccb6ff5b8046` owns `passage.music-room.library` / `MusicRoom_Library`; GUID `3a641d5febbfd7aec481ada678ba9fe4` owns `passage.library.music-room` / `Library_MusicRoom`. Both are Door / `Open Door` data with exact swapped endpoints and direct reciprocal links. `GameDatabase` appends Library, forward, and reverse once after the accepted seven definitions, for ten ordered definitions total.

This data-only slice adds the three assets and their new metas and changes the database plus matching tests/manifest/docs. It changes no production/runtime source, scene, prefab, existing `.meta`/GUID, RoomView, Passage component, direct trigger dependency, canonical caller, or authored approach/arrival anchor. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; Y-axis remains zero hard errors / 38 tracked findings. Exact next safe slice: add one passive Library RoomView to existing root `1367921344` and register it once in GameRoot. Do not add Passage components or change trigger dependencies, callers, or anchors.

Group `02` passive-view status: accepted at inventory state `view-bound`. Existing Library root `1367921344` gains RoomView `4100000004`, bound to definition GUID `8da3a3e936712e7b9f534786110323e4` and existing content `2102000003`; GameRoot registers it once immediately after Music RoomView `4100000003` and before Passage `4100000011`. The exact scene delta is 16 added lines and zero removals: one component-list reference, one GameRoot reference, and one 14-line RoomView document. Gameplay advances `6,014 -> 6,015` documents and `3 -> 4` RoomViews while retaining four Passages. No hierarchy, Transform, SceneRoots, trigger dependency/caller, Passage component, runtime source, prefab, asset, `.meta`, collider, prop, occlusion, activation, or camera ownership changes. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; RoomView scene references advance from three to four, and Y-axis remains zero hard errors / 38 tracked findings. Exact next safe slice: add reciprocal passive Passage components `4100000015` and `4100000016` to the existing trigger owners and GameRoot. Their required finite reference coordinates serialize at `LegacySampling = 0`; keep both triggers null-bound and do not transfer anchor ownership.

Group `02` passive-passage status: accepted at inventory state `passage-bound`. Passage `4100000015` owns Music -> Library and `4100000016` owns Library -> Music; they bind the accepted definitions and RoomViews, directly reverse-link, and register after Passage `4100000014`. Music `(7.439471, -2.846709)` and Library `(-7.244175, -2.799095)` serialize reciprocally at `LegacySampling = 0`, so neither point is consumed. The source-sensitive far Library result `(-7.287828, -2.936489)` remains legacy-authoritative and differs from the passive Library reference. Because the current schema includes the explicit stage scalar, the scene delta is exactly 44 added lines and zero removals: two owner refs, two GameRoot refs, and two 20-line Passage documents. Gameplay advances `6,015 -> 6,017` documents and `4 -> 6` Passages, with four RoomViews unchanged. Both trigger documents retain null dependencies and callers; no runtime source, prefab, asset, `.meta`, hierarchy, Transform, SceneRoots, collider, prop, occlusion, activation, or camera ownership changes. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows; Passage scene references advance from four to six, and Y-axis remains zero hard errors / 38 tracked findings. Exact next safe slice: bind only the four characterized direct dependencies on each trigger, leaving both canonical callers null and both Passages at stage 0.

Group `02` direct-dependency status: accepted at inventory state `dependencies-bound`. Trigger documents `552135204` and `2300000079` each replace only navigation manager, Player, door AudioSource, and door catalog nulls with the already runtime-resolved objects `1878886997`, `81962843`, `2201000013`, and catalog GUID `9a77542e25184fbc945d6a79f77007e7`. Their stair catalogs remain null and canonical callers remain absent. The exact scene delta is eight replacement lines added and eight removed; Gameplay remains 6,017 documents / four RoomViews / six Passages. The two Passage documents, stage-0 values, GameRoot, component/document topology, hierarchy, colliders, props, occlusion, activation/camera ownership, runtime source, prefabs, assets, and `.meta` files remain unchanged. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The seven-line observation hash changes only its bound/stable dependency-profile tokens, to `cb6e7c14702f7e9adcedfda1d5a0fba5f14462581c607d203c53152b5a3b40a7`; all coordinate lines remain exact. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows, and Y-axis remains zero hard errors / 38 tracked findings. Exact next safe slice: add only canonical Passage `4100000015` to trigger `552135204` and `4100000016` to trigger `2300000079`, preserving stage 0 and every dependency.

Group `02` canonical-caller status: accepted at inventory state `caller-bound`. Trigger `552135204` gains only caller `4100000015` and trigger `2300000079` gains only caller `4100000016`, for an exact `+2/-0` scene delta. Gameplay remains 6,017 documents / four RoomViews / six Passages; direct dependencies remain six and canonical callers advance from four to six. Both Passages remain stage 0 with exact reciprocal coordinates. All dependencies, Passage documents, GameRoot, topology, hierarchy, colliders, props, occlusion, activation/camera ownership, runtime source, prefabs, assets, and `.meta` files remain unchanged. The rendered gate poisons/restores all four passive coordinates by more than 100 units while real callers remain bound, then separately nulls/restores the pair's callers. The primary first-entry far Library result stays `(-7.287828, -2.936489)` and both post-primary proofs retain the history-sensitive `(-7.244175, -2.799095)` result plus the exact eight-event/audio lifecycle. Focused gates pass `10/10`, safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The seven-line observation hash changes only `callers=null` to `callers=bound`, to `46d857f57e7e41d6a7facaa3a39d0f97bf6dd4996d7f7d10e3da809d8eced2b7`. Architecture guard/audits pass at 112 runtime files / 48,789 lines / 48 direct `MonoBehaviour` declarations / 155 serialized-script rows, and Y-axis remains zero hard errors / 38 tracked findings. Exact next safe slice: a tests-only arrival calibration/preflight; do not advance stage until one reciprocal arrival pair is collision-safe across every aspect and maximum zoom.

Group `02` bounded ownership/certification status: accepted through `complete`. The five related changes—arrival preflight, arrival ownership, approach preflight, approach ownership, and complete certification—are deliberately gated and committed as one batch. Passages `4100000015` and `4100000016` both advance from stage `0` to `AuthoredAnchors = 2`, with authoritative reciprocal room-side points Music `(7.714471, -3.121709)` and Library `(-7.744175, -3.059095)`. Music -> Library remains at `145`-pixel proximity. Library -> Music changes pair-locally `145 -> 149`: exhaustive rendered search found the structurally safest invariant Library point requires `148.573` pixels at the worst profile, making four pixels the minimal whole-pixel compatibility calibration. Apart from the reciprocal anchor coordinates, two stage scalars, and this one proximity scalar, the batch changes no GUID, `.meta`, topology, caller, dependency, GameRoot registration, serialized object identity, or runtime source. Inventory totals become 6 complete, 34 queued, 2 blocked-one-way, and 3 blocked-parallel; all six scene Passage components are stage 2. Focused contracts pass `10/10`, combined safety passes `28/28`, rendered lifecycle passes `6/6`, and the full suite remains `256/210/46` with the unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings. The exact next safe slice is Group `03` Library <-> Ballroom characterization, continuing with bounded batches.

Group `03` first bounded-batch status: accepted at `dependencies-bound`. The batch groups exactly five related changes: characterization; Ballroom RoomDefinition plus reciprocal PassageDefinitions; Ballroom RoomView; passive reciprocal Passages at stage `0`; and both direct-dependency sets. Characterization freezes Library `L0 = (7.465074, -2.665671)`, Ballroom `B0 = (-8.107888, -2.079877)`, and distinct first far Ballroom arrival `(-8.144631, -2.043134)` across four rendered aspects and widest-aspect maximum zoom. Its pre-edit no-trailing-newline observation SHA-256 is `39f311ffa5df816fe7a9b9d510bda7451109c35cb119b2540b26f5128a0e35db`; the accepted dependency-bound profile changes only that token and hashes to `ab7f0108d462d26d45571923d33ff88469a98fb3b8c5a192bc9142a54fae3017`. `GameDatabase` contains 13 entries; Gameplay contains 6,020 documents / five RoomViews / eight Passages, with stage counts `2 / 0 / 6` for stages `0 / 1 / 2`. Both trigger thresholds remain `145`; direct dependencies are bound, canonical callers remain null, and production continues through the byte-preserved legacy route. Inventory totals are 6 complete, 2 dependencies-bound, 32 queued, 2 blocked-one-way, and 3 blocked-parallel. No runtime source, prefab, or existing `.meta` file changes. Targeted/focused/safety/lifecycle gates pass `1/1`, `11/11`, `29/29`, and `7/7`; the full suite is `258/212/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings. The exact next slice is caller-only binding for this pair, still at stage 0.

Group `03` canonical-caller status: accepted at inventory state `caller-bound`. Trigger `2300000084` gains only `canonicalPassage: 4100000017`, and trigger `2101000025` gains only `canonicalPassage: 4100000018`, for an exact two-line additive scene delta. Gameplay remains 6,020 documents / five RoomViews / eight Passages, with stage counts `2 / 0 / 6`; direct dependencies remain eight bound / 37 fully null, and canonical callers advance to eight bound / 37 null. Both thresholds remain `145`, and both new Passages remain `LegacySampling = 0`, so canonical identity still delegates approach and arrival to the byte-preserved legacy samplers. A rendered proof poisons and restores all four passive coordinates while the callers remain bound, and a separate pair-local fallback proof nulls and restores only these two callers. The observation profile changes only `callers=null` to `callers=bound`, producing no-trailing-newline SHA-256 `22e969882f5c3481bd0957a46db3008ea556726bf395d30c1997f2f775e17118`. Inventory totals are 6 complete, 2 caller-bound, 32 queued, 2 blocked-one-way, and 3 blocked-parallel. No dependency, coordinate, Passage document, threshold, GameRoot entry, topology, hierarchy, collider, set piece, occlusion, runtime source, prefab, asset, `.meta`, or GUID changes. Targeted/focused/safety/lifecycle gates pass `1/1`, `11/11`, `29/29`, and `7/7`; the full suite remains `258/212/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; and architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings. The exact next slice is a tests-only arrival calibration/preflight; do not advance either Passage from stage 0 until one reciprocal arrival pair is collision-safe across every rendered profile.

Group `03` bounded ownership/certification status: accepted through `complete`. The five related changes—arrival preflight, arrival ownership, approach preflight, approach ownership, and complete certification—are gated and committed as one batch. Passages `4100000017` and `4100000018` both advance from stage `0` to `AuthoredAnchors = 2`, with authoritative reciprocal room-side points Library `(7.95, -3)` and Ballroom `(-8.607888, -2.439877)`. Both triggers retain `145`-pixel proximity; the worst rendered distances are `123.877` and `106.234` pixels across the accepted profiles. Apart from the reciprocal anchor coordinates and two stage scalars, the batch changes no GUID, `.meta`, topology, caller, dependency, threshold, GameRoot registration, serialized object identity, runtime source, prefab, or data asset. The seven-line authored observation hash is no-trailing-newline SHA-256 `ba483637abce4dc40b5c2910ff90a8ac9c364f686e1a7f4b45fd0c766b82848f`. Inventory totals become 8 complete, 32 queued, 2 blocked-one-way, and 3 blocked-parallel; all eight scene Passages are stage 2. Targeted/focused/safety/lifecycle gates pass `1/1`, `11/11`, `29/29`, and `7/7`; the full suite remains `258/212/46` with the unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits remain 112 / 48,789 / 48 / 155 / zero hard errors / 38 tracked findings. The exact next safe slice is tests-only Group `04` Grand Entrance Hall <-> Dining Room characterization.

Room-local object cutouts are first-class props:

```text
RoomDefinition -> RoomView -> SetPieceView -> RoomDepthResolver
                         \-> RoomNavigationGeometry -> ActorMotor
```

- `RoomDepthResolver` and the static `SetPieceView` foundation are unit-gated before any scene binding;
- `purple_armchair_back` is the completed second vertical slice: `SetPieceView` owns stable order `8289`, the original art/transform/component identities remain, and the accepted Gameplay blocker polygon is collision-only;
- `purple_sofa` is the completed third vertical slice: `SetPieceView` owns stable order `5385`, the original art/transform/component identities remain, and the accepted Gameplay blocker polygon is collision-only;
- `SetPieceView` owns the visual cutout, a room-local occlusion anchor, and a sorting offset.
- `RoomNavigationGeometry` owns the room boundary and authored no-walk footprints.
- couches, desks, beds, toys, chairs, tables, and similar scenery use shared definitions/views rather than object-specific managers;
- static set pieces do not recompute sorting every frame;
- sorting never depends on world-space `Renderer.bounds` or `Collider.bounds`;
- accepted collider shapes are preserved and registered, not regenerated;
- the Drawing Room's duplicate prop-sort writers migrate one prop at a time;
- the first tea-table baseline exposed the duplicate-writer defect, then the table migrated to `Props / Set Pieces`: `SetPieceView` owns stable order `6627`, the accepted blocker polygon remains, and collision sorting is disabled for that prop;
- the second purple-armchair baseline exposed the same defect, then the armchair migrated in place: `SetPieceView` owns stable order `8289`, the accepted lower-seat polygon remains, and collision sorting is disabled for that prop;
- the third purple-sofa baseline exposed bounds-derived order `1225` against intended room-local order `5385`, then the sofa migrated beneath `Props / Set Pieces` with its accepted seating polygon unchanged and collision sorting disabled;
- Dining Room seat occlusion migrates only after `ActorPresenter` exists and uses serialized `SeatOcclusionSlot` data.

Migrate navigation geometry and set-piece views one room at a time. The Grand Entrance Hall/Drawing Room route remains the first passage slice; Dining Room is the final set-piece slice because it depends on actor presentation.

Remove only after migration:

- `doors.txt` runtime path;
- `DoorCameraSequence` runtime path;
- `RoomVisualCatalog` runtime path;
- prompt/door paths that duplicate the canonical passage system.

Exit condition: one route graph, explicit reverse links, explicit parallel-stairway pairing, and PlayMode tests for every passage round trip.

Set-piece exit condition: one visual/sort writer per prop, one navigation-geometry owner per room, unchanged accepted collision footprints, stable occlusion across zoom/aspect ratios, and no room activation code that repairs descendant renderers.

## Phase 5 — Actors, movement, and presentation

Split `PointClickPlayerMovement` behind a temporary façade:

- `PlayerClickCommandSource`;
- shared `RoomStageMotor`;
- `ActorPresenter` as sole transform/scale/sort writer;
- `ActorAnimator`;
- shared actor footstep/voice emitters;
- `PlayerActorController` and `GuestActorController`;
- dedicated player and guest prefab variants.

Consolidate `ActorRoomState`, `RoomProjectedEntity`, guest scale components, `RoomPersonWalker2D`, `NPCWaypointMover`, and duplicate sorting paths only after behavior tests prove parity.

Exit condition: one motor and one presenter writer per actor; guests contain no player-input components.

## Phase 6 — Story flow and beat extraction

Make one `GameFlowService` the only chapter/beat owner. Extract one beat at a time from the large Chapter 1/2 controllers while preserving existing public entry points as façades.

Exit condition: chapter controllers read like the game script, Story Beats are testable, and there is one transition mechanism.

## Phase 7 — UI, dialogue, audio, camera, and lighting

- one serialized UI root and modal stack;
- one dialogue queue and subtitle/voice coordinator;
- one audio settings/ambience owner;
- one serialized render rig;
- editor-only lighting generation/bake;
- narrow runtime light views.

Exit condition: feature controllers create no canvases, cameras, audio managers, or lighting objects.

## Phase 8 — Final pruning and assembly boundaries

For every deletion, complete the prune-proof checklist. Then add namespaces and assembly definitions after dependencies are acyclic.

Exit condition:

- zero unjustified runtime classes;
- zero missing scripts;
- zero unowned state;
- zero permanent parallel systems;
- all critical game paths covered by tests;
- Hamza can redraw and explain the architecture from requirements.

### Active checkpoint: Group 04 foundation

Grand Entrance Hall <-> Dining Room is `dependencies-bound`: Dining data, its non-null perspective profile, RoomView `4100000006`, reciprocal Passages `4100000019` / `4100000020`, finite reciprocal reference points, and direct trigger dependencies are exact. Both callers remain null and both Passages remain stage `0`, so no anchor ownership has moved. Totals are `GameDatabase 16`, Gameplay `6023`, RoomViews `6`, Passages `10`, stages `2/0/8`, dependencies `10/35`, callers `8/37`, and inventory `8 complete / 2 dependencies-bound / 30 queued / 2 blocked-one-way / 3 blocked-parallel`. Characterization hash `5479746e65e27c0d22985f3e02f9a9c6d77562d2497f5c743168f162c99200b3` becomes dependency-bound hash `b30fa6610435667265b8b3c32965713f1af317ccddb9a5cce11618112dfcc578` solely through the direct-dependency profile token. Gates pass `1/1`, `12/12`, `30/30`, `8/8`, and full `260/214/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; architecture/serialization/Y-axis audits remain `112 / 48,789 / 48 / 155 / zero / 38`. Next: a caller-only Group 04 checkpoint at stage `0`; do not calibrate or transfer anchors in that slice.

### Active checkpoint: Group 04 callers

Grand Entrance Hall <-> Dining Room is `caller-bound`. The only production delta from the foundation checkpoint is the two co-located caller bindings: trigger `340611600` -> Passage `4100000019` and trigger `2300000109` -> Passage `4100000020`. Totals remain `GameDatabase 16`, Gameplay `6023`, RoomViews `6`, and Passages `10`; dependencies are `10/35`, callers are now `10/35`, and stages remain `2 stage 0 / 0 stage 1 / 8 stage 2`. The seven-line caller-bound observation hash is `9519761fb43d7e8eb6f76edb036462d22595a607a8b5844bf9fa720874b76b66`. Inventory is `8 complete / 2 caller-bound / 30 queued / 2 blocked-one-way / 3 blocked-parallel`. No anchor, threshold, topology, dependency, collision, prop, occlusion, runtime, prefab, data, `.meta`, or GUID changed. Next: run tests-only reciprocal arrival calibration/preflight, select no production coordinates in advance, and do not change either stage scalar until every rendered-profile gate passes.

### Active checkpoint: Group 04 complete

Grand Entrance Hall <-> Dining Room is accepted through `complete` in one bounded five-change ownership/certification batch: arrival preflight, arrival ownership, approach preflight, approach ownership, and complete certification. Passages `4100000019` and `4100000020` advance together from stage `0` to `AuthoredAnchors = 2`; Entrance `(8.705841, -2.346406)` and Dining `(-7.192237, -1.740209)` become authoritative in both reciprocal roles. Stage-1 rendered distances are Entrance `23.943, 67.613, 33.707, 5.751, 6.619` and Dining `23.943, 58.835, 33.694, 59.736, 68.753`. Every probe is exact, path-reachable, walkable, non-projected, and within the unchanged envelope; the global threshold distribution remains 44 at `145` and one at `149`.

The production/serialization delta is limited to the two reciprocal anchor vectors and two stage scalars. It changes no GUID, `.meta`, topology, caller, dependency, threshold, GameRoot registration, serialized object identity, runtime source, prefab, data asset, collider, prop, or occlusion behavior. Totals remain `GameDatabase 16`, Gameplay `6023`, RoomViews `6`, and Passages `10`; stages are `0/0/10`, dependencies are `10/35`, callers are `10/35`, and inventory is `10 complete / 30 queued / 2 blocked-one-way / 3 blocked-parallel`. The no-trailing-newline authored observation SHA-256 is `93a264ab16817e8864461da376c9631ae36ae3808f770bee5d5094f89a60e509`. Gates remain targeted `1/1`, focused `12/12`, safety `30/30`, lifecycle `8/8`, and full `260/214/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. The exact next safe step is tests-only Group `05` Dining Room <-> Butlers Pantry characterization; no production authoring begins before that characterization passes.

### Accepted checkpoint: Group 05 foundation (historical)

Dining Room <-> Butlers Pantry is `dependencies-bound` after the five-change foundation batch: characterization, one new RoomDefinition, one passive RoomView, two reciprocal data/scene Passages, and direct dependency binding. Butlers Pantry definition GUID `f2e9016bf08c45ebba8600eabc9e0b4d` owns stable ID `room.butlers-pantry`, background GUID `e73e44419d3782452bb6abd0e8edd452`, null profile, and exact aliases `Butlers Pantry` then `Butler's Pantry`. PassageDefinition GUIDs are `1dedaedb6c544e9e8ca4fd2a5be912cf` / `d42e018868914021a713f19df8fe60e8`; RoomView is `4100000007`; stage-0 Passages are `4100000021` / `4100000022`; reciprocal passive points are Dining `(2.891918, 0)` and Butlers Pantry `(-4.663103, -3.103186)`.

Totals are `GameDatabase 19`, Gameplay `6026`, RoomViews `7`, Passages `12`, stages `2/0/10`, dependencies `12/33`, callers `10/35`, and inventory `10 complete / 2 dependencies-bound / 28 queued / 2 blocked-one-way / 3 blocked-parallel`. Characterization hash `32c4e3227cf5135f264c1b6f720a771ff3a5333a4cec1c032954bed69f5f5dbb` becomes dependency-bound hash `319285f885882c452f985bcb6eafb3a69d79065efc1476a990ade17094610123` solely through the serialized-dependency profile token. Gates pass `1/1`, `13/13`, `31/31`, `9/9`, and full `262/216/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; architecture/serialization/Y-axis audits remain `112 / 48,789 / 48 / 155 / zero / 38`. Next: a caller-only Group `05` checkpoint at stage `0`; do not calibrate or transfer anchors in that slice.

### Active checkpoint: Group 05 complete

Dining Room <-> Butlers Pantry advances directly from the accepted foundation to `complete` in one bounded caller-plus-five-ownership batch. The batch binds trigger callers `2300000119 -> 4100000021` and `2300000139 -> 4100000022`, then gates arrival preflight/ownership, approach preflight/ownership, and final certification together. Passages `4100000021` / `4100000022` become `AuthoredAnchors = 2`; the authoritative reciprocal room-side points are Dining `(3.391918, -0.36)` and Butlers Pantry `(-5.163103, -3.463186)`. Dining rendered distances are `23.943, 38.849, 33.662, 38.87, 44.737`; Butlers Pantry distances are `23.943, 83.785, 33.733, 27.022, 31.101`. The `83.785`-pixel worst case remains safely inside both unchanged `145`-pixel thresholds.

The scene delta relative to foundation is `8 insertions / 6 deletions`: two caller additions plus replacements of four anchor-coordinate lines and two stage lines. No GUID, `.meta`, topology, dependency, threshold, GameRoot registration, serialized identity, runtime source, prefab, data asset, hierarchy, collider, set piece, prop, occlusion, activation, or camera owner changes. Totals remain `GameDatabase 19`, Gameplay `6026`, RoomViews `7`, and Passages `12`; stages become `0/0/12`, dependencies/callers are each `12/33`, and inventory becomes `12 complete / 28 queued / 2 blocked-one-way / 3 blocked-parallel`. The authored seven-line no-trailing-newline SHA-256 is `78267865829752279aaa796771f8d51c92b0fac44dc2fe93a4dd12885ccf2d7e`. Final gates pass targeted `1/1`, focused `13/13`, safety `31/31`, lifecycle `9/9`, and full `262/216/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`; architecture/serialization/Y-axis audits pass at `112 / 48,789 / 48 / 155 / zero hard errors / 38 findings`. Human review must confirm the Butler's feet at both painted doorway sides. The exact next safe step is tests-only Group `06` Butlers Pantry <-> Billiard Room characterization.

### Accepted checkpoint: Group 06 characterized (historical)

The tests-only Group `06` checkpoint is accepted at `characterized`. It changes only the characterization test, the two manifest statuses/notes, and architecture evidence; canonical columns remain empty. The six completed pairs and all production totals remain `GameDatabase 19`, Gameplay `6,026` documents / seven RoomViews / twelve Passages, stages `0 / 0 / 12`, and dependencies/callers each `12 bound / 33 null`. Inventory becomes `12 complete / 2 characterized / 26 queued / 2 blocked-one-way / 3 blocked-parallel`.

The locked ownership graph is:

```text
Room_Butlers_Pantry 2300000020
  RoomContentGroup 2300000022 (background e73e44419d3782452bb6abd0e8edd452 / null profile / RoomView 4100000007)
  Doors 2300000024
    owner 1505671644 / rect 1505671645 / trigger 1505671646
    rect position 304.7408|0.153 / size 187.9324|422.4507

Room_Billiard_Room 2300000010
  RoomContentGroup 2300000012 (background 5987c5a8b3a09fc1ca848ac0ece03658 / null profile / no RoomView)
  Doors 2300000014
    owner 2300000130 / rect 2300000131 / trigger 2300000134
    rect position 565|52.91918 / size 120|333.8383
  foreground cutout + blocker pairs
    billiard_table / PlayerBlocker_billiard_table
    billiard_left_armchair / PlayerBlocker_billiard_left_armchair
    billiard_left_lamp_table / PlayerBlocker_billiard_left_lamp_table
```

Both triggers remain standard Door, threshold `145`, caller null, serialized dependencies null, and runtime dependencies resolved. `doors.txt` has no entry for either exact Inspector door ID, `Butlers_Pantry_BilliardRoom` or `BilliardRoom_ButlersPantry`; the scene pair is authoritative. The exact seven-line rendered record is preserved in the report/template and hashes to `528af110846bebb59cc866eb6f94f14e3db1e4fdaad971434eec2e057e9b872e` with no trailing newline. It locks primary plus four aspects plus widest-aspect maximum zoom and the profile, including primary neutral room-side values Butlers `(2.744461, -2.748338)` and Billiard `(6.575521, -1.484375)`, primary reverse arrival `(5.191498, -2.748338)`, and widest values Butlers `(3.168257, -3.172733)`, Billiard `(7.590901, -1.71359)`, reverse arrival `(5.993162, -3.172733)`.

The final characterization gates pass targeted `1/1`, focused `14/14`, safety `32/32`, rendered lifecycle `10/10`, and full `264/218/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits pass at `112 / 48,789 / 48 / 155 / zero hard errors / 38 findings`. Human review of doorway foot placement, camera/visibility, and the three Billiard cutout/blocker occlusion relationships remains required. Execute exactly one bounded five-change foundation batch:

1. Author Billiard RoomDefinition with the locked exact background and null profile.
2. Author the two reciprocal PassageDefinitions with the exact Inspector door IDs.
3. Bind one passive Billiard RoomView without transferring activation or camera ownership.
4. Add two passive reciprocal Passages on the existing trigger owners at `LegacySampling = 0` using finite characterized reference points.
5. Bind only both triggers' direct navigation manager, Player, shared door AudioSource, and door catalog dependencies.

Then run targeted/focused/safety/rendered lifecycle/full/static/serialization/Y-axis gates and commit that batch only. Do not bind either canonical caller, change either threshold/rectangle, consume arrival/approach anchors, move blockers/cutouts, or alter hierarchy. If any gate fails, stop at the last safe characterized commit. The exact next safe step is this bounded Group `06` foundation batch.

### Accepted checkpoint: Group 06 foundation (historical)

Butlers Pantry <-> Billiard Room is now `dependencies-bound` after exactly the planned five-change foundation batch: characterized baseline, Billiard Room data, one passive view, two passive reciprocal Passages, and both direct-dependency sets. Billiard RoomDefinition GUID `bed158a9affd015fcc961340d9be5dd8` owns stable ID `room.billiard-room`, background `5987c5a8b3a09fc1ca848ac0ece03658`, and a null profile. Reciprocal PassageDefinition GUIDs are `71ea8ce4d4eb8fa7f107abe24d7c903e` / `be2f1b94b724dcfa061876e33bce02ca`; passive Billiard RoomView is `4100000008`; stage-0 Passages are `4100000023` / `4100000024`. Their shared passive room-side points are Butlers Pantry `(2.744461, -2.748338)` and Billiard Room `(6.575521, -1.484375)`.

Totals advance to `GameDatabase 22`, Gameplay `6029`, RoomViews `8`, Passages `14`, stages `2/0/12`, dependencies `14/31`, callers `12/33`, and inventory `12 complete / 2 dependencies-bound / 26 queued / 2 blocked-one-way / 3 blocked-parallel`. Relative to characterization, Gameplay changes by exactly `68 insertions / 8 deletions`. Both triggers retain standard Door, threshold `145`, unchanged hierarchy/rectangles, and null canonical callers. All three Billiard blocker/foreground-cutout pairs, activation, camera, prop, occlusion, and exceptional topology remain untouched.

Every rendered coordinate and event record remains the characterized baseline. Changing only the profile token from `serializedDependencies=null` to `serializedDependencies=bound` advances the no-trailing-newline hash from `528af110846bebb59cc866eb6f94f14e3db1e4fdaad971434eec2e057e9b872e` to `b6fbb280fed2f2310b32de4cefa85ce8dbb7ed5ebfebae72b55951ba0026093f`. Final Group `06` foundation gates pass targeted `1/1`, focused `14/14`, safety `32/32`, rendered lifecycle `10/10`, and full `264/218/46` with unchanged failure-name SHA-256 `544759729ac446b3814a3f206021a23c64fd46cc9edc1e997b179affaa0f69f9`. Architecture/serialization/Y-axis audits pass at `112 runtime files / 48,789 lines / 48 direct MonoBehaviour declarations / 155 serialized script records / zero hard errors / 38 findings`. The exact next safe step is caller-only binding `1505671646 -> 4100000023` and `2300000134 -> 4100000024` with both Passages still at stage `0`. No ownership calibration or migration may be folded into that caller slice.

### Accepted checkpoint: Slice 2.1 Group 06 complete (historical)

Butlers Pantry <-> Billiard Room is now `complete`. Callers `1505671646 -> 4100000023` and `2300000134 -> 4100000024` are bound, and both reciprocal Passages serialize `AuthoredAnchors = 2`. The authoritative room-side points are Butlers Pantry `(3.244461, -3.108338)` and Billiard Room `(6.9, -1.6)`; both trigger thresholds remain `145`.

Gameplay remains `6,029` documents / eight RoomViews / fourteen Passages, all at stage 2. Dependencies and callers are each `14 bound / 31 null`, and inventory is `14 complete / 26 queued / 2 blocked-one-way / 3 blocked-parallel`. The no-trailing-newline authored profile SHA-256 is `cd248f01301448b5cd807cc9331e58d99bd59a139bb772dab352869527b9a6eb`; Passage hashes are `61ae1a519da8a47275e2713b3182b7297c049b0eb9b2d590f5cb5037607abcc2` for `4100000023` and `b46ec8f0b48730348e80287e50393c153ce05621c35db00741dfd7af50e6b1ce` for `4100000024`.

Final gates pass targeted `1/1`, focused `14/14`, safety `32/32`, rendered lifecycle `10/10`, focused PlayMode `1/1`, and cold-start PlayMode `1/1` with unchanged digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. No runtime source, prefab, definition, `.meta`, or GUID changes. Human review remains for Butler foot placement at both doorways, active-room visibility/camera framing, and all three Billiard cutout/blocker pairs. Next, characterize only Group `07` Butlers Pantry <-> Service Corridor in tests; keep its production scene/data ownership unchanged until that baseline passes.

### Accepted checkpoint: Slice 2.2-07 Group 07 complete (historical)

Butlers Pantry <-> Service Corridor is now complete after locking the legacy six-line profile (`1a2d83cdf5e9cce175cf16333875f183629c6e67fe8d82241479bae79b4e8150`), authoring the two reciprocal definitions, binding passive Service Corridor RoomView `4100000009`, adding reciprocal Passages `4100000025/26`, binding direct dependencies and callers, calibrating both room-side anchors, and advancing both Passages to `AuthoredAnchors = 2`. The authoritative points are Pantry `(7, -2.8)` and Service Corridor `(4.2, -3.3)`. Pantry profile distances are `18.338 / 66.306 / 25.839 / 52.36 / 60.263`; Service profile distances are `16.712 / 71.262 / 23.565 / 29.137 / 33.535`. Worst distances `66.306` / `71.262` remain exact, walkable, path-reachable, non-projected, and within the unchanged `145`-pixel thresholds. The authored profile hashes to `7cc7c8706a02443b30c91c7a3cdcce40b7c04fec2f54e3f2eb7d5acad7058ea9`.

Totals advance to `GameDatabase 35 (19 rooms / 16 passages)`, Gameplay `6032`, RoomViews `9`, Passages `16`, stages `0/0/16`, dependencies/callers `16/29`, and inventory `16 complete / 24 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact state hashes are scene `c48e6f4a68cd311e958677136b572e06c05266ca556153dafd8e2656ffb1d30c`, inventory `47440df151a302a10493d14f2258c470a4a07d8683b858caf73f1c82e07c8c15`, database `155e065c03ec20c6b7d5f8e0a8e1746d1da166d74dc8d8c4214db8f7446989f1`, and unchanged legacy `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.

Static/GUID/meta/serialized-reference controls and Unity compilation pass. Relevant EditMode execution accounts for 71 passing cases (67 accepted first-pass cases plus four isolated test-only correction reruns); the real Group07 PlayMode route and independent cold-start visual fingerprint each pass `1/1`, with cold digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. This is not a prune slice: retain both Group07 `doors.txt` entries, both Service Corridor prop cutouts, their two movement blockers, and `PlayerBoundary`. Human review remains for both landing positions, prompt/cursor cleanup, camera/active-room visibility, and prop occlusion/collision. Next, characterize Group `08` Service Corridor <-> Kitchen in tests before changing its production scene/data ownership.

### Accepted checkpoint: Slice 2.2-08 Group 08 complete (historical)

Service Corridor <-> Kitchen is now complete after reusing its locked legacy profile (`9af5d16f40f2f186d02a777fb53ac80fd28fe898e0e8953db9455d047a5432de`) and the committed RoomView-local anchor prerequisite. Existing Kitchen RoomDefinition `70531cbf9a67476f81f54b528029132e` now owns passive RoomView `4100000010`; reciprocal PassageDefinitions `2985cbdd527b4faaec13ff03091dbcd1` / `453ad73cf2df1107f56be7a00daa3145` back Passages `4100000027/28`. Existing trigger callers and direct dependencies are bound, and both Passages advance directly to `AuthoredAnchors = 2` without changing either `145`-pixel threshold. The invariant native room-side anchors are Service Corridor `(589.9897, -419.25894)` and Kitchen `(-478.36285, -156.76599)`.

The five rendered aspect/maximum-zoom records prove both RoomView-local points remain finite, exact, walkable, path-reachable, non-projected, within threshold, and round-trip invariant; their no-trailing-newline SHA-256 is `6d5f83aa399b12d8945a660e0372f65206c2c04a52e2657422ea92966fe0393b`. Totals advance to `GameDatabase 37 (19 rooms / 18 passages)`, Gameplay `6035`, RoomViews `10`, Passages/stage 2 `18`, dependencies/callers `18/27`, and inventory `18 complete / 22 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact state hashes are scene `5203d054870b01b2e6db55286bc4a2d6b25ee6795cb84765306728090b4e1d98`, inventory `b692f1333f0271903fd9ff60e20323ce628311f37592a481a23fad142b468ae3`, database `9b9b4ae428340a9fd6597e18873adbdce9b3a675786dbee568adb9a595d710b1`, and unchanged legacy `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.

Architecture tools pass `4/4`; guard, the exact `113/113` runtime ledger, GUID/meta, serialized-reference, and Unity compilation gates pass. Script integrity is `164` current scripts / `1,947` serialized references / `856` external-package references, and all `7,360` asset `.meta` GUID records are paired and globally unique. All 76 relevant EditMode cases are accounted (`74` consolidated plus two focused test-only corrections), and the real Group08 route plus independent cold start pass `2/2` with unchanged digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. This is not a prune slice: retain both Group08 `doors.txt` rows, all Service Corridor cutout/blocker/boundary content, and Kitchen's work-table cutout/blocker/boundary content. Human review remains for both landing positions, prompt/cursor cleanup, camera/active-room visibility, and collision/occlusion. At that checkpoint, the next safe action was to characterize only Group `09` Service Corridor <-> Chapel before authoring its production ownership.

### Accepted checkpoint: Slice 2.2-09 Group 09 complete (historical)

Service Corridor <-> Chapel is now complete from clean rollback commit `07bb61fff50f63dfdc36acf244b391b26fa48d61`. The six-line legacy behavior fingerprint remains locked at `9574b82e5099f96bdaef11dd20bdbab386f5b54baf9ad06278b40af7a58449b2`. Existing Chapel RoomDefinition `e3102dbfecc44551b6443ca88625a924` now owns passive RoomView `4100000029`; reciprocal PassageDefinitions `fc2a0af2de3f4ade831c53f64fe0271b` / `47e06869bf2b47a2980b0d02a53ee1df` back Passages `4100000030/31`. Existing trigger callers `2300000169` / `2300000179` and their direct dependencies are bound, both Passages serialize `AuthoredAnchors = 2`, both standard-Door thresholds remain `145`, and scene/data reverse links are exact.

The invariant RoomView-local room-side anchors are Service Corridor `(-133.2642, -171.8258)` and Chapel `(461.4019, -190.7613)`. Four rendered aspects plus widest-aspect maximum zoom prove both points finite, exact, walkable, path-reachable, non-projected, within threshold, and round-trip invariant. The accepted authored fingerprint is `bff3067e14fd0710f8de6dd4f7e4434d9025a2b7593adf3cd54571020a622475`. Chapel's background/profile, three bench cutout/blocker pairs, remaining foreground cutouts, 18-point room boundary, and `Ch2_Hide_Guest06` stay byte- and behavior-protected.

Totals advance to `GameDatabase 39 (19 rooms / 20 passages)`, Gameplay `6038`, RoomViews `11`, Passages/stage 2 `20`, GameRoot `40` scene behaviours / `48` total serialized references including eight services, dependencies/callers `20/25`, and inventory `20 complete / 20 queued / 2 blocked-one-way / 3 blocked-parallel`. Anchor serialization is `16` legacy-logical / `4` RoomView-local. Exact state hashes are scene `220009ae5f8d2bbe3a3e62ccab6d9c88da56a4c66d9dde092da11f0cae8b8a93`, inventory `43a8134256b39291e7954141ef15d11c3a179b735f65f6437088779dd8abbc2c`, database `90734ca890e57a4b64062e10a7e2c2e04f281dc783beb96f012c2fa673674ebc`, and unchanged legacy `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.

Architecture/static/serialized-reference controls pass at `113/113` runtime ledger rows, `113` runtime files / `50,339` lines / `48` direct MonoBehaviours, `164` scripts, and `1,952` serialized script references (`1,096` project / `856` external-package). All `7,362` `.meta` GUID records are paired and globally unique, and Unity `6000.4.10f1` compiles without errors. Focused EditMode passes `4/4`, the final affected architecture suite passes `62/62`, rendered lifecycle passes `1/1`, and real Group09 plus cold-start PlayMode pass `2/2`. This is not a prune slice: retain both Group09 `doors.txt` rows, Service Corridor presentation/collision/boundary content, and Chapel presentation/collision/boundary/hide-anchor content. Human review remains for both landing positions, prompt/cursor cleanup, camera/active-room visibility, and collision/occlusion. Next, characterize Group `10` Grand Entrance Hall <-> Grand Entrance Hall Rear in tests, explicitly preserving and resolving the existing `Grand Entrance Hall Rear View` / `Grand Entrance Hall Rear view` case alias before production ownership changes.

### Accepted checkpoint: Slice 2.2-10 Group 10 (historical)

Starting from clean rollback commit `f91ee2338261ee061a8502e06542fd49b5d81831`, Grand Entrance Hall <-> Grand Entrance Hall Rear is accepted as one bounded production slice. The immutable before-state remains the reviewed six-line SHA-256 `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638`. RoomView `4100000032`, PassageDefinitions `aa8a2282356d4ad0aa3c9499a6f6f064` / `d57bc53c2dfb4a10bd63739d37028899`, Passages `4100000033/34`, and existing triggers `1858342503` / `70736571` establish the canonical reciprocal graph. Both direct dependency sets and callers are bound, both Passages are stage `2`, and the forward definition explicitly preserves compatibility spelling `Grand Entrance Hall Rear View` while retaining the scene's `Grand Entrance Hall Rear view` alias.

Because these are bottom-edge screen interactions, the ownership transfer uses deterministic destination regions rather than fabricated arrival points. Forward approach `(0.00030518, -456.4991)` targets Rear region `(-764.707458, -451.0935) / (-764.707458, -423.094452) / (785.200256, -423.094452) / (785.200256, -451.0935)` in `BL/TL/TR/BR` order. Reverse approach `(10.2463989, -437.093964)` targets Entrance region `(-835.9997, -470.4991) / (-835.9997, -442.4991) / (836.0003, -442.4991) / (836.0003, -470.4991)`. Totals become `GameDatabase 41 (19 rooms / 22 passages)`, Gameplay `6041`, RoomViews `12`, stage-2 Passages `22`, GameRoot `43` scene behaviours / `51` references including eight services, dependencies/callers `22/23`, and inventory `22 complete / 18 queued / 2 blocked-one-way / 3 blocked-parallel`.

Accepted evidence hashes are scene `867e9395e38b4a2e675d5e0fc6abbf9449cb03f4e75ab2c3b4397172efde44ae`, database `90417c118dfc1cda7bcb3cf17c1d92fda30af7433be7ae446162146925e2c39a`, inventory `3291b6e856a252d78411de186e76f818dda12bd9b63cae1972bfd7001187aa5d`, and unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; the last file contains neither Group10 ID. Static controls pass at `114/114` runtime-ledger rows, `114` runtime files / `51,062` lines / `48` direct MonoBehaviours, `165` scripts, `1,957` serialized references (`1,101` project / `856` external-package), and `7,365` paired, unique meta GUIDs.

Unity `6000.4.10f1` compilation passes against the exact final bytes in `compile-exact-final.log`. Focused EditMode passes `4/4` in `editmode-focused-exact-final.xml`; affected architecture passes `67/67` in `editmode-architecture-exact-final.xml`; rendered Group10 passes `1/1` with exact SHA `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638` in `rendered-group10-exact-final.xml`; and genuine Group10 plus cold-start PlayMode passes `2/2` in `playmode-group10-and-cold-exact-final.xml`. Human review remains for both landings, automatic reciprocal hover/prompt transfer, camera and active-room visibility, and the protected four railing cutouts plus 49-point Entrance and 5-point Rear boundaries. Delete nothing in this slice. The next safe slice is tests-first Group `11` Grand Entrance Hall Rear <-> Billiard Room; its `doors.txt` endpoint conflict must be characterized before any production authoring.

### Accepted checkpoint: Slice 2.2-11 Group 11 (historical)

Starting from clean rollback commit `9ed538fa209c239f50675dbc0b9a04c0bbaa01ea`, Grand Entrance Hall Rear View <-> Billiard Room is accepted after the committed source-region prerequisite. The immutable six-line legacy fingerprint remains SHA-256 `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`. PassageDefinitions `cd0978fc337c41b982afb4b46c7a2b3c` / `ef375ba8c3744447add18ebec1fd1a83`, Passages `4100000035/36`, RoomViews `4100000032` / `4100000008`, and triggers `357269799` / `2300000124` form one directly reciprocal scene/data graph. Both dependency sets and callers are bound, both Passages are stage `2`, and the reverse definition preserves title-case compatibility destination `Grand Entrance Hall Rear View`.

This standard-door pair owns source and destination **regions**, never fabricated point anchors. Rear -> Billiard destination corners are `(-745.00006, -114.72981) / (-745.00006, 238.13548) / (-501.32404, 238.13548) / (-501.32404, -114.72981)` in `BL/TL/TR/BR` order. Billiard -> Rear destination corners are `(579.6167, -250.84499) / (579.6167, 31.911606) / (702.0674, 31.911606) / (702.0674, -250.84499)`. The reciprocal destination region is the sole source-region owner for the opposite approach. Totals become `GameDatabase 43 (19 rooms / 24 passages)`, Gameplay `6043`, RoomViews `12`, stage-2 Passages `24`, GameRoot `45` scene behaviours / `53` references including eight services, dependencies/callers `24/21`, and inventory `24 complete / 16 queued / 2 blocked-one-way / 3 blocked-parallel`.

Accepted evidence hashes are scene `ae7a5c400b4edc9a874331bb54a1684d759239f9ccbb03975a3975b3e6649585`, database `2c8f1dfbb90fc81f8032e3f8c3010dc3e394e0e2a397f4a46cc34d0b5a05c9a0`, inventory `c5cec68785b0699aedcd8a5dae46ecbb14a7a94ae969d04e1ac75a70cc05d4fe`, and unchanged conflicting `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. Static controls pass at `114/114` runtime-ledger rows, `114` runtime files / `51,224` lines / `48` direct MonoBehaviours, `165` scripts, `1,961` serialized references (`1,105` project / `856` external-package), and `7,367` paired, unique meta GUIDs.

Unity `6000.4.10f1` compilation passes in `compile-production-attempt1.log`. The accepted focused EditMode gate passes `70/70` in `editmode-focused-attempt2.xml`; rendered legacy/canonical region parity passes `2/2` in `rendered-group11-attempt1.xml`; and genuine Group11, representative Group10/09, plus independent cold start pass `4/4` in `playmode-group11-representative-cold-attempt1.xml`. The broader 113-case run retained ten known historical navigation failures after its one in-scope fixture count was corrected; it is diagnostic evidence, not the acceptance gate. Human review remains for landings, prompt/cursor/audio, camera/visibility, and Billiard collision/occlusion. Delete nothing. The next safe slice is tests-first Group `12` Rear View <-> Conservatory characterization; capture the scaled bottom-edge trigger through Unity before any production serialization.

### Accepted checkpoint: Slice 2.2-12 Group 12

Group12 migrates the live Inspector-owned Grand Entrance Hall Rear View <-> Conservatory pair, not the stale front-Entrance catalog edge. Triggers `1119941194` / `2300000074`, their host rectangles, all aliases, and their standard-door / bottom-edge interaction split remain preserved. Existing Rear RoomView `4100000032` and Conservatory RoomDefinition `78d9317381ab411e8adb1aa6c7386263` bind Conservatory RoomView `4100000037`, definitions `2388aec2b64647e2a7b6c50c3ee3c8b6` / `d54f1f34f2fb45428117d7b831c0ef40`, and Passages `4100000038/39` with stable IDs `passage.grand-entrance-hall-rear-view.conservatory` / `passage.conservatory.grand-entrance-hall-rear-view`.

The accepted mixed-interaction transfer gives both Passages `BestReachableInSourceRegion` plus destination-region placement and no point anchors. Rear -> Conservatory consumes the source region for its standard-door proximity walk; the immediate Conservatory -> Rear bottom-edge trigger retains the same canonical region data but does not consume an approach. Only the exact Unity-transformed regions locked by characterization are serialized. Only the two mapped dependency sets and canonical callers, two definitions, three scene behaviours, and two Group12 inventory rows advance. `doors.txt`, room content, trigger geometry, sampling ownership, and compatibility remain unchanged.

The accepted structural result is Gameplay `6046` documents / `13` RoomViews / `26` stage-2 Passages / `45` triggers, `GameDatabase 45 (19 rooms / 26 passages)`, GameRoot `48` scene behaviours / `56` references, dependencies/callers `26 bound / 19 null`, and inventory `26 complete / 14 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact hashes are Gameplay `3138c43665353b0a3a10336ce36ab3c8aa72386ddb5c46f7d46ed683c33bac00`, GameDatabase `7d7520fb992cf2c39ba1ef6b696d4a2a49148c9c208205129b46593a1e14957d`, inventory `8e626c25cbca2e26d7390149540daf6745187707048ce40fc3eaf30e4f31f795`, and byte-identical `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.

Architecture tools pass `4/4`; guard, exact `114/114` ledger coverage, metadata/GUID, and integrity controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours, smells `106/17/67/51/4/6`, `165` scripts / `1,966` serialized references (`1,110` project / `856` external), and `7,369` paired unique meta GUIDs. Unity compiles in `compile.log`. Preserve `editmode-focused.xml` as the real `72/71/1` test-literal failure; `editmode-failed-correction.xml` passes the corrected case `1/1`, accounting for all `72`. Rendered Group12/Group11 passes `2/2` in `rendered-group12-and-group11.xml` with SHAs `b371eb16186010923ac6d6c63851a5fe0e0de0cf642ac3099b4fab2b93db1fa8` / `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`; real Group12, representative Group11/10, and cold start pass `4/4` in `playmode-group12-representatives-cold.xml`. Landings are Conservatory `(33.322, -451.0936)` and Rear `(0.000015, -138.5048)`, with stop `(0.43388, -1.839959)`. Cold SHA `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96` and all approved scale metrics remain unchanged. Human review remains; delete nothing. The exact next safe slice is tests-first Group `13` Service Corridor <-> Side Stair & Mudroom characterization from the forthcoming Group12 commit, without predeclaring its hash.

### Accepted checkpoint: Slice 2.2-13 Group 13

Group13 migrates the existing reciprocal Service Corridor <-> Side Stair & Mudroom Inspector route without changing its two standard-door interactions. Side Stair & Mudroom RoomView `4100000040`, definitions `0491e7071cda47e7b779cf87f71d026e` / `5c0c635bc6c04da19e0909a6f81d0caf`, and Passages `4100000041` / `4100000042` establish canonical identity on triggers `2300000174` / `2300000184`. Stable IDs are `passage.service-corridor.side-stair-mudroom` / `passage.side-stair-mudroom.service-corridor`; preserve aliases and IDs `Service Corridor`, `ServiceCorridor_SideStairMudroom`, `Side Stair & Mudroom`, `Side Stair Mudroom`, and `SideStairMudroom_ServiceCorridor` exactly.

Both Passages advance directly to stage `2`, use the shared source-region approach and destination-region arrival policies, and serialize no point anchors. The forward Side Stair & Mudroom destination region is `BL (-569.47998, -470.50003) / TL (-569.47998, -338.82755) / TR (836.02002, -338.82755) / BR (836.02002, -470.50003)`; the reverse Service Corridor destination region is `BL (52.839996, -166.62186) / TL (52.839996, 188.62186) / TR (172.84, 188.62186) / BR (172.84, -166.62186)`. Only the mapped definitions, scene behaviours, direct dependency sets, callers, and inventory rows advance; trigger geometry, legacy catalog, room content, and compatibility execution remain intact.

The accepted structural result is Gameplay `6,049` documents / `14` RoomViews / `28` stage-2 Passages / `45` triggers, `GameDatabase 47 (19 rooms / 28 passages)`, GameRoot `51` scene behaviours / `59` references including eight services, dependencies/callers `28 bound / 17 null`, and inventory `28 complete / 12 queued / 2 blocked-one-way / 3 blocked-parallel`. Exact hashes are Gameplay `82e6b51a51cc3a5cf34afe6cfb26dfe35a292cafbdd6a3dd58e6221cbac3d119`, GameDatabase `13f8161b0a5f107e984c3c856bdc9f75bb922f2c3082370de571f412a42ab5f3`, inventory `2aaba6eed5c2bd28026dcca5dedb863b2ecbf646574da168799c1249b9c2fc29`, and byte-identical `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.

Architecture tools pass `4/4`; guard, exact `114/114` ledger coverage, metadata/GUID, and integrity controls pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours, smells `106/17/67/51/4/6`, `165` scripts / `1,971` serialized references (`1,115` project / `856` external), and `7,371` paired unique meta GUIDs. Unity compiles in `Slice-2.2-13/compile.log`. Preserve the focused evidence as `73/60/13`, corrected subset `17/14/3`, and final subset `3/3`; all 73 are accounted without rewriting history. The initial rendered run is `3/1/2`: Group13 passes and only prior Group12/11 structural-count fixtures fail; their corrected rerun passes `2/2`. Locked SHAs are Group13 `14315ebb94e0991564058de63ed42aad056f85b3578c8af626acc4e75c1dab3c`, Group12 `b371eb16186010923ac6d6c63851a5fe0e0de0cf642ac3099b4fab2b93db1fa8`, and Group11 `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`. Group13, four representatives/cold-start cases pass `5/5`; cold SHA and approved scale metrics remain unchanged. Human review remains; delete nothing. The exact next safe slice is tests-first Group `14` Side Stair & Mudroom <-> Upper Sitting Hall, explicitly locking its mixed standard-door and inferred-stairway behavior before production authoring.

### Accepted checkpoint: Slice 2.2-14 Group 14

Group14 migrates the existing Side Stair & Mudroom <-> Upper Sitting Hall route as one reciprocal ownership slice. Upper Sitting Hall RoomView `4100000043`, definitions `d5c370a383ecaf7257b8f796d0164e72` / `77a3efbd4a250afa71e5998bb7118532`, and Passages `4100000044` / `4100000045` establish canonical identity on triggers `1915714844` / `2300000189`. Preserve `Side Stair & Mudroom`, `Side Stair Mudroom`, `UpperSittingHall_SideStairMudroom`, and `SideStairMudroom_Stairway_UpperSittingHall`; preserve the standard-door / inferred-upward-stairway split and the trigger's serialized `Door` / `Auto` compatibility values.

Both Passages are stage `2`, use source-region and destination-region placement, and serialize no points. Only the two definition assets/metas, Upper Sitting RoomView, two Passage components, two mapped dependency sets/callers, database entries, root registrations, inventory rows, focused tests, generated evidence, and documentation advance. The accepted matrix is `6,052` scene documents / `15` RoomViews / `30` Passages / `49` definitions / `54` scene behaviours / `62` GameRoot references / `30 bound + 15 null` callers / `30 complete + 10 queued + 2 one-way + 3 parallel` inventory rows. Exact production hashes are scene `7b802932693726a6a1c94736c715b9ffc03b53c0d31cb247db0d9cacb1082407`, database `7bec731d33b2bd1631adfa842533f91af072523d5e9b81b0ef7822a3f8facfef`, inventory `d1c630d178b6c7ce8db765b590273b93eff3ea6372fcec503071f7ec786dd64a`, and unchanged catalog `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.

Static controls, Unity compilation, focused EditMode `74/74`, complete lifecycle `20/20`, and genuine Group14/representatives/cold PlayMode `5/5` pass. Locked SHAs are Group14 `8feac87c6e631543491757c0bab3f8f16383733f1bad102419bd1af4f57f357c` and Group13 `14315ebb94e0991564058de63ed42aad056f85b3578c8af626acc4e75c1dab3c`; cold SHA and scale metrics remain unchanged. Preserve the failed/corrected evidence sequence and do not relabel it. No deletion is authorized.

The next slice is tests-first Group `15` Upper Sitting Hall <-> Upper Gallery. Characterize both standard-door directions across the established five profiles before adding Upper Gallery RoomView `4100000046` or any Group15 definition/Passage/caller/dependency. Protect the Upper Gallery boundary, stairwell blocker, railing, plant cutouts/blockers, Chapter 2 hide anchor, and every neighboring Group16/17/18/22 route. Do not change `doors.txt`.

### Accepted checkpoint: Slice 2.2-15 Group 15

Starting from clean characterization commit `e63e445b00d4f782ad53cc08f02a155b2b79b9a9`, Group15 adds only Upper Gallery RoomView `4100000046`, reciprocal Passages `4100000047/48`, and PassageDefinition GUIDs `9968ef27c1a4121f14100399fe85ab32` / `54dc2ad728e40e59f280711e65b3474d` to the existing triggers and canonical graph. Both directions remain Door / `Open Door`, stage `2`, source-region mode `1`, destination-region mode `1`, and point-free. Exact aliases, trigger rectangles, existing GUIDs, protected Upper Gallery content, neighboring routes, and the legacy catalog are unchanged.

Accepted totals are Gameplay `6,055` documents / sixteen RoomViews / thirty-two Passages / 45 triggers, `GameDatabase 51` (`19` rooms / `32` passages), GameRoot `57` scene behaviours / `65` references, callers/dependencies `32 bound / 13 null`, and inventory `32 complete / 8 queued / 2 blocked-one-way / 3 blocked-parallel`. Static/GUID controls and Unity compilation pass. Focused EditMode is honestly accounted as `75/68/7`, `75/74/1`, then isolated `1/1`; rendered Group15/14 passes `2/2`; genuine Group15, three representative prior groups, and cold start pass `5/5`. Group15 remains locked at SHA `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2`.

No pruning is authorized. The next safe slice is tests-first `2.2-16a` for Upper Gallery <-> Master Bedroom Suite using existing triggers `2300000204` / `2300000214`; lock its legacy rendered behavior before production authoring and leave Group22 parallel topology untouched.

### Accepted checkpoint: Slice 2.2-16 Group 16

Starting from clean characterization/rollback commit `9fb4a3d9131368818e6c6fb1f8009b844b9b3a5e`, Group16 adds only Master Bedroom Suite RoomView `4100000049`, reciprocal Passages `4100000050/51`, and PassageDefinition GUIDs `1dff2bb730264e9c81ecb2e35985f5a6` / `f1fd515e16c44606af75bf593f67bc07` to existing triggers `2300000204` / `2300000214` and the canonical graph. Both directions remain Door / `Open Door`, stage `2`, source-region mode `1`, destination-region mode `1`, and point-free. Exact aliases, trigger rectangles and transforms, existing GUIDs, protected Upper Gallery/Master content, neighboring routes, and the legacy catalog are unchanged.

The reciprocal destination regions are Master `BL (-770.5059814, -210.125) / TL (-770.5059814, 264.125) / TR (-599.4940186, 264.125) / BR (-599.4940186, -210.125)` and Gallery `BL (-722.8327637, -70.00001526) / TL (-722.8327637, 195.9999847) / TR (-604.0205078, 195.9999847) / BR (-604.0205078, -70.00001526)`. Each source approach derives from its reverse Passage's region. The data audit confirms all `856` RectTransforms are byte-identical to the rollback state and maximum region float error is `0.00003220`.

Accepted totals are Gameplay `6,058` documents / seventeen RoomViews / thirty-four Passages / 45 triggers, `GameDatabase 53` (`19` rooms / `34` passages), GameRoot `60` scene behaviours / `68` references, callers/dependencies `34 bound / 11 null`, direct catalogs `33` door / `1` stair / `11` unbound, inventory `34 complete / 6 queued / 2 blocked-one-way / 3 blocked-parallel`, and placement serialization `12` source regions / `14` destination regions / `10` explicit coordinate-space values. Exact production hashes are scene `5f5b975165b3ed02d529cea132d6943278ef709f11c8913efda769b405617b5b`, database `6df8c80cb0f98c78ac0a5fb651a44bf3096020ecc46bb02acab8116ad78e338d`, inventory `e901dde6232c67612ca877fd2258cf10f90f5eb2921eac7fded2aaa2e75a9477`, and unchanged catalog `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.

Architecture tools pass `4/4`; guard, ledger, script-integrity, serialized-reference, and meta/GUID controls pass at `114 / 51,224 / 48`, smells `106/17/67/51/4/6`, `165` scripts / `1,986` references (`1,130` project / `856` external), and `7,377` paired unique metas. Unity compiles after one test-only syntax repair. Focused EditMode accounts for all `76` cases (`74/2`, then repaired `2/2`); rendered Group16/15 passes `2/2` at exact SHAs `f97e30d0807e46ee1846bc1421ebcb0f72a694988c8fda915bb7e87b4b3acb0f` / `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2`; genuine Group16/15/14/13 plus cold-start PlayMode passes `5/5`, with cold SHA `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96` unchanged.

No pruning is authorized; eleven compatibility callers remain and human review is still required. The next safe slice is tests-first Group `17` Upper Sitting Hall <-> Nursery on existing triggers `2300000224` / `2300000234`.

### Current checkpoint: Slice 2.2-17 Group 17 accepted production

Group17 characterization is committed and preserves the exact six-line legacy fingerprint SHA-256 `57c5dccca03303a443cde3756c02047021d934c5230e51da8e087d48a9649bc4`. The accepted production slice adds only Nursery RoomView `4100000052`, reciprocal Passages `4100000053/54`, PassageDefinition GUIDs `e40848fe5ab54ec99ff3b664859e7c1b` / `c05a3dd92c9f45cf8231f1679bd06506`, their database/root registrations, the two existing trigger bindings, inventory rows, focused tests, and current-state documentation. Both directions remain Door / `Open Door`, stage `2`, source-region mode `1`, destination-region mode `1`, and point-free. Legacy IDs `UpperSittingHall_Nursery` / `Nursery_UpperSittingHall`, exact trigger rectangles/transforms, the legacy catalog, protected Nursery content, and neighboring routes remain unchanged.

The reciprocal destination regions are Nursery `BL (157, -88.39423) / TL (157, 209.43507) / TR (277, 209.43507) / BR (277, -88.39423)` and Upper Sitting Hall `BL (-78.9274, -32.718) / TL (-78.9274, 178.862) / TR (46.9532, 178.862) / BR (46.9532, -32.718)`. Each source approach derives from its reverse Passage region; neither direction serializes a point anchor.

Accepted totals are Gameplay `6,061` documents / eighteen RoomViews / thirty-six Passages / 45 triggers, `GameDatabase 55` (`19` rooms / `36` passages), GameRoot `63` scene behaviours / `71` references, callers/dependencies `36 bound / 9 null`, direct catalogs `35` door / `1` stair / `9` unbound, inventory `36 complete / 4 queued / 2 blocked-one-way / 3 blocked-parallel`, and placement serialization `14` source regions / `16` destination regions / `10` explicit coordinate-space values. Blue Bedroom is the sole remaining RoomDefinition without a RoomView.

Architecture tools pass `4/4`; architecture guard, exact `114/114` runtime-ledger coverage, script-integrity, serialized-reference, and metadata/GUID controls pass at `165` scripts / `1,991` references (`1,135` project / `856` external) and `7,379` paired unique metas. Unity compiles. Focused EditMode accounts for all `77` cases (`76/1`, the one failure being a stale prior Group15-neighbor expectation) and the isolated test-only repair passes `1/1`. Rendered Group17/16 passes `2/2` at SHAs `57c5dccca03303a443cde3756c02047021d934c5230e51da8e087d48a9649bc4` / `f97e30d0807e46ee1846bc1421ebcb0f72a694988c8fda915bb7e87b4b3acb0f`; Group17/16/15/14 plus cold PlayMode passes `5/5`, retaining cold SHA `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. Human doorway, camera, audio, and Nursery occlusion review remains. No pruning is authorized. Begin tests-first Group `18` Upper Sitting Hall <-> Blue Bedroom from this accepted checkpoint.
