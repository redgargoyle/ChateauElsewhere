# Architecture-overhaul execution state

## Repository

- Branch: `refactor/final-architecture-overhaul`
- HEAD / starting commit: `57e4ff104092ffbbdc426a7133211e5a12457e9b`
- Unity: `/home/hamzak/Unity/Hub/Editor/6000.4.10f1/Editor/Unity` (`6000.4.10f1`, revision `feeafc12a938`)
- Working tree clean at tests-first Slice `2.2-16a` start; no staged, untracked, ignored test-scene, or generated evidence mutation is carried
- Last passing / rollback commit: `57e4ff104092ffbbdc426a7133211e5a12457e9b`
- Resulting Group `09` commit: `aa0e24d163437975e7747fc60829817ff01e88c6`
- Resulting destination-region prerequisite commit: `f91ee2338261ee061a8502e06542fd49b5d81831`
- Resulting Group `10` commit: `eef71f3e7dd9ac9f181818169b9a911fd1498d58`
- Resulting Group `11` characterization commit: `78f61103b6c4582f72ee6d12f4031dd5c763d93e`
- Resulting source-region prerequisite commit: `9ed538fa209c239f50675dbc0b9a04c0bbaa01ea`
- Resulting Group `11` production commit: `46a109bf27f30a0ab538fdf7f737725e4a0f92a9`
- Resulting Group `12` characterization commit: `661514fe82a1325424b9ca9705dbfd772ae4f886`
- Resulting Group `12` production commit: `1189022453a21accafcf13e375a121320b0e343a`
- Resulting Group `13` characterization commit: `49e0617f93b015e2951ccb5b8b78d08d612f06ca`
- Resulting Group `13` production commit: `364e0ff88cd69efecb18c7bc5b8074a0f80acd6e`
- Resulting Group `14` characterization commit: `e5c6ad322fe3e290d0ee9b50ea7c841b75b9b9ad`
- Resulting Group `14` production commit: `1869cf63459506536f047489b25d85be15cef8bb`
- Resulting Group `15` characterization commit: `e63e445b00d4f782ad53cc08f02a155b2b79b9a9`
- Resulting Group `15` production commit: `57e4ff104092ffbbdc426a7133211e5a12457e9b`
- Resulting Group `16` characterization commit: assigned by this accepted commit

## Current slice

- Slice ID: `2.2-16a` — tests-first Upper Gallery <-> Master Bedroom Suite characterization
- Status: accepted; resulting commit is assigned by this commit.
- Sole ownership change: none. Freeze the existing reciprocal Group16 legacy standard-door behavior and exact protected-content boundary before any scene/data ownership transfer.
- Starting commit: `57e4ff104092ffbbdc426a7133211e5a12457e9b`
- Allowed files/assets: this execution-state record and `Assets/Editor/GameplayLifecycleCharacterizationTests.cs`. Ignored evidence may be written only under `Logs/ArchitectureOverhaul/Slice-2.2-16/`.
- Production files forbidden: every runtime source, scene, prefab, ScriptableObject, `.meta`/GUID, `doors.txt`, inventory row, generated architecture report, package, ProjectSettings file, large asset, trigger rectangle/scale, hierarchy, boundary, cutout, blocker, art, audio clip, lighting, camera, and presentation geometry.
- Compatibility surface to preserve: every existing GUID/file ID/public API and all sixteen accepted route pairs; Group16 trigger/host/RectTransform identities `2300000204/2300000200/2300000201` and `2300000214/2300000210/2300000211`; exact strings `UpperGallery_MasterBedroomSuite`, `MasterBedroomSuite_UpperGallery`, `Upper Gallery`, and `Master Bedroom Suite`; serialized standard-door behavior, `Open Door` prompt, `Door` cursor, compatibility door audio, `145 px` threshold, event order, far approach, arrival, camera/active-room visibility, and cleanup; exact scaled trigger geometry; locked Group15 and prior fingerprints plus cold-start SHA/scale metrics; the Upper Gallery and Master Bedroom Suite boundaries, cutouts/blockers/props/anchors, neighboring routes, blocked Group22 parallel topology, and all exceptional topology.
- Characterization test: `UpperGalleryMasterBedroomSuiteStandardDoorLegacyPassagesAreCharacterizedAcrossRenderedAspects` locks five rendered profiles plus one structural line at SHA-256 `f97e30d0807e46ee1846bc1421ebcb0f72a694988c8fda915bb7e87b4b3acb0f`.
- Focused gate: `git diff --check`; architecture tools/guard; exact runtime ledger, metadata/GUID, and serialized-reference checks; Unity `6000.4.10f1` compilation; locked Group16 plus Group15 rendered characterization with nonzero exact XML counts; and independent cold start.
- Rendered/PlayMode/manual gate: Group16 must traverse both null-Passage legacy routes across all locked profiles while preserving far movement, arrival, event/audio order, prompt/cursor cleanup, camera/visibility, and protected content; Group15 must remain exact; independent cold start must retain its approved digest. Production and human-review claims remain forbidden in this characterization-only slice.
- Rollback commit: `57e4ff104092ffbbdc426a7133211e5a12457e9b`

## Current slice evidence

- Clean start: branch `refactor/final-architecture-overhaul` at accepted Group15 production commit `57e4ff104092ffbbdc426a7133211e5a12457e9b`; `git status --short` was empty before this declaration, and no ignored Test Framework scene was present.
- Immutable production baseline: Gameplay `4ba54fac49bb37c9c693c0fee0e7c32e90aae0e29c75a5989b67da8fba64f5f2`; GameDatabase `db24627071eff29beaa11c072363d4c2abb1ebeb0012504ab39c17fee394fb7f`; inventory `e7e066f477ce3599526d0a9e3fb8404636752508113fe55dc005ba5a8d4c9820`; `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.
- Baseline totals remain Gameplay `6,055` documents / sixteen RoomViews / thirty-two stage-2 Passages / 45 triggers; GameDatabase `51` definitions (`19` rooms / `32` passages); GameRoot `57` scene behaviours / `65` references; dependencies/callers `32 bound / 13 null`; inventory `32 complete / 8 queued / 2 blocked-one-way / 3 blocked-parallel`; `165` scripts / `1,981` serialized references and `7,375` paired unique meta GUIDs.
- Exact Group16 legacy owners: Upper Gallery trigger `2300000204` on GameObject/RectTransform `2300000200` / `2300000201` is `UpperGallery_MasterBedroomSuite -> Master Bedroom Suite`; Master Bedroom Suite trigger `2300000214` on `2300000210` / `2300000211` is `MasterBedroomSuite_UpperGallery -> Upper Gallery`. Both serialize null navigation/audio/player/catalog dependencies and no canonical caller.
- Canonical-data boundary: Upper Gallery already owns RoomView `4100000046`. Master Bedroom Suite RoomDefinition GUID `5acd179b4f7543438b4e55e5c0e5f6ca` exists with stable ID `room.master-bedroom-suite`, but root/content `2300000045/2300000047` has no RoomView. Group16 has no PassageDefinition, Passage, caller/dependency binding, database/GameRoot registration, or completed inventory row.
- Isolation baseline: Upper Gallery retains completed Group15 and blocked-parallel Group22; Master Bedroom Suite has only the Group16 return. Characterization may observe but must not mutate, bind, reclassify, or infer topology for any neighboring or exceptional route.
- Characterization boundary: no production identity, point, region, or ownership conclusion was predeclared. Five rendered profiles were captured from real legacy traversal before any Group16 authoring decision.
- Characterization totals remain byte-identical to the baseline. Only the existing characterization test file and this execution record changed; generated script/reference counts, scene/data/catalog hashes, metadata, and inventory did not move.

## Completed Slice `2.2-16a` evidence

- Exact geometry: Gallery -> Master trigger `2300000204` remains position `(-663.42664, 62.99999)`, size `(118.8122, 266)`, unit scale, with room-local corners `BL (-722.8327637, -70.00001526) / TL (-722.8327637, 195.9999847) / TR (-604.0205078, 195.9999847) / BR (-604.0205078, -70.00001526)`. Master -> Gallery trigger `2300000214` remains position `(-685, 27)`, size `(120, 140)`, deliberately non-unit scale `(1.4251, 3.3875)`, with effective corners `BL (-770.5059814, -210.125) / TL (-770.5059814, 264.125) / TR (-599.4940186, 264.125) / BR (-599.4940186, -210.125)`.
- Protected-content proof: Upper Gallery retains its twelve-point boundary, sixteen-point stairwell blocker, railing, two plant cutouts/blockers, Chapter 2 guest anchor, and completed Group15 route. Master Bedroom Suite retains its exact 26-child order, twenty-point boundary, sixteen flame instances, chair/bed/loveseat cutouts, three four-point movement blockers, and no RoomView. Group22 remains blocked-parallel and untouched.
- Characterization result: both legacy null-Passage directions traverse at `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest maximum zoom. Far approach, scaled geometry, arrival mapping, event/audio ordering, prompt/cursor cleanup, camera/active-room visibility, catalog behavior, and protected-content isolation remain exact. The independently reconstructed six-line SHA-256 is `f97e30d0807e46ee1846bc1421ebcb0f72a694988c8fda915bb7e87b4b3acb0f`.
- Static/compile gate: architecture-tool tests pass `4/4`; guard and exact `114/114` runtime ledger pass; `git diff --check` passes. Unity `6000.4.10f1` compilation exits `0` with no C# compiler error in `compile-characterization.log`. The test-only file addition changes no runtime audit, generated reference, metadata, scene, data, or inventory count.
- Rendered gate: the calibration XML is a deliberate `2/1/1`, where Group15 passes and Group16 reaches only the all-zero hash sentinel while reporting the reviewed digest above. The identical locked suite in `group16-group15-characterization-locked.xml` passes exact `2/2`; Group15 remains exact at `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2`. A final review added exact Master cutout sorting-layer/order and non-trigger boundary assertions; the isolated unchanged Group16 case passes `1/1` in `group16-occlusion-final.xml` with the same SHA.
- Independent golden gate: `cold-start.xml` passes exact `1/1`; SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.752865`, and sort `1075`.
- Immutable production result: Gameplay remains `4ba54fac49bb37c9c693c0fee0e7c32e90aae0e29c75a5989b67da8fba64f5f2`; GameDatabase remains `db24627071eff29beaa11c072363d4c2abb1ebeb0012504ab39c17fee394fb7f`; inventory remains `e7e066f477ce3599526d0a9e3fb8404636752508113fe55dc005ba5a8d4c9820`; `doors.txt` remains `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; generated evidence remains `182347940cd5bd5c8dbaf3bbed11a69740a368599a541002a75a94970aa06c31` / `1555c98a0905eea5963000d9b4721b87dd2d971f9db2168a8793d06c680dd22c`. No scene, prefab, runtime source, ScriptableObject, existing metadata/GUID, catalog, inventory, package, ProjectSettings, or large asset changed.

## Failed-gate audit within Slice `2.2-16a`

- `group16-group15-calibration.xml` is an intentional `2 total / 1 passed / 1 failed` result. Group15 passed; Group16 traversed every profile and failed only at the deliberate all-zero SHA sentinel, reporting `f97e30d0807e46ee1846bc1421ebcb0f72a694988c8fda915bb7e87b4b3acb0f`.
- Exactly five profile lines and one structural line were extracted from the preserved log, joined with `\n` and no trailing newline, and independently hashed to the same digest. Only the sentinel changed. The identical locked rerun passes `2/2`; no production byte or assertion threshold was changed.

## Completed Slice `2.2-15` production evidence

- Production identity: Upper Gallery root `2300000040` gains RoomView `4100000046` bound to existing RoomDefinition GUID `d89c065ce9d049d4b6f5ba012b16761d` and content `2300000042`. Existing triggers `2300000209` / `2300000219` gain Passages `4100000047` / `4100000048`, new PassageDefinition GUIDs `9968ef27c1a4121f14100399fe85ab32` / `54dc2ad728e40e59f280711e65b3474d`, reciprocal links, canonical callers, and their existing manager/player/audio/door-catalog dependencies.
- Placement: both directions remain standard Door / `Open Door`, serialize stage `2`, source-region mode `1`, destination-region mode `1`, and no point anchors. Gallery -> Upper stores `BL (-174.1340942, -42.82170105) / TL (-174.1340942, 207.995697) / TR (-135.8659058, 207.995697) / BR (-135.8659058, -42.82170105)`. Upper -> Gallery stores `BL (587.2802124, -69.99983978) / TL (587.2802124, 225.1838379) / TR (710.7197876, 225.1838379) / BR (710.7197876, -69.99983978)`.
- Accepted shape: Gameplay `6,055` documents / sixteen RoomViews / thirty-two stage-2 Passages / 45 triggers; GameDatabase `51` definitions (`19` rooms / `32` passages); GameRoot `57` scene behaviours / `65` references; dependencies/callers `32 bound / 13 null`; inventory `32 complete / 8 queued / 2 blocked-one-way / 3 blocked-parallel`; serialized audio ownership `31` direct door catalogs / one direct stair catalog / `13` unbound callers.
- Exact state hashes: Gameplay `4ba54fac49bb37c9c693c0fee0e7c32e90aae0e29c75a5989b67da8fba64f5f2`; GameDatabase `db24627071eff29beaa11c072363d4c2abb1ebeb0012504ab39c17fee394fb7f`; inventory `e7e066f477ce3599526d0a9e3fb8404636752508113fe55dc005ba5a8d4c9820`; unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; definition assets `609af3da28ab839e4731b0fe03dc3f245baf4d0620fe8b1a251703e3d11a8ef9` / `affd222c694ea6b4a23bb39cde1c037ae7d8702f5c9670349458fee3cde2658f`.
- Static evidence: architecture-tool tests pass `4/4`; guard, audit, exact `114/114` runtime ledger, script integrity, serialized-reference scan, and all-meta pairing/uniqueness pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours, smells `106/17/67/51/4/6`, `165` scripts / `1,981` references (`1,125` project / `856` external), and `7,375` paired unique GUIDs. Generated hashes are runtime `35b78190fc8820883abde1b8db531edbcd13108742fbfb515a7fb187ac3c8f69`, summary `6748d21ca5f2f6f424fc2c62c42fce36cc076e92ef2498958fc717614eef059c`, script integrity `182347940cd5bd5c8dbaf3bbed11a69740a368599a541002a75a94970aa06c31`, and serialized references `1555c98a0905eea5963000d9b4721b87dd2d971f9db2168a8793d06c680dd22c`.
- Unity evidence: `compile-production.log` exits `0` with no C# compiler error. The first focused XML records `75/68/7`; after one batched stale-total correction, the second records `75/74/1`; the isolated final stale Group10 region-count correction passes `1/1`, accounting for all 75 cases without redundantly rerunning the complete suite.
- Rendered and PlayMode evidence: `rendered-group15-group14-production.xml` passes exact `2/2`, preserving Group15 SHA `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2` and Group14 SHA `8feac87c6e631543491757c0bab3f8f16383733f1bad102419bd1af4f57f357c`. Genuine Group15 plus representative Group14/13/12 and cold start pass exact `5/5` in `playmode-group15-representatives-cold.xml`. Cold SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`; Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.752865`, and sort `1075` remain unchanged.
- Diff boundary: no runtime source, existing `.meta`/GUID, prefab, `doors.txt` row, threshold, trigger rectangle/scale, hierarchy, boundary, stairwell blocker, railing, plant/cutout, generated blocker, guest anchor, art, audio clip, lighting, camera, package, ProjectSettings file, or large asset changed. No deletion or pruning is authorized. Human doorway/feet/landing, prompt/cursor/audio, camera/visibility, and Upper Gallery collision/occlusion review remains outstanding.

## Failed-gate audit within Slice `2.2-15`

- `editmode-focused-production.xml` is a real `75/68/7` result. Its seven failures were test-only stale current-graph totals: four navigation fixture root-registration assertions still expected `54` root behaviours rather than `57`, one service-side assertion expected `13` rather than `16` RoomViews, and two certification strings still described Group14 rather than the live Group15 graph. Production bytes and frozen fingerprints were not implicated.
- One batched test correction produced `editmode-focused-production-attempt2.xml` at `75/74/1`; the sole remaining failure was Group10's historical global arrival-region total `10` rather than `12`. Correcting those two fixture literals produced `editmode-focused-last-correction.xml` at exact `1/1`. The complete `75` cases are therefore accounted, and all failed XML/logs remain preserved rather than relabeled as passing.

## Completed Slice `2.2-15a` evidence

- Clean start and rollback: branch `refactor/final-architecture-overhaul` at accepted Group14 production commit `1869cf63459506536f047489b25d85be15cef8bb`; `git status --short` was empty, and no ignored Test Framework scene was present before declaration. That commit remains the mechanical rollback point.
- Exact legacy owners: Upper Sitting Hall -> Upper Gallery is trigger/host/RectTransform `2300000219/2300000215/2300000216`, ID `UpperSittingHall_UpperGallery`; Upper Gallery -> Upper Sitting Hall is `2300000209/2300000205/2300000206`, ID `UpperGallery_UpperSittingHall`. Both remain four-component standard-door facades with null serialized dependencies/callers, `145 px` proximity, auto-walk/activate, `Open Door`, `Door` cursor, compatibility-resolved door audio, and exact reciprocal `doors.txt` rows.
- Exact geometry: Upper -> Gallery remains position `(-155, 82.587)`, size `(38.2682, 250.8174)`, unit scale, with room-local corners `BL (-174.1340942, -42.82170105) / TL (-174.1340942, 207.995697) / TR (-135.8659058, 207.995697) / BR (-135.8659058, -42.82170105)`. Gallery -> Upper remains position `(649, 77.592)`, size `(123.4396, 295.1837)`, unit scale, with corners `BL (587.2802124, -69.99983978) / TL (587.2802124, 225.1838379) / TR (710.7197876, 225.1838379) / BR (710.7197876, -69.99983978)`.
- Protected ownership boundary: Upper Sitting Hall retains RoomView `4100000043`; Upper Gallery retains its existing passive RoomDefinition `d89c065ce9d049d4b6f5ba012b16761d` and no RoomView. Its twelve-point boundary, sixteen-point stairwell blocker, railing, two plants/cutouts, two generated plant blockers, Chapter 2 guest anchor, hierarchy, and neighboring Groups14/16/17/18/22 remain unchanged.
- Characterization result: the new Group15 case exercises both legacy directions at `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest maximum zoom. All profiles preserve far approach, exact geometry, arrival mapping, event/audio ordering, prompt/cursor cleanup, camera/active-room visibility, catalog behavior, and protected-content isolation. The independently reconstructed six-line SHA-256 is `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2`.
- Static/compile gate: architecture-tool tests pass `4/4`; guard, audit, exact runtime ledger, Unity script integrity, serialized-reference scan, and all-meta pairing/uniqueness pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours, unchanged smells `106/17/67/51/4/6`, `165` scripts / `1,976` serialized references (`1,120` project / `856` external), `114/114` ledger rows, and `7,373` paired globally unique metadata GUIDs. Unity `6000.4.10f1` compilation exits `0` with no C# compiler error.
- Rendered/golden gate: `group15-group14-characterization-locked.xml` passes exact `2/2`; Group15 retains SHA `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2`, and prior Group14 remains exact at `8feac87c6e631543491757c0bab3f8f16383733f1bad102419bd1af4f57f357c`. Independent `cold-start.xml` passes exact `1/1`; SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.752865`, and sort `1075`.
- Immutable production result: Gameplay remains `7b802932693726a6a1c94736c715b9ffc03b53c0d31cb247db0d9cacb1082407`; GameDatabase remains `7bec731d33b2bd1631adfa842533f91af072523d5e9b81b0ef7822a3f8facfef`; inventory remains `d1c630d178b6c7ce8db765b590273b93eff3ea6372fcec503071f7ec786dd64a`; `doors.txt` remains `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. Only the characterization test and this execution record changed; no production source/data, scene, prefab, existing metadata/GUID, boundary, trigger, cutout, blocker, art, audio, camera, package, or ProjectSettings file changed.
- Human doorway/feet/landing, prompt/cursor/audio, camera/visibility, and Upper Gallery collision/occlusion review remains required after production authoring. No pruning is authorized by this characterization slice.

## Failed-gate audit within Slice `2.2-15a`

- The first two-test calibration recorded `0/2`. One test-only RawImage correction had matched a prior Group14 child-list assertion as well as the intended new Group15 assertion; the prior assertion was restored exactly. The new Group15 case also expected the inactive Gallery trigger's compatibility door catalog before its `Awake` path had run; the characterization now invokes the existing private catalog resolver alongside its other explicit inactive-trigger dependency resolvers. No production, scene, data, GUID, geometry, or frozen hash changed.
- The second calibration then passed prior Group14 and reached only the deliberate Group15 all-zero SHA sentinel, recording exact `1/2`. All five Group15 profile lines and the structural line independently hash to `ed8fad6c038ea9d1ab346cbc7e463403a09b6aab562ad5564d9c48519c29fda2`; only that expected digest was locked. The final identical two-test suite passes exact `2/2`. All calibration XML/logs remain under `Logs/ArchitectureOverhaul/Slice-2.2-15/` and are not relabeled as passing.
- The batched metadata counter initially used whitespace-unsafe shell expansion and stopped on the existing path `Assets/Scenes/New Main Menu.unity` after every architecture/script/ledger check had already passed. The corrected NUL-safe read-only counter reports exactly `7,373/7,373` unique paired GUIDs. No repository file changed because of the command error, and no validation result was bypassed.

## Completed Slice `2.2-14` evidence

- Clean start and rollback: branch `refactor/final-architecture-overhaul` at accepted characterization commit `e5c6ad322fe3e290d0ee9b50ea7c841b75b9b9ad`; `git status --short` was empty before the production declaration. That commit remains the mechanical rollback point.
- Production identity: Upper Sitting Hall root `2300000050` gains RoomView `4100000043` bound to existing RoomDefinition `2ce92b6f43ff440e95570359e18cce0f` and content `2300000052`. Existing triggers `1915714844` / `2300000189` gain Passages `4100000044` / `4100000045`, definitions `d5c370a383ecaf7257b8f796d0164e72` / `77a3efbd4a250afa71e5998bb7118532`, direct reciprocal links, dependency sets, and canonical callers.
- Interaction/audio compatibility: Upper -> Side remains standard Door / `Open Door` with direct door catalog. Side -> Upper retains serialized `Door` / `Auto` but resolves as upward Stairway / `Use Stairway` with the sole direct stair catalog. All existing prompts, cursors, movement thresholds, event order, camera/visibility behavior, and `Side Stair Mudroom` alias remain protected. Runtime door-catalog fallback resolution on the stair trigger is distinguished from direct serialized ownership.
- Passage placement: both Passages are stage `2`, source-region mode `1`, destination-region mode `1`, and own no points. Upper -> Side stores `BL (253.170868, -202.263962) / TL (253.170868, 236.559952) / TR (471.909576, 236.559952) / BR (471.909576, -202.263962)`. Side -> Upper stores `BL (70.8670044, -32.7176476) / TL (70.8670044, 130.0000458) / TR (129, 130.0000458) / BR (129, -32.7176476)`.
- Exact state hashes: Gameplay `7b802932693726a6a1c94736c715b9ffc03b53c0d31cb247db0d9cacb1082407`; GameDatabase `7bec731d33b2bd1631adfa842533f91af072523d5e9b81b0ef7822a3f8facfef`; inventory `d1c630d178b6c7ce8db765b590273b93eff3ea6372fcec503071f7ec786dd64a`; unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; new definition assets `0c28d779529e3040ad643c91ca6e26773bf8114d4f4c7996d604ad1650b0450f` / `120d892342696765591d679d87f5b1eee256b853300e4c672521c0081b30160b`.
- Static evidence: architecture-tool tests `4/4`, guard, exact `114/114` ledger, Unity script integrity, serialized-reference scan, and all-meta pairing/uniqueness pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours, smells `106/17/67/51/4/6`, `165` scripts / `1,976` references (`1,120` project / `856` external), and `7,373` paired unique meta GUIDs. Generated hashes are runtime `35b78190fc8820883abde1b8db531edbcd13108742fbfb515a7fb187ac3c8f69`, summary `6748d21ca5f2f6f424fc2c62c42fce36cc076e92ef2498958fc717614eef059c`, script integrity `7cf0fc1cb714f2c7274b2e7574fa3bcc5dd4ef4da7ff80231a17760b91ad04c8`, and serialized refs `593b79c2eacdec231849e7fe8f10201038616a542a30a9265c8a7dd61fb75c77`.
- Unity/focused evidence: the initial compile preserves one test-only undefined-name error; `compile-attempt2.log` exits `0` with no compiler error. The first focused XML records `73/65/8`; after correcting only stale test totals and the new method name, `editmode-focused-attempt2.xml` passes exact `74/74`.
- Rendered evidence: the first complete lifecycle XML records `20/13/7` from stale pair-null totals, one stale stair-catalog expectation, and cleanup cascades. One test-only batch correction yields exact `20/20` in `editmode-full-lifecycle-attempt2.xml`. Group14 SHA remains `8feac87c6e631543491757c0bab3f8f16383733f1bad102419bd1af4f57f357c`; Group13 remains `14315ebb94e0991564058de63ed42aad056f85b3578c8af626acc4e75c1dab3c`; Group12/11/10 SHAs also remain exact.
- PlayMode/golden evidence: `playmode-group14-representatives-cold.xml` passes exact `5/5`. Group14 reports Upper local `(124.7773, -44.8677)`, Side local `(216.851, -209.032)`, forward stop `(2.823581, -2.721771)`, and reverse stop `(1.372878, -0.505951)`. Cold SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.752865`, and sort `1075`.
- Expanded PlayMode audit: `playmode-full-architecture-baseline.xml` records `13/12/1`; every architecture/navigation case passes. Only the pre-existing Chapter 2 panic golden misses after an order-sensitive center shift to `(480.541, 302.813)` while retaining exact `255.668 px` height and sort `1620`. The unchanged isolated case passes `1/1` at `(477.265, 300.745)` in `playmode-chapter2-panic-isolated.xml`, within its approved one-pixel tolerance. No production or golden threshold changed.
- Protected-content/diff result: no runtime source, existing `.meta`/GUID, prefab, catalog row, threshold, trigger rectangle/scale, hierarchy, boundary, cutout, blocker, art, audio clip, lighting, camera, presentation geometry, package, ProjectSettings file, or large asset changed. No deletion or pruning is authorized. Human stair/door feel, feet/landings, prompt/cursor/audio, camera/visibility, and collision/occlusion review remains outstanding.

## Failed-gate audit within Slice `2.2-14`

- `compile.log` failed on one test-only identifier typo (`serializedGameplayTriggers` versus the locally declared `serializedTriggerDocuments`). Production code and serialized content compiled; the exact identifier correction produced the accepted compile.
- `editmode-focused.xml` discovered 73 tests and recorded `65` pass / `8` fail. Seven failures were stale current-graph totals or prior canonical-summary lines, and the Group14 method name did not match the declared filter. The corrected filter/source contract discovers and passes all `74/74`; no runtime, scene, data, GUID, or behavior changed.
- `editmode-full-lifecycle.xml` recorded `20/13/7`. Four assertions still expected the Group12 pair-null state `26/19` instead of current `30/15 -> 28/17`; one assertion still required all stair catalogs null; three cleanup NREs were downstream of those failed paths. The test-only batch correction passes `20/20` without changing frozen SHA inputs or production bytes.
- `playmode-full-architecture-baseline.xml` recorded `13/12/1` solely on the historical Chapter 2 panic center tolerance, while sprite identity, height, sort, visible guest count, room, and panic state remained correct. The unchanged isolated rerun passes `1/1`. This order-sensitive evidence is retained and not relabeled as a full-fixture pass.

## Completed Slice `2.2-14a` evidence

- Clean start: branch `refactor/final-architecture-overhaul` at accepted Group13 production commit `364e0ff88cd69efecb18c7bc5b8074a0f80acd6e`; `git status --short` was empty before this declaration.
- Immutable accepted baseline: Gameplay `82e6b51a51cc3a5cf34afe6cfb26dfe35a292cafbdd6a3dd58e6221cbac3d119`; GameDatabase `13f8161b0a5f107e984c3c856bdc9f75bb922f2c3082370de571f412a42ab5f3`; inventory `2aaba6eed5c2bd28026dcca5dedb863b2ecbf646574da168799c1249b9c2fc29`; `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.
- Baseline totals remain Gameplay `6,049` documents / 14 RoomViews / 28 stage-2 Passages / 45 triggers; GameDatabase `47` definitions (`19` rooms / `28` passages); GameRoot `51` scene behaviours / `59` references; dependencies/callers `28 bound / 17 null`; inventory `28 complete / 12 queued / 2 blocked-one-way / 3 blocked-parallel`.
- Exact Group14 legacy owners: Upper Sitting Hall trigger `1915714844` on GameObject/RectTransform `1915714842` / `1915714843` is `UpperSittingHall_SideStairMudroom -> Side Stair Mudroom`; Side Stair trigger `2300000189` on `2300000185` / `2300000186` is `SideStairMudroom_Stairway_UpperSittingHall -> Upper Sitting Hall`. Both currently serialize null navigation/audio/player/catalog dependencies and no canonical caller.
- Mixed-profile baseline: both owners serialize `triggerKind: 0`, `stairwayDirection: 0`, proximity `145`, walk-when-far, and auto-activate. The reverse owner's stairway semantics are inferred from its name/inventory classification rather than an authored kind. The legacy catalog contains neither Group14 ID and instead retains unrelated Side Stair / Upper Gallery rows; this mismatch must be characterized, not normalized.
- Canonical-data boundary: Upper Sitting Hall RoomDefinition GUID `2ce92b6f43ff440e95570359e18cce0f` exists with stable ID `room.upper-sitting-hall`, exact display/legacy name `Upper Sitting Hall`, and background GUID `45a669872351e5ba0b2a6749ecc2065f`, but its room root has no RoomView. Group14 has no PassageDefinition, Passage, caller/dependency binding, database/GameRoot registration, or completed inventory row.
- Isolation baseline: Side Stair retains completed Group13; Upper Sitting Hall also owns queued Groups15, 17, and 18. Group14 characterization may observe but must not mutate, bind, reclassify, or infer topology for any neighboring route.
- Serialized rectangle baseline: Upper Sitting Hall -> Side Stair is anchored at `(99.9335, 48.6412)` with size `(58.133, 162.7177)` and unit scale; Side Stair -> Upper Sitting Hall is anchored at `(362.54022, 17.147995)` with size `(218.7387, 438.8239)` and unit scale. Rendered room-local corners and navigation results will be calibrated and frozen by the characterization test rather than hand-derived.
- Characterization result: the new Group14 lifecycle case passes across `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and maximum-zoom `2560x1080`. It freezes exact trigger geometry, source/arrival aliases, far approach and landing results, the inferred upward-stairway / normal-door asymmetry, `Use Stairway` / `Open Door` prompts, `StairsUp` / `Door` cursors, stair/door catalogs, event/audio ordering, active-room camera background, cleanup, protected boundaries/cutouts, catalog absence/conflict, and neighboring-route isolation. The independently reconstructed six-line SHA-256 is `8feac87c6e631543491757c0bab3f8f16383733f1bad102419bd1af4f57f357c`.
- Rendered gate: `group14-group13-characterization-locked.xml` passes exact `2/2`; Group14 retains SHA `8feac87c6e631543491757c0bab3f8f16383733f1bad102419bd1af4f57f357c`, and prior Group13 retains SHA `14315ebb94e0991564058de63ed42aad056f85b3578c8af626acc4e75c1dab3c`.
- Static/compile gate: architecture tools pass `4/4`; guard, exact `114/114` runtime ledger, metadata/GUID pairing, and serialized-reference integrity pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours with smell totals `106/17/67/51/4/6`, `165` scripts / `1,971` serialized references (`1,115` project / `856` external), and `7,371` paired globally unique asset metadata GUIDs. Generated evidence remains byte-identical at `35b78190fc8820883abde1b8db531edbcd13108742fbfb515a7fb187ac3c8f69`, `d9d3a8f47cea7b94609d16d9f2c48b86e71a005df3fc83ec34237eb622a1794d`, `47ef37376e7f1a75db4ac4501f55a3e5166ba7731ec23d4c399012c410482127`, and `6748d21ca5f2f6f424fc2c62c42fce36cc076e92ef2498958fc717614eef059c`. Unity compilation exits `0` with `Exiting batchmode successfully now!` and no compiler error.
- Independent golden: `cold-start.xml` passes exact `1/1`; SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.752865`, and sort `1075`.
- Immutable production result: Gameplay remains `82e6b51a51cc3a5cf34afe6cfb26dfe35a292cafbdd6a3dd58e6221cbac3d119`; GameDatabase remains `13f8161b0a5f107e984c3c856bdc9f75bb922f2c3082370de571f412a42ab5f3`; inventory remains `2aaba6eed5c2bd28026dcca5dedb863b2ecbf646574da168799c1249b9c2fc29`; `doors.txt` remains `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. No production source/data, scene, prefab, existing metadata/GUID, boundary, trigger, cutout, blocker, art, audio, camera, package, or ProjectSettings file changed.
- Human review remains required for feet/landings, stairway transition feel/audio, prompt/cursor cleanup, camera/visibility, and both rooms' collision/occlusion relationships. No pruning is authorized by this characterization slice.

## Failed-gate audit within Slice `2.2-14a`

- The first calibration reached the protected-room assertion and failed exact `0/1` because activating Side Stair correctly reparents the shared camera `RawImage` to the active room stage as its first runtime child. The scene still serializes only `PlayerBoundary`, `AnimatedPatches`, `Lighting`, and `Doors`; production bytes were unchanged. The characterization now distinguishes the expected runtime presentation child from the protected serialized room content. The failed XML/log remain preserved as `group14-characterization-calibration.*`.
- The second calibration reached the intended final sentinel mismatch after every behavioral assertion passed. Its exact five rendered profile lines plus one structural line independently hash to the emitted `8feac87c6e631543491757c0bab3f8f16383733f1bad102419bd1af4f57f357c`; only the expected SHA constant was then locked. The intentional failed evidence remains `group14-characterization-calibration-2.*`.

## Completed Slice `2.2-13` evidence

- Clean start: branch `refactor/final-architecture-overhaul` at accepted characterization commit `49e0617f93b015e2951ccb5b8b78d08d612f06ca`; `git status --short` was empty before the production declaration.
- Production identity: Side Stair & Mudroom RoomView `4100000040` reuses RoomDefinition GUID `c5153d08442348c49bf2c92c935d8035`. Passage components `4100000041` / `4100000042` use new globally unique definition GUIDs `0491e7071cda47e7b779cf87f71d026e` / `5c0c635bc6c04da19e0909a6f81d0caf`, stable IDs `passage.service-corridor.side-stair-mudroom` / `passage.side-stair-mudroom.service-corridor`, and bind only trigger callers `2300000174` / `2300000184` plus their four direct dependencies. Group14 trigger `2300000189` remains an unbound four-component legacy caller.
- Production placement: both new Passages serialize stage `2`, `approachPlacementMode: 1`, `arrivalPlacementMode: 1`, reciprocal RoomView/definition ownership, and no point anchors. Forward stores `BL (-569.47998, -470.50003) / TL (-569.47998, -338.82755) / TR (836.02002, -338.82755) / BR (836.02002, -470.50003)`; reverse stores `BL (52.839996, -166.62186) / TL (52.839996, 188.62186) / TR (172.84, 188.62186) / BR (172.84, -166.62186)`. Across Groups 10–13 there are now eight destination-region documents; Groups 11, 12, and 13 serialize no point anchors.
- Final totals: Gameplay `6,049` documents / 14 RoomViews / 28 stage-2 Passages / 45 triggers; GameDatabase `47` definitions (`19` rooms / `28` passages); GameRoot `51` scene behaviours / `59` total references including eight services; direct dependencies and canonical callers `28 bound / 17 null`; inventory `28 complete / 12 queued / 2 blocked-one-way / 3 blocked-parallel`. Fourteen of nineteen rooms now have RoomViews and five remain passive.
- Exact state hashes: Gameplay `82e6b51a51cc3a5cf34afe6cfb26dfe35a292cafbdd6a3dd58e6221cbac3d119`; GameDatabase `13f8161b0a5f107e984c3c856bdc9f75bb922f2c3082370de571f412a42ab5f3`; inventory `2aaba6eed5c2bd28026dcca5dedb863b2ecbf646574da168799c1249b9c2fc29`; unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. New definition assets hash to `cc018912e99d57c0878d3dd0a7f7aa036f45e9b3d47cca285c0832878e1cc57a` / `d55b4f616ab41c6920897d9045681f97a873320aa3521ff51502ec7acd3772e7`; RoomView/Passage documents `4100000040` / `4100000041` / `4100000042` hash to `60a443d02bdbeaef0641a25839a71bcb4d5c5bc15a71b9d98f560e9771904490` / `bb7c23708af06511644fc8523dd7eb648efc1b9fb77d105ef65ae9bb20ea0733` / `c215298a565bc175fbf7d91b3c312c5e7fb136f976e3b1c5b05b002194c90307`.
- Static evidence: architecture tools `4/4`, architecture guard, exact `114/114` runtime ledger, metadata/GUID pairing, and serialized-reference integrity pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours with smell totals `106/17/67/51/4/6`, `165` scripts / `1,971` serialized references (`1,115` project / `856` external), and `7,371` paired globally unique asset metadata GUIDs. Generated evidence hashes are `d9d3a8f47cea7b94609d16d9f2c48b86e71a005df3fc83ec34237eb622a1794d` / `47ef37376e7f1a75db4ac4501f55a3e5166ba7731ec23d4c399012c410482127`.
- Unity/focused evidence: Unity `6000.4.10f1` compiles in `compile.log`. The first focused XML honestly records `73 total / 60 passed / 13 failed`; all failures were stale expected totals or rounded serialized literals after the declared additions. The focused correction run records `17 total / 14 passed / 3 failed`; those three remaining stale `59`-reference/eight-region expectations then pass exact `3/3`. Therefore every one of the original `73` focused cases is accounted without relabeling a failed XML. After synchronizing shared global totals, the complete `GameplayLifecycleCharacterizationTests` fixture passes exact `19/19` in `editmode-full-lifecycle.xml` against the final test and production bytes.
- Rendered evidence: `rendered-group13-group12-group11.xml` proves Group13 exact `1/1` across all five profiles and retains SHA-256 `14315ebb94e0991564058de63ed42aad056f85b3578c8af626acc4e75c1dab3c`; its two prior-route failures were stale 13-RoomView test literals only. `rendered-prior-corrections.xml` passes exact `2/2`, retaining Group11 SHA `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b` and Group12 SHA `b371eb16186010923ac6d6c63851a5fe0e0de0cf642ac3099b4fab2b93db1fa8`.
- PlayMode/golden evidence: `playmode-group13-representatives-cold.xml` passes exact `5/5`: the genuine Group13 reciprocal source/destination-region round trip, representative Group12/11/10 routes, and independent cold start. Group13 reports Side local `(52.84002, -470.5001)`, Service local `(52.84002, -166.6219)`, forward stop `(0.688021, -2.169556)`, and reverse stop `(-2.84142, -6.08485)`. Cold SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.752865`, and sort `1075`.
- Protected-content/diff result: Service Corridor retains its direct 14-point boundary and two named cutout/blocker pairs; Side Stair retains its direct 19-point boundary, four-child room structure, no invented blockers, canonical/legacy aliases, and isolated Group14 route. No runtime source, existing `.meta`/GUID, prefab, catalog row, threshold, trigger rectangle, boundary, cutout, blocker, art, audio, lighting, camera, presentation geometry, package, ProjectSettings file, or large asset changed. No deletion or pruning is authorized; human doorway/feet/prompt/cursor/audio/camera/collision/occlusion review remains outstanding.

## Completed Slice `2.2-11` evidence

- Clean start: branch `refactor/final-architecture-overhaul` at `9ed538fa209c239f50675dbc0b9a04c0bbaa01ea`; `git status --short` was empty before this declaration.
- Production identity: trigger components `357269799` (Rear) and `2300000124` (Billiard); host GameObjects/RectTransforms `357269797` / `357269798` and `2300000120` / `2300000121`; RoomViews `4100000032` / `4100000008`; new Passages `4100000035` / `4100000036`; definition GUIDs `cd0978fc337c41b982afb4b46c7a2b3c` / `ef375ba8c3744447add18ebec1fd1a83`; stable IDs `passage.grand-entrance-hall-rear-view.billiard-room` / `passage.billiard-room.grand-entrance-hall-rear-view`.
- Production placement: both Passages serialize stage `2`, `approachPlacementMode: 1`, `arrivalPlacementMode: 1`, no point anchors, and exact reciprocal source/destination ownership. Rear -> Billiard stores `BL (-745.00006, -114.72981) / TL (-745.00006, 238.13548) / TR (-501.32404, 238.13548) / BR (-501.32404, -114.72981)`; Billiard -> Rear stores `BL (579.6167, -250.84499) / TL (579.6167, 31.911606) / TR (702.0674, 31.911606) / BR (702.0674, -250.84499)`.
- Final totals: Gameplay `6,043` documents / 12 RoomViews / 24 stage-2 Passages / 45 triggers; GameDatabase `43` definitions (`19` rooms / `24` passages); GameRoot `45` scene behaviours / `53` total references; dependencies/callers `24 bound / 21 null`; inventory `24 complete / 16 queued / 2 blocked-one-way / 3 blocked-parallel`.
- Exact hashes: Gameplay `ae7a5c400b4edc9a874331bb54a1684d759239f9ccbb03975a3975b3e6649585`; GameDatabase `2c8f1dfbb90fc81f8032e3f8c3010dc3e394e0e2a397f4a46cc34d0b5a05c9a0`; inventory `c5cec68785b0699aedcd8a5dae46ecbb14a7a94ae969d04e1ac75a70cc05d4fe`; unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.
- Static evidence: architecture tools, guard, exact `114/114` runtime ledger, GUID/meta, and script integrity pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours, smell totals `106/17/67/51/4/6`, `165` scripts / `1,961` serialized references (`1,105` project / `856` external), and `7,367` paired unique meta GUIDs.
- Unity evidence: compile passes in `compile-production-attempt1.log`; accepted focused EditMode passes `70/70` in `editmode-focused-attempt2.xml`; rendered locked/canonical parity passes `2/2` in `rendered-group11-attempt1.xml`; genuine Group11 plus representative Group10/09 and independent cold start pass `4/4` in `playmode-group11-representative-cold-attempt1.xml`.
- Golden evidence: legacy SHA remains `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`; cold-start SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`. Group11 reaches Billiard local `(-498.425, -151.0829)` and Rear local `(579.6167, -250.845)`, with forward stop `(7.480806, -3.396356)` and reverse stop `(-6.471363, -1.841076)`.
- Diff/prune result: only declared definitions, scene/database/inventory registrations, focused tests, deterministic generated evidence, and architecture records changed. Existing GUIDs, `doors.txt`, prefabs, packages, ProjectSettings, large assets, trigger rectangles, thresholds, boundaries, cutouts, blockers, art, audio, and presentation geometry remain unchanged. No deletion is authorized.

## Completed Slice `2.2-11b` evidence

- Production-clean start: branch `refactor/final-architecture-overhaul` at `78f61103b6c4582f72ee6d12f4031dd5c763d93e`; no staged file and no production/test/untracked candidate file existed. The failed-gate audit already carried in this execution-state file was retained and converted into this prerequisite declaration.
- Baseline production state remains Gameplay `6,041` documents / 12 RoomViews / 22 Passages / 45 triggers; GameDatabase `41` definitions; GameRoot `43` scene behaviours / `51` total references; direct dependencies/callers `22 bound / 23 null`; inventory `22 complete / 18 queued / 2 blocked-one-way / 3 blocked-parallel`.
- Backward compatibility design: `PassageApproachPlacementMode.ExactAuthoredPoint = 0` is the omitted-field default for every existing Passage. `BestReachableInSourceRegion = 1` is valid only at fully authored stage and resolves the current Passage's source interaction rectangle through its reciprocal Passage's already-authored destination region, avoiding a duplicate corner owner or misleading fixed point.
- Shared resolver requirements: preserve the exact legacy preferred-first/lower-edge/quarter/corner/outward sample order, `<= 1 px` deduplication, weights `10` / `0.01` / `25`, offset `max(36 px, min(width,height)*0.35)`, strict first-on-tie behavior, `WouldMove` filtering, preferred exact-point early acceptance, and no approach world fallback. Arrival behavior retains its existing no-preferred/no-movement-requirement/world-fallback policy.
- Fail-closed rule: a region-mode canonical Passage with missing/mismatched reciprocal region, invalid RoomView ownership, invalid transform geometry, or unreachable selection must fail. It must never consume a point anchor or fall back to legacy trigger-owned sampling.
- Forbidden production hashes remain Gameplay `867e9395e38b4a2e675d5e0fc6abbf9449cb03f4e75ab2c3b4397172efde44ae`, GameDatabase `90417c118dfc1cda7bcb3cf17c1d92fda30af7433be7ae446162146925e2c39a`, inventory `3291b6e856a252d78411de186e76f818dda12bd9b63cae1972bfd7001187aa5d`, and `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` throughout this prerequisite.
- `doors.txt` must remain exact SHA-256 `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`; this slice deletes nothing.
- Mechanical rollback proof remains: `Gameplay.unity`, `GameDatabase.asset`, `RemainingRouteInventory.csv`, the lifecycle fixture, and the four untracked definition asset/meta files were restored/removed with patch-based edits. Production hashes and counts match committed `78f61103`; no candidate GUID, Passage, binding, registration, inventory completion, or fixed anchor remains.
- Runtime result: `PassageApproachPlacementMode.ExactAuthoredPoint = 0` preserves every omitted-field scene document. `BestReachableInSourceRegion = 1` reuses the reciprocal Passage's validated arrival region, requires fully authored reciprocal topology and a matching source `RectTransform`, and fails closed. `PassageArrivalResolver` now solely owns the shared 28/29-sample approach policy; canonical and unmigrated callers delegate to it, while the arrival path retains its seven-world-point fallback.
- Static result: architecture-tool unit tests pass `4/4`; `git diff --check`, architecture guard, exact `114/114` runtime ledger, and Unity script integrity pass at `114` runtime files / `51,224` lines / `48` direct MonoBehaviours and `165` scripts / `1,957` serialized references / `856` external-package references. The generated `script_integrity.csv` remains deterministic and unchanged. All `7,365` existing asset metadata records remain paired and globally GUID-unique.
- Unity compile result: Unity `6000.4.10f1` exits `0` without a compiler error against the exact accepted runtime bytes in `Logs/ArchitectureOverhaul/Slice-2.2-11b/compile-final.log`.
- Focused EditMode result: the first filter selected the three navigation cases and passes exact `3/3` in `editmode-focused.xml`; its namespace-qualified canonical fixture token selected no canonical cases, so the four missing canonical contracts were run once in `editmode-focused-canonical.xml` and pass exact `4/4`. The accepted focused evidence is exact `7/7`. After the fail-closed repair, `editmode-affected-architecture-final.xml` preserves `66/68` passing cases; its two test-only corrections pass exact `2/2` in `editmode-failclosed-corrections-retry.xml`, so all `68/68` affected cases are accounted without rerunning the other 66.
- Rendered parity result: `rendered-source-region-and-locked-final.xml` passes exact `2/2`. The transient Passage-owned source regions are bit-exact with both preserved Group11 legacy triggers across all five rendered profiles, two far starts, and null/15/50/85-percent preferred clicks; poisoned data fails closed. The separate locked production characterization retains exact SHA-256 `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`.
- PlayMode/golden result: representative Service Corridor/Chapel exact-point traversal, Grand Entrance Hall/Rear destination-region traversal, and independent cold start pass exact `3/3` in `playmode-representative-and-cold-final.xml`. The cold fingerprint remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, with Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, and sort `1075`.
- Final diff/prune result: only the five declared runtime integration files, three focused test fixtures, execution/migration/ledger evidence, and no serialized production file are eligible for this commit. No scene, prefab, ScriptableObject, existing `.meta`/GUID, catalog, route inventory, package, ProjectSettings, large asset, threshold, boundary, cutout, blocker, art, audio, or presentation geometry changed. No deletion or production Group11 completion is authorized; human doorway/feet/prompt/cursor/collision/occlusion review remains for the production slice.

## Failed-gate audit within Slice `2.2-11b`

- Final independent diff review found that `CanTraverse` originally validated reciprocal region corners but not their exact match to the source Passage `RectTransform`. A near player could therefore bypass geometry matching because the builder only ran on the far-approach path. Before commit, the minimal repair extracted one pure shared geometry check, made Passage policy validity require fully authored reciprocal topology and exact transform agreement, gated both `CanTraverse` and the public builder on it, and added a real near-player `TryTraverse` rejection regression. No scene/data/geometry threshold or compatibility behavior changed.
- The first post-repair affected run produced a real `68 total / 66 passed / 2 failed` XML. One new test cleanup tried to warp back to the characterized starting coordinate, which the existing boundary correctly rejected; the test can safely continue from the already-certified near coordinate. One source-text assertion still expected the pre-refactor inline reciprocal-region expression instead of the new Passage policy property. These were test-only expectations; the 66 passing cases, compilation, and production bytes were retained.
- Only those two corrected cases were rerun. The first correction runner stalled during Unity initialization before discovery, produced no XML, and left the repository unchanged; its log is preserved as `editmode-failclosed-corrections.log`, the process was terminated after a bounded wait, and a fresh identical runner passes exact `2/2` in `editmode-failclosed-corrections-retry.xml`. The rendered and PlayMode final gates then pass from the exact accepted bytes.

## Completed Slice `2.2-11a` evidence

- Starting/rollback state: clean accepted Group10 production commit `eef71f3e7dd9ac9f181818169b9a911fd1498d58`.
- Scope/result: exactly the rendered lifecycle characterization and execution-state record changed; Gameplay, GameDatabase, inventory, `doors.txt`, runtime source, definitions, prefabs, metadata, GUIDs, and every production asset remained byte-identical.
- Locked conflict/behavior: gameplay follows the Inspector-owned Rear View / Billiard route while the stale catalog describes front Entrance / Billiard. `group11-characterization-locked.xml` passes exact `1/1` across five viewport/zoom profiles with six-line SHA-256 `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`.
- Static/golden evidence: architecture tools `4/4`, guard, `114/114` ledger, `165` scripts / `1,957` serialized references, all `7,365` paired unique meta/GUIDs, Unity compilation, and independent cold start `1/1` pass. Cold SHA remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.
- Resulting commit: `78f61103b6c4582f72ee6d12f4031dd5c763d93e`. No pruning is authorized; human production review remains future work.

## Completed Slice `2.2-10` evidence

- Starting/rollback state: clean prerequisite commit `f91ee2338261ee061a8502e06542fd49b5d81831` on `refactor/final-architecture-overhaul`.
- Production serialization: Gameplay is exactly `6,041` documents / twelve RoomViews / twenty-two stage-2 Passages / 45 triggers; `GameDatabase` is exactly `41` definitions (`19` rooms / `22` passages); GameRoot owns `43` scene behaviours / `51` total references including eight services; direct dependencies and canonical callers are each `22 bound / 23 null`; inventory is `22 complete / 18 queued / 2 blocked-one-way / 3 blocked-parallel`.
- Exact state hashes: Gameplay `867e9395e38b4a2e675d5e0fc6abbf9449cb03f4e75ab2c3b4397172efde44ae`; GameDatabase `90417c118dfc1cda7bcb3cf17c1d92fda30af7433be7ae446162146925e2c39a`; inventory `3291b6e856a252d78411de186e76f818dda12bd9b63cae1972bfd7001187aa5d`; unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e` with neither Group10 ID.
- Static evidence: architecture guard and exact `114/114` runtime ledger pass at `114` runtime files / `51,062` lines / `48` direct MonoBehaviours; script integrity passes at `165` scripts / `1,957` serialized references (`1,101` project / `856` external-package); all `7,365` asset `.meta` GUID records are paired and globally unique.
- Unity evidence: Unity `6000.4.10f1` compiles against the exact final bytes in `compile-exact-final.log`. Focused EditMode passes exact `4/4` in `editmode-focused-exact-final.xml`; affected architecture passes exact `67/67` in `editmode-architecture-exact-final.xml`; rendered Group10 passes exact `1/1` with locked SHA-256 `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638` in `rendered-group10-exact-final.xml`; genuine Group10 plus independent cold-start PlayMode passes exact `2/2` in `playmode-group10-and-cold-exact-final.xml`.
- Final behavior: region validation uses serialized RoomView-local corners; runtime projection derives bit-exact corners from the canonical reverse Passage RectTransform without consulting `DoorTriggerNavigation`; legacy evaluation order is retained without eager `Physics2D.SyncTransforms`. Frozen-camera A/B calibration against preserved legacy accepts Rear `(785.5335, -391.4933)` and Entrance `(418.3665, -440.4239)`.
- Diff/prune: no runtime source, existing `.meta`/GUID, prefab, threshold, boundary, trigger geometry, railing cutout, compatibility route, or legacy catalog row is deleted. Human visual review remains explicitly outstanding.
- Exact next gate: tests-first Group `11` Grand Entrance Hall Rear <-> Billiard Room characterization. Lock the `doors.txt` endpoint conflict before any Group11 production authoring.

## Completed Slice `2.2-10a` evidence

- Starting/rollback state: prerequisite Slice `2.2-10a` began cleanly from committed Group `10` characterization checkpoint `cfd3c3524a0dcd062fd0d4324c740d206d631f96` on `refactor/final-architecture-overhaul`.
- Baseline production state: Gameplay is exactly `6,038` documents / eleven RoomViews / twenty Passages; `GameDatabase` is exactly `39` definitions; GameRoot owns `40` scene behaviours / `48` total serialized references; direct dependencies and canonical callers are each `20 bound / 25 null`; and inventory is `20 complete / 20 queued / 2 blocked-one-way / 3 blocked-parallel`.
- Read-only mapping: Group `10` uses existing trigger components `1858342503` and `70736571`. Both are bottom-edge Doors with `28 px` activation, no proximity/auto-walk requirement, null serialized dependencies/callers, and reciprocal Inspector destinations split between canonical `Grand Entrance Hall Rear View` and legacy source alias `Grand Entrance Hall Rear view`.
- Locked evidence: rendered legacy characterization passes exact `1/1` in `group10-legacy-locked.xml`, with six-line SHA-256 `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638`. The route retains exact Title-case arrival events, no `ArrivedAtDestination`/`MovementStopped` events, and reciprocal bottom-edge hover/prompt transfer.
- Behavioral finding: a single static logical or RoomView-local anchor cannot preserve this route. For example, the forward left-lane rear arrival changes from `(10.2283, -5.097569)` at `1366x768`, to `(0.512234, -3.812039)` at `1440x1080`, to `(-7.128999, -5.87218)` at `1920x1080`, then `(11.80638, -6.123572)` at `2560x1080`; other lanes/directions also diverge. Forcing one point would change approved Butler placement.
- Canonical contract: `PassageArrivalPlacementMode.ExactAuthoredPoint = 0` preserves all twenty existing Passage documents. `BestReachableInAuthoredRegion = 1` owns four ordered RoomView-local destination corners and resolves them through the reciprocal Passage's destination RoomView; region mode does not serialize a misleading unused point. `PassageDefinition.CompatibilityDestinationRoomName` defaults to the existing primary legacy spelling and validates any explicit override against the destination aliases.
- Shared behavior: `PassageArrivalResolver` is the sole destination-region sampler. It preserves the exact ordered 28-screen-sample scoring, weights, first-on-tie rule, deduplication, outward bands, and seven-world-point fallback. `DoorTriggerNavigation` now only translates its temporary RectTransform edge to runtime corners and delegates; `RoomNavigationManager` dispatches typed point/region placement only after destination activation; `PointClickPlayerMovement` exposes the narrow `IPassageArrivalQuery` capability without new state or movement behavior.
- Static/serialization evidence: `git diff --check`, architecture guard, `114/114` runtime ledger, `165` scripts / `1,952` serialized script references / `856` external references, and `7,363` paired unique meta GUIDs pass. New script GUID `b8e4d9c74a2f4ce8a7c2b39f5d6810ae` has zero serialized references. Gameplay (`220009ae5f8d2bbe3a3e62ccab6d9c88da56a4c66d9dde092da11f0cae8b8a93`), GameDatabase (`90734ca890e57a4b64062e10a7e2c2e04f281dc783beb96f012c2fa673674ebc`), and `doors.txt` (`8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`) are byte-identical to rollback `cfd3c352`; no scene, prefab, existing data asset/meta, package, ProjectSettings, threshold, collision, or presentation file changed.
- Unity evidence: explicit Unity `6000.4.10f1` compilation exits `0`. Focused contracts pass `6/6`; the final affected architecture suite passes `65/65`; the rendered five-profile/three-lane behavior/adapter gate passes exact `1/1` with all `30` direct geometry-adapter comparisons and unchanged SHA-256 `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638`; representative fixed-point Service Corridor/Chapel plus the independent cold-start golden pass `2/2`. Cold-start SHA-256 remains `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96` with Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, and sort `1075`.
- Diff/prune: this prerequisite adds one canonical resolver script/meta, updates only its five declared runtime/domain integration files, focused tests, deterministic serialized-reference evidence, and migration ledgers/state. It authorizes no deletion or production route change. Resulting commit: `f91ee2338261ee061a8502e06542fd49b5d81831`.
- Recorded next gate: restart production Slice `2.2-10` from that clean commit and author only the Grand Entrance Hall / Rear View RoomView, reciprocal definitions/passages, exact four-corner regions, callers/dependencies, inventory, and tests.

## Completed Slice `2.2-09` evidence

- Starting/rollback state: clean committed Group `08` checkpoint `07bb61fff50f63dfdc36acf244b391b26fa48d61`.
- Production serialization: Gameplay is exactly `6,038` documents / eleven RoomViews / twenty stage-2 Passages / 45 triggers; `GameDatabase` is `39` definitions (`19` rooms / `20` passages); GameRoot owns `40` scene behaviours / `48` total serialized references; direct dependencies and callers are `20 bound / 25 null`; and inventory is `20 complete / 20 queued / 2 blocked-one-way / 3 blocked-parallel`.
- RoomView-local proof: Service Corridor `(-133.2642, -171.8258)` and Chapel `(461.4019, -190.7613)` remain exact across four aspects and widest maximum zoom. The six-line authored fingerprint is `bff3067e14fd0710f8de6dd4f7e4434d9025a2b7593adf3cd54571020a622475`.
- Static/Unity evidence: architecture guard, `113/113` runtime ledger, `164` scripts / `1,952` serialized references / `856` external references, and `7,362` paired unique meta GUIDs pass. Unity compiles; focused EditMode passes `4/4`; final affected architecture passes `62/62`; rendered characterization passes `1/1`; and Group `09` plus cold-start PlayMode passes `2/2`.
- Resulting commit: `aa0e24d163437975e7747fc60829817ff01e88c6`. Human doorway/camera/prompt/collision/occlusion review remains explicitly outstanding; no pruning is authorized.

## Completed Slice `2.2-08` evidence

- Starting/rollback state: clean committed RoomView-local prerequisite `fdeefcfd032bf86962f8fbad5d8f9f965aa56303`. The prior tests-first legacy characterization remains preserved at exact `1/1` in `group08-legacy-locked.xml`, with six-line SHA-256 `9af5d16f40f2f186d02a777fb53ac80fd28fe898e0e8953db9455d047a5432de`.
- Production serialization: Gameplay is exactly `6,035` documents / ten RoomViews / eighteen stage-2 Passages / 45 triggers. Kitchen RoomView `4100000010` reuses RoomDefinition GUID `70531cbf9a67476f81f54b528029132e`; reciprocal Passage documents `4100000027` / `4100000028` use new unique definition GUIDs `2985cbdd527b4faaec13ff03091dbcd1` / `453ad73cf2df1107f56be7a00daa3145`. GameRoot remains eight services and advances to 37 unique scene behaviours. `GameDatabase` is `37` definitions (`19` rooms / `18` passages), direct dependencies and canonical callers are each `18 bound / 27 null`, and inventory is `18 complete / 22 queued / 2 blocked-one-way / 3 blocked-parallel`.
- RoomView-local proof: Service Corridor `(589.9897, -419.25894)` and Kitchen `(-478.36285, -156.76599)` are exact finite, walkable, path-reachable, non-projected native-room points. `group08-room-local-targeted-final.xml` passes `1/1`; its five rendered observations at `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and maximum zoom hash to `6d5f83aa399b12d8945a660e0372f65206c2c04a52e2657422ea92966fe0393b`. The unchanged `145 px` threshold, boundaries, trigger rectangles, caller behavior, arrival mapping, blockers, and authored room content all pass.
- Static/integrity: architecture-tool tests pass `4/4`; guard reports no new debt; audit remains `113` runtime files / `50,339` lines / `48` direct MonoBehaviours with unchanged smells `106/17/67/51/4/6`; runtime ledger passes `113/113`; script integrity passes `164` scripts / `1,947` serialized references / `856` external-package references. All `7,360` asset meta/GUID records are paired and globally unique.
- Unity compile/tests: explicit Unity `6000.4.10f1` compilation exits `0` without a C# compiler error. The consolidated relevant EditMode launch discovered `76` cases and passed `74`; its two stale test assumptions are repaired and pass in focused reruns, so all `76/76` are accounted. Genuine real-menu Group `08` traversal plus independent cold start pass exact `2/2` in `playmode-group08-and-cold.xml`.
- Golden/serialization: cold-start SHA-256 remains exactly `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, and sort `1075`. Final hashes are Gameplay `5203d054870b01b2e6db55286bc4a2d6b25ee6795cb84765306728090b4e1d98`, inventory `b692f1333f0271903fd9ff60e20323ce628311f37592a481a23fad142b468ae3`, database `9b9b4ae428340a9fd6597e18873adbdce9b3a675786dbee568adb9a595d710b1`, Kitchen RoomView `96dd9c03e34083d898a7d3cc0aa266f7f8ac035ea55acc5add9f6b445f7838b7`, Passage documents `46b4a659b72f253be991d54356f6ccfabdb357d4b1f530156d5107cfaf2ba263` / `99f834cdf2d77ffd58135f3f1f44e97f69e57e64c3364724cab33c5a82292072`, and unchanged `doors.txt` `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.
- Diff/prune: no runtime source, prefab, existing `.meta`/GUID, threshold, boundary, package, ProjectSettings, large asset, unrelated route, or compatibility catalog entry changed. This slice adds only the two definition assets/metas and the declared Kitchen/Group08 scene-data ownership. No deletion is authorized while 27 trigger callers remain legacy.
- At the completed Group `08` checkpoint, automated gates were complete and human review remained for Butler feet at both painted door sides across default/maximum zoom, prompt/cursor cleanup, camera/active-room visibility, both Service Corridor cutout/blocker relationships and boundary, and Kitchen table occlusion/collision/boundary. Its recorded next safe slice was tests-first Group `09` Service Corridor / Chapel, not pruning or a claim that Stage 2 was complete.

## Completed Slice `2.2-08a` evidence

- Tests-first contract: `anchor-contract-red.xml` verifies exact `0/1` at the missing coordinate-space discriminator before production implementation. `anchor-contract-green.xml` then passes `1/1`, and the complete canonical contract fixture passes `6/6`. The representation is an explicit discriminated union: `LegacyPlayerLogical = 0` preserves missing-field deserialization and the existing `logicalPosition`/`LogicalPosition` compatibility surface; `RoomViewLocal = 1` resolves only through `IRoomViewLocalCoordinateMapper`; unknown modes, missing mappers, non-finite authored coordinates, and non-finite mapper output fail closed to `false`/zero.
- Production behavior: `PointClickPlayerMovement` implements the mapper through the already-owned active RoomView stage and existing logical/world conversion. `DoorTriggerNavigation` resolves the approach before applying its unchanged screen threshold; `RoomNavigationManager` resolves arrival only after destination-room activation and before its unchanged exact warp. `Passage` and `CanTraverse` validate the selected coordinate without adding a singleton, discovery path, fallback sampler, runtime repair, threshold change, boundary change, second state owner, route, or scene mutation.
- Rendered proof: `roomview-local-rendered-probe-final.xml` passes exact `1/1`. One in-memory RoomView-local Passage anchor remains attached to native Service Corridor point `(663.7111, -18.4943)` at `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest maximum zoom. It matches the direct room-stage world and screen positions and round-trips to the same native coordinate. Its required aspect-specific logical values are `(8.642072, -0.240811)`, `(7.482427, -0.208498)`, `(8.639962, -0.240753)`, and `(9.976567, -0.277997)`; maximum zoom remains correctly attached at `(9.976567, -0.277997)`.
- Static/integrity: architecture-tool tests pass `4/4`; guard reports no new debt; audit passes at `113` runtime files / `50,339` lines / `48` direct MonoBehaviours with unchanged smells `106/17/67/51/4/6`; runtime ledger passes `113/113`; script integrity passes `164` scripts / `1,942` serialized references / `856` external-package references. All `7,358` asset meta GUID records are paired and globally unique.
- Unity tests: explicit Unity `6000.4.10f1` compilation exits `0` without a C# compiler error. The consolidated relevant EditMode launch discovered `73` cases and passed `71`. Its two failures were test-only: one stale source assertion still named direct `LogicalPosition` access, and one real physical mouse click leaked into the rendered Entrance/Drawing harness during the `1920x1080` resize. The source assertion now requires mapped resolution; the harness locks input between its explicit synthetic actions. The source correction passes in `editmode-two-corrections.xml`, the isolated lifecycle correction passes `1/1` in `editmode-lifecycle-correction-final.xml`, and together with the preserved `71` passing cases all `73/73` are accounted. The real PlayMode route plus cold-start launch passes exact `2/2`.
- Golden/serialization: cold-start SHA-256 remains exactly `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, and sort `1075`. Gameplay remains byte-identical at `c48e6f4a68cd311e958677136b572e06c05266ca556153dafd8e2656ffb1d30c`; `doors.txt` remains `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. All sixteen Passage documents still omit the new fields and therefore retain their exact legacy-logical anchors.
- Diff/prune: production changes are limited to the five declared runtime consumers/contracts; tests and deterministic evidence/ledgers are updated. No file is added or deleted. No scene, prefab, existing `.meta`, GUID, definition asset, route caller, threshold, boundary, package, ProjectSettings file, or large asset changes. No deletion is authorized by this prerequisite.
- At completion of prerequisite Slice `2.2-08a`, its recorded next safe step was to restart Group `08` Service Corridor / Kitchen from the clean prerequisite commit, reuse its locked legacy fingerprint, author that pair's anchors in RoomView-local coordinates, and certify the unchanged approach threshold/boundaries across every rendered profile before committing the route migration.

## Historical blocked Slice `2.2-08` evidence

- At that historical checkpoint, the slice was blocked at the last passing production commit `8be472bde503f2971cb8a4998aff4cc054e8aaea`; all uncommitted Group `08` production, scene, data, asset, and test changes were removed. No Group `08` ownership change, new asset, `.meta`, GUID, scene document, database entry, runtime-source change, commit, push, or merge remained in the repository at that checkpoint.
- Tests-first legacy characterization succeeded exact `1/1` in `Logs/ArchitectureOverhaul/Slice-2.2-08/group08-legacy-locked.xml`. Its six-line rendered fingerprint hashes to `9af5d16f40f2f186d02a777fb53ac80fd28fe898e0e8953db9455d047a5432de` and preserves the standard-door event/audio order, prompt cleanup, room/camera transition, null serialized dependencies/callers, two Service Corridor blockers, and one Kitchen blocker at all required profiles.
- The first tentative authored reciprocal points were Service Corridor `(8.6, -5.6)` and Kitchen `(-7.2, -2.2)`. They passed exact walkability/path reachability/non-projection and threshold margin at `1366x768`; the authored primary round trip placed the player exactly at those coordinates. At `1440x1080`, the existing movement boundary rejected the Service point and the unchanged trigger correctly refused traversal. The failure is preserved in `group08-authored-calibration.*`.
- Two tests-only Service sweeps then searched the unchanged movement boundary and `145 px` trigger threshold without weakening either. The fine sweep covered `12,221` points over logical `x=[7,10]`, `y=[-6.5,-4]` at `0.025` increments. At `1440x1080`, `3,420` candidates were exact walkable, path reachable, non-projected, and within threshold; at `2560x1080`, none of those candidates retained all four properties. The zero intersection is preserved in `group08-service-anchor-intersection-fine.*`. The broader `0.1` sweep over `x=[4,11]`, `y=[-7,0]` likewise reached zero candidates after all four aspect profiles in `group08-service-anchor-intersection.*`.
- Exact blocker: with the current aspect-dependent logical-to-world conversion and authored Kitchen/Service geometry, there is no calibrated invariant Service approach point that passes both `1440x1080` and `2560x1080` under the unchanged boundary and threshold. Completing stage-2 authored approach ownership would therefore regress a currently working legacy door at a supported aspect. Runtime source and anchor-representation changes are forbidden in Slice `2.2-08`, and retaining legacy sampling or increasing the threshold would bypass the declared completion gate.
- The recorded next safe step at that checkpoint was to define and approve a separate tests-first prerequisite slice for an aspect-invariant room-relative Passage anchor representation (including compatibility/serialization migration and regression coverage), or explicitly amend the migration requirement to permit Group `08` to remain on legacy approach sampling. After that decision passed its own gates, Group `08` could restart from clean commit `8be472bd` without reapplying the removed production attempt as stage 2.

## Failed-gate audit within Slice `2.2-08`

- The first legacy characterization run stopped on a test-only assumption that runtime dependencies would remain null. The scene serialization was null as expected, but `Awake` had correctly resolved the compatibility dependencies. The assertion was narrowed to serialized truth; no production change was made.
- The measured legacy run then intentionally stopped at its placeholder digest, producing `9af5d16f40f2f186d02a777fb53ac80fd28fe898e0e8953db9455d047a5432de`; the locked rerun passed exact `1/1`.
- The tentative authored run proved correct exact placement at `1366x768` and then failed the real `1440x1080` boundary gate. Coarse and fine tests-only sweeps found no cross-profile invariant Service candidate. The boundary, threshold, trigger geometry, route behavior, and runtime source were not modified to hide the failure.
- The complete uncommitted production attempt was mechanically reversed, and its four untracked PassageDefinition asset/meta files were deleted explicitly. At the end of that failed attempt, `HEAD` remained the last passing Group `07` commit and no partial Group `08` architecture remained.
- The successful retry's consolidated relevant EditMode run produced `76 total / 74 passed / 2 failed`. Both were test-only: the Library/Ballroom GameRoot substring skipped newly inserted Kitchen RoomView `4100000010`, and the Group07 Service Corridor sorting assertion sampled updated collider bounds before explicitly refreshing the existing `ObjectMovementBlocker2D` sorting owner. No production value, threshold, geometry, route, or sorting formula changed.
- The first two-case correction runner stalled during Unity project initialization before test discovery and produced no XML. Its log is preserved as `editmode-two-corrections-hung.log`; the process was terminated after nine minutes with no repository mutation. A fresh runner produced one passing registration case and the still-stale sorting case in `editmode-two-corrections-final.xml`.
- Minimal test-only repair: include `4100000010` in the exact registration sequence and invoke the existing `ApplySourceSortingNow` after the EditMode harness changes maximum zoom and synchronizes physics. `editmode-lifecycle-correction-final.xml` then passes exact `1/1`; together with the initial 74 and the independently passing registration correction, all 76 relevant cases are accounted without rerunning the other 74.

## Completed Slice `2.2-07` evidence

- Starting/rollback commit: `2400df2a86867b1632f7870d67d13b176107b9cd`; the repository was clean before Slice `2.2-07` was declared.
- Tests-first characterization: legacy traversal passes exact `1/1` and hashes to `1a2d83cdf5e9cce175cf16333875f183629c6e67fe8d82241479bae79b4e8150`. Invariant exact-walkable reciprocal points are Pantry `(7, -2.8)` and Service Corridor `(4.2, -3.3)`. Across `1366x768`, `1440x1080`, `1920x1080`, `2560x1080`, and widest-aspect maximum zoom, Pantry distances are `18.338 / 66.306 / 25.839 / 52.36 / 60.263 px`; Service distances are `16.712 / 71.262 / 23.565 / 29.137 / 33.535 px`, all below the unchanged `145 px` threshold.
- Production serialization: Gameplay is `6,032` documents / nine RoomViews / sixteen Passages / 45 triggers, with all sixteen Passages at authored-anchor stage 2. The new Service Corridor RoomView and reciprocal Passage documents are `4100000009`, `4100000025`, and `4100000026`; GameRoot remains eight services and advances to 34 exact scene behaviours. `GameDatabase` is `35` definitions (`19` rooms / `16` passages), direct dependencies are `16 bound`, canonical callers are `16 bound / 29 null`, and inventory is `16 complete / 24 queued / 2 blocked-one-way / 3 blocked-parallel`.
- Identity/integrity: new PassageDefinition GUIDs `1b2d5f64523942a08e10402e24e88738` and `b485e8a6f574414a84f77437e02147f1` are globally unique. Architecture-tool tests pass `4/4`; guard reports no debt above baseline; audit remains `113` runtime files / `50,212` lines / `48` direct MonoBehaviours with smell totals `106/17/67/51/4/6`; runtime ledger and script integrity pass at `113/113` and `164` scripts / `1,942` serialized references / `856` external-package references.
- Compile/tests: Unity `6000.4.10f1` compilation exits `0` with no C# compiler error. Targeted final Group `07` lifecycle passes `1/1`. The consolidated relevant EditMode launch accounts for all `71` cases (`67` passed initially; its four test-harness corrections then pass exact `4/4`). Genuine real-menu Group `07` PlayMode passes `1/1`; independent cold start passes `1/1` with unchanged digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`, Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, and sort `1075`.
- Final hashes: authored six-line profile `7cc7c8706a02443b30c91c7a3cdcce40b7c04fec2f54e3f2eb7d5acad7058ea9`; Gameplay `c48e6f4a68cd311e958677136b572e06c05266ca556153dafd8e2656ffb1d30c`; inventory `47440df151a302a10493d14f2258c470a4a07d8683b858caf73f1c82e07c8c15`; database `155e065c03ec20c6b7d5f8e0a8e1746d1da166d74dc8d8c4214db8f7446989f1`; RoomView/Passages `d24b113a61e8322284eb4667cb80876fa07c5b9771a1aa7ec3d8151e9b5b50dd`, `dd5939bae9c6b3611738401388176aff137d0acf8bb9adcc6849ced6484b3b6e`, and `83753f9883d85f8330a5e2ee853923398e9d31b5e1ab3d07141be99f60ae1ce9`. `doors.txt` remains byte-identical at `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`.
- Diff/prune result: no runtime source, prefab, existing `.meta`, existing GUID, threshold, large asset, package, ProjectSettings, or unrelated route changed. No prune is authorized: legacy `doors.txt` rows, both Service Corridor cutouts, their two blockers, and `PlayerBoundary` remain required compatibility/visual-collision data.
- Automated gates are complete. Human review remains required for Butler feet at both painted door sides, prompt/cursor cleanup, camera/active-room visibility, both Service Corridor cutout/blocker occlusion relationships, and `PlayerBoundary` collision.

## Failed-gate audit within Slice `2.2-07`

- The first exact anchor candidate used Service Corridor `(0, -2)` and failed the `1366x768` exact-walkability gate. The boundary and threshold were not weakened. A tests-only sweep selected `(4.2, -3.3)` with reciprocal Pantry `(7, -2.8)`; both are exact walkable/path-reachable/non-projected at every required profile. The failed calibration log remains preserved as `group07-anchor-calibration.log`, and the accepted calibration passes in `group07-anchor-calibration-2.*`.
- The first final authored lifecycle run produced exact `0/1` at a test-only Service Corridor cutout assertion: the inactive serialized SpriteRenderer order is `1000`, while the assertion incorrectly expected active-room blocker-derived values. Scene data and production behavior were not implicated. The serialized baseline remains asserted as `1000`; after Service Corridor activation, the existing `ObjectMovementBlocker2D` footprint formula, People-layer assignment, pivot sorting, and exact renderer/blocker equality are asserted at every rendered profile. The failed XML/log remain preserved as `group07-authored-final.*`; only the targeted lifecycle gate is rerun for this harness correction.
- The first targeted correction then reached a later dormant serialization assertion and produced exact `0/1`: it requested GameObject `2300000025` as Unity type `114` instead of type `1`. The call now names the correct type explicitly; no serialized data or production path changed. This second failed XML/log remain preserved as `group07-authored-certified.*`, and only the targeted lifecycle gate is rerun again.
- The consolidated relevant EditMode launch discovered all `71` cases and passed `67`. Its four failures were test-harness assumptions: Pantry gained a third source Passage; a prior-pair registration substring omitted the newly inserted Service Corridor RoomView; the foreign-scene fixture could reopen the already-active MainMenu scene after rendered tests; and maximum zoom changed collider bounds one frame before the existing blocker `LateUpdate` refreshed sorting. The exact count/list assertions now include Group `07`, the fixture chooses a saved scene distinct from its active fixture scene, and maximum-zoom sorting settles one frame before comparison. No runtime source, scene, data asset, GUID, threshold, route, or production behavior changed. `editmode-relevant.*` is preserved, the other `67` passing cases are not rerun, and only these four corrections receive a focused rerun.
- The four-case correction rerun passes exact `4/4`; together with the preserved `67` passing cases, all `71` relevant EditMode cases are accounted for. The final Group `07` PlayMode and independent cold-start gates both pass exact `1/1`.

## Completed Slice `2.1` evidence

- Starting/rollback commit: `4346f8f53a740f6b0557ff6e719c2a0eacc30170`; the repository was clean before the Slice `2.1` declaration.
- Production serialization: Gameplay remains `6,029` documents / eight RoomViews / fourteen Passages / 45 triggers. The exact scene delta is `8 insertions / 6 deletions`: callers `1505671646 -> 4100000023` and `2300000134 -> 4100000024`, four reciprocal anchor replacements, and two stage `0 -> 2` replacements. Final room-side points are Pantry `(3.244461, -3.108338)` and Billiard `(6.9, -1.6)`; both thresholds remain `145`. Gameplay SHA-256 is `c207301bc56a5096de0c6377606a281f99e2bc5084ca9beecafdbf8ff2d47d53`; inventory SHA-256 is `36534307882904596b3e294220b7ba1b77499ea7391561b8afd920166ed50cdb`.
- Static/GUID/reference gates: architecture-tool unit tests pass `4/4`; guard reports no debt above baseline; audit remains `113` runtime files / `50,212` lines / `48` direct MonoBehaviours with smell totals `106/17/67/51/4/6`; runtime ledger passes `113/113`; script integrity passes `164` scripts / `1,937` serialized references / `856` external-package references. No `.meta`, GUID, prefab, definition, package, ProjectSettings, large asset, or unrelated scene document changed.
- Compile: Unity `6000.4.10f1` exits `0` with no C# compiler error in `Logs/ArchitectureOverhaul/Slice-2.1/compile-final.log`; every subsequent current-source Unity test runner also compiled successfully without unexpected tracked mutation.
- EditMode: targeted authored Group `06` lifecycle passes exact `1/1`; canonical/manifest certification passes `14/14`; combined architecture/navigation safety passes `32/32`; the full rendered lifecycle fixture passes `10/10`. Verified XMLs are `group06-lifecycle-passing.xml`, `editmode-focused-final.xml`, `editmode-safety.xml`, and `editmode-lifecycle.xml` under `Logs/ArchitectureOverhaul/Slice-2.1/`.
- PlayMode/golden: the genuine real-menu Group `06` authored caller round trip passes `1/1` in `playmode-group06-final.xml`; cold start passes `1/1` in `cold-start.xml` with Butler `114.417 px`, entrance door `141.481 px`, ratio `0.808710`, presentation `0.7528645`, sort `1075`, and exact digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.
- Authored evidence: worst stage-one screen distances are Pantry `78.646 px` and Billiard `87.01 px`, both below the unchanged `145 px` threshold. The seven-line profile hashes to `cd248f01301448b5cd807cc9331e58d99bd59a139bb772dab352869527b9a6eb`; Passage documents `4100000023` / `4100000024` hash to `61ae1a519da8a47275e2713b3182b7297c049b0eb9b2d590f5cb5037607abcc2` / `b46ec8f0b48730348e80287e50393c153ce05621c35db00741dfd7af50e6b1ce`.
- Automated gates are complete. Human review remains required for Butler feet at both painted door sides, Group `06` prompt/cursor behavior, camera/active-room visibility, and the three Billiard foreground-cutout/blocker occlusion pairs. No pruning is authorized by this slice.

## Failed-gate audit within Slice `2.1`

- The first rendered lifecycle produced exact `0/1`: candidate Billiard point `(7.075521, -1.844375)` worked at primary and `1366x768` but the `1440x1080` reverse approach was rejected by the movement boundary. The threshold and boundary were not weakened. A tests-only five-profile sweep selected invariant `(6.9, -1.6)` at worst `87.01 px`; the temporary calibration was removed, and the final lifecycle passes `1/1` plus the full `10/10` fixture.
- The calibrated lifecycle then exposed two previously unvalidated test-only fallback fingerprints: caller-bound stage `0` far arrival `(6.545288, -1.559969)` differs from pair-local null-caller fallback `(6.575521, -1.484375)`. The shared helper now accepts the precise expected path; no production path changed and tolerances were not widened.
- The first canonical/manifest run passed `13/14`; one source-text assertion demanded the obsolete widest-aspect legacy arrival literal after authored arrival ownership. Removing only that stale assertion yielded exact `14/14`; rendered lifecycle coverage remained intact.
- The first genuine Group `06` PlayMode run passed route setup but stopped at `0/1` because Chapter 1's intro correctly kept input disabled. The harness now explicitly enables input and resets the click-frame guard before simulating each trigger, matching the accepted characterization; the final real-trigger round trip passes exact `1/1`. All failed and repaired XML/log pairs remain preserved under `Logs/ArchitectureOverhaul/Slice-2.1/`.

## Completed Slice `1.5` evidence

- Static guard: passed for Slice `1.5`; architecture-tool tests pass `4/4`, the guard reports no debt above baseline, and generated audit reports `113` runtime files / `50,212` lines / `48` direct MonoBehaviours with unchanged smell totals `106/17/67/51/4/6`.
- Runtime ledger: passed at `113` current runtime files / `113` exact final-ledger rows. `GameDatabase` is recorded as a typed fail-closed index owner, all 19 RoomDefinitions are recorded, and the new focused fixture is classified in the final Editor-tool ledger.
- Unity script integrity: passed at `164` current scripts / `1,937` serialized script references / `856` external-package references. New test GUID `63be092c1c1640789df8a05643140f1d` and all eleven new room-asset GUIDs are unique; RoomDefinition data references advance from eight to nineteen while every existing script/asset GUID remains unchanged.
- Compile result: Unity `6000.4.10f1` batch compilation passes with no compiler errors in `Logs/ArchitectureOverhaul/Slice-1.5/compile.log`. Unity import leaves GameDatabase and all nineteen room assets byte-identical to their pre-import candidate bytes.
- EditMode XML: `Logs/ArchitectureOverhaul/Slice-1.5/editmode-focused-final.xml` verifies exactly `38/38/0/0`: GameDatabase definitions `9`, stable identities `12`, canonical room/passage `5`, passage migration `9`, room/startup roots `2`, and rendered Group `06` characterization `1`.
- PlayMode XML: `Logs/ArchitectureOverhaul/Slice-1.5/cold-start.xml` verifies exactly `1/1/0/0`, including strict GameDatabase validation and the existing eight initialized service roles/order.
- Manual/golden result: passed; Butler remains `114.417 px` against the `141.481 px` entrance door (`0.808710` ratio), presentation `0.7528645`, sort `1075`, and all guest records retain exact digest `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.
- Scene/prefab diff reviewed: passed; no scene, prefab, existing asset, existing `.meta`, route inventory, legacy route source, package, ProjectSettings, or large asset changed. Gameplay remains `bb851d116bb616d5d8e25dc863bdb71f48c9314bfcd6b63d6e23acc1b3aced0d`, route inventory remains `1e821937d2561ac2239f5d5d5c1765c04f83dcb6d66e8b15f0b144b36b85b2cb`, and `doors.txt` remains `8dc956b84e8436054a8835a7fa7f12f0aa2ce14d1d9a90701e8d98c3f001798e`. No ignored `InitTestScene` scratch remains.

## Completed slices

| Slice | Commit | Tests | Notes |
|---|---|---|---|
| `0.1` | `872875aa4e3381993c3bb5d9c32a4393a7defe17` | Baseline evidence inherited | Recovery branch and tag established from the reviewed architecture head. |
| `0.2` | `62a389254d4cedfc8128b181d0a17bbe5fdaebf3` | Static controls | Final ledgers, verifier, integrity scanner, and slice-gate controls installed. |
| `0.2 evidence` | `8ec90df46a0e2917a52356692e4938140ed66081` | Integrity scanner `155/1926/856`; guard and ledger pass | Deterministic `script_integrity.csv` tracked as its own commit. |
| `0.2.1` | `daa11ed6d746e1a74c82ddec985ed8286d2b6cf9` | Verifier tests `3/3`; exact baseline XML `264/218/46/0` | NUnit failure evidence now excludes failed suite paths and verifies exact result counts. |
| `0.3` | `bb64c7c38ec16b6aa35c8bdf8026a723bce20b9e` | Real PlayMode `4/4`; lifecycle `10/10`; focused EditMode `31/31`; full EditMode `264/218/46/0`; static controls pass | Real startup, movement/collision, canonical room round trip, current hidden-child defect, Butler visual baseline, and Chapter 2 panic measurements committed without production changes. |
| `0.4a` | `c86beaea48f4910c2c1937d54574ec7360df8bd5` | Ten cold starts `10 x 1/1`; combined real PlayMode `5/5`; exact fingerprint; static controls pass | Certified deterministic Butler/guest scale and sort without production, editor, scene, prefab, or meta changes. |
| `0.4b` | `e795181c0e27d1cb9d85eed2694013ac45995b46` | Focused EditMode `62/58/4/0` with the exact four known legacy failures; cold fingerprint `1/1`; static controls pass | Calibration-window reads are non-mutating and intentional reference writes record Unity Undo; approved scale data and output remain exact. |
| `1.1` | `4501c51bb5175f18e56a233a8765631d6cb58986` | Identity `12/12`; compatibility `34/34`; cold fingerprint `1/1`; static controls pass | Six stable-ID value contracts added without changing any serialized identity storage or compatibility API. |
| `1.2` | `0880a998e50ee4e4dd5743142f99d05c1f55adfb` | Context `7/7`; compatibility `41/41`; cold fingerprint `1/1`; static controls pass | Seven lifecycle-free typed roles, extensible immutable service order, strict composition validation, and failure cleanup added with no serialized gameplay mutation. |
| `1.3` | `1f5af1c85a5fd337c7ba94ae20f772f488e6cf15` | Lifecycle `5/5`; Core compatibility `29/29`; cold fingerprint `1/1`; static controls pass | Production GameRoot is validation-fatal and lifecycle-safe; services initialize before binders and tear down after binders in exact reverse order without runtime repair. |
| `1.4` | `53a28cdf2bbd238b2c0bc71e011a0a187269b307` | Clock/Scheduler `8/8`; compatibility `37/37`; real PlayMode `1/1`; cold fingerprint `1/1`; static controls pass | Clock is the lifecycle-gated sole time writer; Scheduler consumes deterministic clock advances, owns timed callbacks/cancellation, and Chapter 1 has no fallback scheduling path. |
| `1.5` | `4346f8f53a740f6b0557ff6e719c2a0eacc30170` | Definitions `9/9`; focused `38/38`; cold fingerprint `1/1`; static controls pass | All 19 canonical room definitions and strict typed GameDatabase indexes are complete without changing Gameplay, existing GUIDs, or legacy navigation behavior. |
| `2.1` | `2400df2a86867b1632f7870d67d13b176107b9cd` | Targeted `1/1`; focused `14/14`; safety `32/32`; lifecycle `10/10`; Group 06 PlayMode `1/1`; cold fingerprint `1/1`; static controls pass | Group 06 Pantry/Billiard callers and invariant reciprocal anchors are complete at stage 2 with no runtime-source, GUID, prefab, definition, threshold, blocker, or cutout change. |
| `2.2-07` | `8be472bde503f2971cb8a4998aff4cc054e8aaea` | Group 07 targeted `1/1`; all `71` relevant EditMode cases accounted; Group 07 PlayMode `1/1`; cold fingerprint `1/1`; static controls pass | Service Corridor RoomView and Pantry/Service reciprocal definitions, callers, direct dependencies, and invariant stage-2 anchors are complete with no runtime-source, existing-GUID, prefab, threshold, blocker, cutout, or legacy-catalog change. |
| `2.2-08a` | `fdeefcfd032bf86962f8fbad5d8f9f965aa56303` | Canonical `6/6`; rendered probe `1/1`; all `73` relevant EditMode cases accounted; route+cold PlayMode `2/2`; static controls pass | Backward-compatible RoomView-local Passage anchors resolve through the existing movement owner and fail closed; every existing scene/route remains byte- and behavior-identical. |
| `2.2-08` | `07bb61fff50f63dfdc36acf244b391b26fa48d61` | Group 08 targeted `1/1`; all `76` relevant EditMode cases accounted; Group 08 plus cold PlayMode `2/2`; static controls pass | Kitchen RoomView and Service Corridor/Kitchen reciprocal definitions, callers, direct dependencies, and RoomView-local stage-2 anchors are complete with no runtime-source, existing-GUID, prefab, threshold, blocker, cutout, or legacy-catalog change. |
| `2.2-09` | `aa0e24d163437975e7747fc60829817ff01e88c6` | Focused `4/4`; affected architecture `62/62`; rendered `1/1`; Group 09 plus cold PlayMode `2/2`; static controls pass | Chapel RoomView and Service Corridor/Chapel reciprocal definitions, callers, direct dependencies, and RoomView-local stage-2 anchors are complete with no runtime-source, existing-GUID, prefab, threshold, blocker, cutout, or legacy-catalog change. |
| `2.2-10a` | `f91ee2338261ee061a8502e06542fd49b5d81831` | Focused `6/6`; affected architecture `65/65`; rendered parity `1/1`; representative fixed route plus cold PlayMode `2/2`; static controls pass | Passage owns a default-compatible four-corner destination-region arrival policy and sole deterministic resolver; all existing scene/data remains byte-identical and Group 10 production authoring is still separate. |
| `2.2-10` | assigned by this commit | Compile; focused `4/4`; affected architecture `67/67`; rendered `1/1` exact locked SHA; Group 10 plus cold PlayMode `2/2`; static controls pass | Rear RoomView and reciprocal Entrance/Rear definitions, Passages, callers, dependencies, and destination-region ownership are complete; no legacy path, trigger, boundary, railing cutout, or catalog row is pruned. |
| `2.2-11a` | `78f61103b6c4582f72ee6d12f4031dd5c763d93e` | Locked rendered characterization `1/1`; cold start `1/1`; static controls pass | Live Rear View/Billiard Inspector ownership and the conflicting front-Entrance legacy catalog are frozen before production authoring. |
| `2.2-11b` | `9ed538fa209c239f50675dbc0b9a04c0bbaa01ea` | Focused `7/7`; affected architecture `68/68` accounted; rendered `2/2`; representatives plus cold PlayMode `3/3`; static controls pass | Passage owns a backward-compatible source-region approach policy and sole ordered resolver; no Group11 production content is authored in this prerequisite. |
| `2.2-11` | assigned by this accepted commit | Compile; focused EditMode `70/70`; rendered `2/2`; Group11, representatives, and cold PlayMode `4/4`; static controls pass | Reciprocal Rear View/Billiard definitions, Passages, callers, dependencies, and source/destination-region ownership are complete; no trigger, catalog row, cutout, blocker, boundary, or compatibility path is pruned. |
| `2.2-12a` | `661514fe82a1325424b9ca9705dbfd772ae4f886` | Locked rendered characterization `1/1`; cold start `1/1`; static controls pass | Mixed standard-door/bottom-edge Rear View/Conservatory behavior and exact scaled trigger corners are frozen before production authoring. |
| `2.2-12` | `1189022453a21accafcf13e375a121320b0e343a` | Compile; all `72` focused EditMode cases accounted; rendered `2/2`; Group12, representatives, and cold PlayMode `4/4`; static controls pass | Conservatory RoomView and reciprocal Rear View/Conservatory definitions, Passages, callers, dependencies, and source/destination-region ownership are complete; no trigger, catalog row, cutout, blocker, boundary, or compatibility path is pruned. |
| `2.2-13a` | `49e0617f93b015e2951ccb5b8b78d08d612f06ca` | Compile; rendered Group13 characterization `1/1`; independent cold start `1/1`; static controls pass | Reciprocal standard-door behavior, exact region geometry, ampersand/no-ampersand aliases, protected room content, and Group14 isolation are frozen before production authoring. |
| `2.2-13` | assigned by this accepted commit | Compile; all `73` focused EditMode cases accounted; rendered Group13 `1/1` plus prior-route corrections `2/2`; Group13, three representatives, and cold PlayMode `5/5`; static controls pass | Side Stair & Mudroom RoomView and reciprocal Service Corridor/Side Stair definitions, Passages, callers, dependencies, and source/destination-region ownership are complete; no trigger, catalog row, cutout, blocker, boundary, neighboring Group14 route, or compatibility path is pruned. |
| `2.2-14a` | assigned by this commit | Compile; locked Group14 + Group13 rendered characterization `2/2`; independent cold start `1/1`; static controls pass | Mixed inferred-upward-stairway / standard-door behavior, exact aliases and geometry, catalog absence/conflict, protected room content, and neighboring-route isolation are frozen before Group14 production authoring. |
| `2.2-14` | assigned by this accepted commit | Compile; focused `74/74`; complete lifecycle `20/20`; Group14, representatives, and cold PlayMode `5/5`; expanded fixture `13/12/1` plus isolated unchanged golden `1/1`; static controls pass | Upper Sitting Hall RoomView and reciprocal Side Stair/Upper Sitting definitions, Passages, callers, dependencies, source/destination regions, and mixed stair/door audio ownership are complete; no trigger, catalog row, boundary, cutout, blocker, neighboring route, or compatibility path is pruned. |
| `2.2-15a` | assigned by this accepted commit | Compile; locked Group15 + Group14 rendered characterization `2/2`; independent cold start `1/1`; static controls pass | Reciprocal Upper Sitting Hall/Upper Gallery standard-door behavior, exact aliases and geometry, protected Upper Gallery content, and neighboring-route isolation are frozen before Group15 production authoring. |
| `2.2-15` | assigned by this accepted commit | Compile; all `75` focused EditMode cases accounted; rendered Group15/14 `2/2`; Group15, representatives, and cold PlayMode `5/5`; static controls pass | Upper Gallery RoomView and reciprocal Upper Sitting Hall/Upper Gallery definitions, Passages, callers, dependencies, and source/destination-region ownership are complete; no trigger, catalog row, boundary, stairwell blocker, railing, cutout, generated blocker, neighboring route, or compatibility path is pruned. |
| `2.2-16a` | assigned by this accepted commit | Compile; locked Group16 + Group15 rendered characterization `2/2`; independent cold start `1/1`; static controls pass | Reciprocal Upper Gallery/Master Bedroom Suite standard-door behavior, exact scaled geometry, protected room content, and Group15/Group22 isolation are frozen before Group16 production authoring. |

## Failed-gate audit within Slice `2.2-10`

- The initial runtime-default test incorrectly required a non-null arrival object; its stale assertion was corrected without changing production behavior. The affected coordinate-space count was also stale at `4`; Group10 adds two certified region documents, so the exact current count is `6`.
- The first rendered canonical run produced SHA-256 `cc974cdcbe80a2d15c624c5f1fb73f6bdf4bddf703e77502d8c1c65274b0671b` after an eager `Physics2D.SyncTransforms` changed the legacy evaluation order. A pre-transition capture experiment was rejected because it sampled the wrong state; two intermediate test-only edits also produced compiler placement errors. All failed compiler/rendered artifacts remain diagnostic evidence only and are not acceptance results.
- Diagnosis compared authored and live region resolution after the same changed state and proved them equivalent. The production region data, trigger geometry, boundary, threshold, scene IDs, GUIDs, and legacy six-line baseline were not weakened or rewritten.
- The final repair restores legacy evaluation order, removes eager physics synchronization, validates the serialized RoomView-local region, and projects bit-exact region corners from the canonical reverse Passage RectTransform without referencing `DoorTriggerNavigation`. `rendered-group10-exact-final.xml` is the authoritative exact-source rendered result: exact `1/1` and SHA-256 `9bc19b299d38e9403dcf93da79336a0e45307ec7820f4c36214931b77c46b638`.
- Genuine PlayMode expected coordinates were calibrated A/B under the frozen camera against the preserved legacy route: Rear `(785.5335, -391.4933)` and Entrance `(418.3665, -440.4239)`. `playmode-group10-and-cold-exact-final.xml` is authoritative and passes exact `2/2`; earlier diagnostic/failed artifacts remain audit-only.

## Failed-gate audit within Slice `2.2-10a`

- The first rendered resolver-parity run produced a real `0/1` XML after two full profiles had already matched the locked output. The failure was not an arrival result: the concurrent Chapter 1 intro reached `ArrivalSequence` while the multi-resolution test settled and changed the player's input lease, so fallback hover cleared before the reciprocal-hover assertion. No production, scene, data, GUID, region, movement, or fingerprint value changed. An initial test-only repair re-enabled input and manually dispatched pointer-enter, but final diff review correctly rejected that as capable of hiding an automatic hover-transfer regression. The strengthened test stops only the concurrent Chapter coroutine before the long navigation probe, owns and asserts its input lease, and again requires automatic retained-pointer hover/prompt transfer without dispatching pointer-enter. `group10-resolver-parity-strengthened.xml` passes exact `1/1`, all 30 geometry-adapter comparisons match within `0.0001`, no movement event is emitted, and the exact six-line SHA remains unchanged.
- The first final `65`-case architecture runner stopped during Unity initialization before test discovery, emitted no XML, and left the repository unchanged. Its log is preserved as `editmode-architecture-final.log`; the stalled process was terminated after a bounded wait. A fresh identical runner produced `editmode-architecture-certified.xml` and passes exact `65/65/0/0` without any source or asset adjustment.

## Historical failed-gate audit — Slice `1.1`

- `editmode-focused-final.xml` initially produced `12 total / 11 passed / 1 failed`: the expanded current-asset assertion expected eight rooms but received zero.
- Diagnosis: Unity `AssetDatabase.FindAssets("t:RoomDefinition", ...)` does not resolve the namespaced canonical definition type in this project. The fourteen-passage query used the same unsupported assumption. No runtime code, scene, prefab, ScriptableObject, or existing metadata changed.
- Minimal in-scope repair: enumerate only the two canonical data folders and retain assets through typed `LoadAssetAtPath<T>` filtering. The exact eight-room/fourteen-passage identity, uniqueness, and string-preservation assertions remain unchanged. Preserve the failed XML/log and require the complete `12/12` focused rerun before commit.
- Repair result: `editmode-focused-certified.xml` passes exactly `12/12/0/0`; the failed `editmode-focused-final.xml` and its log remain preserved as diagnostic evidence under `Logs/ArchitectureOverhaul/Slice-1.1/`.

## Failed-gate audit within Slice `1.2`

- The first cold-start command incorrectly passed `-nographics`, exited natively with signal `11` / process code `139`, and produced no result XML. The preserved stack in `Logs/ArchitectureOverhaul/Slice-1.2/cold-start.log` ends in Linux `XcursorImageLoadCursor` from `NavigationCursorController.ApplyCursor`; no managed architecture assertion ran.
- Diagnosis: the certified rendered PlayMode gate requires a graphics device and every prior passing cold-start invocation deliberately omitted `-nographics`. The code batch had already compiled and passed focused/compatibility EditMode gates; this failure is a runner-command mismatch, not a behavioral difference.
- Minimal in-scope repair: make no source or asset change. Rerun the exact same single PlayMode test without `-nographics`, preserve both attempts, and accept the gate only if a real XML verifies `1/1` and the exact fingerprint remains unchanged.
- Repair result: `cold-start-certified.xml` verifies exactly `1/1/0/0`; all seven typed roles and eight ordered scene services match their serialized owners, and the visual digest remains exactly `34ea66772abd7375f965b2277e7342c82dbd853bc1efecc8d82a00e1b403dd96`.
- Crash cleanup: the aborted runner left one ignored Unity Test Framework `Assets/InitTestScene*.unity` plus `.meta`. Their contents were only the generated `PlaymodeTestsController`; both exact ignored files were removed, and the integrity scan returned from the transient `1927/857` count to the certified `1926/856` baseline. No tracked or authored asset was touched.

## Failed-gate audit within Slice `1.3`

- The first focused runner produced a real `5 total / 0 passed / 5 failed` XML before any test body ran. Every case failed in `SetUp` because Unity refuses `EditorSceneManager.NewScene(..., Additive)` beside the Test Framework's unsaved untitled scene. The second preserved attempt used runtime `SceneManager.CreateScene`, which Unity correctly rejected in EditMode for the same five setup-only failures. Neither attempt executed or disproved a production assertion.
- Minimal in-scope repair: use the batch runner's existing empty test scene for synthetic inactive objects, destroy every owned fixture object in teardown, and use owned additive loads only for the saved MainMenu/Gameplay validation fixtures. The final focused XML passes exactly `5/5/0/0` and the compatibility XML passes exactly `29/29/0/0`.
- A later compatibility launch stopped before test discovery because the test used a nonexistent Unity `6000.4.10f1` API, `EditorSceneManager.ClearSceneDirtiness`; no result XML was produced. The unsupported call was removed, while registration-list snapshots, scene dirty-state equivalence, real Gameplay validation, and byte-for-byte Gameplay readback retain the intended read-only proof. The final compatibility rerun passes `29/29/0/0`.
- All failed logs/XML remain under `Logs/ArchitectureOverhaul/Slice-1.3/`. No failed attempt changed production code, serialized registrations, existing metadata, MainMenu, prefabs, ScriptableObjects, packages, ProjectSettings, or large assets.

## Failed-gate audit within Slice `1.4`

- The first focused PlayMode run produced a real `1 total / 0 passed / 1 failed` XML before any Clock/Scheduler assertion. The test called synchronous `SceneManager.LoadScene("Gameplay")` and immediately asserted the active scene, but Unity Test Framework retained its generated `InitTestScene...` as active until the test yielded; the log then shows Gameplay loading successfully. Production code, scheduling behavior, and serialized content were not implicated.
- Minimal test-only repair: wait up to 120 frames for Gameplay to become the active scene before inspecting real services. No production, scene, prefab, data, Core contract, existing `.meta`, or GUID changed; the already-passing static, compile, and EditMode gates were not redundantly rerun after this harness-only adjustment.
- Repair result: `playmode-focused-certified.xml` passes exactly `1/1/0/0`, and the subsequent independent rendered cold-start passes `1/1/0/0` with the exact approved visual digest. Both the failed `playmode-focused.xml`/log and the certified rerun remain preserved under `Logs/ArchitectureOverhaul/Slice-1.4/`.

## Failed-gate audit within Slice `1.5`

- The first 38-test focused launch incorrectly used `-nographics`. The rendered Group `06` characterization loaded Gameplay and reached `NavigationCursorController.ApplyCursor`, where Linux Unity crashed in `XcursorImageLoadCursor` with signal `11` / exit `139`; no result XML was produced. This is the same graphics-runner incompatibility already established by the rendered cold-start gates. The crash log is preserved as `editmode-focused.log`; no source, asset, or serialized content changed.
- The graphics-enabled rerun discovered all `38` tests and passed `37`. Its only failure, `NavigationRegressionTests.GameplayRoomsOwnBackgroundsAndDoorGroups`, expects obsolete prefab name `Room_NewRoom` and is one of the exact pre-existing baseline failures in earlier full-suite XMLs; it is unrelated to room-definition/database ownership and did not reveal a Slice `1.5` behavior change. The `37/38` XML/log remain preserved as `editmode-focused-certified.*`.
- Minimal runner-filter correction: replace only that mistakenly selected known-failing legacy assertion with `NavigationRegressionTests.GameplayHasManualRoomStageRoot`, which checks the current authored room-stage root. No source, test, production asset, or serialized file changed between attempts. The accepted `editmode-focused-final.xml` then passes exactly `38/38/0/0`.

## Failed-gate audit within Slice `2.2-11a`

- The first rendered capture produced a real `1 total / 0 passed / 1 failed` XML before reaching the fingerprint. A real pointer at screen `(0,0)` independently hovered the already accepted Group10 rear-to-front bottom edge after Group11 returned to Rear View; the test incorrectly required the global hover to be null. Group11 traversal, production code, serialized content, and catalog behavior were not implicated.
- Minimal test-only repair: require that neither Group11 trigger remains hovered, explicitly recognize and clear only the valid Group10 bottom-edge hover, then require null Group11 approach/audio/prompt state. No production, scene, data, runtime, metadata, or compatibility file changed. The next capture traversed all five profiles and failed only at the deliberate placeholder SHA assertion, reporting reviewed digest `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`.
- The digest alone was replaced after all six lines were reviewed. Final `group11-characterization-locked.xml` passes exact `1/1`; the independent cold-start result passes exact `1/1`. The failed capture logs/XML remain under `Logs/ArchitectureOverhaul/Slice-2.2-11/` as audit evidence and were not relabeled as passing.

## Failed-gate audit within Slice `2.2-11`

- The uncommitted production candidate used reciprocal scene Passages `4100000035` / `4100000036`, new definition GUIDs `cd0978fc337c41b982afb4b46c7a2b3c` / `ef375ba8c3744447add18ebec1fd1a83`, RoomView-local point approaches Rear `(574.526, -260.8401)` / Billiard `(-497.0006, -141.3946)`, and four-corner destination regions. Scene/data/GUID/inventory structure and `git diff --check` passed, but none of those candidate objects or GUIDs was committed.
- `group11-production-fixed-point-ab.xml` is a real `1 total / 0 passed / 1 failed` result. At the first `1366x768` far approach, `DoorTrigger_GEH_Rear_BilliardRoom` reported that the selected canonical point was rejected by the existing movement boundary. The expected Billiard transition did not occur. The corresponding log/XML are preserved under `Logs/ArchitectureOverhaul/Slice-2.2-11/` and are not relabeled as passing.
- Exact characterization proves that this cannot be repaired by choosing a different invariant point. Legacy Rear approach stops span RoomView-local X `573.6765..575.3106` and Y `-261.2408..-260.6657`; Billiard stops span X `-497.0007..-495.9411` and Y `-141.3946..-141.3768`. A fixed RoomView-local anchor maps back to one fixed RoomView-local value, so it cannot reproduce all five approved aspect/zoom lines or SHA `56e7cc4b59801f8dfb9da547ff584f7b184bea0d15398e7a0b31f85b83b1b39b`.
- Root cause: the legacy standard-door approach is a region policy, not a point. It samples the source trigger in screen space, applies the unchanged `36 px`/proportional outward bands, collision/path projection, `WouldMove`, preferred-click priority, and weighted first-on-tie scoring. Aspect ratio and zoom therefore legitimately select slightly different reachable points.
- The candidate scene/data/inventory/test changes and four untracked definition asset/meta files were mechanically removed with patch-based edits. Production is byte-for-byte back at rollback `78f61103b6c4582f72ee6d12f4031dd5c763d93e`; `doors.txt`, every existing `.meta`/GUID, protected boundaries, rails, cutouts, blockers, and all eleven accepted route pairs remain unchanged.
- Smallest safe prerequisite: add a backward-compatible Passage-owned approach placement policy with default exact-point behavior and an explicit best-reachable source-region mode. Reuse the reciprocal Passage arrival region as the single serialized representation of the source interaction rectangle; share the exact ordered sampler/scoring implementation; require movement for approaches; preserve preferred-click early acceptance; forbid world fallback for approach; validate corners against the Passage host RectTransform; and fail closed without legacy fallback. That prerequisite must pass an exact five-profile A/B test before Group11 production restarts.
- The first broad production architecture/navigation run produced a real `113 total / 102 passed / 11 failed` XML. One in-scope `CanonicalRoomPassageContractTests` assertion still expected each shared Group11 RoomView definition to occur in four references; two source Passages make the exact production count five. That fixture-only expectation was corrected without changing production bytes.
- The other ten failures are unchanged historical `NavigationRegressionTests` failures already present in older baseline XML: Billiard foreground cutouts, Butler/player idle, chapter clock/ticking, Drawing Room hierarchy, gameplay trigger layer, gameplay room prefab naming, important room props, blocker collision, and settings/debug coverage. They are retained debt and were not modified or relabeled as Group11 failures. The accepted relevant rerun passes exact `70/70` in `editmode-focused-attempt2.xml`.

## Remaining adapters and debt

- All migration debt in the final runtime/editor ledgers remains unless explicitly completed above.
- The historical full EditMode baseline is `264/218/46`. Slice `2.2-11b` passes focused `7/7` and affected architecture `67/67`; this does not relabel the historical full-suite baseline as a current full-suite run. Fourteen editor-hosted `[UnityTest]` methods still depend on `UnityEditor`, `AssetDatabase`, and predefined-assembly production types, while nine black-box cases are discoverable in the genuine PlayMode test assembly; mechanically splitting and relocating the editor-hosted tests remains a Phase 8 task.
- `RoomContentGroup` still force-enables an intentionally hidden child when its room is reactivated. Slice `0.3` records that existing defect; the later room-visibility ownership slice must reverse the assertion.
- The `46` exact legacy EditMode failures remain migration debt and are not an accepted final state.
- Typed IDs are additive boundaries only: legacy room display strings, `guest_1` actor aliases, and serialized chapter strings are not migrated in Slice `1.1`. Actor aliases require an explicit Phase `4` mapping; no name-derived conversion is permitted.
- `ChapterId` provides lexical/type safety but not catalog membership. Scheduler event IDs remain strings and must not cross the typed chapter boundary; Phase `1.5` database indexes and Phase `5` chapter definitions must reject unregistered values.
- `StoryBeatBase(string)` remains a compatibility constructor and reports no valid typed ID for a legacy label. New beat implementations must use `StoryBeatBase(BeatId)`.
- The seven typed composition interfaces are lifecycle-free markers until each owning slice supplies its real domain contract. Consumers must not cast them back to legacy concrete facades; `INavigationRuntimeService` coexists temporarily with the behavioral `INavigationService` only at composition time.
- `GameContext.Services` is the immutable dependency-ordered snapshot; `GameRoot.Services` intentionally remains serialized Inspector registration order. Neither list is a new lookup API.
- Most real setup remains in legacy `Awake`/`OnEnable`/`Start`; Slice `1.3` certifies strict root-owned composition/context readiness and rollback, not complete game-wide dependency readiness. Independent legacy callbacks must migrate in their owning slices rather than being mistaken for root-owned startup.
- The completed Group `15` state preserves Gameplay's eight services and advances the exact serialized scene-behaviour set to `57`, for `65` total GameRoot references. Their legacy domain behavior and public facades remain intentionally unchanged until the relevant World, Story, Dialogue, UI, and Save ownership slices.
- Scheduler callback queues are deliberately transient derived runtime state: callbacks contain Story-owned delegates and clear on Scheduler shutdown. A future restore transaction must explicitly clear Scheduler, restore immutable Story/clock values, and let the owning Story beat re-arm schedules. Durable schedule reconstruction belongs with StoryState and SaveService rather than serializing dead callback metadata.
- Every approved room now has canonical passive definition data, but three do not yet have RoomViews or Passage bindings: Master Bedroom Suite, Nursery, and Blue Bedroom. Phase 2 must reuse those exact assets/GUIDs and must not create replacement definitions.
- `RoomVisualCatalog`, `doors.txt`, legacy trigger strings, RoomContentGroup activation, and exceptional one-way/parallel topology remain intentional compatibility debt. Complete replacement data alone is not deletion proof; consumers migrate and reach zero before pruning.
- Group `10` owns its RoomView, reciprocal definitions/scene Passages, callers/dependencies, and deterministic four-corner destination regions without depending on the reciprocal legacy trigger for region projection. Human visual review remains; completion is not consumer-zero deletion proof.
- Group `11` now owns reciprocal definitions/scene Passages, callers/dependencies, and deterministic source/destination regions while preserving the live Rear View/Billiard Inspector route, title-case alias, and conflicting front-Entrance `doors.txt` rows. The remaining 21 null callers, compatibility triggers/facades, and catalog consumers are migration debt until separate consumer-zero proof; Group11 completion does not authorize pruning.
- Group `12` now owns reciprocal definitions/scene Passages, callers/dependencies, and deterministic source/destination regions while preserving the mixed standard-door/bottom-edge interaction split and conflicting catalog state. The remaining 19 null callers, compatibility triggers/facades, and catalog consumers are migration debt until separate consumer-zero proof; Group12 completion does not authorize pruning.
- Group `13` now owns reciprocal definitions/scene Passages, callers/dependencies, and deterministic source/destination regions while preserving ampersand/no-ampersand aliases, standard-door behavior, protected Service Corridor/Side Stair content, and Group14 isolation. The remaining 17 null callers, compatibility triggers/facades, and catalog consumers are migration debt until separate consumer-zero proof; Group13 completion does not authorize pruning.
- Group `14` now owns the Upper Sitting Hall RoomView, reciprocal definitions/scene Passages, callers/dependencies, deterministic source/destination regions, and the mixed stair/door catalog split while preserving serialized trigger kinds, inferred upward-stairway behavior, exact aliases, protected room content, and neighboring-route isolation. The remaining 15 null callers, compatibility triggers/facades, and catalog consumers are migration debt until separate consumer-zero proof; Group14 completion does not authorize pruning.
- Group `15` now owns the Upper Gallery RoomView, reciprocal definitions/scene Passages, callers/dependencies, and deterministic source/destination regions while preserving both standard-door interactions, exact aliases, protected Upper Gallery content, and neighboring-route isolation. The remaining 13 null callers, compatibility triggers/facades, and catalog consumers are migration debt until separate consumer-zero proof; Group15 completion does not authorize pruning.
- Group `16` is characterized but not yet transferred: Master Bedroom Suite still has no RoomView, both Group16 trigger callers/dependencies remain serialized null, and both inventory rows remain queued. The locked fingerprint authorizes only the next production slice, not pruning.

## Exact next safe slice

- Begin production Slice `2.2-16` from the clean accepted Group16 characterization commit. Add only Master Bedroom Suite RoomView `4100000049`; author reciprocal standard-door Passages `4100000050` / `4100000051` on triggers `2300000204` / `2300000214`; create exactly two PassageDefinitions with fresh unique `.meta` GUIDs; bind the existing manager/player/audio/door-catalog dependencies and canonical callers; register only those objects in the existing database/root/inventory; and use the locked scaled reciprocal destination regions with no point anchors. Expected totals are Gameplay `6,058` documents / seventeen RoomViews / thirty-four Passages, GameDatabase `53`, GameRoot `60` scene behaviours / `68` references, callers `34/11`, and inventory `34 complete / 6 queued / 2 one-way / 3 parallel`. Prove the unchanged Group16 SHA plus Group15 and cold start. Preserve every trigger transform, Master flame/cutout/blocker, Gallery boundary/stairwell/railing/plant/guest object, Group15 route, Group22 topology, existing GUID, and catalog row. Do not prune or migrate another route.

## Resume command

```text
Give Codex CODEX_RESUME_PROMPT.md.
```
